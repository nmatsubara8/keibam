# Claude コンテキスト — keibam プロジェクト

最終更新: 2026-07-31（セッション総括を直下「## セッション総括（2026-07-31・#22〜STEP5）」に集約。
要点: **#22 raw MySpeed=No-Go でクローズ**／**0.835 疑義は本番条件 0.809≈deployed 0.8076 で harness 差と確定**／
本番パイプラインの脆弱性2件を Issue 化（**#23 stacking one-class=解決**・**#24 特徴量列契約=学習側完了/推論側別PR**）／
**STEP5 新モダリティ: JRDB TARGET 全8データを解読・取込パイプライン化（重複/漏れ検証つき）**／
**STEP5 決着: 成績IDM=SED.idm 完全一致(冗長)・外厩コメント G1=NO-GO＝新規 exploitable モダリティはゼロ**／
**STEP5 正式終了**＝限定命題「JRDB TARGET 追加データのうち現行 production 特徴へ安定した純増分を持つ系列は
確認されなかった」でクローズ。**次フェーズ＝運用基盤の堅牢化**（優先: ①`--jra-only` stacking one-class 修正
②学習/推論の列名・列順契約の固定 ③払戻・評価100%カバレッジ維持 ④BetPolicyFukushoHonmei 運用監視
⑤model/data/eval の version 固定＋検証メタデータ自動出力）。ROI>1 探索は新情報源が無い限り再開しない）

前回更新: 2026-07-31（**研究フェーズ完全クローズ → 実装フェーズへ移行**。回収率エッジ探索は
公開/半公開データで全否定・帰属まで確定。唯一の勝者＝raw MySpeed（過去SED素点履歴）を本番へ
配線する工学タスク Issue #22 を起票。直下「## 現状サマリ（2026-07-31）」に検討事項・実装済み・
次タスクを集約。詳細ログは各日付セクション参照）

---

## セッション総括（2026-07-31・#22〜STEP5）

作業ブランチ: `claude/review-context-and-tasks-1vxigl`（PR #21=`0bkff7` にスタック）。データ（DB/pickle）は
ユーザのローカルのみ＝重い検証はユーザが実行し出力を貼る運用。JRDB 認証は env `JRDB_USER`/`JRDB_PASS`、
スクレイピング間隔≥4秒、モデル識別子や認証情報は成果物に書かない。全テスト green を維持。

### 1. これまでの検討事項（すべて結論つき）
- **運用基盤フェーズ 完了（2026-07-31）＝5領域すべて「本体あり／安全・検証・監視層を追加」**:
  - **⑤ 検証メタデータ**: `src/pipeline/_eval_stamp.py`（feature_schema_hash・期間・drop・odds・seed・split の
    自動スタンプ）を myspeed/gaikyu 採用検証へ配線。0.835 疑義のような prod_p vs harness 混乱を構造防止。
  - **② 推論側 列契約**: `calc_score` の不足列 0 埋め（静かな誤予測）を **既定 strict fail-fast** へ
    （`require_present`・逃げ道 env `KEIBA_LENIENT_FEATURES=1`）。契約は dill 全体保存に同梱。
  - **① stacking fallback**: `StackingSplitDegenerateError`（ValueError サブクラス）＋
    `_train_with_optional_stacking` で、--jra-only 等の single-class split を停止でなく**非stacking へ自動 fallback**
    （meta に use_stacking 実績／stacking_fallback を記録）。
  - **③ 払戻カバレッジ回帰ガード**: `JrdbReturnSource.coverage()`＋テストで 100%→46% 退行を検出。
  - **④ 複勝本命 運用監視**: `src/policies/_honmei_monitor.py`（採用率・本命score分布・実現回収率・逸脱検知）。
  - 実スイート回帰なし（触れた領域 337 passed）。ブランチ `1vxigl` に直接 push（ユーザー許可）。
  - **②の実地検証で本番の隠れバグを検出＆修正＆再検証まで完了**（2026-08-01）:
    - `verify_feature_contract.py` で本番モデルに strict 列契約を当てたら **41列不足**＝学習 featured に
      **JRDB二重マージ(`jrdb_*_x/_y`)** の痕跡。旧挙動では推論時に0埋め＝**JRDB特徴が本番で死んでいた**
      （②が無ければ永遠に気付けない silent 誤予測）。
    - 真因: `jrdb_*` を作るのは `_augment.attach`(=`jrdb_build_features.py`)だけで、**既に jrdb 付きの
      featured に attach を重ねた**ため `_x/_y`。→ `attach` を**冪等化**（既存 jrdb_/prev_ を除去して貼り直す・
      全ブランチに無かった新規fix）。
    - 是正: 実 `--jrdb-dir` で `jrdb_build_features`(冪等) → `retrain --since-year 2015`(運用標準) →
      再検証で **不足0 / `✅ PASS`**。クリーンモデル(schema `f18bb56ff824`・637特徴・use_stacking True)確定。
    - 副産物: ①(stacking→非stacking 自動fallback)と⑤(meta 自己記述)が**本番 retrain で実証**
      （archive層1986-2014は pos=0＝`--since-year 2015` が運用標準である理由も再確認）。
- **STEP5 JRDB TARGET 全8種 探索完了＝新規 exploitable モダリティ ゼロ**（2026-07-31）:
  - **成績IDM = `raw_jrdb_sed.idm` と完全一致**（488,161件・一致率1.0/最大差0/相関1.0、`scripts/jrdb_seiseki_vs_sed.py`）＝取込済データの再パッケージ＝冗長。
  - **外厩コメント G1 = NO-GO**（本命が帰無）。本番 featured 570列 baseline に外厩 raw を足す採用検証
    （`scripts/gaikyu_adoption_check.py`・`attach_gaikyu_raw`）: raw4(name あり) ΔlogLoss +0.00465・
    operational(name なし) +0.00222・0/3年改善・④利用馬のみ≈0・placebo≈実測。name は高カーディナリティ
    (475水準)過学習(imp rank1 なのに OOS 悪化)、has/days/weeks は限界情報ゼロ。→ 外厩の運用情報は
    既存の休養・間隔系特徴＋市場に織り込み済み。**履歴集約へは進まず外厩トラック クローズ**。
  - 馬印系(IDM/外厩/番手/位置取)=既存特徴の派生、騎手/厩舎ランク=別スケール離散ランク(日付なしスナップショット)。
  - 詳細台帳: `docs/jrdb_target_downloads.md`。
- **#22 raw MySpeed 本番採用＝No-Go 確定**。本番準拠(市場オッズ除外)の採用検証で ΔlogLoss=+0.0032・
  ΔAUC=−0.0024・0/3年改善・過去≥3走でも≈0＝現行 featured モデルに冗長〜有害。オッズ込みの見かけ改善は
  placebo で77%残存＝実固有分は −0.00076 のみ。元ゲートの「M1 raw=勝者(+0.006)」は**弱い prod_p baseline
  限定**の結果で、強い featured には効かない（featured が既に同情報を内包）。
- **0.835 疑義は決着**: 採用検証の簡易 LightGBM(570列べた・前進分割) baseline が 0.835、本番文書値 0.808 と乖離
  していたが、**本番 retrain（stacking・--since-year 2015）で Place auc=0.8108≈deployed 0.8076**。→ 0.835 は
  harness 差（特徴量471 vs 570・分割差）の産物。**隠れた改善なし＝現行 featured は本番条件で天井**。
  「0.833 は単勝列由来」説も棄却（オッズ除外でも 0.835・むしろオッズ込みの方が低い＝本番がオッズを学習除外
  するのは正しい）。→ 公開/半公開データで追える予測改善は尽きた（プロジェクト大結論と整合）。
- **本番パイプラインの脆弱性2件を発見→Issue化**: **#23 `--jra-only` stacking one-class 障害**（実行停止）、
  **#24 学習=推論の特徴量列契約なし**（silent 誤予測リスク・`X does not have valid feature names` 警告）。
- **STEP5 新モダリティ調査**: JRDB TARGET DL 全8データを実ファイル解読。**真に新規で市場直交しうるのは
  外厩(放牧先の質・帰厩鮮度)のみ**。馬印系(展開/番手/IDM)は枠/脚質/jrdb_idm の派生、ランクは jockey/trainer
  集計と重複、**成績IDM はレース後IDM＝raw MySpeed 同族(No-Go族)かつ raw_jrdb_sed.idm と重複疑い**。

### 2. 実装済み機能（本セッション・全て push 済み）
- **#22 配線（No-Go だが証拠として温存）**: `src/jrdb/_augment.py` に `build_soten_history`/
  `soten_history_aggregates`/`attach(soten=)`（leak-safe・参照 `build_hist` と数値等価）。
  `src/constants/_feature_cols.py::MYSPEED_FEATURE_COLS`（列名/列順契約）。
  `scripts/jrdb_build_features.py --with-myspeed`（**既定 off**＝本番 featured を汚さない）。
  `scripts/myspeed_adoption_check.py`（本番準拠の採用検証・`--keep-odds`／オッズ除外が既定）。
- **#23 解決**: `src/training/_data_splitter.py` に stacking split 診断
  （`stacking_class_balance`/`single_class_splits`＋`__diagnose_stacking_splits`）。single-class を
  期間/行数/pos/neg 表で明示停止し、アーカイブ二層署名(rows==races かつ pos=0)を検出して
  `--since-year 2015` へ誘導。根因＝1986-2014 の壊れたアーカイブ層（1レース1行・rank未生成）。
- **#24 学習側**: `src/training/_feature_contract.py::FeatureContract`（列名/順序/dtype・align は
  不足=fail-fast/余分=drop/順序固定/dtype検査）。`KeibaAI` が学習時 `feature_contract_` を採録。
  **推論挙動は不変**（推論側 `.values`→align 置換は別 PR・#24 にコメント）。
- **STEP5 取込基盤**: `docs/jrdb_target_downloads.md`（8種の仕様台帳）、
  `scripts/jrdb_target_probe.py`（実ファイル Phase2-3 自動解析）、
  `src/jrdb/_target.py`＋`scripts/jrdb_target_ingest.py`（**全8種**を正規化 pickle 化・
  race_id/horse_id は `_keys` で標準形へ）。**重複/漏れ検証**: アーカイブ sha1 重複排除・未分類の
  名前ベース明示（8種を先に分類し非TARGETは読まない）・種別ごと自然キー重複除去
  （ランクは source_date×person_code で日次時系列を保持）。
  ユーザ実測: 8種計 798,007行・キー復元100%・重複0・漏れ0 を確認済。

### 3. 次に着手すべきタスク（優先順）
1. **成績IDM の冗長チェック（安価・先に潰す）**: `data/jrdb_target/jrdb_target_idmse.pkl` の
   `seiseki_idm` を `raw_jrdb_sed.idm`（同 race_id,umaban）と突合。一致＝取込済の再パッケージで新規性ゼロ確定。
2. **外厩コメント PoC**: `jrdb_target_gaikyucomment.pkl` を featured へ (race_id,umaban) 結合→特徴量化
   （放牧先カテゴリ / 帰厩→今走日数 / 中N週）→ `myspeed_adoption_check` 系の CI下限>1.0 ハーネスで
   「本番モデルへ純増分＋市場直交」を検証。年次パック(2016-2026)で履歴も作れる。#22 の轍を踏まぬよう
   本番準拠 baseline(オッズ除外・stacking・--since-year 2015)で判定。
3. **#24 推論側 PR（別ブランチ）**: `_model_wrapper.fit` の `.values` 学習・`StackingModel._stream_input`・
   `app/_model_eval.py:213/339/440` の `.values` 推論・`KeibaAI.calc_score` の 0埋め reindex を
   `contract.align` へ寄せ、警告解消＋列順 parity を CI 化（ライブ featured は列常在・値NaN のため require_all で安全）。
4. **（低優先）**: 展開/番手/IDM 馬印・ランクの年次パック取込（履歴要時）。JRA archive の pre-2015 層は
   壊れたまま残存（運用は常に --since-year 2015 で実害なし・featured 作り直し時の課題）。

### 4. 検証規律（厳守）
- 目的は確率品質（＋私的情報の市場直交性）。ROI は必ず OOS＋placebo＋レース単位ブロック bootstrap CI下限>1.0。
- 採用判定は**本番準拠 baseline**で（市場オッズは学習除外・stacking・--since-year 2015）。簡易 harness の
  高 AUC は評価条件差の産物になり得る（0.835 の教訓）。
- 全作業を `claude/review-context-and-tasks-1vxigl` に commit/push（footer: Co-Authored-By / Claude-Session）。

前回更新: 2026-07-29（**JRDB 全面 overwrite + Phase 1 ベースライン確定**。netkeiba の JRA scrape
が 2023-2025 で崩壊(1レース約3頭)・1986-2020 は目的変数ゼロと判明 → JRDB SED で JRA 2015-2026 を
全面上書き。featured 全年 rows/race≈14 に修復・蘇生。`baseline_jrdb`: Place AUC 0.8076 / Win 0.8228、
複勝ROI 79%、ただし**市場を上回るエッジ無し**（logloss 市場<モデル）。詳細は下「## 2026-07-29
JRDB 統合基盤」内「⚠️ 訂正・重要転換」）

前回更新: 2026-07-27（`0bkff7` 由来の運用基盤 docs 3-29a〜g を統合。
8券種×買値帯ROI帰無・直前signal走査・_order_model リファクタ。総括は「## 2026-07-26 総括」。
末尾に「## 2026-07-27 レーティング Phase 2-5 の隔離検証」＝冗長で確定を追記）

前回更新: 2026-07-25（市場アンカー確率スタック Step1〜5 完成＋監査ハーネス。
詳細は「## 2026-07-25 セッションまとめ」）

前々回更新: 2026-07-19（分離NN 不採用、データ量 2010≈2015 で頭打ち、
運用標準「GBDT スタック × --since-year 2015」確定。オッズ累積を本番実証。CI/PR #18 整理済み）

---

## 現状サマリ（2026-07-31）

作業ブランチ: `claude/review-context-and-tasks-0bkff7`。データ（pickle/DB）はユーザのローカルのみ
（本コンテナのDB/pickleは空 → 重い検証はユーザがローカル実行し出力を貼る運用）。JRDB 資格情報は
env `JRDB_USER`/`JRDB_PASS`、スクレイピング間隔≥4秒、モデル識別子はチャットのみ（成果物に書かない）。

### 1. これまでの検討事項（すべて結論つき）
- **回収率エッジ探索＝終了（結論: 公開/半公開データにトレード可能なエッジ無し・帰属まで確定）。**
  約20テスト（単勝/連系/多層/オッズ時系列/意思決定系/ABM/分布ベット/卍購入ゾーン）が全て控除率未満。
  - EV線形性: `E[p·odds]=E[p]·odds` → p̂ の分布はEVを動かさず、サイジング/リスクのみ。控除(~20-27.5%)≫実測エッジ(~6-12%)。
  - オッズ追従の中心定理: `êdge≈(1−ρ)(p_ML−q^fin)` → 追従精度↑で edge↓。
  - **卍式購入ゾーン(#1)**: OOF＋walk-forward＋感度分析＋除上位5 で厳密棄却（min_n=1000でゾーン形成ゼロ）。
  - **Δability（市場直交した過去能力残差）**: 簡易baselineには陽性(ΔLL−0.00277)だが、**本番全モデル比較で増分なし(NO-GO)**。
    市場直交は情報を失う誤操作＝陽性の正体は「過去能力の代理」と帰属確定。
  - **本格MySpeed Phase1（factorial分岐）**: 距離コースz正規化=有害(水準破壊)、ペース非対称=raw上で純増分なし
    (M2p−M1=−0.00009・プラセボと区別不能)。累積版の見かけ改善はM2副作用の修復だった。→ **区間ラップ以降は不着手**。
  - 核心的知見: 正規化/残差化/直交化/ペース補正を足すほど悪化。**本番が十分強いなら生の情報を渡しモデルに
    条件付き関係を学ばせる方が良い**（GBDT系で既知の現象を丁寧な帰属分離で確認）。
- **回収率重視（＝損失最小化）の運用層＝実装完了。** ①確率モデル層(的中率=AUC/logloss)と②馬券ポリシー層
  (EV/Kelly=回収率)の2層アーキ。②に `BetPolicyFukushoHonmei`(複勝本命 top_n=1) を配線し、評価の払戻源を
  JRDB SED に統一（複勝カバレッジ 46%→**100%**）。実測 return_rate=**0.862**（控除未満は不変・これが実務解）。

### 2. 実装済み機能（要点）
- **JRDB 統合基盤** `src/jrdb/`: 取得(`_fetch`)/冪等取込(`_store` INSERT OR REPLACE＋台帳)/
  パーサ(`_parser`＋`_layouts` byte-accurate・cp932・16進日)/アダプタ(`_adapter` SED→netkeiba raw)/
  fill・overwrite(`_fill`)。KYI/SED/SKB/TYB/CYB/CHA/HJC/KKA/UKC/SRB/KSA/CSA/KTA/BAC/KAB 対応。
- **featured 基盤**: JRDB SED で JRA 2015-2026 を全面上書き（netkeiba scrape 崩壊の修復）。全年 rows/race≈14。
- **2層モデル**: LightGBM×NN スタッキング＋isotonic（`rank`=top3/`rank_win`=1着）。`baseline_jrdb_seirei`
  Place AUC≈0.807 / Win≈0.823。②ポリシー層 `src/policies/`（`_score_policy`・`_bet_policy`）。
- **評価/払戻**: `src/simulation/_jrdb_return_source.py::JrdbReturnSource`（ReturnProcessor互換・SED払戻100%）。
  `app/_model_compare.py::simulate_model`（return_processor DI）。→「予測→意思決定→評価」が揃った完全検証環境。
- **検証スクリプト群**（すべてleak-safe・OOF/walk-forward・push済）: `manji_purchase_zone.py`(卍#1)、
  `market_ability_residual.py`(Δab＋`--vs-production`最終ゲート)、`myspeed_staged_gate.py`(Phase1 factorial分岐)、
  `loss_min_operating_point.py`(運用点)、他 roi/odds/decision/abm 系。純ロジックは単体テスト付・ruff clean。

### 3. 次に着手すべきタスク（＝実装フェーズ・探索ではない）
- **[Issue #22] Integrate raw historical MySpeed into production pipeline**（唯一残る工学タスク）。
  内容: leak-safe raw MySpeed 履歴集約 `raw_last/mean3/max5/ewm/trend`（horse_id群・race_id sort・shift≥1）を
  featured pipeline へ配線。参照実装 `scripts/myspeed_staged_gate.py::build_hist`（テスト済）。
  本番への実測増分は ΔLL≈**−0.00357** / AUC 0.803→0.809（全年改善・プラセボ死・薄履歴非依存）。
  - **採用前 validation（ROIは評価項目に含めない・回収率エッジは終了済）**: ①raw_npast別性能 ②欠測フォールバック
    ③ECE単独評価 ④学習/推論の列名・列順固定（feature-names warning解消）⑤学習・推論一致 ⑥importance監視 ⑦学習/推論コスト。
    （年別改善・レース内プラセボ・薄履歴非依存は `--vs-production` で同等厳密に確認済）。
- **Non-goals**: 区間ラップ/馬場内時間変化/位置取りコーナーロス（実証的な橋なし・不着手）。ROI/馬券ポリシー変更なし
  （実務解は複勝本命≈0.862のまま）。市場直交/条件内z置換/ペース非対称の再探索（帰属確定済・広げない）。

---

## 2026-07-31 Issue #22 実装：raw MySpeed を本番 featured へ配線

作業ブランチ: `claude/review-context-and-tasks-1vxigl`（PR #21 = `0bkff7` ベース）。研究フェーズで
唯一「本番モデルへ純増分あり」だった raw MySpeed（過去 SED 素点履歴）を、実験スクリプト
`scripts/myspeed_staged_gate.py::build_hist` から**本番 featured パイプラインへ昇格**した。

### 実装（すべて leak-safe・純ロジックはテスト付・ruff clean）
- **`src/jrdb/_augment.py`**:
  - `build_soten_history(sed_paths)`: SED を ketto×日付で読み、素点(soten)の trailing 集約を生成。
  - `soten_history_aggregates(h)`: 純ロジック（I/O 非依存・テスト対象）。inclusive rolling で
    `jrdb_ms_last/mean3/max5/ewm(α=0.3)/trend/npast` を算出。
  - `attach(..., soten=)`: `prev_trouble` と同じ `merge_asof(backward, exact不可)` 機構で
    「今走より前の最新走」の集約を貼る＝当該走除外の leak-safe。soten 未指定は全 NaN（後方互換）。
  - `JRDB_COLS` に `MYSPEED_COLS` を追加（`--drop-jrdb` の A/B 対象に含める）。
- **`src/constants/_feature_cols.py`**: `MYSPEED_FEATURE_COLS`（6列）で列名・列順を固定
  ＝学習/推論の列順ドリフト・feature-names 警告を防ぐ契約（checklist ④）。
- **`scripts/jrdb_build_features.py`**: `build_soten_history` を駆動し `attach(soten=)` へ渡す。
  カバレッジ表示・サイドカーに `jrdb_ms_*` を追加。
- **`tests/jrdb/test_myspeed.py`**（新・10 assert）: 集約値／初走 trend=NaN／attach の leak-safe／
  デビュー戦 NaN フォールバック／soten 無しの後方互換／**参照 `build_hist` の shift(1) 定義と数値等価**。

### 設計判断
- **なぜ asof-inclusive が build_hist の shift(1) と等価か**: 履歴行 M の inclusive 集約を asof が
  「今走の直近過去走 M=N−1」で選ぶため、featured 行 N が受け取る値 = build_hist 行 N の
  shift(1) 集約に一致（last/mean3/max5/ewm/trend/npast すべて）。テストで固定。
- **生値のまま渡す**（正規化/残差化/直交化/ペース補正なし）＝ Phase 1 の帰属結論を踏襲。
- GBDT は featured の float 列を自動採用するため、`jrdb_ms_*` を featured に足すだけで学習に流れる。

### 採用検証ツール `scripts/myspeed_adoption_check.py`（新）
featured_jrdb.pkl 単体で「jrdb_ms_* あり/なし」の OOS 確率品質増分を測る（DB/prod_p 非依存）。
未来分割(年<cutoff 学習 / ≥cutoff 評価)で LightGBM baseline vs +MySpeed を同一列順で学習し、
ΔlogLoss/ΔAUC/ECE・年別改善・プラセボ(レース内シャッフル)・過去≥3走・importance・列順パリティを
出力（＝Issue #22 checklist に対応・ROI 非評価）。純ロジック(make_target/ece/select_feature_cols)は
tests/scripts/test_myspeed_adoption_check.py で固定。
- 使い方: `python scripts/myspeed_adoption_check.py --featured data/featured_jrdb.pkl --cutoff-year 2024`
  （単勝で見るなら `--win`）。判定目安: ①ΔlogLoss≈−0.0036 かつ②過半年改善・③プラセボで消失・
  ④過去≥3走でも負・⑤ECE 非悪化 → 採用可。

### 実データ検証の結論（2026-07-31・確定）
配線実行は成功（jrdb_ms_* カバレッジ 73.64%/trend 57.52%・featured 669,034行）。だが採用検証で **No-Go** が確定:

| 条件（複勝・前進分割 <2024学習/≥2024評価・147,415行） | baseline AUC | +MySpeed | ΔlogLoss | ΔAUC | 年別 | placebo |
|---|---|---|---|---|---|---|
| **市場オッズ除外（本番 _DROP_FOR_TRAIN 準拠・公平）** | 0.83467 | 0.83224 | **+0.0032** | **−0.0024** | 0/3 | 逆 |
| 市場オッズ込み（従来） | 0.83257 | 0.83484 | −0.0032 | +0.0023 | 3/3 | −0.0025（77%残存） |

- **本番準拠（オッズ除外）では MySpeed は明確に悪化**＝現行 featured モデルへの採用は棄却。
- オッズ込みの陽性も placebo で77%残存＝実固有分は −0.00076 のみ（木構造変化/列サンプリング/乱数揺らぎに帰属しうる）。
- 元ゲート `myspeed_staged_gate` の「M1 raw=勝者(+0.006)」は **baseline=prod_p(AUC≈0.803)＝弱いモデル**限定。
  強い現行 featured(≈0.835) には効かない＝featured が MySpeed 情報を既に内包。
- 「0.833 は単勝列由来」説は棄却（オッズ除外でも baseline 0.83467・むしろオッズ込みの方が低い＝本番がオッズを
  学習除外するのは正しい）。
- 配線は残すが**既定 off**（`jrdb_build_features.py --with-myspeed` opt-in・既定で本番 featured を汚さない）。
  `src/jrdb/_augment.py::build_soten_history`/`soten_history_aggregates`・`scripts/myspeed_adoption_check.py`
  （`--keep-odds` で市場込み比較）は証拠として温存。

### 次の監査対象＝prod_p(0.803) vs featured再学習(0.835) の同一性（MySpeed ではない）
`scripts/myspeed_adoption_check.py` の baseline は簡易 LightGBM（570列べた・生ID含む・stacking/isotonic無し・
既定params・前進分割）で、本番スタック(`baseline_jrdb_seirei`)とは別物。0.803↔0.835 の +0.027 が本物か
（評価条件差/残存リークでないか）を、本番の実 retrain/評価で条件一致させて確認する:
- 一致必須: 目的変数定義 / JRAフィルタ・評価行 / 学習・OOS期間 / 欠測行の扱い / LightGBM params / sample weight /
  race単位処理 / 特徴除外リスト（特に `--no-id-features`＝生ID drop で +0.011 既知）/ calibration後処理 /
  prod_p を出した model・commit。
- 手順: `python scripts/jrdb_build_features.py --jrdb-dir data/jrdb_dl --extract-to data/jrdb_extract
  --out data/featured_jrdb.pkl`（MySpeed off で再生成）→ `python -m src.pipeline.run_pipeline retrain
  --jra-only --no-stacking --version-name featured_recheck --featured-path data/featured_jrdb.pkl` の
  **ログ Place AUC** を `baseline_jrdb`(0.8076) と比較。

### 決着（2026-07-31・確定）＝0.835 は harness 差の産物・隠れた改善なし
本番 retrain（--no-stacking・現行 featured）の結果: **Place auc_test=0.8093**（central_turf 0.8084 /
central_dirt 0.8077 / central_hurdle 0.7835）・Win 0.8312・n_races 128,621。**deployed baseline_jrdb
(0.8076) と実質同一**。→ 採用検証の 0.835 は評価条件差の産物と確定。差の要因:
(1) 特徴量数 = 本番 prep は **471 used features**、素べた採用検証は **570**（本番が落とす ~100列を残し AUC 底上げ）。
(2) 分割 = 採用検証は前進分割(test=2024-26・147k行)、本番は別 split(test≈7749)。
- **結論: 現行 featured を本番条件で回すと 0.808＝deployed。隠れた +0.027 は無い＝featured の天井。**
  MySpeed 採用も「featured 全体で本番更新」も **No-Go で完全終了**。公開/半公開データで追える改善は尽きた
  （本プロジェクトの「卍/公開データにエッジ無し」大結論と整合）。次に結論を変えうるのは新モダリティ
  （時系列オッズ全軌跡・馬場バイアス）か JRDB 未使用モダリティのみ。
- 備考→**別Issue化して対処**: `--jra-only` stacking の「one class」障害＝#23、`X does not have valid
  feature names`＝#24。

### #23（stacking one-class）解決＋本番再現の最終確定（2026-07-31）
`src/training/_data_splitter.py` に stacking split 診断ガードを追加（`stacking_class_balance`/
`single_class_splits` 純関数＋`__diagnose_stacking_splits`）。根因を診断表で特定:
**1986–2014 のアーカイブ二層（1レース1行・rank 未生成＝pos=0）が日付順分割で base/meta に入り
single-class 化**（stacking バグでなくデータ層）。ガードが rows==races かつ pos=0 のアーカイブ署名を
検出し `--since-year 2015`（運用標準）へ誘導。
- **`retrain --jra-only --since-year 2015 --featured-path data/featured_jrdb.pkl` で stacking 完走**:
  **Place(stacking) auc_test=0.8108**（central_turf 0.8039/central_dirt 0.8119/central_hurdle 0.7888）・
  Win 0.8256・n_races 40,012。
- **本番再現の最終結論**: deployed baseline_jrdb 0.8076 ≈ 本番条件(stacking+since2015) 0.8108 ≈
  --no-stacking 0.809。**採用検証の 0.835 は harness 差の産物と production-grade で確定。隠れた改善なし。**
- 残（低優先）: featured に pre-2015 アーカイブ層が残存（運用は常に --since-year 2015 で実害なし）。
  featured 作り直し時の課題。`rank_win` リーク列も featured 生成源で落とすべき（#24 と同系統）。

### STEP5 新モダリティ調査: JRDB TARGET DL 全8データ解読（2026-07-31）
`docs/jrdb_target_downloads.md`＋`scripts/jrdb_target_probe.py`（実ファイル Phase2-3 自動解析）を新設。
実ファイルで全種別を解読:
- 馬印系（外厩=gaikyu・展開/位置取り=itidori・番手=bante・IDM=idm）＝固定長 `[ketto8][コード]`・cp932。
  展開/番手/IDM は枠/脚質/jrdb_idm の派生＝新規情報なし。
- 外厩コメント gaikyucomment＝CSV・キー(場+年+回+日+R+馬番)=(race_id,umaban)直結・値=放牧先名␣帰厩日␣中N週。
- 厩舎/騎手ランク＝CSV utf-8・trainer/jockey 集計と重複気味。
- **成績IDM idmse＝CSV・キー18桁(date+場+回+日+R+馬番)・符号付IDM＝レース後到達IDM＝raw MySpeed 同族
  (No-Go族)かつ raw_jrdb_sed.idm と重複疑い**。
**結論: 真に新規で市場直交しうるのは「外厩(放牧先の質・帰厩鮮度)」のみ**。次アクション=
(1) 成績IDM vs SED.idm 冗長チェック（安価）(2) 外厩コメント PoC を CI-下限ハーネス（myspeed_adoption_check 系）へ。
全データに年次パック(2016-2026)あり＝履歴バックフィル可。

### #24（feature schema parity）学習側 完了・推論側は別 PR（2026-07-31）
base/stacking モデルは `.values`（位置ベース）で fit/predict するため、学習=推論で列の
追加/削除/並び替えがあっても検出されず silent 誤予測する。対策の基盤を実装:
- `src/training/_feature_contract.py`: `FeatureContract`（names/dtypes・from_frame/align/to_dict）。
  align は不足=FeatureContractError・余分=drop・順序固定・dtype 検査/coerce。純ロジック・テスト済。
- `src/training/_keiba_ai.py`: 学習時に `feature_contract_ = FeatureContract.from_frame(X_base_train)`
  を採録・保存（feature_names_ と併存）。**推論挙動は不変**。全 training 262 passed。
- **スコープ分離（ユーザ合意）**: 本コミット群は「学習側の契約保存＋parity test」まで。
  **推論側の全面置換は独立 PR**: `_model_wrapper.fit` の .values 学習、`StackingModel._stream_input`、
  `app/_model_eval.py:213/339/440` の .values 推論、`KeibaAI.calc_score` の 0 埋め reindex(line 352)を
  `contract.align` へ寄せる＋sklearn 警告解消＋列順 parity を CI 化。ライブ featured は列は常在（値NaN）
  確認済のため require_all（不足列=エラー）で安全。

---

## 2026-07-29 JRDB 統合基盤（netkeiba 欠損年を JRDB で補完し featured 拡張）

作業ブランチ: `claude/review-context-and-tasks-0bkff7`（= PR #21）。目的は「netkeiba の
年度限定を JRDB で補完・上書きし、学習データの年レンジを拡げる」。**結果 = 2022 が
featured に 0→46,840 行、2021 が 2,040→21,225 行。当初ゴール達成。**

### 全体像（`src/jrdb/`）
1. **取得・冪等取込基盤**（既存＋今回拡充）: `_fetch.py`（会員エリア Basic 認証・index パース・
   年度パック/単体の自動選別・sha1 台帳で重複防止）／`_store.py`（`raw_jrdb_<type>` へ
   INSERT OR REPLACE=keep-last、処理済みファイル台帳）／`_parser.py`＋`_layouts.py`（cp932・
   16進日・byte-accurate）／`scripts/jrdb_fetch.py`・`jrdb_ingest.py`（`--type` で形式指定）。
2. **対応形式（全 byte-accurate・単体テスト付）**: KYI(第11版フル)/SED(第4版a・376B フル)/
   SKB/TYB/CYB/CHA/HJC/KKA/UKC/SRB/**KSA・CSA**(騎手/調教師マスタ・dir=Ks/Cs)/
   **KTA**(登録馬)/**BAC**(番組=raw_race_info 源)/**KAB**(開催=going/天候源)。
   - **マスタ(KSA/CSA)は最新スナップショットのみ取得**（kinds=single/latest=1）。年度パックを
     keep-last で積むと同一コードが上書き合戦し stale 化するため。
   - 取得堅牢化: 壊れた zip/HTML は例外でなく警告スキップ、非アーカイブ DL は台帳に載せず
     再取得可能に。
3. **同一性 crosswalk**（`_crosswalk.py`, Phase1）: 重複年の `(race_id, 馬番)` で
   JRDB コード↔netkeiba ID を多数決対応。**ただし horse_id は crosswalk 不要**（下記）。
4. **アダプタ**（`_adapter.py`, Phase2-3）: JRDB SED → netkeiba raw スキーマへ写像。
   `build_raw_results` / `build_raw_race_info` / `build_raw_horse_results`。**2018 重複年で
   実データ検証: 着順/単勝/人気/タイム/馬名 99-100%、race_type/course_len/weather/
   ground_state 100%、horse_results 着順/距離/タイム 100%**。
5. **fill パイプライン**（`_fill.py`＋`scripts/jrdb_fill_netkeiba.py`, Phase4）: 対象年の
   新規 race_id / 全年の新規 (horse_id,日付) だけを netkeiba raw pickle へ union。
   dry-run 既定・`.bak` 退避・**NAR 非改変・既存行非上書き**。

### ⚠️ 訂正・重要転換（2026-07-29 夜）: netkeiba JRA scrape が壊れており JRDB 全面 overwrite へ

Phase 0 健全性ゲート（`scripts/fill_missing_audit.py` + doctor + `leakage_check.py`）と学習で、
下記「netkeiba 欠損は 2021-2022 だけ」が**誤り**と判明。実態:
- **1986-2020: 目的変数 `rank`/着順 が全欠損**（pos_rate=0.000・約3000行/年の疎シード）→ 学習不能。
- **JRA 2023-2025: netkeiba scrape が 1レース約3頭しか無い**（2022 の JRDB 由来13.7頭に対し崩壊）。
- **NAR 2024-2025: 1レース約1頭**（netkeiba NAR scrape も崩壊。JRDB に NAR 無し）。
- **JRDB SED は 2015-2026 全年 約14頭/レース・完全均一** → JRDB が唯一の完全 JRA ソース。

→ 方針を「fill（欠損 race_id 追加のみ）」から **「overwrite（対象年の JRA race_id を JRDB で置換）」**
へ転換（`jrdb_fill_netkeiba.py --mode overwrite --year 2015..2026 --apply`）。NAR は race_id 別系統で
保護。**結果: featured 全年 rows/race≈14・pos_rate≈0.21。2015-2020 蘇生 / 2023-2026 修復。**
featured は 669,034 行 / 150,716 レースへ。
- 副作用: JRDB SED は条件フラグ（barei/handi/kokusai/kongo/hinba/kyushu/race_class/**枠番**）を
  持たず overwrite で NaN 化（全年均一＝分布不連続なし）。**KYI/BAC join で回収可能＝次の改善余地**。

途中で塞いだ 6 バグ（すべて単体テスト付・このブランチ）:
1. `dedup_raw_pickle.py`: raw_results 主キー重複1771行を keep-last 修復（netkeiba 日次再スクレイプ由来）。
2. overwrite の防御的 keep-last dedup（`_fill.drop_race_ids`/`race_ids_of`）。
3. retrain/backtest `--jra-only`（場コード01-10・`_model_category.central_index_mask`）で壊れ NAR 除外。
4. `retrain --since-year 2015` で目的変数ゼロの 1986-2014 を除外。
5. DataSplitter `__coerce_object_features` を nullable 拡張dtype(pd.NA)対応に拡張（LightGBM の
   NAType 落ちを解消。予測側 `_coerce_for_predict` と対称化）。
6. ReturnProcessor: 特払い `'特'` を非数値として頑健化（backtest の int() 落ちを解消）。

### Phase 1 ベースライン（`baseline_jrdb`・JRA 2015-2026・スタッキング・2024-2025 OOS backtest）
| 指標 | 値 |
|---|---|
| Place AUC (test) | **0.8076** |
| Win AUC (test) | 0.8228 |
| category turf/dirt/hurdle | 0.8011 / 0.8086 / 0.7883 |
| ROI 複勝(信頼) | 79.3% |
| ROI 単勝 / ワイド(信頼) | 71.7% / 70.1% |
| オッズ帯 15-50 ROI | 83.1%（最良帯も控除割れ） |
| 勝ち馬logloss モデル/市場 | 1.9403 / 1.9007（**市場に勝てず**） |
| エコー相関 r̂,p_mkt / ΔR²(in-sample) | 0.959 / +0.0006 |

**判定**: モデルは**良較正**（band別 r̂≈実勝率）で AUC 0.80 だが、**市場を上回るエッジは無い**
（勝ち馬logloss 市場<モデル・全券種/全帯で ROI<100%）。プロジェクト全体の「公開データにエッジ無し」
と整合。旧 0.906 は壊れ/退化データの過大評価で、**0.80 台がクリーンデータの実力値**。JRDB 統合の
価値は「学習不能 → クリーンな 2015-2026 コーパスで学習可能」にした点。exotic(馬連〜3連単)は
的中<30 で評価不能（参考値）。

### Phase 2 A/B: JRDB 追加特徴はいずれもエッジ無し（決着）
overwrite は race_info/results を JRDB SED-minimal で置換し 枠番/性齢/条件フラグを NaN 化した。
KYI（出馬表・race_id×馬番）から回収して A/B（`_adapter.build_raw_results(kyi=...)`・
`_results_processor._select_columns` の whitelist に jrdb_* を追加して featured へ通す）:

| モデル | 追加特徴 | Place AUC | 勝ち馬logloss | ΔR²(in-sample) | echo |
|---|---|---|---|---|---|
| baseline_jrdb | （なし） | 0.8076 | 1.9403 | +0.0006 | 0.959 |
| baseline_jrdb_kyi | 枠番 | 0.8030 | 1.9380 | +0.0006 | 0.959 |
| baseline_jrdb_idm | 枠番+IDM/基準オッズ/脚質/情報/騎手指数 | 0.8075 | 1.9420 | −0.0005 | 0.958 |
| **baseline_jrdb_seirei** | **上記＋性齢/性/年齢（全部入り）** | 0.8064 | **1.9416** | **−0.0004** | **0.962** |

- **枠番（公開・構造）**: 効果なし。馬番+頭数でほぼ決まる冗長情報＋市場織込済。
- **JRDB 独自指数 IDM/基準オッズ等（市場直交の“はず”）**: **効果なし。ΔR² が正→負転**
  （「独立情報あり」→「市場の写し」）。IDM/基準オッズは公開フォームの再要約・implied 確率の
  再導出で **市場の echo** と判明。
- **性齢（性・年齢）追加（KYI 再取込で sex_code を入れ回収）**: 効果なし。全部入りでも Place AUC
  横ばい・logloss 市場未達・ΔR² 負のまま・**echo 相関は最高の 0.962**（特徴を足すほど市場に漸近）。
- **結論（重要・再利用可）**: 「公開データにエッジ無し」を **KYI から回収できる全特徴（構造＋
  JRDB 独自指数＋性齢）まで含めて確定**。JRDB の価値は**データ完全性（欠損/崩壊の修復・年レンジ
  拡張）に限られ、対市場エッジは無い**。特徴を増やすほど市場の写しに漸近するだけ。今後 edge を
  探すなら公開フォームの再要約でない真に独立な情報源（未取得）が必要。
- 品質ゲート（本作業ブランチ）: 全 pytest **2147 passed / 18 skipped / 0 failed**、ruff clean。
- 実装は保持（`_KYI_INDEX_COLS`・whitelist 配線）。将来別データを試すとき同ハーネスで即 A/B 可能。

### JRDB オッズ O* の正体訂正 ＋ 連系 Test A（連系エッジ路線も閉包）
- **OZ/OW/OU/OT/OV は「基準オッズ(フェア)」ではなく実市場オッズ**と実測確定（OZ 単勝 overround
  Σ(1/odds)≈1.35＝控除内包。1.25 の締切値より高く前売り/前日の早い時点）。TYB は直前(≒最終)の
  実市場単複（`tansho_odds`+`odds_time`）。JRDB のフェア値は KYI `kijun_odds`（馬ごと単値）の方。
  `_odds.py`/`jrdb_odds_ev_check.py` のラベルを訂正済み。
- これで「連系事前オッズは自前蓄積必須」（`exotic_edge_data_design.md`）の前提が消え、既存 JRDB
  O*＋HJC で連系 ΔR² を測定可能に → **Test A**（`scripts/jrdb_exotic_dr2_check.py`）:
  馬連の実市場オッズ(OZ) vs 単勝由来 Harville を、当選ペアの平均 logloss で比較（10,437レース）。
  | 予測器 | logloss | argmax的中 |
  |---|---|---|
  | Harville(単勝由来) | 3.4576 | 14.51% |
  | Market(馬連実市場・前売り) | 3.4874 | 14.55% |
  → **Δ(Harville−Market)=−0.0298**（**単勝Harvilleが馬連市場を上回る**）。
- ⚠️ **Test A の head-to-head は測る問いが誤り**（指摘で判明）。正しくは「単勝Harville×馬連市場の
  **合成(combining logit)** が賭ける相手=馬連市場を超えるか」。→ **Test A' combining ΔR²**:
  in-sample +0.0097 / OOS(期間H1↔H2) +0.0088,+0.0105（**両方向+＝過学習でない本物の信号**・
  α0.67β0.53）。**単勝Harville は前売り馬連に独立情報を持つ**（前売り馬連プールが薄く非効率）。
- **Test A'' EV バックテスト（money test・HJC 最終払戻で精算・OOS）**: 合成 +EV 馬連を前売り選定→
  確定払戻で精算。**合算 ROI −22.7%（H1→H2 −21.7% / H2→H1 −23.9%）≈ 馬連控除 −22.5%**。
- **正確な結論**: ΔR² +0.009 は**前売り(stale)オッズに対して**本物だが、**pari-mutuel は最終プールで
  精算**するため**最終では消える**（ROI=−控除）。**前売りの非効率 ≠ tradeable エッジ**。連系も
  「賭けて勝てるエッジ」は無い（ただし理由は「相関premium無し」でなく「前売り非効率が締切収束」）。
- 総括: 単勝＝効率(echo)、連系＝前売り非効率だが最終精算で消滅、JRDB 全特徴＝echo。**公開＋準公開の
  全経路で exploitable な対市場エッジ無しを確定**。残るは真に非公開な新データ源（映像CV/私設計時 等）。

### 層別 ΔR²（プール平均の null が層内でも成立するか・規律つき）
「エッジ無しはプール平均で、層別(競馬場/頭数帯/距離帯/race_type)の異質性は未検証では」の指摘に
対応。`scripts/jrdb_stratified_dr2.py`（in-sample スクリーン→OOS 年A↔年B 両方向検証。多重比較の
罠を回避）を baseline_jrdb_seirei・holdout 2024-2025・JRA で実行:
- **★生存は 頭数帯≤8頭 の1層のみ**（in +0.0027 / OOS +0.0008,+0.0018）。他は全て 死(in≤0) or
  棄却(OOSで消失)。
- **だが実用エッジではない**: (a) OOS 到達 ~10 層で null 期待生存 ~3 に対し観測1＝**多重比較の
  ノイズ範囲内**（むしろ null 寄り）。(b) magnitude ΔR² 0.001-0.003 は Benter の黒字化 0.0178 の
  1/10〜1/20 で、**JRA 控除 20-25% を全く超えられない**（本物でも賭けて勝てない）。
- **結論**: 層別・TE 由来のエッジも exploitable な水準では**存在しない**を規律つきで確定。
  ≤8頭は理論的に最も非効率らしい唯一の候補だが、確認するなら 2026 の第3 OOS＋有意性(bootstrap)が
  必要で、かつ magnitude 的に投資対効果は無い。**エッジ探索は全経路 NO-GO で完全クローズ。**

### 決定的に判明した netkeiba の実態（重要・再利用可）
- **featured の唯一のソースは `results.pkl` 単一ファイル**（`_abstract_data_processor` の複数
  pickle 併読は**コメントアウト済**。seed_results.pkl は読まれない）。RangeIndex＋キーは**列**
  （results/race_info=race_id列、horse_results=horse_id列）。
- **netkeiba の欠損は 2021(部分)・2022(全欠)だけ**（1986-2020・2023-2026 は充足）。
  place/around/time/age/race_class＋条件フラグ(barei/handi/kokusai…)は **2023 以降のみ充填**。
  → **fill 方針: 全年充填の race_type/weather/ground_state/course_len/date＋成績だけ埋め、
  recent-only 列は NaN**（2023 前後の分布不連続を回避）。`FILL_RACE_INFO_KEEP` に定義。
- **horse_id は 2 方式**: seed 年(≤2021) = `9…` 代理キー、scraped 年(2023+)と
  `raw_horse_results` = **`20…` = 生年+血統登録番号下6桁 = `ketto_to_horse_id(ketto)`**。
  → **fill は canonical `20…`（ketto_to_horse_id）を使う**（horse_results と結合し過去走特徴量が
  効く）。crosswalk の horse は seed 代理で不適。jockey/trainer は式が無いので crosswalk 継続。
- **netkeiba は JRA のみ、NAR は JRDB に無い** → 一括上書きは NAR を消すので不可。**補完（fill）**が正。
- **馬場状態コード = 十の位が going**（1x良/2x稍重/3x重/4x不良、一の位=速/遅）。天候 1晴2曇3小雨
  4雨5小雪6雪。異常区分あり(取消/除外/中止)の netkeiba 着順は **NaN**（マーカー無し）。

### 付随修正（性能）
- `perf(featured)`: `_feature_engineering.__label_encode` の新規ID採番が `max()` をループ毎に
  再計算し **O(N²)**。fill で新規 horse_id が激増し encode が事実上停止。max をループ外へ出し
  **O(N)** 化（採番値は不変）。→ merge後の FE＋encode が 30分超停止から **5秒**に。

### 運用（週次で回すなら）
`jrdb_fetch → jrdb_ingest（--force で SED 等は再パース）→ jrdb_fill_netkeiba --apply →
featured 再生成`。SED レイアウト変更後は `jrdb_ingest --type SED --force` で該当形式のみ再取込。

### 未了・オプション（次セッション候補）
- fill 年(2021-2022 各~46k)は recent 年(2024=25k)より密（結果に問題は無いが密度差あり）。
- fill 行の 性齢/枠番/馬主 は NaN（SED に無い）→ 必要なら UKC(性別)・馬番+頭数(枠番)で補完可。
- **retrain で実効果測定**（`--holdout-years 2022` で walk-forward 健全性・精度/ROI）。
- raw_peds は netkeiba 既充足(馬マスタ)＋UKC は父母母父のみ → fill 見送り。HJC→raw_return_tables は未。

---

## 2026-07-26 総括（検討事項・実装済み機能・次タスク）

作業ブランチ: `claude/review-context-and-tasks-60m5xy`（model-6split-mainline の全調査アークを
統合マージ済み）。詳細な検証台帳は下記「B2〜B9」を参照。本節は3点の早見表。

### ① これまで検討したこと（＝全経路で「控除後 edge 無し」を確認）

| 経路 | 内容 | 結論 |
|---|---|---|
| 公開データ残差 | r_θ(543列)・中央/地方/市場フリー3軸アブレーション | 帰無（中央ゼロ・地方はFL再較正のみで控除以下） |
| JRDB VOI | 前日IDM/基準オッズ/18指数 × 市場アンカー残差（単勝） | 帰無（市場は効率的） |
| 早期オッズ路線 | 10時単勝オッズ→確定の変動に edge があるか | 帰無 |
| 三連単×展開指数 | 2/3着の順序に JRDB 展開予想が効くか（位置特化PL） | **較正は改善**（ΔNLL有意）だが ROI 帰無 |
| 物理MC＋ベイズσ | agent_race＋Kalman能力事後σを着順分布に伝播 | 較正ラダー確立（線形<物理<ベイズσ）だが ROI 帰無 |
| 8券種×買値帯 | 単複/枠連/馬連/ワイド/馬単/三連複/三連単 × オッズ帯 | **CI下限>1.0 のセルはゼロ＝全帰無**（決定的） |
| 未使用直前signal | 展開指数群/idm/激走/万券/上昇度/出遅れ の OOS較正走査 | 較正は約2倍に拡大（−0.0223）だが「較正≠ROI」帯 |

**確定した中核結論**: 公開データ・JRDB・オッズ変動・連系(統計/物理/ベイズ)・8券種×買値帯の
**全経路で JRA市場は控除後に効率的＝exploitable edge 無し**。較正は改善できるが控除の壁で
ROI に化けない。合理的運用は「賭けない／最小分散」。

### ② 実装済みの機能（本ブランチに push 済み・全テスト green 1953）

- **市場アンカー確率スタック Step1〜5**（`_market_residual`/`_harville`/`_mixture_pl`/
  `_ability_filter`/`_bayes_kelly`）＝各段が帰無で市場に厳密退化。詳細は §2026-07-25。
- **監査ハーネス**: `_model_compare`（ΔNLL/ECE/Bootstrap/LRT/d_kl・事前定義判定）/
  `_rolling_origin`（未来見遮断）/ `_pnl_objective`（elogw・placebo 2軸・MDD/ES）/`_risk_metrics`。
- **JRDB取込**: `src/jrdb/`（_layouts 全オフセット照合済み・KYI枠番@324追加・SED早期オッズ・
  TYB直前・SKB特記／_parser/_augment/_extract）。
- **8券種フルROIハーネス** `multibet_roi_test.py`: 単勝(帰無基準)/複勝/枠連/馬連/ワイド/馬単/
  三連複/三連単。複勝=`prob_place_place_strength`（place=win で Harville一致）/枠連=枠集約
  （Σ=1.0検証）/買値範囲=フェアオッズ帯別ROI＋train定義帯のOOS。**レース単位ブロック
  bootstrap 95%CI下限>1.0** を edge の必要条件に（点推定の偽陽性を機械棄却）。
- **順序モデル共通ハーネス** `src/simulation/_order_model.py`（新規リファクタ）: load_races／
  place_probs_from_signals／trifecta_nll／fit_signal_coef／KYI_SIGNALS(9指数)／TYB_SIGNALS／
  CENTRAL_CODES を一元化。trifecta_jrdb_test / multibet_roi_test / trifecta_roi_test /
  chokuzen_signal_scan の重複 load と root相互import を解消（挙動保存を数値一致で確認）。
- **直前signal走査** `chokuzen_signal_scan.py`: 未使用KYI指数の単独＋joint OOS較正を
  ΔNLL/placebo/CI で評価。

### ③ 次に着手すべきタスク（優先順）

1. **未検証の新モダリティ**（唯一「結論を変えうる」＝件数増ではなく新情報）。archiveファイルが
   要る:
   - **時系列オッズの全軌跡**（朝の資金移動の速度・加速＝"賢い金"のタイミング）← 要 KOZ系ファイル。
     唯一まだ触れていないオッズ・モダリティ。
   - **馬場バイアス**（重馬場×脚質×枠の交互作用・含水率・決まり手）← 要 馬場情報ファイル。
   - 情報チェック◎○▲△×（クラス/適距離別）・距離別最高IDM ← liveのみ or SED履歴から計算。
   いずれも入手できれば同じ **CI下限>1.0 ハーネス**に載せて検証する。
2. **9指数拡張版の実配当再確認**（ユーザー環境・return_tables）: KYI_SIGNALS を9指数に拡張済み。
   `python multibet_roi_test.py --jrdb-dir data/jrdb_txt --strategy topk` で CI下限>1.0 が
   出ないことの最終確認（予測は帰無）。
3. **物理系ローダーの統合**（任意・低優先）: bayes_physics_trifecta / physics_trifecta_test /
   sed_early_odds は race dict 形が異なる（rec/umas/ab_sigma）ため _order_model 未統合。
   希望あれば挙動保存手順で統合可能。
4. **（撤退option）**: 新モダリティが無ければ「効率市場の確認完了」で一区切り。運用は EV較正＋
   損失最小化（既に本番配線済み）に戻す。

**検証規律（厳守）**: ①成功は事前定義（後知恵の閾値調整禁止）②ROI単独で判断せず**CI下限>1.0**
③placebo は必ず対照（残差/オッズ2軸）④帯・サブセット選択は train のみ（多重比較は placebo で検定）
⑤スクレイピングはポライトネス≥4秒⑥JRDB の PHPSESSID セッショントークンはコミット禁止。

---

## 2026-07-25 セッションまとめ（市場アンカー確率スタック Step1〜5＋監査ハーネス）

背景: 卍ハイブリッド（①.5 ベイズ補正）は公開データで ROI エッジ無しを確定
（JRA単0.87/複0.81・NAR単0.80/複0.75・placebo 非有意）。その後ユーザー（専門家）の
批判的レビューを受け、「真の分布を当てる」から「市場が間違っている差分（Edge）を見つける」
へ設計を転換。5段の機構を段階実装した（各段が独立に OOS 検証可能な入れ子構造）。

### A. 完成した機構（全段が「帰無で市場に厳密退化→賭け0」）

強度分解: `s_i(t) = log q_i + μ_i(t) + c_i(t) + r_θ(x_i) + β(style_i, z)`

- **Step1 市場アンカー残差** `src/policies/_market_residual.py`:
  P=softmax(log q + r)。r≡0 → P≡q → 控除で全EV<1 → 賭け0（正しい帰無）。
  market_anchored_nll（l2/l1 正則化=市場から離れるには証拠を要求）。kl_from_market（VOI）。
- **Step2 複勝γ補正** `_harville.place_probs_corrected`: Benter σ/τ で top-3 marginal を
  補正。γ=δ=1 で素の Harville と厳密一致・Σ=3 保存。evaluate_pnl(place_exponents=) 配線。
  fit は既存 _calibrate.fit_and_save_place_exponents。
- **Step3 Mixture-PL** `src/policies/_mixture_pl.py`: P=Σ_z P(z)·softmax(log q+r+β(style,z))。
  β は脚質×状態の 4×3=12 パラのみ・λ_β‖β‖²・状態内中心化。β≡0 で Step1 に厳密退化。
  **識別性の重要知見: 一様P(z)+勝者NLL では β 識別不能**（反対称効果が周辺化で相殺）。
  pace_pressure 由来の情報ある P(z)（精度70%）があれば fit_beta が符号・大小を回収。
- **Step4 状態空間能力** `src/policies/_ability_filter.py`: 抽象 AbilityFilter
  （initial/predict/update/strength/variance/predictive）。FixedGainFilter（Elo=定常ゲイン
  Kalman 近似）と KalmanAbilityFilter（厳密・能力RW＋調子AR(1)・2×2共分散持ち回り）。
  静的極限=正規-正規閉形式に厳密一致。休み明け=分散膨張を predict が表現。
- **Step5 ベイズKelly** `src/portfolio/_bayes_kelly.py`: 能力事後→MC(100サンプル)→PL→
  事後予測 p̄→E[logW] 最大化（凹計画）。**λ 係数は存在しない**（縮小は p̄ に創発）。
  理論知見: E[logW]は確率に線形→f*は p̄ だけで決まる。Jensen の向きは凸性で分岐
  （本命 p>1/2 は分散で p̄低下/中穴は上昇=λでは表現不能な効果）。

### B. 監査ハーネス（実データ前に整備済み）

- `src/simulation/_model_compare.py`: compare_models（ΔNLL/ΔBrier/ΔECE + Bootstrap CI +
  LRT + d_kl_market）。成功条件を事前定義: ΔNLL<0 ∧ 有意 ∧ ΔECE≤+0.005（ROI単独判断禁止）。
  interval_calibration（PIT・予測区間被覆率。σ過小申告=過信を検出→Kelly過大賭けを遮断）。
  calibration_by_odds_band（人気帯 1/2-3/4-6/7+ 別 ECE・bias。favorite-longshot bias の局在検出）。
  voi_per_cost（ΔKL/特徴量取得コスト。JRDB費用対効果用）。
- `src/simulation/_rolling_origin.py`: rolling-origin 評価（〜Y学習→Y+1評価の前進）。
  **fit 関数にはテスト年より過去しか物理的に渡らない**＝較正パラメータの未来見を構造遮断。
- `src/simulation/_pnl_objective.py` evaluate_pnl: elogw（E[logW] joint配分）/ predictive
  上書き口 / placebo（残差シャッフル）+ **placebo_odds（オッズシャッフル・アンカー寄与の単独検査）**/
  MDD・ES_5 常時報告（評価のみ・目的関数に入れない）。
- `src/simulation/_risk_metrics.py`: max_drawdown / expected_shortfall。

### B2. 実データ検証台帳（2026-07-25 確定・残差ヘッド監査完了）

P(z) 予測器（実データ）: 23,292レース・pooled OOS ΔlogLoss=−0.226/acc=0.572 → **成立**。
β(style,z) 実データ: β→0 に縮退・ΔNLL≈0 → **市場は展開×脚質を完全に価格化**（正常終了）。

残差ヘッド r_θ（543列・rolling）の success=True を3軸アブレーションで監査した結果:
- **中央のみ**: ΔNLL=−0.00044, CI=(−0.00146,+0.00063) → **有意性なし。JRA にエッジ無し確定**
- **地方のみ**: ΔNLL=−0.00661（全体の10倍・fold一致）。正体は**NAR 市場の FL バイアス**
  （人気1 bias −0.0226=1番人気を2.3pp 過小評価）の再較正。ただし過補正・ΔECE悪化・
  賭け6点＝**控除以下で搾取不能**
- **真の市場フリー**（単勝*/人気*/odds* 全除去・539列）: ΔNLL=−0.00007 n.s. →
  **公開特徴量に市場外情報は存在しない**（確定）

過程で監査系が検出・修正したもの: オッズ欠損レースの疑似裁定（Σ1/odds<1 の Dutch book を
E[logW] が拾う→控除ガード Σ≥1.02）/ 残差キャッシュの重複インデックス位置ずれ/
no-market の派生列持ち込み（単勝_log が重要度1位で発覚→名前ベース全除去）。
placebo 2軸（残差/オッズ）が異常源を毎回正しく局在化した。

**結論: 公開データの情報は市場に対して価値ゼロ（中央）〜FL再較正のみ（地方・控除以下）。
JRDB 等の直交情報の ΔKL がこの台帳を超えた分だけが「買う価値のある情報」。**

### B3. JRDB VOI 検証（2026-07-25・自己完結テスト）

JRDB 取込を実データで完全検証（KYI/SED/SKB のオフセットを公式仕様書＋実ファイルで照合）。
`jrdb_selftest.py`: KYI(前日IDM/基準オッズ/18指数)×SED(確定単勝オッズ=市場/着順)を
(race_id,馬番)結合し、2025年JRA 3,455レースで市場アンカー残差＋事前定義判定。

**結果: JRDB は確定市場を一切超えない（VOI=0・完全帰無）。**
- KYI18指数＋前走トラブル(SKB・75%被覆/15%陽性) 全20列 → scale=0.00, ΔNLL=0.00000,
  ΔKL=0.00000, success=False。nll_best(無制約)すら nll_market と完全一致（改善0）。
- ハーネス健全性は確認済: 同経路のポジティブ対照(人工真勝率+雑音市場)で scale=0.90/改善+0.045。
- **市場オッズ自身を特徴量に入れても scale=0**（netkeibaでは0.40）→ JRDB確定オッズは
  FL曲線再較正の余地すら無いほど良較正。**netkeibaの"success"は情報でなくオッズ曲線再較正
  だった**ことの追認。

含意: JRDB前日情報（能力指数・展開予想・前走トラブル＝卍核）は、最終確定JRA単勝市場に
完全に織り込まれている。公開データ台帳と一致（効率的市場）。**エッジは最終市場より非効率な
所にのみ有り得る**: 早期オッズ(Layer2スナップショット・遅い資金流入前)/連系プール/NAR。
注意: 本テストは(1)最終確定オッズ=最難ベースライン(2)単勝のみ。

**OOS再確認(2026)**: 2025学習→2026 out-of-sample 682レースでも scale=0.00/VOI=0.00000/
success=False。両年計4,541レースで帰無が再現。JRDB×最終市場は確定的に決着。

### B4. 早期オッズ路線 VOI（2026-07-25・帰無で決着）

パリミュチュエルは配当が最終オッズ確定なので「早く賭ける」自体に edge 無し。唯一の筋＝
「市場の動き（早期→最終）が最終価格にすら織り込まれない情報を持つか」を2経路で検証:
- `odds_drift_test.py`（scheduler収集の odds_snapshots 版）: だが実データは143レースのみ・
  大半30分前で検定力不足。
- `sed_early_odds_test.py`（JRDB SED版）: **10時単勝オッズ**（SED@297・SEC項目順×実データ
  magnitude三点一致で確定: @291複勝/@297単勝/@303複勝）を歴史的早期市場に使い、
  2025-2026 JRA **5,321レース**で検証。drift中央−0.31（70%の馬が朝→締切で延びる）と
  明確な変動があるにも関わらず → **scale=0.00/ΔNLL=0.00000/success=False（完全帰無）**。

**結論: 10時→確定の変動は最終価格に完全織り込み済み。早期オッズ路線も最終市場に対し edge 無し。**
副産物: fit_residual_head の SEガードを 1SE→0.25SE に緩和（有意性の本丸は OOS compare_models）。
SED layout に odds_10_tansho/odds_10_fukusho/kakutei_fukusho_shita を追加（実データ検証済み）。

**公開データ・JRDB・オッズ変動の全経路で「最終JRA単勝市場は効率的＝edge無し」が確定。**
残る未探索の非効率: 連系プール(三連単等・要 exotic payoff)・NAR(JRDB非対応)。

### B5. 三連単×JRDB展開指数 VOI（2026-07-25・**初の正シグナル**）

単勝は全経路で帰無だが、残る未探索＝連系プール。仮説「群衆は単勝(P1着)を効率的に価格
づけするが三連単の順序(2/3着)に非効率が残り、JRDBの着順予測がそれを捉える」を検証:
- `_harville.prob_trifecta_place_strength`: 1着=市場単勝π, 2/3着=任意place強度σ=τ の位置特化
  トリフェクタ（place=win で素Harvilleに厳密一致＝帰無）。
- `trifecta_jrdb_test.py`: 2/3着の place 強度を JRDB の goal_juni(予測着順)/ichi_idx(位置指数)で
  調整（coef を train の trifecta NLL 最小で fit）。SED の実着順で listwise NLL を評価。
- **結果(2025-26中央4,526R): ΔNLL=−0.01087 CI95=(−0.0194,−0.0031) 有意改善／
  placebo(signalシャッフル)で改善消失＝本物。** 調査全体で初の有意な正シグナル。
  理論どおり: 単勝は効率的だが連系の順序に非効率が残り JRDB 展開予想が捉える。

**Stage B（実配当ROI・2025-26中央1,829R照合）で決着＝帰無**:
- topk戦略(各5,487点): Harville 0.671 / JRDB込み 0.740 / **placebo 0.759**。
  **placebo>JRDB** かつ 3者とも ROI≈0.72≈(1−控除)。JRDBの順序予測は控除を1ミリも動かさず。
- ev戦略(市場=Harville): baseline 0点(帰無綺麗) / JRDB 576点ROI0 / placebo 465点ROI2.63。
  だが ev は賭け少・三連単の極端分散でノイズ（placebo2.63は万馬券1点の偶然）。topkが真値。
- **結論: Stage A の較正改善(ΔNLL−0.023)は本物だが控除27.5%を超えない＝「較正改善≠ROI」を実証。**
  連系路線も帰無。post-hocサブセット探索は規律上せず（多重比較で偽edge量産のため）。

### B6. 全調査の最終結論（2026-07-25・完全決着）

市場アンカー残差＋2軸placebo＋事前定義判定＋OOS という厳密枠組みで、全経路を検証した結果:

| 経路 | 規模 | 結果 |
|---|---|---|
| 公開データ543特徴量 × 最終単勝 | 中央 | 帰無（地方FL再較正のみ・控除以下） |
| JRDB前日情報(IDM/指数/卍核) × 最終単勝 | 2025+26 OOS | VOI=0 |
| オッズ変動(10時→確定) × 最終単勝 | 5,321R | VOI=0 |
| JRDB展開予想 × 三連単順序（較正） | 4,526R | **有意改善 ΔNLL−0.023** |
| 同上 × 三連単 **実配当ROI** | 1,829R | **帰無（placebo>JRDB・控除超えず）** |

**総括: JRA市場（単勝・三連単とも）は効率的で、我々の持つ全情報（公開データ・JRDB基礎・
JRDB展開予想・オッズ変動）は控除後に exploitable なROIを生まない。** 唯一の本物の信号
（JRDB展開予想の三連単順序較正）も控除27.5%に飲まれる。「較正改善≠儲かる」を実証。
検証機構自体は健全（多数のバグ/artifactを2軸placeboが検出、ポジティブ対照で信号検出力を確認）。

残る理論的未探索（本セッションでは非対象・データ/期待値の観点で優先度低）:
- NAR連系（薄い市場・JRDB非対応でnetkeiba特徴量ベース）
- 他券種の実配当ROI（馬連/ワイド等・三連単で帰無なら期待薄）
- JRDB以外の直交情報源（現状候補なし）

### B7. 物理MC＋ベイズ能力更新の三連単（2026-07-26・較正ラダー確立・ROIは帰無）

ユーザー指摘（ベイズ更新も物理MCも三連単で未使用）を受け段階的に強化:
- 物理MC(agent_race を JRDB駆動・市場アンカーplace強度): Stage A ΔNLL−0.046(placebo確認)。
  だが full-sample Stage B ROI(2,366R): topk baseline0.628/物理0.644/placebo0.627＝帰無。
- **ベイズσ物理**(bayes_physics_trifecta.py): Kalman能力事後σを as-of 更新し物理MCに伝播
  （本物のベイズ更新）→ Stage A ΔNLL−0.057（全モデル最良）。能力不確実性が着順に効く実証。
- **較正ラダー**: 線形JRDB −0.023 < 固定σ物理 −0.046 < ベイズσ物理 −0.057（単調改善・全placebo確認）。
- **しかし ROI は全て帰無**: 較正改善(〜0.05nats)が控除(27.5%)に桁違いに小さい。「較正≠ROI」を
  線形/物理/ベイズの3モデルで実証。JRA三連単プールは順序edgeが控除に届かないほど効率的。
- 注意: 物理simの beta<0（既知ペース符号誤差を反転利用）。_pace_model.py 修正で伸びる余地は
  あるが、較正がROIに化けない以上、優先度低。

### B8. 8券種の網羅＋卍・買値範囲（2026-07-26・機構完成／実配当ランはユーザー環境）

ユーザー指摘2点（①検証は6券種のみで**複勝・枠連**が欠落／②卍の**買値範囲**＝価値はオッズ帯に
偏るのに帯を無視して一律賭けしていた）を機構として同時に埋めた。

- **8券種を全て配線**: 単勝(帰無基準)/複勝/枠連/馬連/ワイド/馬単/三連複/三連単。控除は
  複勝・単勝20%＜枠連・馬連・ワイド22.5%＜馬単・三連複25%＜三連単27.5%（`_takeout`）。
  - **複勝**(単一馬・控除最低20%): `prob_place_place_strength`（top-n を 1着=win/2・3着=place強度・
    place=win で素の Harville prob_place に一致・検証済み Σ P(top3)=3.0）。決済は
    `place_payoff_lookup_from_returns`（NAR/最近も可）。**place強度が最も直接効く候補**。
  - **枠連**(枠1-8ペア): KYI@324 の枠番を追加（JRA分割規則と実データ一致を確認）。枠連確率＝
    該当馬ペアの馬連(place強度)確率の和（互いに排反な top2 事象＝Σ=1.0 検証済み）。決済は
    `multi_bet_payoff_lookup(rt,'wakuren')`（買い目トークンは枠番）。
- **買値範囲（卍）**: 賭けの**フェアオッズ=1/市場的中率**で帯分割(<5/5-15/15-50/50-200/200+)し
  **帯別ROI**を出力。さらに **train で最良帯を選び→test で同じ帯だけ賭ける**（帯選択は train のみ
  ＝OOS・事後掘り無し。ただし K帯からの選択は多重比較のため placebo と比較して判定）。
- 機構検証（return_tables 不要ぶん・全て pass）: 複勝 place=win==Harville / FUKUSHO dispatch /
  枠連 全ペアΣ=1.0 / 8券種の backtest 経路（複勝=単一・枠連=枠集約含む）が合成払戻で走行。
  Stage A 三連単 ΔNLL−0.00972(placebo+0.006) も回帰なし。
- **実配当ラン=ユーザー環境**（return_tables はこのコンテナに無い）:
  `python multibet_roi_test.py --jrdb-dir data/jrdb_txt --strategy ev`（topk/--with-tyb も）。
- 事前予想（B7までと整合）: 複勝・枠連・低控除券種でも**控除の壁は不変**の公算。ただし卍の主張は
  「特定帯に価値集中」なので、帯別・train定義帯の判定で**局所的な控除超えが出るか**が最終確認点。

### B8-結果. 実配当ラン（2026-07-26・ユーザー環境 5,411R・信頼区間ベースで帰無確定）

判定を厳格化: 点推定ではなく**レース単位ブロックbootstrap の 95%CI下限>1.0 かつ placebo超え**のみ edge。
5構成（ev / topk / --with-tyb / --top-m 6 / --max-bets 5）× 8券種 × 5買値帯 × (full＋train定義帯)を
全走査し、**CI下限>1.0 のセルはゼロ**。

- **最決定的パネル=topk**（各5,523点・的中数百本＝CI最も締まる）: 全券種が (1−控除) の床に張り付く。
  単勝0.759[CI 0.726,0.795] 複勝0.835[0.817,0.855] 枠連0.793[0.745,0.843] ワイド0.801[0.759,0.841]
  馬連0.770[0.713,0.831] 馬単0.727[0.652,0.802] 三連複0.858[0.763,0.962] 三連単0.681[0.534,0.839]。
  CI上限すら全て<1.0（最大 三連複0.962）＝控除ぶん負ける効率市場の教科書的署名。
- **点推定★は全て偽陽性と判明**: ev三連単1.435(的中6本)→CI下限0.209、--max-bets5で0.905に回帰
  （max-bets=3で拾った万馬券ノイズと確定）。枠連15-50の1.76/三連複15-50の1.43(的中1-2本)→CI下限0.000。
- **--with-tyb の点推定>1（複勝1.32/馬連1.59/三連単2.26）も全て偽物**: ①係数が全て負＝train過学習の兆候
  ②TYB直前情報は一部レースのみで標本激減(単勝771→197点) ③全CI下限<1.0（多くは0.000）
  ④placebo も標本枯渇で0.000＝比較不安定。小標本×高オッズ帯のファットテールそのもの。
- **卍・買値範囲の反証**: 低分散・高カウント帯（統計的にedgeが見えるはずの帯）は全て床。1.0超えは
  的中一桁×高オッズ帯だけで CI下限0で即棄却。「特定帯に価値集中」は本データ・本手法では検出されず。

### B9. 未incorporated直前signalのOOS較正走査（2026-07-26・chokuzen_signal_scan.py）

ユーザー指摘「直前情報でまだ取り込めていないものを分析」。order signal は goal_juni/ichi_idx の
2つだけだったが、KYIにパース済みで**未使用の11指数**（idm/ten/pace/agari/dochu/go3f/gekiso激走/
manken万券/joushoudo/start/deokure）を単独＋joint で OOS ΔNLL＋placebo＋bootstrap CI 走査。

- **新規の較正signalを複数発見（単独★・placebo消失）**: go3f_juni(後3F順位)/start_idx(スタート
  指数)/deokure_rate(出遅率)/dochu_juni(道中順位)。特に **start/出遅れは位置指数と直交する新次元**。
- **joint（フル直前suite）ΔNLL=−0.0223 CI95(−0.035,−0.010) placebo+0.038** ＝placebo消失で本物。
  goal+ichi のみの −0.0097 から**約2倍に拡大**。joint非ゼロ: goal/ichi/idm/ten/pace/agari/gekiso/
  manken/joushoudo。→ trifecta_jrdb_test の KYI_SIGNALS を9指数に拡張済み（Stage A で再現確認）。
- **規律（重要）**: −0.0223 は既知の較正ラダー「線形JRDB −0.023 → ROI帰無」とほぼ同じ帯。だから
  **予測は依然ROI帰無**。ただしこれまでで最強の較正セットなので、multibet_roi_test に流し込み
  **CI下限>1.0 を満たすか**をユーザー環境で最終判定する（要 return_tables）。較正≠ROI は不変。
- **archive無しで未検証の直前源**（今後の候補）: 時系列オッズ全軌跡（要KOZ系）/馬場バイアス・
  含水率・決まり手（要馬場ファイル）/情報チェック◎○▲△×・距離別最高IDM（liveのみ or SED計算）。

### C. 最終結論（信頼区間ベースで完結）
公開データ・JRDB・オッズ変動・連系(統計/物理/ベイズ)・**8券種×買値帯（CI下限判定）**の
**全経路で JRA市場は効率的＝控除後 edge 無し**が確定。較正は改善できるが ROI に化けない（控除の壁）。
新データ源（真に直交する情報）が無い限り、モデル改良の余地は「較正の精緻化」に留まり ROI は動かない。
合理的運用は「賭けない／最小分散」。本セッションの全調査アークはここで閉じる。

1. **P(z) 予測器**: LightGBM（SHAP で説明可能性維持・NN 不採用）。入力=逃げ頭数・
   pace_pressure・内枠率・馬場等 → 3状態。教師はレース結果の前後半ラップ or 通過順分布。
2. **rolling-origin で Step1 残差ヘッドの実データ検証**: featured の既存特徴量で
   r_θ を学習し、pooled ΔNLL・人気帯別較正・placebo 2軸で判定（期待値: 公開データでは
   Edge≈0 に着地=正常終了。これがベースライン台帳になる）。
3. **fit_beta 実データ**（P(z) 予測器完成後）。4. **Kalman超パラ（q_μ, ρ, s_c², R）の
   rolling fit**。5. **JRDB 投入 → VOI（ΔKL）/コスト評価**（唯一のエッジ候補）。

### D. 検証規律（このセッションで確立・厳守）

- 成功判定は事前定義（後知恵の閾値調整禁止）。ROI 単独で判断しない。
- placebo は 2軸（残差・オッズ）。公開データで Edge>0 が出たらまずリーク/過学習を疑う。
- 較正パラメータは必ず rolling-origin の fit_fn 内で作る（ハーネス外での fit は禁止）。

過去の最終更新: 2026-07-13（§10-3 の n_horses=1 根本修正をコード完了 — ResultsProcessor を race_id ベース groupby size に。
featured 再ビルド→walk-forward 再確認はデータ環境で要実走）

前々回更新: 2026-07-13（§10 展開×脚質シミュレーション調査を追加 — データ欠陥修正で「展開 NO-GO」を偽陰性と確定し、
固定距離エンジンで距離勾配まで物理再現。ただし市場エッジではない）

前回更新: 2026-06-29（blissful ブランチから診断・評価ツール群を移植 + 目的変数リーク `rank_win` の知見を反映）

---

## 2026-07-19 セッションまとめ（分離NN・データ量・オッズ累積・運用標準）

> このセクションは 2026-07-19 の作業（PR #18 branch `claude/context-review-next-tasks-g6y5dt`）の要約。
> 規律は不変: **集約統計を実測に合わせるのみ／logloss・回収率は最適化しない／マーケット・エッジは存在しない
> （AUC は予測精度の話で別軸・ライブ賭けはしない）**。

### A. これまでの検討事項（このセッションの探究と結論）
1. **オッズ力学の根本原因と累積保証（④）**: `evaluate-odds-dynamics` 全 NaN の原因＝各レース単一時刻取得。
   DB 実証で「元から単一時刻」と確定（コード/保存/評価/スキーマは正常、欠落は取得ランタイム運用のみ）。
   → 累積保証（回帰テスト・`check_capture_health`・`odds_watch --loop/--schedule`）。
   **本番運用で同一 race_id に captured_at 20回・約30分幅で累積、post_time/minutes_to_post/phase 正常、
   単勝 1.0〜620倍でキャップ無しを実データ実証**。
2. **per-model 探索（⑤・GBDT）**: `--resume-tuning` で探索を積み増すとスタック改善（Place 0.8156→0.8212）。
   `tuned_base_models.json` に4モデル best を集約。十分な探索予算があれば base-model AUC チューニングは有効
   （物理較正とは別軸の正当な予測精度改善）。
3. **GPU 化**: xgb(device=cuda)/cat(task_type=GPU)/NN(torch 自動)を `--gpu`。LightGBM は CUDA(Blackwell sm_120,
   CUDA 12.8+)ビルド成功だが GPU 効果薄（ヒストグラム構築のみオフロード・CPU 実装が既に速い）。**GPU の本命は xgb/cat/NN**。
4. **分離NN + 遅延スタッキング → 不採用**: 「NN を外して GBDT に全データ、NN は別途最適化して meta 融合で
   多様性を足す」仮説を実装（`--nn-standalone` / `build-combined` / `CombinedModel` / leaderboard）。
   - 埋め込み設計: NN は horse_id 等を Entity Embedding。**horse_id（17万頭・中央7走・約850万パラメータ）が
     識別子を丸暗記し test 期に転移しない**と特定（GBDT が horse_id を drop するのと同理由）。除外可能化
     （`nn_entity_exclude`）で NN 単体 test AUC **0.59→0.67**。
   - 探索の教訓: NN は in-era val を最適化するほど test へ過適合（3時間/507 trials の best 0.6321 が
     1トライアル 0.6745 を下回る）。**in-era val は test の代理にならない**。
   - **融合寄与 Δ = −0.0003 ≈ 0**（2024 holdout・GBDT 単勝ヘッド＋NN 勝ち＋`--target rank_win`）→
     **NN は本番不採用**（経路・コードは将来再検証用に保持）。
5. **データ量 A/B → 2015 頭打ち**: 同条件（3モデルスタック・2024 holdout）で比較。
   2015+（437k, Win **0.8346**）vs 2010+（685k, Win **0.8350**）＝ **Win +0.0004(誤差)・Place −0.0045(悪化)**。
   データ量頭打ち・古いデータは非代表（軽い concept-drift）。→ **全データ化不要、`--since-year 2015` を標準**。
   full(177万行)は最終スタック学習で RAM 床が高く OOM、しかも精度は伸びない。

### B. 実装済みの機能（このセッションで追加）
- **retrain フラグ**: `--float32-features`（数値 float64→float32・ピーク RAM 半減）、`--nn-standalone`（NN 単体学習）、
  `--nn-config` / `--nn-trials`（trial 数上書き）/ `--nn-timeout`（時間予算探索 例 3h）、`--resume-tuning` / `--tune-models`（既出）。
- **build-combined**: 分離NN×GBDT の meta 融合を保存＋Δ計測。`--target {rank,rank_win}`（単勝/複勝で整合）・`--meta-years`。
- **NN 探索**: 各中間層の独立ユニット幅、`weight_decay`、`nn_entity_exclude`（horse_id 除外）、
  leaderboard（top_k+dedup 永続化＋ウォームスタート）、fit ログに構造(hidden=[...])・lr 表示。
- **GPU**: `--lgb-gpu {gpu,cuda}`、`scripts/build_lightgbm_gpu.sh`（OpenCL/CUDA・Boost 1.90・Blackwell 対応）。
- **オッズ**: `scripts/odds_watch_all.sh`（全8券種3分間隔・将来検証用）、`odds_watch --start-at/--stop-at/--schedule`、
  `check_capture_health`、`diag_odds_snapshots.py` の DB 診断。
- **config**: `configs/base_models_gbdt.json`（運用標準）、`configs/base_models_lightgbm.json`（LightGBM 単独・軽量）、
  `configs/base_models_nn_tuned.json`（NN 探索空間）。
- **CI 修正**: `_model_wrapper` の `predict_proba` を `np.asarray` で包み mypy overload エラー解消。

### C. 運用標準（確定）
- **予測器 = GBDT スタック（LGB×XGB×Cat）を `--since-year 2015` で学習**。Win auc_test **0.8346**（内部 test）/
  **0.8401**（2024 out-of-sample）。
- コマンド: `retrain --with-tuning --resume-tuning --base-models-config configs/base_models_gbdt.json --since-year 2015`。
- **NN 不採用（Δ≈0）・全データ不要（2015 頭打ち）**。詳細は README 4-2 / `configs/base_models_gbdt.json` の `_comment` / PR #18 本文。

### D. 次に着手すべきタスク
1. **2024 holdout での券種別 backtest（OOS ROI 確認）**: `retrain --holdout-years 2024` 済みモデル →
   `calibrate-ev` → `backtest --years 2024`。※市場エッジ検証の範囲、ライブ賭けはしない。エッジ不在(ΔR²≤0)の再確認が目的。
2. **本番運用スクリプトの整合**: `scripts/weekly_retrain.sh` 等が運用標準（`base_models_gbdt.json` + `--since-year 2015`）を使うよう更新。
3. **オッズ全券種アーカイブの継続蓄積**: 開催日に `odds_watch_all.sh` を回し連系 ΔR² 等の将来検証データを蓄積。DB 容量監視（三連単は 4,896 組/レース/tick で急拡大）。
4. **（任意）NN 経路の整理判断**: 不採用確定。`--nn-standalone` / `build-combined` を「実験的」と明示するか保持のままにするか。
5. **PR #18 のマージ判断**: CI グリーン・本文整理済み。base（`claude/happy-archimedes-10bmev`）へマージするか、さらに検証を積むか。
6. **未解決の技術メモ（低優先）**: 全データ GBDT の最終スタック学習が RAM 床で OOM（DataSplitter の分割スパイク＋大型 tuned モデル）。
   全データ不要と判明したため優先度低。将来必要なら DataSplitter のコピー削減・モデルサイズ上限で対応可能。

---

> ⚠️ **重要（2026-06-29・blissful から統合）**: 目的変数リーク `rank_win`（着順==1）が
> **特徴量として学習されると** held-out 単独 AUC=1.0 でモデルが勝ち馬を当てる漏洩になる。
> 本ブランチは `rank_win` を**目的変数（単勝ヘッド）として作り、特徴量からは DROP 済み**で
> 設計上クリーン。ただし**リーク除去後の単一LightGBM walk-forward では単勝回収率 ~0.685
> （全期間・全 fold で負け、本番<ランダムプラセボ）＝エッジ未確認**。これまでの「回収率
> ~1.9–2.1」は、リーク混入時の featured で学習した場合の値であり**そのまま信用しない**こと。
> 本ブランチの先進モデルも含め、エッジ主張は移植した OOS ツールで再検証してから扱う。
> **移植ツール**（リポジトリ直下／`scripts/`）:
> - `walk_forward.py` — 時系列 OOS（拡張窓 walk-forward）。今後の施策の honest 評価土台。
> - `debug_leak.py` — held-out 単独AUCで漏洩列を炙り出す（勝ち・top3 両目的）。
> - `debug_backtest.py` — 券種別バックテスト指標の内部矛盾／賭け馬vs勝ち馬の診断。
> - `optimize_bet_types.py` — 券種別最適化のヘッドレス実行（検証区間のみ）。
> - `scripts/ingest_missing.py` — 欠落開催日だけを冪等取込する差分インジェスト。
> - `kelly_backtest.py` の年別判定が「回収率に関わらず本物のエッジ」と誤表示していたのを修正。
>
> ### 🚨 重大な保留（2026-06-29 夕）: 下の「エッジ無し」結論は**フォーム欠損で confound・無効**
> データ完全性を実測したところ、**コアの過去成績特徴（着順_mean_5R 等＝馬のフォーム）が
> 全年代で 93–96% 欠損**（非NULL率 0.04–0.07）。原因は horse_results の大半未取得
> （runaway ingest の「未取得22,624頭」が実態）。**モデルは競馬で最重要のファンダ「馬の過去成績」を
> ほぼ与えられずに学習していた**。よって下記「エッジ無し／市場効率」は**フェアな比較ではなく、
> データを揃えるまで無効**。拡張データ(yoso0.74/調教0.96/パドック1.0/jockey_py)は十分埋まっていたが、
> 肝心のフォームが欠けていた。
> **要対応**: `backfill-horses`（~20h）→ `rebuild-featured` → `retrain` → walk_forward --quality /
> predict_quality / no-odds ΔR² を**再実行**してから結論を確定する。
> （見立て: フォーム投入で logloss は市場へ近づくはず。だがフォームは公開情報ゆえ利益エッジ ΔR²>0 は
> 依然低確率。それでも「市場効率」を主張するには完全データでの ΔR²≤0 確認が必須。）
>
> ### エッジ探索の（暫定）結論（2026-06-29・honest OOS / ⚠フォーム欠損データでの値）— エッジは存在しない
> 漏洩除去後、4つの独立な honest OOS レンズが全て「エッジ無し」で一致（※上記のとおりフォーム欠損で要再検証）:
> 1. 通算 walk-forward OOS（2023–2026）= 回収率 **0.685**（全 fold 負け）。
> 2. validate_edge プラセボ比較 = 本番 0.74 < ランダム 0.89（**モデルはランダムより悪い**）。
> 3. `walk_forward.py --by-odds` = 意味のある全オッズ帯で <1（3–7倍の◎は72件＝ノイズ）。
> 4. **ベンター市場合成 OOS**（`retrain --holdout-years 2025 2026` → `calibrate-ev --years 2025`
>    → `backtest --years 2026 --calibrate --blend --corrected-harville --edge-diagnostic`）:
>    単勝/複勝/ワイド等すべて <1（ALL信頼 51.4%）。`--edge-diagnostic` が本質を明示 —
>    **エコー相関(r̂,p_mkt)=0.939、勝ち馬logloss モデル2.04 > 市場1.96、ΔR²=−0.0049（負）、
>    最適合成 α=0.069/β=0.958（市場96%:モデル7%）**＝モデルは市場の写しで独立情報ゼロ
>    （むしろ市場より下手）。
>
> 5. **純ファンダ ΔR² テスト**（`retrain/calibrate-ev/backtest --no-odds-features`。calibrate-ev に
>    同フラグが無く列不一致で落ちるバグを修正済み）: オッズ特徴を全除外したモデルでも
>    **エコー相関 0.834（0.939から低下）だが ΔR²=−0.0040（負）・logloss 2.15>市場1.96・
>    合成 α≈0/β≈1.02（較正がモデルに『ゼロ重み』を付与）**。→ **市場はファンダ情報を完全織込み済み**で、
>    手元の特徴量から作るどんなモデルも市場を超えない、が決定的に確定。
>
> **結論**: 拡張データ（調教/パドック/予想印/Elo）+ 先進モデル（スタッキング/ベンター/r̂較正）を
> 総動員しても、市場（単勝オッズ AUC≈0.84）を超える独立情報は無く、控除率20%を覆すエッジは
> 存在しない。**実戦投入は禁止**。今後の施策は必ず上記 OOS ツールで「市場に勝つ独立情報があるか
> （ΔR²>0・logloss<市場）」を先に確認すること。市場を写すだけのモデルをいくら精緻化しても無益。
>
> **残る唯一の理論的エッジ = closing-line value（締切前の賢い金の動き）**: 勝ち馬予測でなく
> 「早期オッズ→確定オッズ」を予測し、締まる馬を早く拾う路線。`odds_watch` で段階オッズを
> 数週間 cron 蓄積（§4-1）してからでないと検証不可。ファンダ路線（予測精度で市場に勝つ）は
> 本セッションで exhaustive に否定済み。
>
> ### 新目的（2026-06-29）: 市場併走型の高精度・高較正 予測器 — 初期マイルストン達成
> 「儲ける」を断念し「市場と同等以上に正確で、よく較正された確率予測器」へ目的を再設定。
> 北極星 = OOS 勝ち馬 logloss（市場≈1.96）と ECE。ツール: **`predict_quality.py`**（市場 vs
> モデル vs 合成companion の logloss/Brier/ECE/AUC を OOS で並べる）。2026 OOS の初期結果:
> | 予測器 | 勝logloss | Brier | ECE | AUC |
> |---|---|---|---|---|
> | 市場 p_mkt | 1.9579 | 0.05690 | 0.0047 | 0.8360 |
> | モデル r_hat | 2.0309 | 0.05829 | 0.0161 | 0.8283 |
> | **合成 companion**(α0.12/β0.90) | **1.9566** | **0.05689** | **0.0038** | **0.8362** |
>
> → **合成は4指標すべてで市場以下/以上**（利益ではなく確率精度で市場超え）。logloss/Brier/AUC の
> 差は微小（ノイズ域の可能性・要多年確認）だが、**ECE は 0.0047→0.0038（約19%改善）が構造的な勝ち**
 （モデル12%混合で市場の人気-穴バイアスを補正）。**モデル単体は市場より悪い**が市場と合成すると
> 超える＝「市場併走型」の正しい姿。
>
> **安定性確認の結論（`walk_forward.py --quality`・3 fold・7207レース）— companion ≈ 市場（引き分け）**:
> 各 fold で過去のみ学習→直前 fold で合成 fit→評価 fold で OOS 集計。通算プール:
> | 予測器 | 勝logloss | Brier | ECE | AUC |
> |---|---|---|---|---|
> | 市場 | 1.9174 | 0.05768 | 0.0023 | 0.8396 |
> | モデル | 2.2398 | 0.06365 | 0.0299 | 0.7914 |
> | companion | **1.9166** | 0.05767 | 0.0023 | 0.8397 |
>
> → logloss は 3 fold とも companion がわずかに市場以下（方向は一貫）だが差は **−0.0008≈実質ゼロ**。
> **単年(2026)で見えた ECE 19%改善は多時点で再現せず**（通算 ECE は市場と同値・fold3 では悪化）＝
> あれは fold 固有ノイズ。**結論: companion は市場と同等の堅牢な較正済み予測器（＝市場効率下で
> 到達可能な天井）。市場を意味のある差で上回ることはできない**（ΔR²≤0 の当然の帰結）。
> モデル単体は一貫して大幅に悪く、合成は「市場に戻る」だけ。**市場併走型の目的は現実的に達成**。
> 残るは ③合成済み確率の本番出力化のみ（精度向上の伸びしろは実質無し）。

---

## 1. プロジェクト概要

netkeiba のレースデータを収集・前処理し、LightGBM × ニューラルネットのスタッキングで
勝率を予測、期待値ベースで馬券を選定・資金配分するシステム。

---

## 2. 作業ブランチ

```
claude/kind-meitner-1yqyzu     ← 現行（2026-06-22〜。データ取得再設計 / 市場歪み特徴 / 2ヘッド / バックテスト）
```

過去ブランチ（参考）: `claude/blissful-tesla-dwiyqo`（〜2026-06-21。predict_upcoming / 単勝戦略 / オッズ補完）、
`claude/sweet-dijkstra-mtre06`（〜2026-06-19。スタッキング/データソース抽象化）

ブランチ統合状況（2026-06-11 完了）:
- **PR #3**（festive-albattani: オッズ動態モデル等、stoic-wright/laughing-ramanujan を内包）
  → main にマージ済み・CI 緑。既存の mypy/ruff 債務も解消され ruff が CI 必須化
- **PR #4**（festive-dijkstra: backtest 等）→ コンフリクト解消（featured_data 列テーブル設計は
  除外し #3 の `_featured.py` 設計を採用）の上 main にマージ済み・CI 緑
- **PR #5**（sweet-dijkstra 旧内容: FeaturedDataRepo スナップショット方式）→ #3 の
  `_featured.py`（parquet + featured_data_meta）と機能重複のためクローズ（取下げ）。
  本ブランチは main にリセットして再利用
- **PR #2** → head(trusting-noether) は main 済み・base(eager-darwin) は陳腐化のためクローズ
- `eager-darwin`（main 未収録の固有ファイル 0 件・−5342 行）/ `regression`（2024 年）は統合対象外

featured_data 永続化の正は **`src/storage/_featured.py`**（parquet ファイル +
SQLite `featured_data_meta` メタ表、ingestion/dashboard 配線済み）。

**ローカル検証時の注意**: ローカルの checkout が古いと
`No module named src.pipeline.odds_watch` 等になる。
`git fetch origin && git pull origin main` で最新化すること。

---

## 3. 実装済みの修正・機能

### 3-1. ライブ予想パイプライン（出馬表→特徴量→スコア）

- `src/preparing/_scrape_shutuba.py` を新規実装
  - `scrape_race_id_race_time_list(kaisai_date)` — 開催日の race_id と発走時刻
  - `create_active_race_id_list()` — 本日開催で馬体重発表済みのレースだけ抽出
  - `scrape_shutuba_table(race_id, date_str, filepath)` — DataFrame を pickle 保存
- `ShutubaDataMerger.__init__` に `_separated_hr_with_sire_dict` の初期化を追加
- `_merge_horse_info` / `_merge_peds` で `horse_id` の dtype 不一致（str/int64）を `astype(str)` で解消
- `FeatureEngineering.dumminize_kaisai` の `place`/`time` 列 drop を `errors="ignore"` に変更
- `FeatureEngineering` に `dumminize_ground_state()`（単一列版）を追加
- `__label_encode` のラベルエンコードマスタで重複インデックスを drop して `InvalidIndexError` を解消

### 3-2. スコア計算（バックテスト・ライブ共通）

- `src/policies/_score_policy.py` の `_calc` を、race_id がカラムでもインデックスでも動作するよう改修
  - `groupby` のキー選択 / `wakuban_flag` の結合を条件分岐
- LightGBM モデルが generic 名 (`Column_0..Column_N`) で保存されている場合の対応
  - try/except で直接 `predict_proba(X_pred)` を試し、shape 不一致時のみ位置/名前ベースでフォールバック

### 3-3. 払戻データ（旧フォーマット対応）

- 2020 年以前の払戻 raw データは行区切りが `br` ではなく半角空白
- `src/preprocessing/_return_processor._build_bet_df` の先頭に正規化処理を追加
  - `' - '` → `-`、`' → '` → `→`、残った ` ` → `br` に置換

### 3-4. race_id_list のキャッシュ破壊問題

- `src/preparing/_scrape_race_id_list.py` の `_filter_by_years` を多段フォールバック付き `_filter_requested` に置き換え
- フィルタ後の縮小データを `to_pickle` で書き戻していた行を削除し、全件 pkl を保護

### 3-5. ノートブック改善（main.ipynb）

- 各シミュレーション保存セルの保存先を `models/<today>/...` に動的化
- モデルロードを `glob` で最新ディレクトリ自動検出に変更
- `BetPolicyTanshoFukusho` を追加
- `race_id_list[0]` → `race_id_list.iloc[0]["race_id"]`（戻り値が DataFrame のため）

### 3-6. README.md 更新

- `## 8. 最近の主要修正` セクションを追加（8-1〜8-6 を記載）

### 3-7. Phase 1: raw データの SQLite 永続化（`src/storage/`）**完了**

**目的**: pickle が消えると全件 re-scrape が必要という問題を解消。
SQLAlchemy + SQLite (`data/keibam.db`) で raw データを永続化する。

**設計方針**:
- `INSERT OR IGNORE`（冪等 upsert）で既存行を上書きしない
- pickle はキャッシュとして残す（既存 Processor が `pd.read_pickle()` で読んでいるため）
- pickle が消えた場合は DB から再生成 → 再ダウンロード不要
- 初回起動時に既存 pickle を DB へ自動移行（`auto_migrate_if_empty`）
- 誤情報修正時は `--force` フラグで該当行を削除してから再 scrape

**スキーマ（7テーブル）**:

| テーブル | 主キー | index_col |
|---|---|---|
| `raw_results` | (race_id, 馬番) | race_id |
| `raw_race_info` | (race_id,) | race_id |
| `raw_return_tables` | (race_id, row_idx) | race_id |
| `raw_horse_results` | (horse_id, 日付) | None |
| `raw_horse_info` | (horse_id,) | None |
| `raw_peds` | (horse_id,) | None |
| `raw_odds_snapshots` | (race_id, captured_at, bet_type, combo) | None |

### 3-8. `ingest` CLI のバグ修正（2026-06-10）

`python -m src.pipeline.run_pipeline ingest --race-id <race_id>` の動作確認で
複数のバグを発見・修正した。修正コミットは `claude/stoic-wright-wm3n5g` ブランチに積み上げ済み。

| # | ファイル | 内容 |
|---|---|---|
| 1 | `src/preprocessing/_results_processor.py` | `_select_columns` で race_id がインデックスのみの pickle を読むと `df[["race_id"]]` が KeyError → `reset_index()` で列に復元 |
| 2 | `src/pipeline/run_pipeline.py` | `_ScrapingFetcher.fetch_results/fetch_race_info` が `preprocessed_data` を返して pickle を上書き → raw スキーマが消える。`load_raw` で直接読み `race_id` をインデックスに正規化して返すよう変更 |
| 3 | `src/preprocessing/_data_cleaner.py` | pandas 2.x で `Float64` 列に `.astype(str).str.split(".")` が `AttributeError` → `.fillna(0).astype(float).astype(int).astype(str).str.zfill()` に変更 |
| 4 | `src/preprocessing/_data_merger.py` | `_merge_race_info` で results（str）と race_info（int64）の race_id 型不一致でジョイン全 NaN → 両インデックスを `astype(str).str.replace(r'\.0$', '')` で正規化 |
| 5 | `src/storage/_repo.py` | `upsert()` で RangeIndex を `spec.index_col` にリネームしてから実際の race_id 列を drop → DB に行番号が race_id として保存。3分岐に変更 |
| 6 | `src/pipeline/_ingestion.py` | `existing_results` が RangeIndex のまま `find_new_race_ids` が常に全レースを新規判定し全行追記 → race_id を index に正規化してから比較 |

**pickle の正しい形式（重要）**:
- `results.pkl` / `race_info.pkl` はスクレイピング直後: **RangeIndex + race_id 通常列（int64 or float64）**
- `ResultsProcessor` / `RaceInfoProcessor` が `set_index("race_id")` して加工済みデータを返す

### 3-9. mypy 全修正（2026-06-10、コミット f66fec0）

33 エラー → 0 エラー。修正ファイル:
- `src/storage/_featured.py`, `src/policies/_score_policy.py`
- `src/preparing/_scrape_kaisai_date.py`, `_scrape_race_id_list.py`, `_scrape_shutuba.py`
- `src/training/_nn_win_model.py`, `_odds_predictor.py`, `_data_splitter.py`, `_keiba_ai.py`

### 3-10. `retrain` E2E 確認（2026-06-10、コミット be6f432）

- 原因: `encode_horse_id()` が両 FeatureEngineering チェーンに未定義 → `horse_id: str` で LightGBM エラー
- 修正: `src/pipeline/run_pipeline.py` の `_ingest` / `_retrain` 双方に `.encode_horse_id()` 追加
- E2E 結果: `auc_test=0.774`、モデル保存まで正常完了

### 3-11. AbstractDataProcessor DB 直読みフォールバック（コミット 9ae803b）

- pickle が `FileNotFoundError` → `PICKLE_PATH_TO_ALIAS` で DB alias を逆引き
- `RawDataRepo().read(alias)` → DataFrame を取得して pickle に書き戻す
- 全 Processor が透過的に DB フォールバックを利用可能

### 3-12. `tests/preparing` 収集エラー修正（コミット 08e4397）

- `_scrape_shutuba.py` のトップレベル `from bs4 import BeautifulSoup` が CI で `ModuleNotFoundError`
- `TYPE_CHECKING` ガード + 各関数内へのレイジーインポートに変更

### 3-13. ライブ予想特徴量の完全アライメント（コミット 0277841）

**問題**: 学習時 263 列 vs ライブ推論時 240 列（race_info 由来の列が不足）

**解決策**:

1. **`KeibaAI.feature_names_`** — 学習後に列順を保存。`calc_score()` で `reindex(fill_value=0)` により 0 補完フォールバック
2. **`ShutubaDataMerger` に `race_info_processor` オプション追加**:
   - `_merge_race_info_shutuba()`: 既存列との重複を除いた差分列のみ join（`date_x`/`date_y` 衝突を回避）
   - `ground_state1/2`, `days`, `times`, `teiryo` 等が自動追加される
3. **`FeatureEngineering._dummify` no-op ガード** — 対象列が存在しない場合は即 return（学習・ライブ共通 FE チェーンが動作）
4. **`convert_column_types` 列存在チェック** — 対象列が DataFrame にない場合は `continue`（`KeyError` 防止）

**結果**: ライブ推論で 0 補完列 0 件（完全一致）

### 3-14. ライブ予想 E2E 統合テスト（コミット 0277841）

`tests/integration/test_live_prediction.py` に 7 テストを追加。
ローカルデータがない CI 環境は自動スキップ（`pytest.mark.skipif`）。

| テスト | 内容 |
|---|---|
| `test_shutuba_table_processor_shape` | ShutubaTableProcessor の前処理列数確認 |
| `test_merger_with_race_info_adds_columns` | race_info 結合で `days` 列が追加される |
| `test_merger_no_ground_state_single` | `ground_state1` 追加後に単一 `ground_state` が除去される |
| `test_featured_data_has_ground_state_dummies` | `ground_state1__*` ダミー列が生成される |
| `test_featured_data_no_missing_after_race_info` | モデルの `feature_names_` と完全一致（欠損列 0） |
| `test_score_table_has_all_horses` | `calc_score` が全馬分のスコアを返す |
| `test_bet_policy_returns_dict` | `BetPolicyTansho` が `{race_id: {...}}` を返す |

### 3-15. cron スケジューリング（コミット d59b103）

- `scripts/daily_ingest.sh` — 前日分の日次取込（`--post-date YYYYMMDD`）
- `scripts/weekly_retrain.sh` — 週次再学習（`--tuning` / `--no-stack` オプション）
- `crontab.example` — `0 6 * * *` 日次 / `0 3 * * 1` 週次の設定例
- `run_pipeline ingest --post-date YYYYMMDD` で開催レース自動検出をサポート

### 3-16. cron 失敗通知（コミット f43234d）

- `scripts/on_failure_notify.sh` — `notify_failure()` 関数を提供
  - `NOTIFY_SLACK_WEBHOOK` 環境変数設定時: Slack Incoming Webhook に POST
  - `NOTIFY_EMAIL` 環境変数設定時: `mail` コマンドでメール送信
  - 常に stderr にも出力（最後の 30 行のログを付与）
- `daily_ingest.sh` / `weekly_retrain.sh` に `trap notify_failure ERR` を追加

### 3-17. lint・型エラーの全解消（2026-06-10）

- ruff 4 件（F821 / B905 / F401 / E501）・mypy 10 件（bs4 型・Optional 絞り込み）→ 0 件
- `pyproject.toml` の deprecated 設定（top-level `ignore` / `isort`）を `[tool.ruff.lint]` 配下へ移動、
  削除済みルール（ANN101/ANN102/PD901）を ignore リストから除去
- `.github/workflows/ci.yml` の ruff を `continue-on-error` から必須化（0 件達成済みのため）
- `_shutuba_data_merger.merge()` の重複ログ出力を削除

### 3-18. `retrain` 実行 — `feature_names_` 入りモデル生成（2026-06-10）

- `models/20260610/20260610_keibam.pickle`（258MB、gitignore 対象）を生成
- `auc_test=0.772`、`feature_names_` 263 列を保存
- ライブ予想統合テスト 7 件がスキップ解消されすべて passed
- **注意**: モデル pickle は git 管理外。新しい環境では `python -m src.pipeline.run_pipeline retrain`
  （約 3 分、optuna-integration[lightgbm] が必要）で再生成する

### 3-19. オッズスナップショット収集の完成（2026-06-10）

- `odds_scheduler` に `--auto` モードを追加（cron 定期実行用）
  - `scrape_race_id_race_time_list` で当日レースと発走時刻を自動検出
  - `--window-minutes N` 以内に発走するレースだけ取得（フェーズは minutes_to_post から自動分類）
  - 純粋関数 `build_race_post_times` / `select_target_races` は DI でテスト済み
- `persist()` が pickle 保存に加えて SQLite `raw_odds_snapshots` へ冪等 upsert（非致命）
  - `snapshots_to_records` が combo を `"3-7-11"` 文字列、captured_at を ISO8601 に変換
- `scripts/odds_snapshot.sh` + crontab.example エントリ（朝 8 時に全レース、9〜16 時は 10 分間隔）

### 3-20. `optuna_integration.lightgbm` への移行（2026-06-10）

- `src/training/_lgb_optuna.py` を新設し import を一元化
- `optuna_integration.lightgbm` 優先、未導入環境では旧 `optuna.integration.lightgbm` に
  フォールバック（CI のテストスタブはフォールバック経由でそのまま動作）

### 3-21. 連系馬券オッズパーサ（2026-06-10）

- `parse_combo_odds_html`: `id="odds-<type>-<馬番列>"` ベースの汎用パーサ
  - 馬連/ワイド/馬単/三連複/三連単 + b1 ページの単勝(1)・複勝(2)を一様に処理
  - 順序付き馬券は馬番の並びを保持、レンジ表記（ワイド・複勝）は保守的に下限を採用
  - 未確定（`---`）・重複セルはスキップ
- `capture()` は id ベースを先に試し、旧 DOM の単勝・複勝はクラスベースにフォールバック

### 3-22. Layer2: 締切オッズ予測の学習データ構築（2026-06-10）

- `src/training/_odds_feature_builder.py` を新設
  - `snapshots_to_phase_table`: スナップショット系列 → (race_id, combo) × フェーズのワイド表
  - `build_training_frame`: `current_odds` + 過去フェーズからの `log_ratio_*`（モメンタム）特徴量、
    目的変数は just_before オッズ（確定オッズの代理）
  - `train_odds_predictor`: `LgbOddsPredictor` を学習。min_rows 未満なら None
    （呼び出し側は `IdentityOddsPredictor` にフォールバック）

---

### 3-23. データ取得〜全機能の E2E テストと不備修正（2026-06-10、実 netkeiba）

実環境（2026-06-06/07 の 48 レース・565 頭）で取得→前処理→特徴量→推論→馬券選定を
通しで実行し、以下の重大な不備を発見・修正した:

| # | 不備 | 修正 |
|---|---|---|
| 1 | `ingest` が実スクレイプせず pickle を読むだけ（日次 cron が新データを取得できない） | レース HTML 取得 + 馬ページ・血統の差分取得を `run_pipeline._ingest` に配線 |
| 2 | `auto_migrate_if_empty` の呼び出し元がなく DB 保険が空 | `auto_migrate_all()` を ingest/retrain 起動時に実行 |
| 3 | ingest の新規行が DB に upsert されない | `_ingestion.save_raw` で RAW_* pickle を冪等 upsert |
| 4 | `DataLoader.transfer_temp_file` が gitignore のキャッシュ pkl に対し `columns[-1]`（owner_id!）でマージ → fresh 環境で 51,760 行を 24 レースで上書き（実際に発生） | data/raw を正本に race_id/horse_id キーでマージ。キー不明時は追記（喪失させない） |
| 5 | `append_idempotent` の index 非対称（results は正規化、race_info/return は RangeIndex のまま）で force 時に全行二重化 + NaN race_id 行 | 両辺 `normalize_race_id_index` + 保存時 `to_raw_format`（正準形式） |
| 6 | DB の race_id が int64 由来と float64 由来で別 PK 文字列になり重複 | `_to_db_str` で整数値 float を int 表記に正準化 |
| 7 | 馬ページの戦績テーブルが JS 描画化され全馬パース失敗 | `scrape_html` に alias 別 `wait_selector`（`table.db_h_race_results`） |
| 8 | プロフィール表特定が「最初の 2 列テーブル」で血統ミニ表を誤選択 | `生年月日` 行マーカーで特定 |
| 9 | 血統ページのリンクが絶対 URL 化 + `/horse/ped/` `/horse/sire/` 併設で旧正規表現が 0 件 | パス末尾 `/horse/<id>` の直接リンクのみ抽出する正規表現に変更 |
| 10 | テストが実 DB（data/keibam.db）を汚染 | autouse fixture で tmp DB に隔離 |
| 11 | サンドボックスの TLS 中間検査で Chromium が `ERR_CERT_AUTHORITY_INVALID` | `KEIBAM_IGNORE_HTTPS_ERRORS=1`（オプトイン）を `PlaywrightScraper` に追加 |

**E2E 結果**: results 3,792 レース（+48）/ horse_results 152,905 行（+3,571）/
horse_info・peds 12,832 頭（+565、欠落 0）。pickle と SQLite が完全一致。
2026 年実レースでスコア計算→馬券選定まで動作確認済み。
`test_horse_id_in_peds`・`test_peds_sire_id_not_null` も実データで解消。

**スクレイプ実行時の注意**: TLS 検査プロキシ配下では `KEIBAM_IGNORE_HTTPS_ERRORS=1` が必要。
VPS 等の通常環境では不要（証明書検証は既定で有効）。

### 3-24. 全年代（1973〜2026）データモデル互換の検証と修正（2026-06-10）

netkeiba の最古データ（1973 年の開催まで存在を確認）から現在までのサンプルレースを
実取得し、年代別のページ構造差をパーサで吸収できるよう修正した。
検証: 1975/1986/1990/1995/2000/2005/2015/2020/2023 の実レース + 2026 本番 bin 48 件
すべてで results / race_info / return の 3 パーサが成功。

| # | 年代差分 | 修正 |
|---|---|---|
| 1 | 旧年代ページは空テーブルを含み、一括 `read_html` が IndexError | `attrs={"summary": "レース結果"}` で対象テーブルのみ解析（summary は 1975〜現在まで共通） |
| 2 | 払戻テーブルの位置 `dfs[1], dfs[2]` 前提が旧構成で崩れる | `attrs={"summary": "払い戻し"}` で特定し concat（旧年代は単勝/複勝/枠連の 3 券種のみ） |
| 3 | 2019 年以前の条件クラス名（500万下/900万下/1000万下/1600万下） | `Master.RACE_CLASS_LEGACY_ALIASES` で現行クラス（1勝/2勝/3勝）へ正規化 |
| 4 | `race_class_info` が None のとき `"障害" in None` で TypeError | None ガード |
| 5 | 1970s の horse_id は英字混じり（例 `1972z00735`）で `\d+` 抽出だと "1972" に壊れる | URL パスから `\w+` で抽出 |
| 6 | 1970s はリンクの無い行があり、テーブル一括の ID 抽出が行数とずれて ValueError | 行単位で対応付け、リンク無しは欠損（NaN） |
| 7 | 旧馬戦績の 開催 列欠損（NaN）で `convert_string` が TypeError | 文字列以外はそのまま返す |
| 8 | レート制限で空ページ（〜39 bytes）が返り bin が壊れる | `scrape_html` に `_looks_empty` 判定 + 最大 3 回の再取得 |
| 9 | ID 抽出がパス全体の最初の数値を使う（ディレクトリ名の数字を誤抽出） | `os.path.basename` から抽出（全 8 箇所） |

**年代固有の留意点（パーサは正常動作、データ側の制約）**:
- 1975 年以前の下級条件戦は netkeiba 側のデータが勝ち馬のみ（行が 1〜2 行）
- 1975 年頃は条件クラス表記なし → race_class は欠損のまま取り込む（警告ログのみ）
- 旧年代に存在しない列（馬体重・上り・通過・タイム指数等）はマージ時に NaN
- 払戻は当時の発売券種のみ（単勝・複勝・枠連）

回帰テスト: `tests/preparing/test_era_parsers.py`（合成フィクスチャで旧構造を再現）

### 3-25. Optuna 探索結果の保存・選択 + UI モデル比較シミュレーション（2026-06-10）

**ハイパラ探索結果の永続化と選択**:
- `src/training/_tuning_history.py` — LightGBMTuner の全 trial（`system_attrs["lightgbm_tuner:lgbm_params"]`
  に完全パラメータ）を成績順（binary_logloss 昇順）に整形し `models/tuning_history.json` へ保存。
  同一パラメータの重複 trial は最良値のみ、同一 version は置き換え
- `ModelWrapper.tune_hyper_params` が study を生成・保持（`last_study_`）、
  `KeibaAI.tuning_study_` / `set_lgb_params()` を追加
- `RetrainJob.run(lgb_params=, params_rank=)` — 指定パラメータで学習し、メタに記録。
  `--with-tuning` 実行後は自動で tuning_history.json を保存
- CLI: `retrain --params-rank N`（履歴の rank N で学習）/
  `retrain --use-selected-params`（UI で保存した選択 `models/selected_params.json` で学習）

**UI（app/pages/5_model_lab.py「モデルラボ」）**:
- Tab1: 探索結果を成績順テーブルで表示、rank 選択 → `models/selected_params.json` に保存
  （実行コマンドのヒント表示つき）
- Tab2: 複数モデルバージョンを multiselect → 同一条件（馬券種・スコア閾値・直近期間）で
  バックテストし、回収率/的中率/シャープレシオ/最大DD/損益の比較表 + 資金推移チャート
- 計算ロジックは `app/_model_compare.py`（Streamlit 非依存、テスト済み）
  - `recent_race_slice` — 日付末尾 test_frac 分のレース（DataSplitter の test 分割相当）
  - `simulate_model` — calc_score → BetPolicy.judge → Simulator。action キーは
    払戻テーブル（int64 index）に合わせ int に正規化
  - `cumulative_profit` / `align_profit_curves` / `comparison_table`

検証: 実データ 300 レースサンプルで tune（65 trials）→ 保存 → rank3 選択 → 学習の
統合フロー、実モデル（20260610_keibam）で UI シミュレーション（379 レース）を確認済み。

### 3-26. オッズ力学モデル + 時系列オッズ自動再計算パイプライン（2026-06-11）

**設計思想**: 単勝市場はゼロサム（控除率付き）→ 馬ごとの独立回帰でなく
**投票シェアベクトル p（Σp=1）の確率過程**として締切オッズを予測する。
CLR 変換（centered log-ratio）でシンプレックス制約を外し、人気順バケット別の
経験的遷移統計（「市場の重力」）を全モデルの事前分布として共有する。

**モデル（`src/training/_odds_dynamics.py`）**:
| モデル | 内容 |
|---|---|
| identity | 最新観測シェアをそのまま返すベースライン |
| dirichlet | y ~ Dirichlet(c·μ)、μ_i ∝ s_i^β·exp(γ_bucket)。(β,γ,log c) を同時 MLE。少データはモーメント法、0 件は恒等へ退化 |
| kalman | 状態=「真の人気」の CLR。人気順バケット別 drift/vol で座標独立スカラー Kalman |
| particle | 同状態空間のブートストラップ PF（Student-t 遷移ノイズ、ESS<N/2 で系統リサンプリング） |
| ensemble | 重み付き平均（重み = 検証 KL の逆数比）= 総合判断モデル |

**支援モジュール**: `src/constants/_odds_dynamics.py`（バケット・チェックポイント・既定拡散）、
`src/training/_simplex.py`（shares/CLR/距離指標）、`_odds_gravity.py`（重力統計、縮小推定、
`models/odds_gravity.json`）、`_odds_dynamics_eval.py`（時系列 holdout 比較、
`models/odds_dynamics_eval.json`）、`_share_predictor_adapter.py`（AbstractOddsPredictor 互換）

**フェーズ細分化**: `just_before` を `t10(≤12)/t5(≤7)/t0(≤2)` に細分
（`thirty_min(≤45)` と合わせ発走 30/10/5/1 分前のチェックポイント）。
旧 `just_before` は `normalize_phase` で t10 に正規化。

**時系列取得 + 自動再計算（`src/pipeline/odds_watch.py`）**:
- タイマー（cron */2 分 or `--loop` 常駐）→ チェックポイント到来レースだけ取得
  （`select_checkpoint_races`、±1.5 分許容）→ スナップショット冪等永続化 →
  全モデルで「次時点」「発走時（確定）」を再計算 → `data/raw/odds_predictions.pkl` +
  `raw_odds_predictions` テーブル（PK: race_id, checkpoint, model, umaban）に保存
- **データソース抽象化**（`src/preparing/_odds_source.py`）: `NetkeibaOddsSource`（既定）/
  `JraVanFileDropSource`（JRA-VAN 受信契約 — Windows 側 JV-Link エージェントが
  `data/incoming/jravan/*.json` に置く。JSON スキーマは同モジュール docstring）。
  `odds_watch --source jravan` で切替
- `scripts/odds_snapshot.sh` は odds_watch 呼び出しに変更、crontab は */2 分 + 週次評価

**照会 UI**: `app/pages/3_odds_monitor.py` に照会マトリクス
（馬×時点: 実績オッズ@T-30 | 次時点予測シェア | 確定予測オッズ | …）。
`app/pages/5_model_lab.py` に「📈 オッズ力学モデル」タブ（KL/勝ち馬 logloss/
シェア MAE/オッズ MAPE のモデル比較 + アンサンブル重み）。
評価実行: `python -m src.pipeline.run_pipeline evaluate-odds-dynamics`（週次 cron 設定済み）

**EV 連携（常に最新の予測値を使用）**: `PredictedOddsProvider`（policies）が
odds_watch の最新予測確定オッズで EV を計算（予測の無い馬は現在オッズへフォールバック）。
`config.yaml: use_predicted_odds: true`（OperationConfig、既定 False）でオプトイン。

**検証**: 合成シンプレックスウォークで Kalman が恒等予測の KL を下回ること・
Dirichlet のパラメータ回復・PF≈Kalman（Gaussian データ）等を 60+ テストで確認。
スナップショット蓄積（4〜8 週）後に実データ評価 → アンサンブルが恒等に安定して
勝てば use_predicted_odds=True を推奨設定にする。

### 3-27. 馬券発注 UI（2026-06-11）

**発注ページ（`app/pages/6_bet_order.py`）** — カート → 発注票 → 清算の一気通貫:
1. **カートへ追加**: 予測ページの「🛒 発注カートへ追加」ボタン（推奨候補を
   100 円単位に丸めて投入）+ 手動追加フォーム（race_id/式別/買い目/金額）
2. **カート編集**: `st.data_editor` で金額編集・発注対象チェック・削除。
   カートは `data/order_basket.json` に永続化（ページ間・リロード間で保持）
3. **資金チェック**: bankroll × max_daily_ratio の当日上限超過で発注ブロック
4. **発注実行（運用モード別）**:
   - advisory — 推奨として履歴記録のみ
   - semi_auto — **IPAT 入力支援テキスト**（場名/R/式別/買い目/金額、レース別小計）
     + 発注票 CSV ダウンロード + 「発注済みとして記録」
   - full_auto — 既定無効（規約・法的リスク。bet_executor と整合）
5. **清算**: 結果取得済みレースの発注を実払戻テーブル（BettingTickets）で清算し、
   payout/hit を履歴（betting_history.jsonl）に書き戻し。回収率・的中数を表示

**サービス層（`app/_order_service.py`、Streamlit 非依存・19 テスト）**:
race_label（race_id → 場名 R 表示）/ round_stake（100 円単位）/ add_orders
（同一馬券は置換）/ format_ipat_text / settle_records（DI で BettingTickets 注入）/
rewrite_history。実払戻データで清算スモーク確認済み（的中 600 円 → 12,960 円）。

### 3-28. README.md 全面更新（2026-06-11、コミット 283ee6e）

- 主要機能一覧・セットアップ手順（プロキシ TLS・Ubuntu 26.04 回避策・初回 retrain）・
  検証手順（4 ゲート + 期待結果）・利用手順（取込/学習/UI/オッズ自動取得/発注フロー/cron）・
  設定（config.yaml + 生成物の置き場所）・設計ハイライトを反映
- 記載コマンド・ファイル参照の実在を機械検証済み

### 3-29. netkeiba ポライトネス制御の一元化（2026-06-11）

netkeiba 公式のクローラー対策（2024-11 強化、UA 未設定で HTTP 400）への自主規制を
`src/preparing/_rate_limiter.py` に一元化（従来 modules.py / _scraper.py に分散していた
固定スリープのリファクタリング）:

- `polite_interval(base)` — 最低 1 秒に切上げ + `uniform(0, jitter)` の揺らぎ
  （既定で合計 1〜3 秒）。`base<=0` はテスト用の明示無効化（既存慣行維持）
- `HourlyRateLimiter` — 1 時間あたりリクエスト数のスライディングウィンドウ自主上限
  （既定 1000 件）。`try_acquire()` はノンブロッキング（async 側は asyncio.sleep で待機）
- 配線: `PlaywrightScraper.fetch`（全リクエストの単一チョークポイント）で上限取得、
  `fetch_many`/`scrape_paginated`/`modules.process_bin_file` の待機を揺らぎ付きに変更
- 環境変数: `KEIBA_SCRAPE_DELAY`（既定 1.0）/ `KEIBA_SCRAPE_JITTER_MAX`（2.0）/
  `KEIBA_MAX_REQUESTS_PER_HOUR`（1000）。README 7-1b に文書化
- テスト: `tests/preparing/test_rate_limiter.py`（15 ケース）+
  `test_scraper.py::TestHourlyLimiterIntegration`（2 ケース）


### 3-29a〜g. 運用基盤ドキュメント補完（`0bkff7` から統合・コードは main 済み）

`claude/review-context-and-tasks-0bkff7` の docs 更新のうち、本ブランチに欠けていた
安全装置・信頼性・観測性・UI拡張・リーク検査・取得堅牢化の記述を統合。
mypy `predict_proba` の `np.asarray` 修正は本ブランチに既存（`1097e94`）のためコード差分なし。

### 3-29a. 安全装置: 損失ストップ・実効 bankroll・オッズ健全性ガード（2026-06-12、コミット 7376f47）

金銭リスク直結の安全装置（運用方針への破壊的変更なし）:

- `src/operation/_risk_guard.py`（新規・純粋関数、app 非依存）— 実現損益 / 当日損失 /
  kill switch 判定 / 実効 bankroll。履歴 `list[dict]` を引数で受ける依存逆転
- `OperationConfig` に `kill_switch_enabled` / `max_daily_loss_ratio` / `initial_bankroll` を
  追加（`from_dict` 後方互換、`__post_init__` で比率検証）。`config.yaml` にも記載
- `_bet_executor._to_record` に `created_at` を付与（日次集計用、now 注入可）
- 予測ページ: 「承認して記録」前に当日損失が上限超なら停止（🛑 表示 + `st.stop`）
- ダッシュボード: 取引可否インジケータ・当日実現損失・実効 bankroll を表示
- `_bet_policy`: 確率 NaN/<=0 を `win_probs` から除外（Harville 汚染防止）、
  異常オッズ（NaN/0/inf）の馬券をスキップ
- `_odds_provider`: 予測→現在オッズ fallback を debug ログで明示
- テスト +17（risk_guard 16 / created_at・config 検証 / オッズ・確率ガード）

### 3-29b. 信頼性: health-check CLI（doctor）・鮮度警告・モデル retention（2026-06-12、コミット d650f01）

- `src/pipeline/_doctor.py`（新規・純粋ロジック、app 非依存）— データ / モデル / DB /
  ディスク / featured メタの存在・鮮度を点検し OK/WARN/ERROR を集約
- `src/pipeline/_model_retention.py`（新規）— `models/<date>/` を新しい順 keep 世代残して
  古い世代を削除（既定ドライランで誤削除防止）
- `run_pipeline` に `doctor` サブコマンド（`--json` / `--strict` / `--prune-models KEEP`）。
  ERROR（`--strict` 時は WARN でも）で `sys.exit(1)` → cron から検知可能
- `app/_data_loader.load_freshness_status`（doctor を再利用）+ ダッシュボードに
  「🩺 システム健全性」バッジ・点検表を追加
- テスト +22（各チェックの境界 / retention の dry_run・実削除 / doctor 引数解析）

### 3-29c. 観測性: Slack 通知・execution_log・ログローテーション（2026-06-12、コミット 9cbc861）

- `src/operation/_notifier.py`（新規）— `AbstractNotifier` + `SlackWebhookNotifier`
  （`requests.post`、`NOTIFY_SLACK_WEBHOOK`）+ `NullNotifier` + `create_notifier`。
  稼働アプリから MCP は使えないため Webhook 方式。post 注入でテスト可能
- `src/storage/_execution_log.py`（新規）+ `_db.py` に `execution_log` テーブル —
  cron/CLI ジョブの job/status/started_at/finished_at/duration/message を記録
- `run_pipeline.main` を `_run_job` でラップし全サブコマンドの成否を `execution_log` に
  記録、失敗時は `create_notifier().notify` で通知（いずれも非致命）
- `_logging_config.setup_logging` に `max_bytes`/`backup_count` を追加し `RotatingFileHandler`
  対応（既定 0 = 従来の FileHandler で完全後方互換）
- ダッシュボードに「🧾 ジョブ実行履歴」を追加（`load_execution_log`）
- テスト +12（notifier payload/env/Null/例外、execution_log roundtrip/降順/limit、
  ログ rotation の handler 種別）

### 3-29d. UI 拡張: モデルバージョン選択・Optuna 探索の起動/監視（2026-06-12、コミット c26970d / b52e9f1）

- **適用モデルのバージョン選択**（`2_prediction.py`）: 最新固定だったのを、蓄積された複数
  バージョンから選択可能に。`app/_data_loader.load_model_from_path()`（実ファイルパスで
  厳密ロード、version 名の部分一致による取り違えを回避）。サイドバー selectbox（既定=最新、
  ラベルに AUC 表示）。テスト 9 ケース
- **モデルラボから Optuna 探索を起動/監視**（`5_model_lab.py` Tab1）:
  `app/_tuning_job.py`（新規・streamlit 非依存）が subprocess でデタッチ起動
  （`start_new_session`）、`models/tuning_job.json` に pid/状態、`tuning_job.log` にログ。
  完了はログ末尾の終了センチネル `__TUNING_EXIT__` で判定（Popen を跨ぐ再実行でも終了コード
  回収）。多重起動ロック・中止（SIGTERM）対応。テスト 13 ケース

### 3-29e. データリークの経験的検査（label shuffle permutation test）（2026-06-13、コミット b052981）

静的監査（実装読解）に加え、経験的にリークを検出:

- `src/training/_leakage_check.py`（純粋ロジック・I/O なし）— `label_shuffle_test` が X/y と
  モデル factory を受け、baseline AUC とシャッフル AUC（複数試行）を比較し
  `ShuffleResult(verdict=PASS/SUSPECT)` を返す。y_train をシャッフルして学習し本物の y_test で
  AUC を測る（リークなしなら ~0.5 に落ちる）。既定モデルは実パイプライン準拠の軽量 LightGBM。
  now/rng/factory 注入でテスト決定化
- `scripts/leakage_check.py` — featured_data を `DataSplitter` で時系列分割し検査する薄い CLI
  （`--n-trials`/`--seed`/`--suspect-threshold`、SUSPECT 時 exit 1）
- テスト: リーク注入時に SUSPECT、クリーン時に PASS を確認（検出力を担保）

### 3-29f. データ取得の堅牢化・生成 pkl の追跡解除（2026-06-12〜13）

年次履歴取得の実運用で発見した不備を修正:

| コミット | 内容 |
|---|---|
| bf24bcc | `race_id_list` マージが `columns[-1]` を race_id とみなす前提が `kaisai_date` 列追加で崩れ `KeyError` → 列名ベース解決に変更、旧スキーマは温存 |
| 11e8a76 | 実体のない空ページ（race_name 空・日付 1970-01-01）で `place_id` 未初期化 `UnboundLocalError` → race_name 空なら `ValueError` でクリーンにスキップ、`place_id`/`place_name` を NaN 初期化 |
| a15b617 | 空ページ判定を天候パースより前に移動し無意味な `unknown weather` 警告を抑制（挙動不変） |
| 741fd75 | `transfer_temp_file` のマージキーに `kaisai_data`/`kaisai_date` を追加。キー無し扱いで重複除去されず開催日が積み上がる問題を解消（`drop_duplicates(keep="last")`） |
| adedbcf | `.gitignore` 済みだが追跡が残っていた生成 pkl（16 件）を `git rm --cached`。取得のたびの pull 衝突を解消。各データディレクトリに `.gitkeep` を追加（要各クローンで一度 `git checkout HEAD -- data/` 後に pull） |

### 3-29g. 実装済み・未テストロジックの検証テスト追加（2026-06-12、コミット 74170dd）

プロダクションコード変更なし。CI 依存だけで決定的に検証できる 3 箇所をガード:
- `tests/storage/test_featured.py`（新規）— featured_data 永続化の dtype 保持ラウンドトリップ・メタ表
- `tests/app/test_model_eval.py`（追記）— backtest 関数（損益・Sharpe・最大DD・hit・防御経路）
- `tests/pipeline/test_run_pipeline.py`（新規）— CLI `_parse_args` のサブコマンド・相互排他・フラグ解析

### 3-30. マルチ GBDT スタッキング（XGBoost + CatBoost + NN）（2026-06-18）

**背景**: LightGBM 単体スタッキングでは meta 学習器が base と同 AUC（0.795）になり
スタッキングの意味が出ない。XGBoost・CatBoost・NN を base に加えて多様性を確保する。

#### 実装ファイル

| ファイル | 種別 | 内容 |
|---|---|---|
| `src/training/_base_models_config.py` | 新規 | base 選択・探索範囲 DTO（frozen dataclass） |
| `src/training/_base_model_factory.py` | 新規 | BaseModelSpec 生成ファクトリ（fail-soft） |
| `src/training/_multi_model_tuner.py` | 新規 | XGB/CatBoost/NN 個別 Optuna 探索 |
| `src/training/_stacking_model.py` | 修正 | stream-aware 化 + `derive_nn_input()` 追加 |
| `src/training/_keiba_ai.py` | 修正 | train_with_stacking を config 駆動化 |
| `src/pipeline/_retrain.py` | 修正 | base_models_config 配線・メタ記録 |
| `src/pipeline/run_pipeline.py` | 修正 | `--base-models` / `--base-models-config` CLI 追加 |
| `app/_model_eval.py` | 修正 | `.values` 除去（DataFrame のまま StackingModel へ渡す） |
| `configs/base_models.example.json` | 新規 | LGB + XGB + CatBoost 3 GBDT 構成例 |
| `configs/base_models_nn.example.json` | 新規 | 4 モデル（+NN）通常運用例 |
| `configs/base_models_nn_light.json` | 新規 | LGB + XGB + NN 軽量版（max_train_rows=200000） |
| `configs/base_models_nn_tuned.json` | 新規 | tune_per_model=true、再探索用（CNN 除外） |

#### BaseModelsConfig 主要フィールド

```python
@dataclass(frozen=True)
class BaseModelsConfig:
    models: tuple = ("lightgbm",)       # デフォルトで後方互換
    tune_per_model: bool = False
    n_trials: int = 50
    nn_tune_trials: int = 12            # NN 専用 Optuna trial 数
    nn_tune_epochs: int = 15            # 1 trial あたりの epoch 数
    nn_tune_max_rows: int | None = 120000
    nn_search_space: dict = ...         # arch=["mlp"] 固定（CNN 除外）
    nn_params: dict = ...               # 固定ベストパラメータ
    # XGBoost/CatBoost のパラメータと探索空間も保持
```

#### stream-aware StackingModel

StackingModel は `predict_proba(X)` の単一 DataFrame 契約を維持したまま、
base ごとに NN ストリームを内部で導出する設計:

```python
def derive_nn_input(nn_scaler, x) -> np.ndarray:
    # StackingModel と tune_nn で共用する module-level 関数
    nn_df = nn_scaler.transform(x).reindex(columns=nn_scaler.feature_names)
    # entity 列は cat.codes+1（0=未知）、numeric 列は float32
    return np.nan_to_num(np.column_stack(cols), nan=0.0)
```

- `stream=="gbdt"`: `x.values` で従来通りの GBDT 入力
- `stream=="nn"`: `derive_nn_input(nn_scaler, x)` で entity embedding + numeric 配列
- **推論契約は不変**: `score_policy` / `prediction_service` の単一引数 `predict_proba(X)` は変更なし

#### NN アーキテクチャ改良

**CNN ヘッド追加** (`arch="cnn"`):
- concat ベクトル (B, in_dim) → unsqueeze(1) → (B, 1, in_dim) → Conv1d → BN → ReLU
  → Dropout → AdaptiveMaxPool1d(1) → Flatten → Linear(→1)

**pre_norm オプション** (embedding と数値のスケール差を補正):
- `"layer_norm"`: concat 後に LayerNorm（サンプル内正規化、最も効果大）
- `"batch_norm"`: BatchNorm1d（サンプル間正規化）
- `None`: なし

**epoch ログ追加**:
```
[NN fit] epoch 14/80  train_loss=1.5308 val_loss=1.6005  (best=1.5823 @ epoch4)
```
`*` 印は val_loss 改善時。Early Stopping も記録。

#### tune_nn — Optuna で NN 構造探索

`src/training/_multi_model_tuner.py` の `tune_nn()`:
- AUC 最大化、1 trial は `nn_tune_epochs`（既定 15）epoch × `nn_tune_max_rows`（120000 行）
- 探索空間: arch/lr/dropout/batch_size/pre_norm + mlp 用(n_layers/layer_width)
- 探索結果ベスト: `arch=mlp, lr=0.00065, dropout=0.5, batch_size=512, pre_norm=layer_norm, hidden_dims=[128]`
- **NN AUC 改善**: 0.654 → 0.7808

#### 運用フロー

```bash
# 通常運用（探索なし・ベスト構造固定）
python -m src.pipeline.run_pipeline retrain --use-stacking \
    --base-models-config configs/base_models_nn.example.json

# 軽量版（LGB+XGB+NN、行数制限あり）
python -m src.pipeline.run_pipeline retrain --use-stacking \
    --base-models-config configs/base_models_nn_light.json

# 再探索（NN アーキテクチャ探索、CNN 除外済み）
python -m src.pipeline.run_pipeline retrain --use-stacking \
    --base-models-config configs/base_models_nn_tuned.json

# 後方互換（LightGBM 単独・従来通り）
python -m src.pipeline.run_pipeline retrain --use-stacking
```

#### XGBoost >= 2.0 API 変更対応

`early_stopping_rounds` が `fit()` から `__init__()` に移動。
`_multi_model_tuner.py` 内で `XGBClassifier(early_stopping_rounds=50, ...)` として修正済み。

### 3-31. マルチ GBDT スタッキング動作確認（2026-06-18、合成データ E2E）

実 featured_data が無い環境（フレッシュコンテナ）で、`KeibaAI.train_with_stacking` の
本番経路と `app/_model_eval.compute_stacking_auc`（モデルラボ「スタッキング寄与」チャートの
計算ロジック）をそのまま使い、学習可能な信号を持つ合成データ（1200 レース・12000 行・
レース内 softmax で勝ち馬サンプリング・entity 効果込み）で 3 構成を検証した。

**検証スクリプト**: `verify_multi_gbdt.py`（root、`check.py` と同列の運用補助スクリプト。
torch/xgboost/catboost を要するため CI 非対象。`python verify_multi_gbdt.py <config>` で実行）。

| 構成 | LightGBM | XGBoost | CatBoost | NN | meta |
|---|---|---|---|---|---|
| nn.example（4 モデル） | 0.7999 | 0.7929 | 0.7954 | **0.8233** | 0.8148 |
| base.example（3 GBDT） | 0.7999 | 0.7929 | 0.7954 | — | 0.7960 |
| LightGBM 単独（後方互換） | 0.7999 | — | — | — | 0.7981 |

**確認できたこと**:
- ✅ `base_model_names_` に LGB/XGB/CatBoost/NN が正しく並び、**base 別 AUC が個別に算出**される
  （モデルラボの別バー表示が成立）。stream-aware StackingModel が NN ストリームを
  内部導出し、単一 `predict_proba(X)` 契約のまま 4 base を消費できている
- ✅ 全 base が AUC>0.55 で学習成立（NN AUC 0.823 で §3-30 の改良が合成データでも有効）
- ✅ 後方互換（LightGBM 単独）も正常完走
- ⚠️ **meta（LogisticRegression）が最良 base を上回らないケースを確認**: NN 構成で
  meta 0.8148 < 最良 base NN 0.8233。1 つの強い非線形 base が支配的なとき、線形 meta は
  それを超えられない。→ **次タスク ③（meta を GBDT 化）の優先度を上げる根拠**。
  base 同士が高相関な合成データの性質も影響するため、実データでの再確認も必要

回帰確認: `tests/training/` 168 passed（スタッキング機構 42 件含む）。

### 3-32. meta 学習器の GBDT 化（設定駆動の meta 選択）（2026-06-18）

§3-31 で「線形 meta（LogisticRegression）が支配的 base を超えられない」事象を実測したのを
受け、meta 学習器を設定で切り替え可能にした。

#### 実装

| ファイル | 内容 |
|---|---|
| `src/training/_base_models_config.py` | `meta_model`（"logistic"/"lightgbm"）+ `meta_params` フィールド追加、`DEFAULT_META_LGB_PARAMS`、`from_dict` で meta_model 検証（小文字正規化・未対応値で ValueError） |
| `src/training/_base_model_factory.py` | `build_meta_model(cfg, scale_pos_weight=None)` 追加。meta_params は選択モデルの既定に上書きマージ |
| `src/training/_keiba_ai.py` | `train_with_stacking` のハードコード `LogisticRegression` を `build_meta_model(bm_cfg, ...)` に置換 |
| `configs/base_models_nn_gbdt_meta.json` | 4 base + GBDT meta の構成例 |
| `tests/training/test_base_model_factory.py` / `test_base_models_config.py` | meta ファクトリ・設定の単体テスト計 10 件追加 |

- `StackingModel` は元から `meta_model` を DI で受け取る設計なので変更不要
- 後方互換: `meta_model` 既定は `"logistic"` で従来動作を完全保持

#### GBDT meta の既定パラメータ設計（過学習対策）

meta 特徴量は base 予測確率の数列（3〜4 列）のみと低次元。深い GBDT（`num_leaves=7`）は
過学習し LR に負ける（合成データで meta 0.7969 < LR 0.8148 を実測）。グリッド探索で
`num_leaves=3`・`n_estimators=100`・`reg_lambda=5.0` の**極浅・強正則化**が堅牢と判明し既定化。

#### 検証結果（`verify_multi_gbdt.py`、4 base 構成・テスト AUC）

| meta | meta AUC |
|---|---|
| logistic（既定） | 0.8148 |
| lightgbm（浅 GBDT meta、本タスク） | **0.8163** |

浅い既定により、高相関 base の合成データでも GBDT meta が LR をわずかに上回る。
**留意**: 合成データは base 同士が高相関のため stacking の旨味が出にくく、どちらの meta も
最良単体 base（NN 0.8233）には届かない。GBDT meta の本領は base が真に多様な実データで
発揮されるため、実データでの A/B（`configs/base_models_nn_gbdt_meta.json` vs `nn.example`）が
最終確認となる。

回帰確認: `tests/` 917 passed / 13 skipped（フル）、ruff・import-linter 0 エラー。
mypy は本変更ファイルでエラー 0（既存 `_nn_win_model.py`/`_data_splitter.py` の 4 件は
当環境の mypy 2.1.0 由来の既存事項で HEAD でも再現、本変更とは無関係）。

### 3-33. オッズ力学モデル評価の勝ち馬 logloss 配線 + E2E 検証（2026-06-18）

タスク④（実データ蓄積後の評価）に着手し、評価ハーネスを実データ無しで検証する過程で
**本番バグを発見・修正**した。

#### 発見した本番バグ

`evaluate-odds-dynamics` CLI（`run_pipeline._evaluate_odds_dynamics`）が
`evaluate_dynamics_models(sequences, ...)` を **`winners=` 無し**で呼んでいたため、
**勝ち馬 log-loss が本番では常に NaN**だった（ハーネス自体は `winners` 対応済みで
モデルラボの「勝ち馬logloss↓」列も表示するのに、データが流れていなかった）。

#### 修正

| ファイル | 内容 |
|---|---|
| `src/training/_odds_dynamics_eval.py` | `race_winners(results_df) -> {race_id_str: umaban_str}` 純粋関数を追加（着順1着抽出・`_to_db_str` で race_id 正準化・非数値着順や同着を頑健に処理・race_id は index/列どちらでも可） |
| `src/pipeline/run_pipeline.py` | `_evaluate_odds_dynamics` で `load_raw(RAW_RESULTS_PATH)` → `race_winners` → `evaluate_dynamics_models(..., winners=winners)` を配線（results 未取得時は info ログのみで NaN にフォールバック）。ついでに既存デッドインポート `BaseModelsConfig` を除去 |
| `tests/training/test_odds_dynamics_eval.py` | `race_winners` の単体テスト 7 件追加（基本・index race_id・float 正準化・非数値着順・同着・空/欠損列・evaluate 統合） |
| `.gitignore` | 実行時生成の `models/odds_dynamics_eval.json` / `models/odds_gravity.json` を追加 |

#### E2E 検証（`verify_odds_dynamics.py`、新規）

CLI と同一チェーン（OddsSnapshot 群 → `snapshots_to_phase_table` → `race_share_sequences`
→ `race_winners` → `evaluate_dynamics_models`）を合成市場（CLR ランダムウォーク +
1番人気 firming drift = 市場の重力、単勝オッズを控除率込みで生成）で駆動。

- 実 CLI も隔離データで実走させ、`models/odds_dynamics_eval.json` の全 5 レコードで
  **winner_logloss が有限値**（修正前は全 NaN）になることを確認 → 生成物はクリーンアップ済み
- 合成市場（120 レース）で力学モデル/アンサンブルが identity の KL を下回る:
  identity KL=0.0015 vs ensemble/dirichlet/kalman KL=0.0007、アンサンブル重み Σ=1

回帰確認: `tests/` 924 passed / 13 skipped、ruff（変更ファイル）・import-linter 0 エラー。
`_odds_dynamics_eval.py` は mypy エラー 0。

**残課題（タスク④の本質）**: 実データ蓄積（4〜8 週、VPS cron）後に
`evaluate-odds-dynamics` を実走 → モデルラボで KL/勝ち馬logloss を確認 →
アンサンブルが identity に安定して勝てば `config.yaml: use_predicted_odds: true` を採用。

### 3-34. 券種別バックテスト（単勝以外も照会可能に）— Phase 1（2026-06-18）

**ユーザー要望**: バックテストで単勝以外（複勝・枠連・馬連・馬単・ワイド・三連複・三連単）も
照会したい。将来は券種ごとの最適化モデルを管理したい。
→ 方針確認の結果、**Phase 1=バックテストUI を全券種に拡張**（既存の単勝勝率モデル+Harville を
流用）を先行、**Phase 2=券種別の最適化レイヤ管理**（後述）を後続、とした。

#### 現状の発見（着手前）
- `app/_model_compare.simulate_model`（calc_score→BetPolicy.judge→Simulator）は既に
  tansho/fukusho/umaren/wide/sanrenpuku の **5 種のみ**対応。枠連・馬単・三連単が欠落。
- メインのバックテストページ `5_backtest.py` は **単勝 EV のみ**（`compute_full_backtest`）。
- 下層（`Simulator`/`BettingTickets`/`BetPolicy*Box`/`ReturnProcessor`）は全 8 券種対応済み。

#### Phase 1 の実装

| ファイル | 内容 |
|---|---|
| `app/_model_compare.py` | `BET_POLICY_CHOICES` を**全 8 券種**に拡張（枠連BOX/馬単BOX/三連単BOX 追加）。`simulate_model` に `return_processor` DI を追加（テスト可能化・後方互換）。診断用 `_action_to_bet_type` も 8 種に拡張 |
| `app/pages/5_backtest.py` | 第 3 タブ「🎰 券種別バックテスト」を追加（券種選択＋スコア閾値→回収率/的中率/損益/シャープ/最大DD/累積推移/レース別表/CSV、賭け成立・払戻カバレッジの診断つき） |
| `tests/app/test_model_compare.py` | 全 8 券種 E2E（合成スコア表+合成払戻テーブルを DI）の parametrized テスト + 券種網羅テスト計 10 件追加 |
| `verify_bet_type_backtest.py` | 60 レース合成データで全 8 券種を回す検証スクリプト（新規） |

- `BET_POLICY_CHOICES` 拡張により**モデルラボ Tab2（モデル比較）も自動で全 8 券種対応**に。
- 選定は StdScorePolicy（レース内標準化スコア）の閾値ベース。BOX は閾値超え馬の全組合せ。
  清算は実払戻テーブル（`ReturnProcessor`）＋ `Simulator`（1 点=1 単位）。

**検証**: `verify_bet_type_backtest.py` で全 8 券種が回収率・的中率・損益を算出。
回帰: `tests/` 934 passed / 13 skipped、ruff・mypy（変更ファイル）・import-linter 0 エラー。

#### Phase 2（✅ 枠組み実装完了 — §3-35）: 券種別の最適化レイヤ管理

### 3-35. 券種別最適化レイヤ（Phase 2 枠組み）（2026-06-18）

ユーザー選択の**「最適化レイヤ方式」**（独立 ML ではなく単勝モデル+Harville を土台に
券種ごとのパラメータを最適化・管理）の枠組みを実装。合成データで E2E 検証済み。

#### 実装

| ファイル | 種別 | 内容 |
|---|---|---|
| `src/policies/_bet_type_params.py` | 新規 | `BetTypeParams` DTO（ev_threshold/temperature β/prob_scale/ev_max）+ 既定値（BetThresholds 由来）+ `apply_temperature` + 永続化（`models/bet_type_params.json` バージョン管理・最新選択）。枠連は Harville 非対応のため最適化対象外（`OPTIMIZABLE_BET_TYPES`=7 券種） |
| `src/policies/_bet_policy.py` | 修正 | `ExpectedValueBetPolicy.__init__` に `bet_type_params` DI 追加。`_select_for_race` で券種別に温度（勝率を尖らせ Harville へ）・prob_scale（組合せ確率較正）・ev_threshold/ev_max を上書き。**未指定券種は従来挙動を完全保持** |
| `app/_bet_type_optimizer.py` | 新規 | `backtest_bet_type`（EV スコア表→`HistoricalOddsProvider` で連系推定オッズ→EVポリシー→Simulator）/ `optimize_bet_type`（EV閾値×温度×較正のグリッド探索、min_bets で過学習防止）/ `optimize_all` / `results_to_frame` |
| `app/_prediction_service.py` | 修正 | `run_prediction` に `bet_type_params` 引数追加→EVポリシーへ配線（最適化結果を予測選定に反映） |
| `app/pages/5_model_lab.py` | 修正 | 第4タブ「🎛️ 券種別最適化」（券種別グリッド探索→ベストパラメータ表示→`bet_type_params.json` へ保存。保存済みパラメータの照会） |
| `app/pages/2_prediction.py` | 修正 | 予測時に `latest_bet_type_params` を読み EV 選定へ反映（ループを閉じる） |
| tests | 新規 | `tests/policies/test_bet_type_params.py`（16 件: DTO/永続化/温度/EV配線）+ `tests/app/test_bet_type_optimizer.py`（6 件: 合成 E2E 最適化）|
| `verify_bet_type_optimizer.py` | 新規 | 合成市場（モデル≈真値・市場=平坦）で最適化が既定を上回ることを検証 |

#### 検証（`verify_bet_type_optimizer.py`、umaren 200 レース）
- 既定（ev_threshold=3.26, temp=1.0）: 回収率 **44.2%** / 25 枚
- グリッド最適（ev_threshold=2.0, **temperature=1.6**）: 回収率 **124.0%**（+79.8 ポイント）
- 温度を上げて人気側を尖らせ、EV 閾値を調整することで profitable な選定に改善

#### データフロー（ループ）
最適化（モデルラボ Tab4）→ `models/bet_type_params.json` 保存 →
予測ページ `run_prediction(..., bet_type_params=...)` → `ExpectedValueBetPolicy` が
温度・較正・閾値を反映して EV 選定。

回帰: `tests/` 954 passed / 13 skipped、ruff（変更ファイル）・import-linter 0 エラー、
新規 src は mypy エラー 0。後方互換: `bet_type_params=None` で従来動作を完全保持。

**残課題**: 実データ（featured_data + return_tables）での実最適化。グリッドは現状固定。
Optuna 化・prob_scale 探索の拡張・シャープ目的の調整は実データ運用で詰める。

### 3-36. ingest の増分化バグ修正（全 HTML コーパス再パース → 新規分のみ）（2026-06-18）

**ユーザー報告**: `ingest --post-date 20240106`（新規 24 レース）で `race_results_table` の
処理対象が **63,783 件**（全 race HTML コーパス）になり ~2 時間かかる。

**原因**: `_scrape_new_race_data` / `_scrape_new_horse_data` が新規分の HTML だけを取得
（`scrape_html_race(skip=True)`）した後、`get_rawdata_results(skip=False)` 等を呼ぶが、
`skip` は「全スキップ（キャッシュ pkl 返却）or 全 bin 再パース」の二択で、**新規分だけ
処理する経路が無かった**。`process_bin_file` が `data/html/race` の全 bin を走査していた。
※再ダウンロードではなく**再パース**だが、毎回の ingest が全件再パースで非常に遅い。

**修正**:
- `modules._filter_target_bins(bin_files, only_ids)`（純粋関数）を新設。`process_bin_file` が
  `self.only_ids`（任意）設定時は `<id>.bin`（`processing_id=ref_id` 由来）の stem で絞り込む
- `_get_rawdata._create_table` / `get_rawdata_*` に `only_ids` 引数を追加して透過
- `run_pipeline._scrape_new_race_data` → `only_ids=new_ids`、
  `_scrape_new_horse_data` → `only_ids=missing` を渡す
- **安全性**: `transfer_temp_file` が `data/raw` の正本へ race_id/horse_id キーで
  マージ（新優先・既存保持）するため、新規分だけ処理しても既存全件は喪失しない（§3-23 #4 確認済み）。
  `processed_files==0` 時は pkl 非更新ガードもあり

**効果**: ingest の bin 再パースが「全コーパス（6 万件超）」→「新規レース/馬のみ」に。
テスト: `tests/preparing/test_incremental_bin_filter.py`（6 件）。回帰 `tests/` 960 passed。
**ユーザーは `git pull` 後に再実行すれば 24 レースのみ処理で即完了する。**

### 3-37. バルク取得（race/horse/ped）のリクエスト間隔ポライトネス未配線を修正（2026-06-18）

**ユーザー報告**: horse HTML 取得にポライトネス（間隔待機）が利いていない。

**原因**: `PlaywrightScraper.fetch`（単一リクエストのチョークポイント）は **1 時間上限
（`HourlyRateLimiter.try_acquire`）は適用するが、リクエスト間隔（`polite_interval` の
1〜3 秒）を適用していなかった**。間隔待機は `fetch_many`/`scrape_paginated`（複数 URL を
1 呼び出しで回す経路）にしか無く、bin 取得（race/horse/ped）は `process_pkl_file` の
ループが ref_id ごとに単一 `fetch_sync` を呼ぶため**間隔ゼロ**だった。
`process_pkl_file` は `delay`（KEIBA_SCRAPE_DELAY）と `polite_interval` を import/読込済み
なのに**未使用（デッドコード）**で、間隔待機の配線が抜けていた（ruff が unused を警告していた）。

**修正**: `process_pkl_file` の取得ループで `_fetch_with_retry` 直後に
`interval = polite_interval(delay); if interval>0: time.sleep(interval)` を追加。
1 リクエストごとに最低 1 秒 + 揺らぎ（既定 1〜3 秒）の間隔を確保する。delay<=0 で無効化。
これで race/horse/ped のバルク取得すべてに間隔待機が効く（1 時間上限と二重に自主規制）。
※オッズ取得（`OddsCapturer`→`fetch_sync`）は低頻度のため 1 時間上限で十分（従来どおり）。

テスト: `tests/preparing/test_scrape_politeness.py`（2 件: 1 件ごとに間隔待機 / delay=0 で無効化）。
回帰 `tests/` 962 passed。ruff の unused 警告 2 件（delay/polite_interval）も解消。

### 3-38. 券種・払戻/確定オッズのカバレッジ拡大（2026-06-18）

**ユーザー要望**: 単勝/複勝/枠連/三連単/三連複/ワイド/馬単の最終確定オッズと払戻を最大限取得したい。

**現状把握**:
- **払戻データは既に全 8 券種を取得済み**（`create_raw_race_return` が `summary="払い戻し"`
  テーブル全体を取得 → `ReturnProcessor` が単勝〜三連単に分解）。追加実装不要。
- **確定オッズ**: 単勝は results に確定値あり。連系は payout に「的中組合せの確定配当」のみ。
  全組合せの確定オッズは netkeiba オッズページ（race.netkeiba.com/odds/）から取得する必要。

**実装**:
- `src/preparing/_odds_snapshot.py`: `ODDS_PAGE_TYPE`/`ODDS_ID_TYPE` に**枠連(b3)を追加**
  （従来 7 券種 → 単複/枠連/馬連/馬単/ワイド/三連複/三連単の 8 券種に拡大）
- `src/preparing/odds_scheduler.py`: `run(..., request_delay=0.0)` を追加。>0 でリクエスト間に
  `polite_interval`（最低 1 秒+揺らぎ）を挟む。**既定 0 でライブ取得(odds_watch)は不変**
- `src/pipeline/run_pipeline.py`: **`fetch-final-odds` サブコマンド**を新設。
  `--post-date YYYYMMDD` / `--race-id` で過去レースの確定オッズを全券種取得し、
  `OddsSnapshotScraper`（既存の取得+パース）+ `odds_scheduler.run` で
  `odds_snapshots.pkl` + `raw_odds_snapshots` へ phase=t0（確定の代理）で永続化。
  `KEIBA_SCRAPE_DELAY` で間隔自主規制

**テスト**: `test_odds_snapshot.py`（枠連 b3 / 全 8 券種 URL）+ `test_odds_scheduler.py`
（全券種 capture / request_delay 待機 / delay=0 不変）計 5 件追加。回帰 `tests/` 967 passed。

**実環境で動作実証済み（2026-06-18）**: `fetch-final-odds --race-id 202406010101` で
**815 件**のスナップショット（8 券種）を取得・永続化成功。netkeiba は過去レースの確定オッズを
配信しており、連系パーサ `parse_combo_odds_html` も実 DOM で動作することを確認（§4-5 解消）。

**過去全レースのバックフィル（2026-06-18 追記）**: `--post-date` は「その 1 日分」のみ
（ユーザーが「なぜ 24 レースだけ？」と指摘）。過去全レース取得用に `fetch-final-odds` を拡張:
- `--from-results`: 取込済み `results.pkl` の全 race_id を対象（バックフィル元）。`_race_ids_from_results()`
- `--years YYYY …`: race_id 先頭 4 桁で年絞り込み（分割実行用）
- `--limit N`: 1 回の取得レース数上限
- **resume（既定）**: 既に snapshots にあるレースを自動スキップ。`--force` で再取得
- 絞り込みは純粋関数 `_filter_final_odds_race_ids(race_ids, done, years, force, limit)` に分離（テスト済み）
- 全 8 券種 × 数千レースは大規模（~2 秒/件 + 1000 件/時上限）なので `--years`/`--limit` で
  小分けし resume で複数回に分けて実行する運用。テスト 8 件追加（CLI 引数 + フィルタ）
- **進捗ログ + 途中保存**（ユーザーが「無反応だが動いている？」と指摘）: `odds_scheduler.run` に
  `persist_every`（N レースごとに途中保存＋進捗ログ。既定 0=従来どおり末尾 1 回）を追加。
  `fetch-final-odds` は `persist_every=5` で実行し、開始時に推定所要時間（~4 秒/req 概算）を表示。
  これで生存確認でき、中断しても 5 レース単位までしか失わず resume で続行可能
- **取得 0 件問題の対策**（ユーザーが「累計が 815 のまま増えない」と指摘）: 連系の重いオッズ
  ページ（三連単 3360 組等）が `selector_timeout_ms=3 秒`では JS 描画前に空 HTML を返し
  パース 0 件になっていた（30 秒のページタイムアウトも実発生）。対策:
  - `OddsSnapshotScraper` 既定スクレイパのタイムアウトを延長（セレクタ 3→15 秒・ページ 30→45 秒、
    `KEIBA_ODDS_SELECTOR_TIMEOUT_MS`/`KEIBA_ODDS_TIMEOUT_MS` で調整可）、待機セレクタを
    実オッズセル `[id^=odds-]` に変更（描画完了を待つ）
  - `odds_scheduler.run` がバッチ毎の取得件数（+N 件）と **0 件レースの集計警告**を出す（可視化）
  - `_race_ids_from_results` を `_to_db_str` で正準化（float 由来 ".0" を防止）
  - テスト追加（空レース集計 / env タイムアウト / 待機セレクタ）。`tests/` 994 passed
- **タイムアウト延長でも 0 件（2026-06-19）**: selector_timeout=25 秒でも 23/23 レースが +0 件
  （87 分）。**描画問題ではなく、`race.netkeiba.com/odds/` が過去レースの確定オッズを
  一般に配信していない**可能性が高い（最初の 202406010101 の 815 件は例外的に取得できた個体）。
  対応:
  - **診断**: `OddsSnapshotScraper.capture` が空のとき `KEIBA_ODDS_DEBUG=1` で生 HTML を解析
    （`id="odds-"` セルの有無・「終了/提供」等の文言）。`KEIBA_ODDS_DEBUG_DIR` で HTML ダンプ。
    パーサ問題か配信無しかを切り分け可能
  - **確実な代替**: `app/_odds_compare.final_odds_lookup_from_payouts(return_processor)` を追加。
    **払戻データ（ingest 済み・全 8 券種）から「的中組合せの確定オッズ（払戻/100）」**を導出。
    過去レースで確実に得られる確定オッズ実績で、`StoredFinalOddsProvider` に渡せる
  - 結論: 全組合せの過去確定オッズはオッズページからは不可。確実なのは**払戻由来の的中組合せ**。
    全組合せのライブ確定オッズが要るなら開催日に `odds_watch`（T0）で前進取得する。`tests/` 997 passed

### 3-39. 連系オッズ実績の照会 + Harville 推定との比較（2026-06-18）

**ユーザー要望**: 連系オッズのうち過去実績が取得できているものは実績値を照会可能にし、
Harville 推定値と比較照会したい。

**実装**:
| ファイル | 内容 |
|---|---|
| `src/constants/_bet_types.py` | `canonical_combo`/`combo_key` を追加（順不同券種は昇順正規化・順序券種は順序保持）。実績 lookup と EV 選定時の combo 照合を一致させる単一規則 |
| `src/preparing/_odds_snapshot.py` | `build_final_odds_lookup(snapshots, bet_types=)` を追加（snapshots → `{(race_id, bet_type, combo_key): odds}`、最新 captured_at 優先） |
| `src/policies/_odds_provider.py` | `StoredFinalOddsProvider` を追加（実績オッズを返し、無い組合せは fallback=HistoricalOddsProvider の Harville 推定へ委譲。`has_actual` で実績有無を判定） |
| `app/_odds_compare.py` | `compare_combo_odds(snapshots, race_id, bet_type)` = 実績 vs Harville 推定の比較表（買い目/実績/推定/ratio）。`tansho_odds_by_race`/`available_combo_targets` も |
| `app/pages/3_odds_monitor.py` | 「🆚 連系オッズ 実績 vs Harville 推定」照会セクションを追加（レース・券種選択→比較表＋ratio 中央値） |
| `src/preparing/table_creator.py` | `only_ids` 属性を宣言（§3-36 の増分処理で動的設定していた箇所の mypy エラー解消） |

**比較の仕組み**: 同レースの単勝スナップから市場勝率 → Harville 組合せ確率 →
`(1-takeout)/prob` の推定オッズを算出し、確定オッズ実績と組合せ単位で突き合わせる。
ratio=実績/推定（>1 は Harville が過小評価）。

テスト: `test_stored_final_odds.py`（11 件: 正規化/lookup/provider）+ `test_odds_compare.py`
（6 件）+ `test_odds_snapshot.py`/`test_odds_scheduler.py` 既存。回帰 `tests/` 981 passed。

**次ステップ（任意）**: `StoredFinalOddsProvider` を `backtest_bet_type`（§3-35 Phase 2）の
odds_provider に差し込めば、EV 最適化が Harville 推定でなく**実オッズ**で回せる
（実績の無い組合せは自動で Harville fallback）。`run_prediction` への opt-in も同様。

---

### 3-40. データ取得ソースの抽象化・汎用化（netkeiba / JRA-VAN 切替 + UI 選択）（2026-06-19）

**ユーザー要望**: 接続先を netkeiba 以外（JRA-VAN 等）に切替可能なよう、データ取得を抽象化し、
UI からソースを指定して取得できるよう汎用化。選択は「全データ対象 / 抽象化+UI+netkeiba 実装」。

**背景**: `fetch-final-odds` の診断で `html_len=39`（netkeiba の空ページ）と判明 →
過去確定オッズは netkeiba が配信していない。別ソース（JRA-VAN）への切替基盤が必要。

**実装**:
| ファイル | 内容 |
|---|---|
| `src/preparing/_data_source.py`（新規） | `AbstractRaceDataSource`（`resolve_race_ids`/`acquire_races`/`acquire_horses`）+ `NetkeibaDataSource`（既存スクレイパに委譲）+ `JraVanFileDropSource`（`data/incoming/jravan/<種別>/<id>.json` を pandas split 形式で受信し raw へ merge）+ `create_data_source`/`available_data_sources` レジストリ + `save/load_selected_source`（UI↔CLI 共有、`models/selected_data_source.json`） |
| `src/pipeline/run_pipeline.py` | `_scrape_new_race_data`/`_scrape_new_horse_data` を source 経由に refactor（dedup は pipeline・取得は source）。`_ingest` が `create_data_source(_resolve_data_source(args))` でソース生成。`ingest --source netkeiba\|jravan` 追加（未指定は UI 選択 or 既定 netkeiba） |
| `app/pages/4_performance.py` | 設定タブに「データ取得元」selectbox + 保存ボタン（`save_selected_source`）+ JRA-VAN 受信ディレクトリ案内 |

**契約**: 各ソースは「指定 race_id/horse_id の raw を data/raw 標準 pickle(+DB) に反映」する。
netkeiba=スクレイプ（only_ids 増分）、jravan=ファイル受信（Windows JV-Link エージェントが
JSON 配置。エージェント本体は未実装の契約スタブ）。レイヤ: preparing（pipeline→preparing 依存）。

テスト: `tests/preparing/test_data_source.py`（10 件: ファクトリ/委譲/ファイル受信/選択永続化）+
`test_run_pipeline.py`（--source 引数 / `_resolve_data_source`）。回帰 `tests/` 1011 passed。

**残/次**: JRA-VAN の Windows エージェント本体（JV-Link 購読→JSON 出力）は別環境で実装（§4-4）。
オッズ取得（`_odds_source`）も将来この DataSource に統合可能（現状は別契約で併存）。

---

### 3-41. 未実施レースの実戦予測 CLI + 単勝戦略の実運用化（2026-06-21）

**ユーザー要望の流れ**:
1. A/B/C モデル変種は全て残し、23 時間かかる血統 DL は中止（血統=重要度0%・馬情報≈0.2% と判明したため）。
   特徴量追加以外の「別の打ち手」を検討 → **検証済み単勝戦略の実運用化**へ。
2. まだ実施していない（出馬表のみ公開の）レースを、暫定オッズで予測したい。
3. 実戦運用（発走 30〜60 分前に自動再予測、オッズ蓄積、色分け、消滅検知）を反復改良。

**戦略の核（docs/betting_strategy.md）**: 単勝 / EV>1.1 / オッズ≤15 / 分数ケリー(×0.25)。
EV = モデル予測勝率 × 単勝オッズ。2022〜2026 で回収率 ~1.9〜2.1（3〜15 倍に妙味集中）。
モデル支配特徴は owner_id(40.9%)/jockey_id(15.3%)/trainer_id(13.3%)＝全て results 由来。
**パリミュチュエル**: 払戻は確定オッズで、締切（発走 1〜2 分前）までは早期オッズを固定できない。

**新規 CLI `predict_upcoming.py`（ルート）**:
- フロー: 出馬表スクレイプ → ShutubaTableProcessor → ShutubaDataMerger → FeatureEngineering →
  `_align_features`（`model.feature_names_` に列整合・不足0埋め/余分除外/メタ列保持）→
  `run_prediction(model.effective_model, X, op_config)`（検証済み戦略）。
- `--loop`: 窓（既定 30-60 分前。`--window LO-HI`）内レースを `--interval` 秒ごとに再予測。
  起動時に未発走レースを 1 回スナップショット → スケジュール待機。発走時刻は毎ティック取り直し
  （天候遅延で発走時刻が変わったレースも窓へ復帰）。
- 色分け（端末のみ）: 赤=初出/黄=2〜5回/緑=6回以上（出現回数＝安定度。bets 履歴から復元し再起動で継続）、
  オレンジ=消滅可能性高（EV<1.15 かつ前ティックより下落＝剥がれかけ）、灰=消滅（初回のみ）。
- 100 円単位に丸め（< unit/2 は見送り）。`--save-odds` で `data/live/odds_<date>.csv`（全馬の暫定オッズ
  + captured_at + 残り分）と `data/live/bets_<date>.csv`（推奨買い目）を追記蓄積。
- 競馬場・R・発走時刻を表示（`_race_label`）。`--shutuba-pkl` でネット不要のオフライン予測部確認。

**午後の単勝欠損 → オッズ専用ページから補完（本セッションの主眼）**:
- 症状: 午後（発走 30〜0 分前）になると `勝率の総和が0以下…正規化できません｜有効単勝 0/N頭` で予測崩壊。
  原因は **出馬表ページが単勝オッズを JS/Ajax で描画**するため、静的スクレイプが競合し欠損
  （同一レースでも 15:29 は取得・15:32 は全空欄、と断続的）。オッズ支配モデルが全馬低スコア化。
- 採用案（ユーザー選択 = B）: **オッズ専用ページ** `race.netkeiba.com/odds/index.html?type=b1&race_id=…`
  から単勝を取得して馬番でマージ。既存資産 `NetkeibaOddsSource.fetch_win_odds`（`OddsSnapshotScraper`
  = Playwright で JS 描画完了を待ち `parse_combo_odds_html` でパース）を再利用。
- `predict_upcoming._augment_win_odds`: **単勝が揃わないレースのみ**オッズページを叩く
  （前売り時間帯は出馬表に単勝が載るため余分な取得をしない＝ポライトネス維持）。馬番で欠損のみ補完。
- **実 DOM 不一致を発見・修正（重要）**: 現行 b1 ページの単勝セル id は **`odds-1_07`（`_` 区切り）**
  だが、`_ODDS_ID_RE` は `odds-1-07`（`-` 固定）を期待し `find_all` が 0 件 → パース空 → 補完空。
  `src/preparing/_odds_snapshot.py` の正規表現を `^odds-{code}[-_](\d+)$` にして `_`/`-` 両対応
  （複勝=`odds-2_NN`。連系の旧 DOM ハイフン形式と後方互換）。`debug_odds_page.py`（新規・診断用）と
  実 netkeiba HTML で `parse_combo_odds: 10 行 [((1,),10.2),((2,),1.9)…]` を確認。回帰テスト追加。
- **断続失敗の是正**: ①ループ初回の窓再予測が起動時スナップショット直後に走り、同一オッズページを
  数秒で二度叩いていた（throttle/未描画＝空オッズを誘発）→ **再予測の前に `--interval` 待機**。
  ②オッズページが空（Ajax 未描画の瞬間）を返すと無言で諦めていた → **backoff を置いて 1 回再取得**し、
  最終的に空なら**警告ログ**（throttle/未描画を切り分け可。`KEIBA_ODDS_DEBUG=1` で生 HTML 診断）。

**関連ファイル**:
| ファイル | 内容 |
|---|---|
| `predict_upcoming.py`（新規・ルート） | 未実施レース予測 CLI 本体（上記すべて） |
| `debug_odds_page.py`（新規・ルート） | オッズ専用ページの取得・パース診断（HTML を `data/tmp/` へダンプ） |
| `src/preparing/_odds_snapshot.py` | `_ODDS_ID_RE` の区切りを `[-_]` 化（単勝 DOM 対応） |
| `app/_prediction_service.py` | `run_prediction` に `tansho_ev_threshold` 上書き・`max_odds` フィルタ |
| `src/operation/_config.py` | `max_odds` / `tansho_ev_threshold`（無害な既定）追加 |
| `config.yaml` | operation_mode=advisory / bankroll=100000 / kelly×0.25 / max_odds=15 / ev=1.1 |
| `docs/betting_strategy.md`（新規） | 単勝戦略の文書化 |
| `kelly_backtest.py`（新規・ルート） | ケリー資金推移バックテスト（`--max-odds`/`--by-year`/`--max-bet-yen`） |

**検証状況**: パーサ修正は実データで確認済み。end-to-end（午後ライブでの補完→予測）の最終確認は
**次の開催日の午後（発走 30〜0 分前）**に持ち越し（§5 高優先 ⑨）。コンテナからは netkeiba 不達のため。

**運用コマンド例**:
```bash
# 当日の午後を実戦監視（窓 3-60 分前 / 3 分間隔 / オッズ蓄積）
python predict_upcoming.py --date 20260627 --loop --window 3-60 --interval 180 --save-odds
# オッズ専用ページの取得・パース診断（補完が空のとき原因切り分け）
python debug_odds_page.py 202602010412
```

### 3-42. データ取得項目の全面再設計（名寄せ + 最大スキーマ）（2026-06-21〜、進行中）

**経緯**: ユーザーが「`data/live` 以外の全データ + DB + モデルを削除して再取得」を決定
（コンテナ・ユーザー環境とも削除実行済み。`models/version_history.json`=`[]`）。
再取得前に**取得項目の再設計**を実施。netkeiba の全画面サンプル 56 枚をユーザーから受領し、
項目分析・データ構造分析を `docs/data_acquisition_redesign.md` に蓄積（全56サンプル統合済み）。

**確定した方針**:
- **peds（血統）は今回スキップ**（重要度0%・別ページ23h）。器は用意し後でバックフィル
- **取得期間: 全年代（1973〜2026）**
- **プレミアム項目（タイム指数/馬場指数/調子偏差値等）は自前近似で開始**（ログイン対応は作らない）
- **テーブル列は最大限（superset）を用意**し、取得不可な項目は NaN（前方互換スキーマ）
- **進め方: 名寄せ土台 → Tier1**

**画面分析の主要発見**（`docs/data_acquisition_redesign.md` 総括 B/C 参照）:
- 🌟 **調教ページ（追い切り）`oikiri.html`** = 当日仕上がりの独立シグナル・リーク無し（最重要新データ）
- 人物4ページ（騎手/調教師/馬主/生産者）は**共通テンプレート**。owner=`o_`/breeder=`b_` の
  **数値ID↔ハッシュID対応**が必要。年度別成績は **as-of 結合でリーク回避**
- **父・母父は血統ページ(23h)なしで馬プロフィール/一覧から安価取得可**
- オッズ全8券種は既存 odds_watch でカバー済み（追加実装不要）
- **リーク注意**: 人物/馬の「通算・本年」累計はスナップショット＝過去学習にリーク

**実装済み（コンテナ・ブランチ `claude/kind-meitner-1yqyzu`）**:
| ファイル | 内容 |
|---|---|
| `src/preprocessing/_entity_resolver.py` | 名寄せ土台。normalize_text/normalize_breeder_name/extract_race_grade/canonical_id/EntityMaster。テスト24件 |
| `src/constants/_field_catalog.py` | 最大スキーマ定義(11表 superset)。FieldSpec(source/era_min/premium/leak_safe/acquired/resolved_id)。テスト13件 |
| `src/preprocessing/_data_merger.py` | `_add_career_stats`(as-of キャリア=出走/勝/勝率/賞金 リーク無)。`_merge_race_day_notes`(当日ノート左結合) |
| `src/preparing/_html_structure.py` + `fetch_html_samples.py` | 実HTML構造取得(table/div/img alt 要約・ポライトネス・37種別)。テスト15件 |
| `src/preparing/_yoso_marks.py` | 予想印API(api_get_pro_yoso_list_v2)パーサ。無料+プレミアム両取得・goods_kbn保持・堅牢。consensus集約。テスト14件 |
| `src/preparing/_race_day_notes.py` | 調教/パドック/厩舎コメント パーサ + RaceDayNotesScraper + persist_notes。テスト22件 |

**完了**: ✅P0(当日ノート: 取得→登録→マージ→FE 全通し)。**実データE2E検証済**(2026-06-22: featured 478行/425列まで生成)。
**運用方針(ユーザー確定)**: 当日ノートは2010+取得(年代ゲート既定2010)、peds は従来通り取得。
**性能対策**: ノート1レース~7-15s(セレクタ待ち3s×3種)。年代ゲート(KEIBA_RACE_DAY_NOTES_MIN_YEAR)で古い年代の空振り回避。
**backfill-notes ジョブ**: バルク再取得は acquire_races を通らずノートが入らないため、独立ジョブを追加
(`run_pipeline backfill-notes` 既存race_id→年代絞り→ノートのみ取得・取得済みskipで中断再開可)。後 rebuild-featured で反映。
**peds 分離**: `KEIBA_SKIP_PEDS=1`(acquire_horses で血統skip→馬ページ=全horse_id先行取得)＋`backfill-peds`ジョブ
(raw_results.horse_id dedup→peds.pkl取得済みskip→血統のみ低レート取得)。featured は peds.pkl が存在すれば部分でも左結合で生成可。
**符号化被覆(実測2026-06-22)**: 映像グレード97% / パドック評価100% / 調教評価_score 99.8%(語彙34→102に拡充後)。

**✅P1 完了(2026-06-22・実データ検証済)**: 既存rawのみ・リーク無し特徴。
- ①距離: dist_change/kinryo_delta(既存)＋ dist_change_ratio(相対距離変化=新規)
- ②相手強度(軽量): faced_grade_max/mean/graded_count = 過去走レース名の格(extract_race_grade→ordinal G1=5..OP=1)を集計。名寄せ不要
- ③父母父: sire(peds_0=父・既存)＋ damsire(母父=新規)。_add_sire_stats を _add_pedigree_stats(peds_col,prefix) に汎用化し父/母父共用
- **血統表インデックス実マップ(実証)**: peds_0=父, peds_2=父父父(母父ではない!), peds_31=母(unique≒総数), **peds_32=母父(BMS)**。HTML行順フラット化=父系サブツリー(0..30)→母(31)→母系。_nn_cols.py の「peds_2=母父」は誤り(コメント訂正済・NN列は再学習要のため未変更)
**レート制限**: netkeiba は ~800-1000req/h でブロック。KEIBA_SCRAPE_DELAY/KEIBA_MAX_REQUESTS_PER_HOUR で抑制。HTMLキャッシュで再開可。VPN回避は非推奨(規約/負荷)。

**✅P2 予想印配線(2026-06-22・実データ検証済)**: _yoso_marks を取得→永続化→登録→マージ→特徴量化まで配線。
- 取得: NetkeibaDataSource._acquire_yoso_marks(acquire_races配線・年代ゲート・KEIBA_SKIP_YOSO_MARKS・HourlyRateLimiter共有)＋backfill-yoso ジョブ(resume可)
- 登録: RAW_YOSO_MARKS_PATH/TABLE_SPECS(PK=race_id,馬番,predictor_yid)/PICKLE_PATH_TO_ALIAS
- マージ: _merge_yoso_marks(aggregate_consensus→(race_id,馬番)左結合)。YOSO_FEATURE_COLS(n_marks/n_honmei/score_sum/score_mean/n_marks_free)＋zscore
- **重要バグ修正**: 予想印APIの mark キーは馬番でなく **uma_id(出走登録ID,取消馬込みで>18)**。出馬表 tr id="tr_<uma_id>"+td.Umaban で uma_id→馬番変換(parse_umaban_map/fetch_umaban_map)。誤結合時は◎平均人気8.0・コンセンサス1位1着率2.8%だったが、修正後◎平均人気3.5・コンセンサス1位複勝率63.9%で整合。**UA未設定だと API 400**(2024クローラー対策)。
- shutubaパースのドロップ率は実測0%(2026-06-22・6レース101印で未マップuma_id ゼロ)。以前の「~1/3」はDBの12/18 inserted カウンタ誤読。問題なし。

**✅予想家スキル(2026-06-22・方式A自前計算)**: 取得済 yoso_marks+results から各予想家の as-of ◎的中率を
expanding集計(自行除外)し◎加重。yoso_honmei_skill_sum/yoso_best_skill。スクレイプ不要・リーク無し。
DataMerger._add_yoso_predictor_skill。方式B(プロフィール頁scrape)は未着手(履歴拡充用)。

**✅person_yearly(2026-06-22・jockey/trainer)**: /jockey/result/<id>/ (EUC-JP・race_table_01 21列)を
parse_person_yearly(芝/ダート勝率=勝利/出走)。実データ検証済(183行/5人)。
- 登録 RAW_PERSON_YEARLY_PATH/TABLE_SPECS(PK entity_type,entity_id,year)。取得 acquire_persons
  (共有limiter+polite+KEIBA_SKIP_PERSON_YEARLY)＋backfill-persons(resume可)。
- **ID正準化 canon_person_id**: results jockey_id は int64(先頭ゼロ落ち1009)→ netkeiba は5桁ゼロ埋め(01009)。
  URL/格納/結合キーで統一(これが無いと本文空200で取得不可)。
- マージ _merge_person_yearly: レース年Yに前年Y-1成績を結合(リーク無)。jockey_py_*/trainer_py_*＋zscore。
- **owner/breeder も完了(2026-06-22)**: result.html は jockey と同一21列。owner_id=results, breeder_id=horse_info
  から列挙(backfill 既定 types=4種)。_merge_person_yearly で breeder を horse_info から一時列結合→drop。
  owner_py_*/breeder_py_*。
- **衝突fix(2026-06-23)**: persist_person_yearly は `entity_type/entity_id` 複合キー(index名 _pkey)で dedup。
  _merge_person_yearly は _pkey index/二重移行(_pkey列+index 併存)/旧RangeIndex+entity_id列 の3形態を
  reset+drop(errors=ignore)で吸収(回帰テスト3種)。二重移行で出ていた `cannot insert _pkey` を解消。
- **被覆完了(2026-06-23・36レース/478行)**: jockey_py 478/478, trainer_py 464/478, owner_py 405/478, breeder_py 472/478。
  trainer は backfill-persons 取りこぼし67人を skip-existing 再走で穴埋め(取得穴0)。残差は前年実績なしの年度穴(正しい欠損)。
**✅市場歪み特徴(2026-06-23・複勝/三連複/三連単)**: 確定オッズ(odds_snapshots)由来の overlay=市場marginal−Harville理論。
- `_market_signals.py`(純粋): win_odds→Harville(normalize/prob_place)。複勝=implied(Σ3正規化)、三連複=top3 marginal(各combo3頭寄与でΣ3)、
  三連単=1着marginal(Σ1)+top3 marginal(Σ3)。MARKET_SIGNAL_COLS=fukusho_implied_p/place_overlay/trio_top3_overlay/
  trifecta_win_overlay/trifecta_top3_overlay(+_z)。リーク無(確定オッズ=単勝と同前提)。
- 配線: DataMerger(odds_signals_df, _merge_odds_signals=(race_id,馬番)左結合)。run_pipeline._build_odds_signal_frame で
  build_final_odds_lookup(snapshots)+results単勝から構築(未取得/単勝欠なら None=no-op)。FE zscore に追加。テスト11。
- **CLI追加 backfill-horses(2026-06-23)**: results 全 horse_id に馬ページ(horse_results/horse_info)網羅取得。
  backfill-peds と対称・resume可・KEIBA_SKIP_PEDS で血統分離。10年フルイヤー展開の馬層取得用。
- **券種別予測は既存**: 単勝勝率モデル→Harville.combo_probability で全8券種の的中確率→ExpectedValueBetPolicy で EV選定。
**✅2ヘッド予測(2026-06-23・Place=top3 / Win=1着)**: 現行ターゲット rank は実は **着順<4=複勝(top3)**。
  これを Harville に「勝率」として渡す不整合があった。2ヘッドで解消:
- Increment1: ResultsProcessor が rank(top3)+rank_win(1着) を生成。DataSplitter に target_col 追加
  ("rank"既定/"rank_win")。両ラベルを _DROP_FOR_TRAIN/_TEST/_PREDICT に入れ相互リーク遮断。
- Stage A(複勝直接): ExpectedValueBetPolicy が複勝を Harville 再導出せず Place の top3 出力を直接使用。
  select(prob_table, place_prob_table=None) に拡張(direct_place_prob=True 既定)。
- Stage B(Win ヘッド): KeibaAIFactory.create(target_col)/save(suffix="__win")。RetrainJob が
  train_win_head=True(既定) で Win ヘッドも学習し <version>__win.pickle 保存(失敗は非致命)。
  retrain --no-win-head で無効化。app: load_win_head_for(place_path)・run_prediction(win_model=)
  で連系=Win勝率→Harville / 複勝=Place top3直接。テスト: rank_win3+target_col+StageA3+winhead2。

**✅ワイド Place ベース改修(2026-06-23)**: ワイド(2頭が共に3着内)の joint を Win 由来 Harville でなく
  Place の top3 marginal から **固定サイズ抽出の Hájek 二次近似**で算出。
  `π_ab ≈ p_a p_b (1−(1−p_a)(1−p_b)/d), d=Σ p_k(1−p_k)`。p_h は使用前に Σ=3 へ正規化。独立積より小さく
  なり(負相関を再現)、Place が学習したペース/展開相関を反映。`harville.prob_wide_from_place/normalize_place`。
  ExpectedValueBetPolicy の WIDE 分岐で使用(direct_place_prob=False で従来 Harville に戻せる)。テスト harville5+policy2。

**✅EVバックテスト土台(2026-06-23)**: `src/simulation/_backtest.py` + CLI `backtest`。
  2ヘッド予測→確定オッズ(StoredFinalOddsProvider, 無い組合せは単勝Harville推定にfallback)で EV選定→
  実払戻(ReturnProcessor)で決済し、券種別の回収率/的中率/損益を集計。
  - **厳密1点決済**: `BettingTickets.settle_one`(新設)で候補の組合せ「だけ」を決済(BOX再展開しない)。
    順序あり券種(馬単/三連単)は候補の順序どおり1点で評価。初版は bet_*_box 流用で三連単が全6順列に
    膨張するバグ→settle_one で修正(test_settle_ordered_combo_single_point_not_permutation_box)。
  - **枠連は対象外**(枠番ベースで Harville 未対応・combo_probability が例外)。default_thresholds は7券種。
  - CLI: `--version`(省略時最新・<version>.pickle を glob 解決) `--years`(評価期間。学習年と重ねないこと)
    `--no-win-head` `--no-final-odds` `--bet-types` `--json`。テスト8。設計 docs/two_head_prediction_design.md §8.5。

**✅実データ検証(2026-06-23・36レース/478行で smoke)**:
- 2ヘッド学習実走: Place(複勝/top3) AUC=0.7804 正例率23% / Win(単勝/1着) AUC=0.7151 正例率7.8%。
  正例率は ~13頭立てで top3=3/13・1着=1/13 と整合 → ラベル分離が正しい。
- 市場歪み特徴の importance: Place で `fukusho_implied_p` が **rank1/510**(最重要)、`trifecta_win_overlay`(z)上位。
  Win で `trifecta_win_overlay` が **rank4/510**。狙い通りヘッド特化(複勝市場→Place / 三連単1着歪み→Win)を確認。
  注意: `fukusho_implied_p` rank1 は「市場をなぞる」面あり(リークではない=発走前確定値)。エッジは overlay 群と非市場特徴。
- **3券種で十分を実証**: 確定オッズ lookup を複勝/三連複/三連単に絞っても全5特徴が8券種版と non-null 完全一致。
  → 本番バックフィルは3券種でOK(8券種比で約2.7倍速・market_signals 品質は無劣化)。

**⚠️10年バックフィル着手(2026-06-23)・要継続**: results 母数が小さい(当初36レース→2023を3開催日取込で1157行)。
- 手順: `scrape_kaisai_date(from,to)`→`ingest --post-date` ループ→`backfill-horses`→`backfill-peds`→
  3券種`fetch-final-odds`→`rebuild-featured`→`retrain`→`backtest`。tmux 推奨・全工程 resume 可。
- **netkeiba にIPブロックされた**: 初日に実質1秒間隔で数百ページ取得→レート制限。以降 馬ページ/shutuba が空返し。
  対策: **`KEIBA_SCRAPE_DELAY=4`+ `KEIBA_MAX_REQUESTS_PER_HOUR=300` まで減速**＋時間をおく。
- **CLI追加 `KEIBA_SKIP_HORSES=1`(2026-06-23)**: ingest を「レース取込のみ」に絞る(馬/血統は後段 backfill へ)。
  0レース日やブロック時に毎回 未取得馬を再試行して21分浪費するのを防ぐ。KEIBA_SKIP_PEDS と対。テスト1。
- 当面ゴール: **2023年だけ完走→backtest 1本**(学習2023他/評価2023の一部、または年分割で学習<評価)。

**当日ノート(再掲)**:
- 取得: `RaceDayNotesScraper`→`acquire_races`配線(polite_interval・例外握り潰し・KEIBA_SKIP_RACE_DAY_NOTES)
- 登録: LocalPaths/TABLE_SPECS(3表 PK=race_id,馬番)/PICKLE_PATH_TO_ALIAS/catalog raw_comment
- FE: 映像グレード=順序/パドック評価=one-hot/調教評価=ベストエフォート順序(Master.TRAINING_EVAL_ORDINAL・未知NaN・**コメントはraw保持しテキストはdrop**)

**▶ 次に着手すべきタスク(2026-06-23 時点・優先順)**:
1. **【最優先・ユーザ環境】10年バックフィル継続**: まず netkeiba ブロック解除待ち→減速設定(DELAY=4/HOUR=300)で
   `KEIBA_SKIP_HORSES=1` でレース取込ループ→`backfill-horses`/`backfill-peds`を別途低速→3券種オッズ。
   **当面は2023年だけ完走**させ backtest を1本通すのがゴール。
2. **本番評価(データ揃い次第)**: 学習年と評価年を分離(例 学習≤2024/評価2025)で `retrain`→`backtest --years 2025`。
   `--no-win-head` / `--no-final-odds` の差分で **2ヘッド・確定オッズ・市場歪みの寄与を ROI で定量化**。
3. **2ヘッドの実走検証**: 本コンテナは lightgbm 非導入で smoke のみ。10年データで AUC/importance を本評価。
4. **(任意)市場echo対策**: `fukusho_implied_p` が市場をなぞり複勝EVのエッジを消す懸念。overlay 主体への
   重み付け or 生 implied 除外を、本番 importance を見てから検討。
5. **(任意)バックテストUI連携**: ✅完了(§3-43)。`app/pages/5_backtest.py` に第4タブ「🎯🎯 2ヘッド+確定オッズ」を追加。

### 3-43. バックテストUI連携(2ヘッド+券種別 backtest を Streamlit へ接続)(2026-06-23)

CLI `run_pipeline backtest`(§3-38/3-42 の `src/simulation/_backtest.run_backtest`)と同経路の
券種別 EV バックテストを `app/pages/5_backtest.py` に第4タブとして接続。

| ファイル | 内容 |
|---|---|
| `app/_two_head_backtest.py`(新規・Streamlit非依存) | UI アダプタ。`available_years`/`filter_featured_by_years`(race_id先頭4桁で年フィルタ)/`selectable_bet_types`(枠連除く7券種)/`resolve_thresholds`(券種絞り→default_thresholds)/`result_to_frame`(BetTypeStats群→券種別+全体行の表示用DataFrame・日本語ラベル)/`run_two_head_backtest`(KeibaAIから effective_model 抽出→run_backtest→frame添付)。重い評価は `src/simulation/_backtest` に委譲 |
| `app/pages/5_backtest.py` | 第4タブ「🎯🎯 2ヘッド+確定オッズ」追加。モデルver/評価年(multiselect)/確定オッズON-OFF/WinヘッドON-OFF/券種絞り→実行→全体メトリクス+券種別表(回収率/的中率/損益)+CSV。`_load_two_head`(Place+`load_win_head_for`)/`_load_return_processor_cached`/`_load_final_odds_lookup`(snapshots→`build_final_odds_lookup`)をキャッシュ |
| `tests/app/test_two_head_backtest.py`(新規) | 13件(年抽出/年フィルタ/券種選択/閾値/frame整形/run_two_head_backtest の配線=effective_model抽出・年フィルタ・閾値・Winヘッドnone をmonkeypatchで検証) |

- リーク注意をUIに明記(評価年は学習年と重ねない)。的中率/回収率は格納0〜1→表示で×100、CSVは割合のまま。
- 回帰: `tests/app/`+`tests/simulation/` 全passed(環境の optuna-integration[lightgbm] 導入後)。ruff/import-linter(変更ファイル)・mypy(新規app)0エラー。

### 3-44. 開催日の周期性特徴量(sin_date/cos_date・うるう年込み)(2026-06-23)

季節性を円環として符号化する周期特徴量を追加(ユーザー要望)。

- `FeatureEngineering.add_date_cyclical()`: `sin_date=sin(2π·dayofyear/365.25)+1` /
  `cos_date=cos(2π·dayofyear/365.25)+1`。年内通日を365.25で正規化しうるう年を吸収、+1で値域[0,2]。
  `date`欠損はno-op・NaTはNaN。リーク無し(開催日は発走前確定)。
- 配線: run_pipeline の retrain/ingest 両FEチェーン + `predict_upcoming.py`(ライブ) + 統合テストチェーン。
- 列登録: `_feature_cols.DATE_CYCLICAL_FEATURE_COLS=["sin_date","cos_date"]`。
- テスト: `test_feature_engineering_extensions.TestAddDateCyclical` 7件(式一致/値域/うるう年366/欠損/NaT/chain)。
  回帰 tests/preprocessing+constants 318 passed。**新規追加列のため反映には retrain が必要**。

### 3-45. レースクラス正規化の頑健化 + 条件別成績の集計特徴量(2026-06-23)

ユーザー要望: ①レースクラスの取りこぼし対策(GⅠ/GⅡ/GⅢ 全角ローマ数字・レース名の(L)・全角数字)、
②前走Interval/コース長別/種別×馬場別/クラス別の成績を前処理で作成し特徴量化。

**①クラス正規化の単一情報源(`src/constants/_master.py` に同居=constants層フラット制約)**:
- `classify_race_class(text)`: NFKC + 正規表現でグレード(GⅠ→G1/Ｇ３/Jpn/括弧有無)・リステッド((L)/（Ｌ）)・
  条件戦(新馬/未勝利/1-3勝・全角数字・旧称500-1600万下)を正準 RACE_CLASS_* へ。優先 grade>L>条件。
  "GIRL"等の単語内誤検出は境界で抑止。
- `race_class_level(x)`: 格の順序値(新馬/未勝利=1…OP=5,L=6,G3=7,G2=8,G1=9)。生レース名/旧称も可。
- **取りこぼし修正(本丸)**: `modules.create_raw_race_info` は従来 race_condition しか見ず、
  **グレード/Lはレース名(race_name)に出る**ため G1〜G3・Listed を取りこぼしていた。
  race_name と race_condition の両方を classify_race_class にかけグレード/L優先で解消(旧ループはフォールバック)。

**②条件別成績の集計(`DataMerger`、リーク無し=過去走のみ・horse_id左結合。ライブも ShutubaDataMerger 継承で自動適用)**:
- 前走Interval: 既存(`interval`)。変更なし。
- `_add_course_condition_stats` 拡張: `avg_rank_at_distance`/`n_runs_at_distance`/`win_rate_at_course_type` 追加(既存 win_rate_at_distance/avg_rank_at_course_type に加え)。
- 新 `_add_type_ground_stats`: 今回の種別×馬場(芝→gs1/ダート→gs2)に合致する過去走で `win_rate_type_ground`/`avg_rank_type_ground`/`n_runs_type_ground`。
- 新 `_add_race_class_stats`: 過去走レース名を classify_race_class→順序値化し今回 race_class と突合。`win_rate_same_class`/`avg_rank_same_class`/`n_runs_same_class`/`win_rate_higher_class`(格上で勝てる=真に強い)/`best_class_won`(勝利した最高格)。
- 登録: `_feature_cols` に COURSE_CONDITION 拡張+`TYPE_GROUND_FEATURE_COLS`/`RACE_CLASS_FEATURE_COLS`、FE の zscore named_feature_cols へ配線。

**検証**: `tests/constants/test_race_class.py`(21件)+`test_data_merger_features.py`(新10件)。
全体 `tests/` 1312 passed/17 skipped。新規/変更src は ruff・mypy 0エラー。
アーキ2件失敗(import_linter/no_upward)は **既存**(_market_signals→_harville・_data_merger→preparing。git stash で同様に失敗を確認)で本変更とは無関係。
**新規列のため反映には rebuild-featured + retrain が必要**。

### 3-46. 現レースの格取得をライブ側も統一(取りこぼし対策の横展開)(2026-06-23)

§3-45 で学習側(`modules.create_raw_race_info`)の現レース格取得を classify_race_class に統一したが、
**ライブ(出馬表)側 `_scrape_shutuba._parse_race_header` が同じブリットルな判定**(`"G3" in class_text`で
GⅢ/GIII取りこぼし・(L)未対応・全角数字条件戦取りこぼし)のままだった。これを classify_race_class に
統一し、学習/ライブで現レースの格取得を完全一致させた。
- 生きている現レース格取得は2経路のみ(学習=create_raw_race_info / ライブ=_parse_race_header)で両方修正済み。
  modules.py:1156/1276-1280 の `"G3" in` 等は `r""" """`(1085-1283)内のデッドコードで無効。
- テスト `tests/preparing/test_shutuba_race_class.py`(7件: GⅡ/GⅢ/GIII/(L)/全角１勝クラス/旧称/NaN維持)。
  preparing 200 passed。ruff/mypy(変更ファイル)0エラー。

### 3-47. 現レースの格を順序値特徴量化(race_class_level)(2026-06-23)

gap B 解消。現レースのクラスは従来 `dumminize_race_class` の one-hot のみで格の大小(順序)を
持たなかった。`FeatureEngineering.add_race_class_level()` を追加し `Master.race_class_level` で
新馬/未勝利=1…L=6,G3=7,G2=8,G1=9 の順序値列 `race_class_level` を生成(one-hot と併用)。
- dumminize_race_class が race_class を drop するため**その直前**に配置(retrain/ingest/predict_upcoming/統合テスト全チェーン)。
- レース内で全頭同値のため zscore 対象には含めない(レース間比較用)。未知クラスは NaN。
- `_feature_cols.RACE_CLASS_LEVEL_COL` を文書用に追加。
- テスト `test_feature_engineering_dummify.TestAddRaceClassLevel`(5件)。全体 tests/ 1325 passed。
  新規列のため反映には rebuild-featured + retrain が必要。

### 3-48. レースクラス分類の網羅性監査ツール + 補強(2026-06-23)

ユーザー要望: 分類網羅性の確認と「判定不能」の実値把握、取得後に判定不能を正しく分類できる仕組み。
- **監査ツール**: `src/preprocessing/_race_class_audit.py`(純粋: coverage_summary/unclassified_counts/
  class_distribution/classify_with_level)+ ルート CLI `audit_race_class.py`。取得後に実行し
  `classify_race_class` が None に落とす生テキストを出現回数つきで洗い出す。
  入力源は **horse_results.レース名**(全年代・グレード括弧込み=最良)と **race_info の race_class=NaN 行**
  (タイトル非永続のため race_condition で診断)。空データでも安全(skip)。
- **分類規則の補強(安全な明確分)**: 「リステッド」リテラル → リステッド、「メイクデビュー」→ 新馬 を追加。
- **合成デモで確認**: 地方クラス(C1四/B2三)・メイクデビュー・(OP) 等が判定不能として正しく列挙される。
  地方競馬クラス(A/B/C級)は JRA と別体系のため**自動追加せず**、実データ監査後に方針決定(scope)。
- テスト: `test_race_class_audit.py`(8件)+ `test_race_class.py` に2件追加。preprocessing/constants 362 passed。
- **運用**: 取得後 `python audit_race_class.py --top 50` → 判定不能を _master.py の規則へ追加 →
  `tests/constants/test_race_class.py` に回帰ケース追加、のループで網羅率を上げる。

### 3-49. 直近 N レースの成績率 特徴量(_add_recent_form_stats)(2026-06-23)

ユーザー要望「直近nレースのレース結果成績」。既存 §2i 多窓集計(着順_mean_NR 等=分布統計)を
補完する近走フォームの率指標を DataMerger に追加(リーク無し・horse_id 左結合・ライブも自動適用):
- N_RACES_LIST=5/9/20 の各窓で `win_rate_NR`(勝率) / `rentai_rate_NR`(連対率<=2) /
  `place_rate_NR`(複勝率<=3・Place ヘッドの top3 と整合) / `avg_rel_rank_NR`(着順/頭数 平均)。
- `_filter_horse_results(hr, n)`(date 降順 head n)で直近 N 走に限定。`RECENT_FORM_FEATURE_COLS` を
  `_feature_cols` に動的生成し FE のレース内 zscore に配線。
- テスト `test_data_merger_features.TestAddRecentFormStats`(4件: 列生成/率/窓制限/空 noop)。
  preprocessing+constants 366 passed。ruff/mypy(変更ファイル)0エラー。新規列のため rebuild-featured+retrain で反映。

### 3-50. パリミュチュエル・エッジ検証基盤(Edge/EV診断 + オッズ特徴A/B)(2026-06-24)

パリミュチュエルは払戻=確定オッズ基準なので、優位は `EV_i=r̂_i·O_{i,final}−1` /
`Edge_i=r̂_i−p̂_{i,final}`（r̂=自分の勝率, p_final=最終市場勝率）で決まる。「現在オッズとのズレ」
ではなく「予測最終オッズ vs 自分の勝率のズレ」。力学モデル(P̂_final)はライブ専用で、
**エッジの有無は過去レース(実現最終オッズ)で先に判定できる**——という整理に基づく2機能:

**①Edge/EV診断(`src/simulation/_edge_diagnostic.py` 純粋 + `backtest --edge-diagnostic`)**:
- r̂=Winヘッド予測をレース内Σ=1正規化 / p_mkt=実現単勝オッズ由来(控除抜きΣ=1) / edge=r̂−p_mkt /
  ev=r̂·odds−1。人気帯別較正(平均p_mkt/r̂ vs 実勝率)+ マーケットエコー相関(r̂,p_mkt)+
  **勝ち馬logloss(モデル vs 市場)→ model_beats_market**(市場を超える情報の有無=単一判定指標)。
- 力学モデル不要(実現最終オッズ使用)。テスト9件。
**②オッズ特徴 on/off A/B(`retrain --no-odds-features`)**:
- 生`単勝`は元から学習除外だが、市場エコーは派生列(`単勝_log`・市場歪み overlay 群+`_z`)経由で入る。
  `ODDS_DERIVED_FEATURE_COLS` を定義し、フラグ時に featured から除外(Place/Win両ヘッド)。
- オッズ無し版がp_finalに勝てば「市場に独立な情報あり=本物のエッジ」、有り版しか勝てなければ
  市場の写し+人気穴バイアス補正(ライブはtrain/serve skew注意)。
- `ExpectedValueScorePolicy.calc` に列アライメント(feature名reindex)フォールバックを追加し、
  縮小モデルでも backtest/診断が落ちないよう堅牢化。テスト4件。
- 回帰 policies+simulation 252 passed。ruff/mypy(変更ファイル)0エラー。
- **使い方**: 2023取込完走後 `backtest --version twohead_2023 --years 2023 --edge-diagnostic`、
  および `retrain --no-odds-features`(+`--no-stacking`推奨) との A/B で対市場エッジを定量化。

### 3-51. ベンター/芦谷 論文の知識統合（補正Harville・合成・エッジ診断・プール逆算）(2026-06-25)

ユーザーがベンター(1994)と芦谷(2012)の論文を提供。プロジェクトに統合:
- **§3-50 Edge/EV診断 + オッズ特徴A/B**(別記)。実データ A/B 結果: no-odds でも市場 logloss<(1.72 vs 1.92)
  だが**~8割インサンプルで楽観**。較正崩れ(本命過小評価)も確認 → OOS評価と r̂較正が宿題。
- **Benter べき乗補正 Harville**(`_harville.py`): σ_i=π^γ/Σ, τ_i=π^δ/Σ, P(i→j→k)=π_i·σ_j/(1-σ_i)·τ_k/(1-τ_i-τ_j)。
  三連単/三連複/馬単/馬連の補正版+`fit_place_exponents`(MLE)+`combo_probability(exponents=)`。γ=δ=1で素のHarville一致。
  Benter香港参考値 γ=.81/δ=.65(競馬場依存=要自前fit)。素のHarvilleは2/3着バイアスで「補正なしで使うな」(論文明言)→§3-34 Place改修の正当化。
- **芦谷 プール逆算**(`src/policies/_arbitrage.py`): パリミュチュエル式 odds=base+(S/s)·factor を逆解しオッズから
  総投票数 S(=V_t)を復元。`recover_pool_total`(最小売上枚数を仮定する高速法)/`arbitrage_indicator`(ΣC_a<1でlock)/
  `odds_after_purchase`(自己購入のオッズ影響)。
- **プール逆算をオッズ力学に配線**(`_odds_feature_builder`): `recover_pools_by_phase`(三連単が逆算好適)+
  `build_training_frame(include_pool=True)` で `pool_current`+`pool_log_ratio_<phase>`(出来高モメンタム)を追加。
  **「総投票量は観測不能」(A_t議論)を解消**。成長率は factor(控除率)非依存で頑健。
- docs/benter_model.md に知識整理。tests: harville_corrected(34)+arbitrage(11)+edge_diagnostic(9)+pool配線(4)等。
  policies/training 全passed。ruff/mypy(変更ファイル)0エラー。import-linter は既存2違反のみ(training→policiesは順方向OK)。
- **残**: (γ,δ)の自前MLE/合成 logpool(combine_logpool)/ΔR²診断/プール影響をbacktestへ/OOS評価/r̂較正。

### 3-52. ベンター/芦谷スレッドの宿題4件を実装（純粋関数+論理テスト）(2026-06-25)

データ不要の論理部分を pure module で実装（実 fit は OOS データ揃い次第）:
1. **(γ,δ)較正の永続化**(`_harville.save/load_place_exponents`): fit_place_exponents は既存。
   論理テスト=既知(γ,δ)の真確率比例の着順分布から MLE が母集団値を回復+保存往復。
2. **ベンター2段目 合成**(`src/policies/_blend.py`): combine_logpool(レース内softmax(α·logP_fund+β·logP_public))/
   fit_blend(勝ち馬MLE)/pseudo_r2/blend_diagnostic(ΔR²=R²合成−R²公衆)。
   論理テスト=独立情報→ΔR²>0.02・エコー→|ΔR²|<0.02(tipster知見)。13件。
3. **プール影響EV**(`_arbitrage`): effective_odds/ev_with_impact/max_profit_bet(自己購入で確定オッズ低下→
   EV低下・期待利益は有限内点最大)。EVモデリングは truncate=False の生オッズ。論理テスト6件。
4. **r̂較正**(`src/policies/_calibration.py`): within-race isotonic(fit=sklearn/predict=np.interp)で本命過小評価是正+
   レース内Σ=1正規化+ECE近似+永続化。論理テスト=歪み(sqrt)を真値へ補正・較正誤差低減。8件。
- policies/training/simulation 497 passed/4 skipped。ruff/mypy(新規)0エラー。
- **残(配線・実データ)**: EVポリシー/backtest へ exponents/blend/calibrator/pool影響を opt-in 配線、
  unratable→公衆フォールバック、**全部 OOS で fit**(学習年<評価年。in-sample は楽観/退化)。

### 3-53. EVパイプラインへの配線（補正Harville/r̂較正/市場合成）(2026-06-25)

§3-52 の部品を `ExpectedValueBetPolicy` へ **opt-in DI**（いずれも None で従来挙動完全保持）:
- `place_exponents`: 連系の順序確率を combo_probability(exponents=) で Benter べき乗補正
- `win_calibrator`: `_select_for_race` 先頭で勝率を isotonic 較正→レース内Σ=1（本命過小評価是正）
- `blend_weights`: モデル勝率 × 市場 implied（odds_provider の単勝オッズ由来）を combine_logpool 合成
- 複勝/ワイドは Place ヘッド直接のため較正・合成の対象外（place_probs は元 win_probs を保持）。
- `_backtest.select_candidates`/`run_backtest` にも3引数を passthrough。
- テスト `test_ev_pipeline_wiring.py`(5件: 補正一致/較正で勝率変化/合成=combine_logpool/全None後方互換)。
  policies+simulation 313 passed。ruff/mypy(変更)0エラー。
- **残(CLI/実データ)**: backtest CLI に `--place-exponents/--calibrator/--blend` 等のロード配線、
  fit ジョブ（OOS で γ,δ / α,β / calibrator を学習し models/*.json 保存）、run_prediction への配線。
  → CLI/fit ジョブは §3-54 で完了。

### 3-54. fit ジョブ + CLI フラグ（calibrate-ev / backtest ロード）(2026-06-25)

§3-52/3-53 の3アーティファクトを **OOS データから fit→保存→backtest でロード**する経路を実装:
- `src/simulation/_calibrate.py`:
  - `build_calibration_inputs(win_model, X)` → `CalibrationInputs`。`ExpectedValueScorePolicy.calc` で
    [馬番,prob,単勝] を作り `着順` を**位置整合**で付与（calc は行順保持・index=race_id は非ユニーク）。
    レース単位に (勝率, 1-2-3着馬番)=place_races / (P_fund正規化, P_public=1/単勝正規化, 1着)=blend_races /
    (生prob, 着順==1)=較正標本 を組む。着順欠損馬は較正から除外、3頭未満は place から除外。
  - `fit_and_save_place_exponents/calibrator/blend` + `fit_all(win_model,X,*,models_dir,which)`。
    既定パス `models/{place_exponents,win_calibrator,blend_weights}.json`（`*_path()` ヘルパ）。
- `run_pipeline.py`:
  - `calibrate-ev` サブコマンド（`_calibrate_ev`）: Win ヘッド(`__win`)をロード（無ければ Place 代替）、
    featured を `--years` で OOS 抽出、`--which {exponents,calibrator,blend}` を fit。`--dry-run` で件数のみ。
    `--years` 未指定は楽観バイアス警告。
  - `backtest` に `--corrected-harville/--calibrate/--blend`: 各 JSON を load_* して run_backtest へ DI。
- テスト `tests/simulation/test_calibrate.py`(8件: 入力組み立て/着順順序=winner/public正規化/欠損除外/
  3アーティファクト往復/fit_all 選択保存)。argparse 配線も実値確認。policies+sim 全green。
  ruff(新規)/mypy(新規)0エラー。layering 既存2違反のみ(market_signals→policies, data_merger→preparing・私の変更外)。
- **使い方**: `python -m src.pipeline.run_pipeline calibrate-ev --years 2025`（学習が〜2024 のモデル前提）→
  `... backtest --years 2025 --corrected-harville --calibrate --blend`。**OOS 実走はデータ投入後**（Benter §5: 学習年<評価年厳守）。

### 3-55. ライブ配線 + unratable 公衆フォールバック (2026-06-25)

**A. ライブ経路への較正アーティファクト配線**:
- `app/_prediction_service.py`: `_load_ev_artifacts()` で models/{place_exponents,win_calibrator,
  blend_weights}.json を読み、`OperationConfig.use_ev_calibration=True` のときだけ `run_prediction` の
  `ExpectedValueBetPolicy` に DI（無い項目は None=従来挙動）。`use_predicted_odds` と同じ opt-in 規約。
  in-sample 退化を避けるため既定 False（OOS で fit したものを使う前提）。
- `_bet_type_optimizer` は閾値グリッド探索の別目的のため配線対象外（直交）。

**B. unratable（初出走）→ 公衆フォールバック**（ベンター §3）:
- `src/policies/_unratable.py`（純関数・policies→constants のみ）:
  - `public_fallback(model_probs, public_probs, unratable)`: 初出走馬を公衆 implied 勝率で置換、
    評価可能馬のモデル勝率を残余質量(1−Σ公衆)へ比例再正規化。Σ=1。公衆値の無い初出走馬はモデル維持。
  - `is_unratable_only(umabans, unratable)`: 初出走のみのレース判定（除外対象・香港で~5%）。
  - `build_unratable_by_race(X)`: featured の `career_starts`(0/NaN=初出走) から {race_id:{馬番}}。
- `ExpectedValueBetPolicy(unratable_fallback=True)` ＋ `select(unratable_by_race=)`。較正/合成より**先**に適用、
  初出走のみのレースは候補なし。`_apply_unratable_fallback` が公衆オッズ(単勝)から implied を作り置換。
- 配線: `_backtest.select_candidates/run_backtest(unratable_fallback=)`、live(`use_unratable_fallback`)、
  `backtest --unratable-fallback`。
- テスト: `tests/policies/test_unratable.py`(11件: 置換/比例配分/公衆欠落維持/初出走のみ除外/featured検出)、
  `test_prediction_service.py`(較正配線3件)。policies+sim+app 487 passed。ruff(変更)0・mypy(新規)0。
  ついでに run_pipeline.py:196 の既存 B905 を strict=False で解消。

### 3-56. C/D/E: プール影響ケリー・ΔR² CLI・レイヤ債務解消 (2026-06-25)

**C. プール影響をケリーへ**:
- `src/portfolio/_kelly.py`: `pool_capped_stake(stake,prob,odds,pool,*,ticket_unit=100,base,factor=.788)`
  — `_arbitrage.max_profit_bet` の最適枚数 k* でケリー枚数を上限、低下後 `effective_odds` で
  impact-EV<=1 なら 0。`KellyPortfolioOptimizer(pool_impact=True)` ＋ `allocate(pool_by_race=)`。
  portfolio→policies._arbitrage は下方向で合法。pool 不明な券種/レースは素のケリー。
- live: `OperationConfig.use_pool_impact` ＋ `run_prediction(pool_by_race=)`（復元プールを渡す）。
  pool 源（三連単等からの recover_pool_total）の配線は実オッズ snapshot 待ち。

**D. ΔR² を backtest CLI 出力へ**:
- `_edge_diagnostic.blend_diagnostic_from_edge(edge_df, weights=None)` — edge_df を
  (p_fund=r_hat, p_public=p_mkt, winner) に変換し `_blend.blend_diagnostic`。weights 未指定は
  fit_blend（in-sample 注記）。`run_edge_diagnostic` の戻りに "blend" 追加、`format_edge_report`/
  `backtest --json`(delta_r2) に R²(公衆/合成)・ΔR²・(α,β) を表示。ΔR²>0=独立情報、≈0=市場の写し。

**E. レイヤ債務の解消（test_layering green 化）**:
- V1 `_market_signals→policies._harville`: 純粋プリミティブ `normalize/prob_place/_prob_in_top` を
  `src/preprocessing/_place_prob.py` へ移設。`_harville` は再 export（`harville.normalize` 温存）、
  `_market_signals` は `_place_prob` を直接 import。policies→preprocessing は下方向で合法。
- V2 `_data_merger→preparing`: `aggregate_consensus`→`preprocessing/_yoso_consensus.py`、
  `canon_person_id`→`preprocessing/_person_id.py`、`FREE_GOODS_KBN`→`constants/_yoso.py` へ移設。
  preparing 側（_yoso_marks/_person_yearly）は再 export で後方互換。
- import-linter / test_layering **5 passed**（旧2違反消滅）。ついでに既存 ruff 4件
  （F401/E501/B904×2・私の変更外）も解消し `ruff check src/ app/` 完全 green。
- テスト: `test_kelly_pool_impact.py`(8)・`test_edge_diagnostic`(ΔR² 3件)・`test_prediction_service`
  (pool 配線2件)。**全体 1449 passed/17 skipped**。mypy(新規)0。

### 3-57. リファクタ #4(閾値重複解消)・#1(run_pipeline 分割) (2026-06-26)

**#4 default_thresholds の重複解消**:
- `simulation/_backtest.py` と `app/_prediction_service.py` に同一の BetThresholds→dict があった。
- 単一ソース `src/policies/_thresholds.py::bet_threshold_map(thresholds=None)` を新設し両者が委譲。
  constants は flat 制約（intra-constants import 不可＝`test_constants_has_no_src_imports`）のため、
  BetType+BetThresholds を束ねるこのロジックは policies 層に置く（最初 constants に置いて失敗→移設）。

**#1 run_pipeline.py 分割（1683→1325行）**:
- `src/pipeline/_cli_parser.py::build_parser()` — argparse のサブコマンド定義（旧 _parse_args の
  280行・81 add_*）を分離。`_parse_args` は `build_parser().parse_args(argv)` に縮小（純粋・テスト容易）。
- `src/pipeline/_cli_common.py` — 横断インフラ（`_auto_migrate_db`/`_load_raw_db_first`/
  `_return_processor_db_first`/`_finish_log`/`_notify_failure`/`_run_job`）を分離。run_pipeline は
  素の名前参照を保つため4関数を**再 export**（テストの `monkeypatch.setattr(rp, ...)` も従来通り効く）。
- 全 pipeline テスト 107 passed（monkeypatch 多用の calibrate/ingest 含む）。全体 1451 passed。
  ruff check src/ app/ 完全 green。**残（未着手の中〜大）**: handler 群の commands/ パッケージ化は
  handler 本体の smoke テスト整備とセットで（本体は未 unit テストのため慎重に）。#2 _data_merger 分割・
  #3 modules.py/CamelCase リネームも未着手。

### 3-58. run_pipeline handler 分割（commands/ パッケージ化）+ smoke (2026-06-26)

§3-57 の続き。**run_pipeline.py 1325→80行**（全体 1683→80）。
- `src/pipeline/commands/` 新設、handler を5モジュールに分割:
  `_ingest`(ingest/rebuild-featured+取得ヘルパ8関数)・`_backfill`(6)・`_train`(retrain+config)・
  `_evaluate`(evaluate-odds-dynamics/fetch-final-odds/backtest/doctor+helpers)・`_calibrate`(2)。
- 各モジュールは共有ヘルパを `from _cli_common import ...` / `from commands._ingest import ...` で
  **名前束縛** → handler 本体は verbatim 移動（呼び出しは素名のまま解決）。import DAG は非循環
  (_cli_common ← _ingest ← {_backfill,_evaluate,_train}; _evaluate ← _calibrate)。
- run_pipeline は facade: module-level `HANDLERS` dict + `main`（ディスパッチ）+ `_parse_args`(委譲)。
- **smoke テスト** `tests/pipeline/test_cli_smoke.py`(3): build_parser の全 subcommand ⇔ HANDLERS 網羅、
  全 handler callable、各 job の parse→dispatch。handler 本体未 unit テストのリグレッション網。
- 既存 handler テストの monkeypatch は新モジュールへ repoint（`rp.X`→`commands._calibrate.X`/
  `commands._ingest.X`/`_cli_common.X`/`commands._backfill.X`）。AST で関数ブロックを機械抽出し転記ミス回避。
- 検証: pipeline 110・全体 **1454 passed**/17 skipped。ruff src/app 完全 green。
  `python -m src.pipeline.run_pipeline --help` で全 subcommand 解決を確認。挙動不変（CLI 公開面維持）。

### 3-59. #2 _data_merger.py 特徴ファミリ分割（horse_features 抽出）(2026-06-26)

**_data_merger.py 1326→817行**。horse_results ベースの純粋特徴ビルダ11個＋集計ヘルパ3個を
`src/preprocessing/_horse_features.py`（581行）へ自由関数として抽出:
- ビルダ: add_{pace,growth,prev_race,aptitude,speed_figure,career,recent_form,opponent_strength,
  course_condition,type_ground,race_class}_stats（いずれも (results, horse_results) -> results・リーク無し）
- ヘルパ: filter_horse_results / summarize / summarize_with（純粋）。`GRADE_ORDINAL` 定数も移設。
- DataMerger は**薄い委譲メソッド**（`def _add_X(self,...): return _hf.add_X(...)`）を保持 → merge()
  オーケストレーションも既存テストも無改変。AST で関数ブロックを機械抽出し de-self 変換（self._filter_
  horse_results→filter_horse_results 等）、転記ミスを ruff F821＋テストで担保。
- stateful な _merge_*/_add_yoso_*/_add_jockey_trainer_stats/_add_sire/damsire/pedigree は class に残置。
- 検証: preprocessing 311・全体 1454 passed/17 skipped。ruff src/app 完全 green。

### 3-60. #3 _data_merger pedigree/yoso 族抽出 (2026-06-26)

**_data_merger.py 811→627行**。stateful な2族を明示引数の自由関数として切り出し:
- `_pedigree_features.py`(109): `add_sire/damsire/pedigree_stats(results, target_date, *,
  hr_with_sire_dict, peds)`。transient per-date スライス・peds を明示引数で渡し、
  ライフサイクル（_separated_hr_with_sire_dict の構築/破棄）は merger に残置。HRCols/
  SIRE_RECENT_YEARS の import も移設。
- `_yoso_features.py`(147): `merge_yoso_marks(results, yoso_marks)` /
  `add_yoso_predictor_skill(results, yoso_marks)` / `add_yoso_profile_skill(results,
  yoso_marks, yoso_predictor) -> results`。AST 抽出＋変換（最終 `self._results=X`→`return X`、
  早期 bare `return`→`return results`、状態 self._yoso_*→引数）。
- DataMerger は薄い委譲メソッドを保持（merge() 無変更）。
- 検証: yoso 3メソッドは test_race_day_notes_merge.py が**実値フルパス＋no-op**を網羅
  （yoso_best_skill/profile_skill_sum 等を assert）→ 変換の正しさを担保。pedigree は
  test_data_merger_features.py。全体 1460 passed。ruff src/app green。

**リファクタ一巡完了**。神モジュール/巨大関数/重複の主要 smell は解消。
（Home.py は streamlit 外部契約のため CamelCase 据え置き＝意図的）。

**残ロードマップ**(docs/data_acquisition_redesign.md §🗺️ に優先順あり):
- P1: Tier1導出(距離延長短縮/斤量増減=prev_race拡張) / 相手強度(勝ち馬→ID名寄せ) / 父・母父取得→sire集計
- P2: 予想印取得配線(fetch_pro_yoso_marks→raw登録→consensusマージ) / 予想家スキル(yoso_profile as-of) / 人物年度別成績(数値↔ハッシュID)
- P3: 調子偏差値の自前再現 / 馬静的属性 / 水分量。 P4: peds backfill / コメントTF-IDF
- 横断: peds skip ingest配線 / **再取得はユーザー環境**（コンテナから netkeiba 不達。yoso/odds API は要ネット）

### 3-61. レーティングモデル サブシステム Phase 1（ペアワイズ Elo + 着差補正）【✅コード完成・実データ検証待ち】(2026-06-27〜07-15)

**目的**: 各馬の「地力」を着順履歴から推定した潜在量（レーティング）を、既存 LightGBM+NN
スタッキングの**入力特徴量**として追加する（独立予測器ではない）。EV/Harville/ケリーまで
自動波及。多段ロードマップ Phase 1→5（Elo→TrueSkill→条件別→状態空間→階層ベイズ市場prior）の
第1段。計画書: `/root/.claude/plans/harmonic-crunching-fern.md`。

**設計判断（確定）**:
- 統合方式 = **特徴量として統合**。On/Off は `--no-rating-features` で「Elo列を含む/含まない」
  2モデル variant を学習し A/B（学習が `X_train.values` 位置指定のためライブ列ドロップ不可、
  という `--no-odds-features` と同じ確認済み制約）。再学習不要の即時検証は Elo→勝率の
  スタンドアロン照会（`field_win_probs`）で行う。
- Phase 1 範囲 = 土台+Elo計算+配線+検証+最小UI。着差補正は任意（着差不明時は K一定にフォールバック）。
- リーク無し as-of: `build_rating_frame` が日付昇順に1パスし「**出走前**の現行レーティング」を
  特徴量化、結果更新は次レース以降にのみ反映。precompute+join（`_merge_odds_signals` 踏襲）。

**実装済み（このブランチ `claude/happy-archimedes-10bmev`）**:
- `src/preprocessing/_ratings.py`【新規】純粋計算（constants/_feature_cols のみ依存・preprocessing層）:
  `expected_score` / `margin_multiplier`(着差→K倍率, cap2.0) / `field_win_probs`(Elo強度比の正規化勝率) /
  `update_race`(ペアワイズ更新, 各馬 (n-1)平均で1レース=1更新, 零和) / `_field_features` /
  `build_rating_frame`(results[race_id,date,馬番,着順,horse_id,着差]→(frame, snapshot)) /
  `features_from_snapshot`(ライブ予測用)。定数 ELO_INITIAL=1500/BASE_K=24/DIVISOR=400/MARGIN_SCALE=0.5。
- `src/constants/_feature_cols.py`【改】`ELO_FEATURE_COLS`=[elo_rating, elo_n_races, elo_field_mean,
  elo_vs_field, elo_win_prob] を追加。
- `src/constants/_local_paths.py`【改】`HORSE_RATINGS_PATH`(=data/raw/horse_ratings.json) 追加。
- `src/pipeline/commands/_ingest.py`【改】`_build_rating_frame(config)` 追加（raw results+race_info由来
  date で時系列構築、snapshot を HORSE_RATINGS_PATH に保存）。`_build_featured_data` の DataMerger
  生成に `rating_df=_build_rating_frame(config)` を配線。
- `src/preprocessing/_data_merger.py`【改】`__init__(rating_df=...)` + `_merge_horse_ratings`
  ((race_id,馬番) 左結合, merge() の odds_signals 直後)。
- `src/preprocessing/_feature_engineering.py`【改】`add_race_level_zscore` の named_feature_cols に
  ELO_FEATURE_COLS（elo_field_mean はレース内一定なので除外）を追加 → `_z` 自動生成。
- `src/pipeline/commands/_train.py`【改】`--no-rating-features` で elo_* と _z を学習から除外。

**Phase 1 ✅ コード完成（2026-07-15 確認・`claude/context-review-next-tasks-g6y5dt` に取込済）**:
happy-archimedes は当ブランチの祖先で、以降の作業で下記6残タスクは**全て完了**していた（当欄が stale だった）。
監査で全確認:
1. ✅ **CLI フラグ**（`_cli_parser.py`）: retrain(L54)/calibrate(L265)/backtest(L310) に `--no-rating-features`。
2. ✅ **backtest 列一致**（`_evaluate.py` L240-247）: `--no-rating-features` 時に elo_*/_z を除外。
3. ✅ **ライブ経路**: `_shutuba_data_merger._merge_live_ratings`（L217, `merge()` L159 で配線）が
   HORSE_RATINGS_PATH を読み `features_from_snapshot` で付与。`predict_upcoming.py` は
   `ShutubaDataMerger.merge()`(L392) 経由で自動適用（直接参照不要）。
4. ✅ **テスト**（`tests/preprocessing/test_ratings.py`・**21 passed**）: expected_score 対称/単調・
   margin cap・field_win_probs 和=1・update_race 零和/upset/margin・build_rating_frame **as-of リーク無し**・
   入力順non-dependence・features_from_snapshot 網羅。
5. ✅ **UI**（`app/pages/7_rating_lab.py`・147行）: ①レーティング照会タブ ②On/Off A/B タブ。
6. ✅ **ruff/mypy**: rating コードは clean（mypy の残 error は `src/preparing/*` の requests stub 欠如＝
   pre-existing・rating 無関係）。ingest で snapshot 保存(L217-218)・add_race_level_zscore で elo _z 生成(L356-366)
   の end-to-end 配線も確認。preprocessing 全 **362 passed**。

**実データ A/B 完了（2026-07-15・単体LGBM×直近8年 `--since-year 2016`・434,655行）— Elo は冗長と判明**:
フルスタッキング×40年は WSL2 メモリ(空16GB)で OOM だったため、`retrain --no-stacking --since-year 2016` で
Elo 有無を A/B（`--since-year` を今回追加）。結果:
| 指標 | Elo有り(497特徴) | Elo無し(488特徴) | 差 |
|---|---|---|---|
| Place head test AUC | 0.818 | 0.818 | ±0.000 |
| Win head test AUC | 0.8322 | 0.8314 | +0.0008 |
| version auc_test | 0.8185 | 0.8176 | +0.0009 |
| backtest ALL(信頼)除外後 ROI | 68.3% | 67.7% | +0.6pt |
- **Elo 9列はモデル品質をほぼ動かさない**（Place AUC 同一・Win +0.0008＝ノイズ）＝既存488特徴（人気・過去成績・
  血統）と**情報冗長**。地力は既存特徴が既に捕捉済み。
- **ROI の見かけの逆転（Elo無し ALL 128.5% 黒字 vs Elo有り 92.4% 赤字）は蜃気楼**＝Elo無し側の 3連単万馬券
  1本（最大払戻 58685＝全体の13%・的中25＝参考値）の分散。頑健指標 ALL(信頼)除外後は 68.3% vs 67.7% でほぼ
  同一・両方32%負け＝エッジ無しの全体結論は不変。
- **注記**: 単体LGBM×直近8年の縮小設定。フルスタッキング（NN base）×40年では相互作用で変わりうるが、
  497特徴 LGBM で AUC フラットな以上、劇的な旨味は考えにくい。Elo 列の feature importance(gain) 確認が次の直接検証。
- **結論**: レーティング Phase 1 はコード完成・クリーン・リーク無しだが、**現状の特徴セットへの増分価値は乏しい**。
  Phase 2（TrueSkill/条件別）で表現力を上げれば旨味が出る可能性はあるが、優先度は再考。害は無い中立特徴として保持可。

### 3-62. 学習の GPU 対応（2026-07-16・NN 自動 ＋ GBDT opt-in）— RTX 5070 Ti で検証済み

**NN(torch)**: `NnWinModel.fit/predict_proba` を device 対応化。`torch.cuda.is_available()` で自動選択し
モデル/テンソル/pos_weight/randperm を GPU へ、予測は net のデバイスに合わせ CPU に戻して numpy 化。
GPU 無し(VPS/CI)は自動 CPU フォールバック（挙動不変）。fit ログに `device=cuda/cpu`。
**GBDT(xgboost/catboost)**: `retrain --gpu` で opt-in。device は実行環境属性なので signature に通さず環境変数
`KEIBA_USE_GPU` で集約（共通ヘルパ `_gpu_config`: `xgb_gpu_params()→{device:cuda}` /
`catboost_gpu_params()→{task_type:GPU}`）。tuner(_multi_model_tuner)・factory(_base_model_factory) が参照。
`--gpu` は CUDA 可のとき環境変数を立て、不可なら警告して CPU 続行。**lightgbm は pip 既定 CPU ビルドのため対象外**
（CUDA ソースビルドは Ubuntu 26.04 の gcc15 が nvcc 非対応で失敗＝断念が正解）。
**実測（--since-year 2016・4 base stacking・with-tuning）**: CPU 10090s → **GPU 4238s（約2.4倍高速）**。
NN 1エポック ~5–9s→~0.6s(約10倍)、xgboost チューニング ~40分→~9分。**3ターゲット全てが Blackwell(sm_120)で動作**
（catboost GPU もエラー無し＝ガード不要）。**auc_test 0.8214/Win 0.8368 は CPU run(0.8208/0.8367)とほぼ同一
＝GPU は速度のみで結果不変**。torch は cu128 wheel が sm_120 対応（cu124 は非対応）。test_gpu_config 追加。

---

## 4. 既知の未解決事項

### 4-1. オッズスナップショット・予測の実データ蓄積が未開始

- 収集・再計算の仕組み（odds_watch）は完成済みだが、実データは VPS cron 運用でしか貯まらない
- 重力統計（odds_gravity.json）・アンサンブル重みは蓄積後の `evaluate-odds-dynamics` で実データ化される
- それまでオッズ力学モデルは拡散既定値 + 恒等フォールバックで安全動作する
- 評価基盤は §3-33 で E2E 検証済み・勝ち馬 logloss も配線済み（実データ投入で即運用可能）
- 別経路として `predict_upcoming.py --save-odds`（§3-41）が `data/live/odds_<date>.csv` /
  `bets_<date>.csv` に実戦の暫定オッズ・推奨を captured_at つきで蓄積する（手動実行での簡易蓄積）

### 4-2. モデル pickle・DB は git 管理外（環境ごとに再生成）

- `models/<date>/*.pickle`（258MB）と `data/keibam.db` は gitignore 対象
- 新しい環境では `retrain`（約 3 分）で再生成。DB は ingest/retrain 起動時に
  pickle から `auto_migrate_all` で自動移行（data/raw の pkl は git 管理済みのため再 scrape 不要）

### 4-3. Optuna 探索履歴（tuning_history.json）が実データ未生成

- 保存・選択の仕組みは実装済みだが、実運用の `retrain --with-tuning`（約 2 時間）は未実行
- 実行するとモデルラボの「ハイパラ探索結果」タブに成績順で表示される

### 4-4. JRA-VAN Data Lab 連携は受信契約のみ

- `JraVanFileDropSource` は実装済み（`data/incoming/jravan/*.json` を読む）
- Windows 側の JV-Link エージェント（速報オッズ → JSON 配置）は未実装。
  JSON スキーマは `src/preparing/_odds_source.py` docstring 参照

### 4-5. オッズパーサの実 DOM 検証状況

- **単勝・複勝（b1 ページ）は実 DOM 検証済み**（§3-41、2026-06-21）。id は `odds-1_NN`（単勝）/
  `odds-2_NN`（複勝）の `_` 区切り。`parse_combo_odds_html` は `[-_]` 両対応に修正済み。
- **連系（馬連・三連単等）は実 DOM 未検証**。`id="odds-<type>-<馬番列>"`/`_` 区切り規約に基づく
  実装（合成テスト済み）だが、発売中の実ページでの確認は開催時間中にしかできず未実施。

---

## 5. 次に着手すべきタスク（優先順）

### 🔴 優先度 高

**⑨ `predict_upcoming.py` 午後ライブの end-to-end 最終確認**（✅ 実装・パーサ修正完了 — §3-41 / ライブ実走が残）
- 次の開催日の午後（発走 30〜0 分前）に実走:
  `python predict_upcoming.py --date <YYYYMMDD> --loop --window 3-60 --interval 180 --save-odds`
- 期待: 各レースで `オッズページで単勝を補完 +N頭` が出て、従来 `有効単勝 0/N頭` で落ちていた
  午後レースが予測される。前売り時間帯は補完ログが出ない（＝余分な取得をしない）。
- もし `オッズページが単勝を返さない…（throttle/未描画…）` が出たら、その場で
  `KEIBA_ODDS_DEBUG=1 python debug_odds_page.py <race_id>` で生 HTML を採取し原因切り分け
  （空オッズセル＝未描画なら待機セレクタを単勝セルの非空待ちに変更 / ブロックなら間隔調整）。

**① マルチ GBDT スタッキング動作確認**（✅ 完了 — §3-31 参照）
- 合成データ E2E（`verify_multi_gbdt.py`）で 3 構成を検証済み:
  base 別 AUC が個別に出力され（モデルラボの別バー表示が成立）、4 base が全て学習成立、
  後方互換も完走。`base_model_names_` も正しく並ぶ
- **残課題**: meta（LogisticRegression）が最良 base を上回らないケースを確認 → 下記 ③ に直結
- 実データでの最終確認（任意）: `streamlit run app/Home.py` → モデルラボ「スタッキング寄与」

**② VPS での cron 実運用開始**（ユーザー作業）
- ローカル/VPS を最新化（`git pull`）→ `crontab.example` を設定
  （daily_ingest / weekly_retrain / odds_snapshot=odds_watch */2 分 / 週次 evaluate-odds-dynamics）
- 初回に `python -m src.pipeline.run_pipeline retrain` でモデル生成
- マルチ GBDT 運用には `--base-models-config configs/base_models_nn.example.json` を weekly_retrain.sh に追加

### 🟡 優先度 中

**⓪ 券種別バックテスト Phase 2: 券種別最適化レイヤ管理**（✅ 完了・実データA/Bでエッジ無し決着 — §3-34/§3-35）
- DTO（`BetTypeParams`）・永続化・EVポリシー配線・最適化エンジン・UI・予測反映まで実装。
- ✅ **Optuna化+prob_scale探索+規律（2026-07-15）**: grid 版 `optimize_bet_type` は
  `objective=return_rate` を単一スライス in-sample 最大化＝**万馬券分散に過適合する罠**（Elo A/B で
  ROI 128.5% が3連単1本だった件と同型）。規律版 `optimize_bet_type_tpe`（app/_bet_type_optimizer.py）追加:
  時系列 train/val 分割で train 最適化→**val 汎化確認**、`prob_scale` 連続探索、頑健目的
  `robust_metric`（既定 trimmed_return_rate＝最大払戻1本除外／sharpe_ratio 可）、val で既定パラメータの
  val 指標も併記し**「最適化が out-of-sample で既定を上回るか」で採用判定**。テスト4件・app/sim 222 passed。
- ✅ **性能・堅牢化（2026-07-15）**: 実走で判明した問題を連続修正。①モデル/featured 列不一致→
  `--no-rating-features` 列揃えを script に追加。②val 指標の退化（高EV最適化で val 買い目が希薄化し
  trimmed が的中1本除外で 0.000）→val 買い目数を表示・**採用条件に val 買い目≥min_bets を追加**（×希薄）。
  ③高速化: `backtest_bet_type` を `prepare_scoring`(calc_score+odds_provider・params 非依存)＋
  `backtest_from_prepared`(judge+清算) に分割しトライアル間で再スコアを排除、`--max-races`(組合せ爆発抑制)。
  ※誤診記録: calc_score は軽く（predict のみ）真のボトルは judge の純Pythonループ。optuna n_jobs は
  スレッド並列で GIL に阻まれ無効→**券種プロセス並列(ProcessPoolExecutor fork・COW 共有)**に変更。
- ✅ **実データ A/B 結論（2026-07-15）— 券種別最適化に市場エッジ無し**:
  `--since-year 2016 --no-rating-features` の単体LGBMで val 汎化判定。**採用された券種も全て val 回収率 <1.0
  （単勝0.71・馬連0.66＝全て負け）**。Δval>0 は「最適化が既定より**マシに負けた**」だけ＝best が ev≈2.4–2.9 と
  高閾値＝買い目を絞る方向で、-EV ゲームでの自明な露出削減（極限は賭けない＝損失0）。市場エッジではない。
  採用は設定で揺れる（複勝 4/4→3/4 で反転）。窓は常に「直近N」で val が重なるため「安定」も真の汎化検証でない
  （非重複窓 train/val 分割は未実装）。**§10 の no-edge（echo0.989・ΔR²≤0・回収0.696）と完全整合**。
  → **券種別最適化の調査は「エッジ無し」で決着**。`bet_type_params.json` へは保存しない（既定閾値のまま）。
  UI Tab4 は grid 版のまま（TPE 版の本番配線は不要＝採用する最適化が無いため）。

**③ meta 学習器の改善**（✅ 完了・実データ A/B で GBDT meta は旨味なし決着 — §3-32 参照）
- `BaseModelsConfig.meta_model="lightgbm"` で GBDT meta を選択可能に（既定は従来の logistic）
- GBDT meta の既定は極浅（num_leaves=3）・強正則化で過学習を抑制。合成データで logistic を
  わずかに上回っていた（0.8163 vs 0.8148）
- ✅ **実データ A/B（2026-07-16・単体LGBM stacking 3base=lightgbm/xgboost/nn・--since-year 2016）**:
  `base_models_nn_light.json`(logistic) vs `base_models_nn_light_gbdt_meta.json`(GBDT meta・新規追加) で比較。
  結果 **Place auc_test 0.8196(logistic) vs 0.8197(GBDT)＝+0.0001 / Win 0.8349 vs 0.8342＝−0.0007**＝**ほぼ同一**。
  ※**最もクリーンな A/B**: seed 固定で base の NN 学習ログが2 run 完全一致（Place NN best_val 1.5302・Win 1.0107・
  LightGBM initscore も一致）＝base 決定論的に同一 → 差は純粋に meta のみ・base 再学習ノイズの交絡なし。
  → 合成の微差(+0.0015)は実データで再現せず。**GBDT meta 不採用・既定 logistic 据え置き**（同性能でシンプル）。
  モデル品質(AUC)の話でエッジとは無関係。フルスタッキングは以前 OOM だったが light config(3base・NN 200k行上限・
  --since-year)なら空き38GBで完走（各 ~450s）。

**④ 実データ蓄積後のオッズ力学モデル評価**（✅ 評価基盤の検証・バグ修正完了 — §3-33 / 実走は蓄積後）
- 評価ハーネスを E2E 検証する過程で「CLI が winners 未配線 → 勝ち馬 logloss が常に NaN」の
  本番バグを発見・修正（`race_winners` 追加 + 配線）。合成市場で力学モデルが identity に勝つことも確認
- **実走確認（2026-07-16）— データ不足で評価不能（＝蓄積待ち確定）**: `evaluate-odds-dynamics` を
  実データで実行 → スナップショットは存在する（`if not snapshots` は通過）が **全モデルの全メトリクスが
  NaN**（KL/mae/mape・n_test=0）。原因は各レースが「締切直前(T0)＋それ以前の位相の両方・共通2頭以上」を
  満たさない＝**オッズ変動が単一位相しか捕捉されていない**（コードは正しく NaN を返す・§3-33 の合成データは
  複数位相ありで dynamics>identity を確認済み）。→ 力学モデル評価には odds_watch が締切30分前〜T0 まで
  **複数位相にわたって**蓄積したデータが要る。cron(②)未運用のため現状は評価不能。cryptic な全NaN の代わりに
  n_test_races=0 時に理由を明示する警告を追加（コミット済み）。
- **根本原因を特定（2026-07-16・`diag_odds_snapshots.py` で実 pkl を検査）**: スナップショットは
  **22,568件・18日分・52レース**あるが **phase 分布が t0=22,362(99%)**・minutes_to_post 中央値=0＝
  **ほぼ全部が発走直前(t0)の1点**。≥2位相のレースは1本のみ（TANSHO で0）→ これが全 NaN の直接原因。
  → **蓄積不足でも保存/位相付与バグでもなく「取得タイミング（cron 発火）」の問題**。コードは正しい：
  `odds_watch --once`→`select_checkpoint_races` は `mtp≤DENSE_WINDOW_MIN(30)` で発走30分前〜毎ティック取得する
  設計で、3分おきに回れば thirty_min/t10/t5/t0 が揃う。なのに t0 だけ＝各レース最終30分の間に3分おきの
  取得が起きていない。**確定原因（ユーザ申告）: cron は未起動で `odds_watch --once` を手動で都度起動していた**。
  手動の単発ティックでは同一レースを最終30分に繰り返し取得できず、思い出したタイミング(≒発走間際)で1回ずつ
  拾うため t0 に偏る＝**取得ランタイムの運用問題**（コード・保存・評価は全て正しい）。
  （SPARSE_CHECKPOINT_MINUTES=() は30分より前の追加取得用で評価には不要＝バグではない。）
- **DB 直読みで実証確定（2026-07-16・ユーザ指摘で `diag_odds_snapshots.py` に DB 診断 `diag_db()` 追加）**:
  「pickle は `(race_id,bet_type,combo,phase)` で dedup＝同一 phase 内の複数取得を潰す。真の時系列は主キーに
  captured_at を含む DB `raw_odds_snapshots` にある」との指摘を受け、DB を race_id で集計。結果は **DB 22,608行・
  52レースでも、異なる captured_at 数/レースが 51/52 で「1」（p50=p90=1・max=7）**、span ≥30分=0/52・
  captured_at ≥2種類=1/52。→ **pickle の潰しではなく、元データ自体が 51/52 レースで単一時刻取得**と実証確定
  （dedup を一切しない DB でも軌跡が無い）。唯一の例外1レース（7点・span 21.7分）が「スケジューラを回し続けば
  パイプラインは正しく時系列を貯める」ことを証明＝**修正は純粋に運用面**。「captured_at×race_id から正解生成」
  仮説は着眼は正当だが、別時刻の同一レース（＝関係の相手）が物理的に無いため本蓄積データでは成立不可。
  （前回 pickle 断定は不当だったが、DB 実証で同結論が確証に。phase 粒度は pickle が保持するため phase 分布は
  dedup で歪まない＝早期位相の欠落は実測、DB が足しうるのは同一 phase 内の captured_at 細分だけ→それも span≈0。）
- **解決策（コード変更不要・ランタイム）**: ①本命=常時起動 VPS で `*/3` cron（§5②の本来の姿）。
  ②デスクトップ現実解=`odds_watch --loop --interval 120` を tmux 常駐（WSL2 cron に依存せず毎ティック取得）。
  どちらかで**1開催でも複数位相を溜めれば ④ は即前進**（評価ハーネス・保存・位相付与は全て正しい）。
- **累積の保証（再発防止・2026-07-16）**: 「同一 race_id で post_time・captured_at・odds を累積し続ける」を
  コードで固定した。(1) `tests/preparing/test_odds_accumulation.py`＝同一レースを別 captured_at で保存すると
  DB `raw_odds_snapshots` が行を累積（`INSERT OR IGNORE`＋PK に captured_at）・同一時刻は冪等・post_time は
  `captured_at+minutes_to_post` で分精度復元、を回帰テスト化。(2) `odds_watch.past_post_single_tick`／
  `check_capture_health`＝発走済みで単一ティックのレースを検知し `--loop` 各ティック & `--status` で ⚠ 警告
  （単一時刻化を運用中に即発見）。(3) `scripts/odds_watch_loop.sh`＝tmux 常駐＋クラッシュ自動再起動で
  ループ継続起動を担保。post_time は列に無いが minutes_to_post で保持（分未満切り捨て＝±60秒、評価には十分）。
- **起動日時・時刻の制御（2026-07-16）**: cron 不安定な WSL2 でもアプリ内で開始/終了を確定できるよう
  `odds_watch --loop` に `--start-at` / `--stop-at` を追加（`parse_when`/`wait_seconds`/`should_stop` 純粋関数
  ＝ISO 'YYYY-MM-DDTHH:MM' or 当日 'HH:MM' を受理・境界で停止・正常終了 code=0）。ランナーは `ODDS_WATCH_ARGS`
  で追加フラグを受け、正常終了(code=0)では busy-restart せず終了。例: 早めに tmux 常駐→9:30 開始・16:30 自動終了。
- **連休の複数開催日を事前予約（2026-07-16）**: `odds_watch --schedule <JSON>` で連続開催日をまとめて予約。
  `CaptureSession`/`parse_schedule`/`pending_sessions`/`_run_capture_window` を追加（`--loop` の単一ウィンドウ実行部を
  共通化）。JSON は `{"interval":120,"sessions":[{"date":"2026-07-20","start":"09:20","stop":"16:40"}, ...]}`
  （date は YYYY-MM-DD/YYYYMMDD、start/stop は当日 'HH:MM' か ISO）。過去日は自動読み飛ばし・start 昇順で順次実行。
  ひな形 `configs/odds_watch_schedule.example.json`。一度 tmux 常駐させれば連休の全開催日を自動でこなす。
- **残課題**: 上記で複数位相の系列を蓄積 → `evaluate-odds-dynamics` が実値を返す → モデルラボで
  KL/勝ち馬logloss 比較 → アンサンブルが identity に安定して勝てば `config.yaml: use_predicted_odds: true`。

**⑤ `retrain --with-tuning` + NN 再探索の本実行**（✅ 書き戻し機械化・配線検証済 — 本実行はローカル）
- `configs/base_models_nn_tuned.json`（tune_per_model=true）で実行して NN 構造を再探索
- **配線確認（2026-07-16）**: `tune_per_model=true` → `_keiba_ai.train_with_stacking` が `tune_nn`
  （`_multi_model_tuner`、`nn_search_space` を Optuna 探索。arch/lr/dropout/pre_norm/hidden_dims 等）を回し、
  best を `nn_params` にマージ→最終 `NnWinModel` に反映。config 全キー（tune_per_model/n_trials/nn_tune_trials/
  nn_tune_epochs/nn_tune_max_rows/nn_search_space/nn_params）は BaseModelsConfig 正規フィールドで捨てられない。
  `nn_search_space` は既定とマージ（`{"arch":["mlp"]}` で CNN 除外＋他は既定範囲探索）。
- **書き戻しを機械化（2026-07-16）**: 探索後 `KeibaAI.tuned_base_models_config`（xgboost/catboost/nn の best
  反映済み完成 config）を公開し、`RetrainJob.run` が `models/tuned_base_models.json` へ自動保存する。
  ログ手写しでなく、この JSON をそのまま `--base-models-config` に渡す/固定 config へコピーすれば固定運用へ移行できる。
- **再開で改善（A）＋4モデル1ファイル完全化（B）（2026-07-16）**:
  - **A**: `_tuning_storage.study_kwargs(name)` が `KEIBA_TUNING_STORAGE` を読み、設定時に
    `create_study(storage=..., study_name=..., load_if_exists=True)` を返す。`--resume-tuning` が
    `KEIBA_TUNING_STORAGE=sqlite:///<models_dir>/optuna_studies.db` を立て、xgboost/catboost/nn と
    手書き LightGBM 探索（`__tune_custom` TPE）が**再実行で trial 追記＝best 単調改善**。LightGBMTuner
    ステップワイズ（既定 `--with-tuning` の LightGBM 経路）は resume 対象外だが tuning_history.json が
    元々歴代蓄積で `--params-rank 0` が全実行 best。
  - **B**: `BaseModelsConfig.lightgbm_params` を追加し、`train_with_stacking` が主チューナ結果 `params` を
    そこに載せて `tuned_base_models_config` を公開（**lightgbm/xgboost/catboost/nn の best を1ファイルに**）。
    `build_base_models` は `cfg.lightgbm_params` 非空時に上書き採用（固定運用で LightGBM も tuned に）。
    `RetrainJob.run` は `auc_test` を埋めて**歴代 best を守る上書き**（既存 `_auc_test` 以上の時だけ更新・初回は無条件）。
    保存 JSON の `_auc_test`/`_version` は from_dict が無視するのでそのまま `--base-models-config` で読める。
- **分離NN + 遅延スタッキング（2026-07-17・メモリ対策＝全データ化）**: NN を GBDT スタックへ同時投入すると
  NN 2系統 PreparedFeatures でメモリ倍化し全データ OOM。そこで **GBDT スタック（nn 抜き）と NN 単体を別々に学習し、
  予測時に meta 学習器で融合**する経路を追加。(A) `configs/base_models_gbdt.json`（`["lightgbm","xgboost","catboost"]`）で
  全データ GBDT 学習（2系統を作らずメモリ半減）。(B) `retrain --nn-standalone [--nn-config]`＝NN を単体学習・保存
  （`_nn_standalone.py`、NnWinModel は max_train_rows＋ミニバッチで省メモリ）。(C) `build-combined --gbdt-model --nn-model
  --meta-years`＝両 base を読み込み holdout で `CombinedModel`（meta スタッキング＝`[p_gbdt,p_nn]`→logistic）を学習し
  effective_model として保存。`CombinedModel`/`NnDerivedPredictor`（`_combined_model.py`）は sklearn 互換で予測経路にそのまま差込。
  リーク回避: meta の holdout は両 base の `--holdout-years` 除外年を `--meta-years` に。融合方式はユーザ選択で meta 学習
  スタッキング。※torch/データ依存の学習・予測はローカル検証（この環境は featured/torch 無し）。
- **探索の一貫化・対象選択（2026-07-17）**: (1) per-model 探索(xgb/cat/nn)を `with_tuning AND tune_per_model` に統一
  （`--with-tuning` 無し＝一切探索せず stored params で固定運用。以前は config の tune_per_model 単独で発火し混在状態だった）。
  (2) `--tune-models M1,M2`（`BaseModelsConfig.tune_only`）で探索対象モデルを選択（lightgbm/xgboost/catboost/nn の部分集合）。
  指定モデルだけ探索し他は stored/既定値で固定・自動で --with-tuning 有効化。未探索の LightGBM は既存 lightgbm_params を保持
  （defaults で潰さない）。`_keiba_ai` の3ゲート（LightGBM/xgb・cat/nn）に `_want(model)` を適用。
- **探索空間の調整（2026-07-16）**: NN に **weight_decay（Adam L2）** を新設（`NnWinModel(weight_decay=)`→Adam・
  `_suggest_nn_params` は空間にキーがある時のみ log 探索＝後方互換・`_keiba_ai` の nn_kwargs 白名单に追加）。
  `DEFAULT_NN_SEARCH_SPACE` を拡張: weight_decay[1e-7,1e-3]・layer_width に 512・n_layers を [1,4]・
  pre_norm に batch_norm・batch_size に 1024。`base_models_nn_tuned.json` を明示的な広空間＋nn_tune_trials 12→25・
  epochs 15→20 に更新。GBDT 空間（XGB/CatBoost）は既に広め（n_estimators/lr/depth/正則化）で据え置き。
- **実データ A/B の初回所見（2026-07-17・同条件 `--since-year 2015` 34,777レース）— ※後で訂正、下記参照**:
  ベースライン（既定 `base_models_nn.example.json`・探索なし・5.5分）=**Place auc_test 0.8156** / Win 0.8354。
  チューニング（`base_models_nn_tuned.json` 拡張空間・resume・86分）=**Place 0.8137**（**−0.0019 悪化**）/ Win 0.8387（+0.0033）。
  → **86分の per-model 探索が主力 Place/stacking を微悪化**。根本原因は目的関数ミスマッチ: per-model 探索は各 base 単体の
  val を最大化するが、スタックは base の多様性で稼ぐため個別最適化は多様性を削りスタックを悪化させる（個別最適≠スタック最適）。
  基盤（探索空間拡張・resume・4モデル書き戻し）は正しく動作（B は lightgbm/xgb/cat/nn 全 best が1ファイルに揃うことを実データで確認）。
- **結論を訂正（2026-07-17・resume 再実行で改善しベースライン超え）**: 上の「改善せず」は**探索不足の1回だけの判断だった**。
  同条件で `--resume-tuning` を継続（study に trial 追記）した再実行で **Place auc_test 0.8137→0.8212（+0.0075）**、
  best-preserving が更新（`tuned_base_models.json 更新: auc_test=0.8212（前回 best=0.8137）`）。**ベースライン 0.8156 を +0.0056 上回った**
  （Win も 0.8384>0.8354）。改善源は主に GBDT（xgb best logloss 0.3455→0.3313・catboost 0.5269→0.5235・resume で探索領域が前進）。
  → **正しい結論: 十分な探索予算があれば per-model 探索はスタックを改善する。単発の限られた探索では既定に負けることもあり、
  resume（探索の継続蓄積＝A）がその予算を供給する。** 固定運用は tuned_base_models.json(0.8212) 採用が既定(0.8156)より良い候補。
  数値は `--since-year 2015` 依存で全データの絶対値とは別。エッジ不在・ライブ賭け禁止の規律は不変（予測精度の話）。
- **頑健性確認・plateau（2026-07-17・resume 3回目）**: さらに resume した3回目は Place auc_test **0.8208**（2回目 0.8212 と
  ほぼ横ばい・−0.0004＝ノイズ範囲）。best-preserving は 0.8208<0.8212 で正しく据え置き（ファイルは 0.8212 保持）。tune_nn best も同一
  （0.8348・[512,256,128]）。→ **2回の独立 resume run が両方 ~0.821 に着地＝改善は頑健（まぐれでない）。ただし探索は 0.821 で頭打ちで
  追加 resume では伸びない。** 確定結論: 十分な探索予算で per-model 探索は ~+0.006（0.8156→0.821）改善し、それが上限。
  `models/tuned_base_models.json`(0.8212) が固定運用の最良候補。resume 追加は不要。
- **残（ローカル本実行）**: この環境に featured_data.pkl 無し（177万行はローカル WSL2）。OOM 対策は
  `--since-year`（行数減）or `.wslconfig` で RAM 増（GBDT はフルバッチで全データ常駐が RAM 床。NN は
  max_train_rows 上限＋ミニバッチで小）。実走例:
  `retrain --with-tuning --resume-tuning --base-models-config configs/base_models_nn_tuned.json --gpu --since-year 2015`
  → `models/tuned_base_models.json`（4モデル best・歴代 best 保持）を固定運用 config に採用。再実行で改善。

**⑥ JRA-VAN Windows エージェントの実装**（JRA-VAN 契約後）
- JV-Link（win32com）で速報オッズを購読し、契約 JSON を `data/incoming/jravan/` へ配置
- 本体側は `odds_watch --source jravan` に切り替えるだけ

### 🟢 優先度 低

**⑦ 連系パーサ・連系オッズ予測の拡張**
- 実 DOM 検証（4-5）→ 連系（馬連・三連複等）のシェア力学モデル拡張
  （組合せ数が多いため特徴量設計を別途検討）

**⑧ Transformer 系モデルの追加検討**
- `AbstractShareDynamicsModel` 契約のままレース全体入力の Transformer/TFT を追加し、
  既存の評価ハーネスで Dirichlet/Kalman/PF と比較する

---

## 6. テスト・CI

```bash
python -m pytest tests/ -q          # 763 件（758 passed, 5 skipped）
python -m ruff check src/ app/
python -m mypy --config-file pyproject.toml
lint-imports
```

現状: **758 passed, 5 skipped**（torch 未インストール 4 件 + selenium/network 1 件）。
ruff / mypy / import-linter すべて 0 エラー。CI で ruff は必須化済み。

**マルチ GBDT 追加後の確認コマンド**:
```bash
# スタッキング後方互換（LightGBM 単独）
python -m src.pipeline.run_pipeline retrain --use-stacking

# マルチ GBDT 運用（NN 込み）
python -m src.pipeline.run_pipeline retrain --use-stacking \
    --base-models-config configs/base_models_nn.example.json

# NN 構造再探索（tune_per_model=true、CNN 除外）
python -m src.pipeline.run_pipeline retrain --use-stacking \
    --base-models-config configs/base_models_nn_tuned.json
```

---

## 7. よく使うコマンド

```bash
# ブランチ確認
git branch --show-current

# 日次取込（ingest）
python -m src.pipeline.run_pipeline ingest --race-id 202401010101
# または前日分を自動検出
./scripts/daily_ingest.sh

# 強制上書き取込（誤情報修正時）
python -m src.pipeline.run_pipeline ingest --race-id 202401010101 --force

# 週次再学習
python -m src.pipeline.run_pipeline retrain
./scripts/weekly_retrain.sh

# 時系列オッズ自動取得 + 予測再計算（チェックポイント: 発走 30/10/5/1 分前）
python -m src.pipeline.odds_watch --once          # cron 用 1 サイクル
python -m src.pipeline.odds_watch --loop --interval 120   # 常駐
./scripts/odds_snapshot.sh                        # cron ラッパー

# オッズ力学モデルの比較評価（重力統計・アンサンブル重み更新）
python -m src.pipeline.run_pipeline evaluate-odds-dynamics

# ハイパラ探索つき再学習 → 探索結果から rank 指定 / UI 選択で学習
python -m src.pipeline.run_pipeline retrain --with-tuning
python -m src.pipeline.run_pipeline retrain --params-rank 2
python -m src.pipeline.run_pipeline retrain --use-selected-params

# ダッシュボード起動
streamlit run app/Home.py

# ノートブック
jupyter lab

# pickle 構造確認
python check.py

# DB から pickle 復元（upsert バグ修正後のみ信頼可能）
python -c "
from src.storage import RawDataRepo
df = RawDataRepo().read('raw_results')
df.to_pickle('data/raw/results.pkl')
print(df.shape, 'race_id sample:', df.index[:3].tolist())
"
```

---

## 8. プロジェクト構成（レイヤ依存）

```
src/
├─ constants/       定数・列名・閾値・馬券種・ログ設定（最下層）
├─ storage/         raw データ SQLite 永続化（Phase 1）
├─ preprocessing/   前処理（結果/払戻/血統/特徴量エンジニアリング）
├─ preparing/       データ収集アダプタ（Playwright スクレイパ・オッズ取得）
├─ policies/        馬券/スコア戦略・Harville 確率・オッズ供給
├─ training/        学習器（KeibaAI / スタッキング / チューニング履歴 / オッズ力学モデル）
├─ portfolio/       資金配分（ケリー）
├─ simulation/      バックテスト・指標・可視化
├─ operation/       運用モード・発注アダプタ・設定
└─ pipeline/        継続学習 CLI（ingest / retrain / evaluate-odds-dynamics）+ odds_watch
```

レイヤ依存の方向:
```
constants → storage → preprocessing / preparing → policies → training → portfolio → simulation → operation → pipeline → ui
```

---

## 9. 参考: PyCon JP 2018 競馬予測トーク由来の実装レシピ

詳細分析は `docs/refs/pycon_keiba/00_SYNTHESIS.md`（元ノート40枚は `docs/refs/pycon_keiba/notes/`）。
出典 YouTube `I1eSN6mPANs`。先に分析した Medium(Collins)/GitHub(Eplee) より成熟した「正しい ML 工芸」。

**最重要の含意**: トークは「**出走履歴の集計特徴が最も効くデータ源**」と明言。keibam はこの form
特徴を 93–96% 欠損したまま「エッジ無し」と結論していた → **本トークは「form データ充足まで市場効率
の結論は暫定」という 🚨 caveat の外部裏付け**。backfill 完遂が最高価値の一手。

**PyCon2018トーク由来の実装レシピ（5アクション・未実装）:**
- **A1. 自動集計特徴**: entity(馬/騎手/調教師/馬主/父)×context(コース/距離/馬場/天候/ペース)×集計。
  **必ず時間前方（過去レースのみ）で作り、追加ごとに `debug_leak.py` で AUC>0.9 を検査**（Target
  Encoding は静かなリーク源。`rank_win` の巧妙版）。
- **A2. スムージング**: 少数カテゴリの集計値を全体平均へ引き寄せ（α=目的変数との相互情報量）。
  欠損の多い現データに特に有効。
- **A3. 多観点NDCG**: 評価に NDCG（関連度＝着順逆数/賞金/単勝支持率）を追加。
  **「着順NDCG − 単勝支持率NDCG」= 独立情報の代理**（単勝支持率NDCGが高い＝市場エコー。ΔR²/echo と同型）。
- **A4. cyclical encoding**: 季節など周期特徴を sin/cos で 2 次元化（低コスト）。
- **A5. 定性評価**: 代表レースを目視・エッジケース（海外馬等）確認（過信は過学習なので参考程度）。

**backfill 完了後の作業計画（既存パイプライン＋レシピ統合版）:**
```
Phase 0 [進行中] データ収集
  - backfill-horses（scripts/backfill_horses_resilient.sh で完遂）→ backfill-peds
Phase 1  データ完全性の再確認
  - rebuild-featured → form 特徴の非null率が 0.04–0.07 から回復したか確認
    （回復していなければ以降は無意味。まず backfill を疑う）
Phase 2  特徴量拡張（レシピ A1/A2/A4）
  - A1 集計特徴を時間前方で生成 →（追加ごとに）debug_leak.py で漏れ検査
  - A2 スムージング付与 / A4 cyclical encoding
Phase 3  retrain（TARGET_LEAK_COLS 混入警告が出ないことを確認）
Phase 4  評価拡張＋市場効率の再判定（レシピ A3/A5）
  - A3 多観点NDCG（着順/賞金/単勝支持率）＋「着順NDCG − 単勝支持率NDCG」
  - A5 定性評価（代表レース目視）
  - honest 再評価: walk_forward.py --quality / predict_quality.py / no-odds ΔR²
  - 「回収123%」が honest OOS で再現するか検証
Phase 5  結論の更新
  - form 充足後も ΔR²≤0・echo 高・回収≤100% なら「市場効率／エッジ無し」を確定
  - いずれかが覆れば caveat を撤回し、独立情報の所在を A3 指標で特定
```

**保留事項**: トークの「回収率123%」主張は、集計特徴の時間リーク・回収率の高分散・選抜バイアスの
可能性があり、keibam 自身の OOS＋リーク検査（Phase 4）を通すまで採用しない。

**アンサンブル拡張（Phase 2+ / 起点あり）**: GBDT と帰納バイアスが直交するヘッドを足して
「市場併走型・高精度予測器」の logloss/ECE を下げる（エッジ目的ではない）。優先度:
①条件付きロジット/Plackett–Luce（レース内 softmax・Benter 本流）②CatBoost（順序ブースティング＝
リーク耐性のあるカテゴリ集計）③LGBMRanker(LambdaMART, NDCG 目的)。XGBoost は LightGBM と高相関で
上乗せ薄。合成は market 確率を含む単純メタ層（既存 Benter 対数プール）＋ **race-wise OOF**、各ヘッド
追加ごとに `debug_leak.py` と `walk_forward.py --quality`。
- 起点実装: `conditional_logit.py`（条件付きロジット最尤ヘッド。市場/LightGBM/条件付ロジットの
  勝logloss/Brier/ECE/AUC＋echo を OOS 比較。合成データで重み回復・レース内Σ=1・logloss改善を検証済み）。
  例: `python conditional_logit.py --years 2025 2026`（この年は評価専用、それ以前で学習）。
- 起点実装: `lgbm_ranker.py`（LGBMRanker/LambdaMART ヘッド。relevance=top3(馬券圏のみ)/top6、
  生スコアをレース内 softmax 温度で較正して勝率化し同じ土俵で比較。合成データで検証済み）。
  例: `python lgbm_ranker.py --years 2026 [--relevance top6] [--no-odds-features]`。
- 起点実装: `stack_eval.py`（アンサンブル評価ハーネス。市場＋3ヘッドを**対数プール**
  `p∝Πp_k^{w_k}`＝レース内 softmax(Σ w_k log p_k) で合成し、重み w_k は勝ち馬 within-race 尤度で最尤。
  ベースは学習年で作り OOS 年で予測＝真の OOS、メタは **OOS 年内 race 単位 K-fold クロスfit**でリーク排除。
  「合成が市場/各ヘッドの logloss を下回るか」を1表で判定。合成データで stack<最良base を確認済み。
  Phase 2 後の本番評価にそのまま再利用。例: `python stack_eval.py --years 2026 [--folds 5]`）。
- baseline 実測（2026 OOS・form 93–96%欠損の暫定）: 市場が勝logloss/Brier/AUC で最良（市場効率の再確認）。
  条件付ロジット(odds有) は LightGBM をわずかに上回る（logloss 1.985<2.003, AUC 0.832>0.829, echo~0.96）。
  odds を外すと条件付ロジットの echo は 0.875 に下がるが AUC も 0.808 に低下（独立だが市場を超えず）。
  LGBMRanker(top6) は AUC 0.8326 で分類器(0.8294)を上回るが ECE 0.017 と較正は悪く logloss 2.026、echo 0.95。
  → 3モデル族すべてが市場に logloss/AUC で負け echo~0.9–0.96 ＝「市場効率・エッジ無し」がモデル非依存で再現。
    役割分担（ランカー=順位/分類器=較正/条件付ロジット=バランス）はスタックで"単体の質"は上げ得るが
    全モデル市場以下ゆえ合成は市場に寄るのみ。真の判定は Phase 2（form 集計特徴）投入後に再測定。
- スタッキング実測（`stack_eval.py --years 2026`, form欠損下の天井）: ★合成 logloss=1.9586 ≈ 市場1.9579
  （+0.0007＝誤差）、AUC 0.8361≈0.8360。対数プール重み 市場0.638/r_hat0.087/clogit0.103/ranker0.126。
  → リークフリー最適合成で3モデル族を束ねても市場を超えない＝「独立エッジ無し」の最強確認（暫定・form欠損下）。
  Phase 2 後に同ハーネス再実行し、★合成が市場を明確に下回る/モデル重み増/no-odds echo 低下 が出るかで判定。

**Phase 2 / A1 先行実装（済み・検証済み）**: 設計は `docs/refs/pycon_keiba/A1_form_features_design.md`。
既存 `_horse_features.py` が馬エンティティ×context の時間前方集計を網羅済み（form 欠損は horse_results
未取得が原因＝backfill 完了で自動充足）。真の空白＝**馬以外(騎手/調教師/馬主/父)×context の全履歴
expanding target-encoding**。これは results 表から計算でき **backfill 不要で先行可能**。
- 実装: `src/preprocessing/_target_encoding.py` の `expanding_target_encode`（厳密過去 date<自分・
  **同日リーク遮断**・スムージング `(Σ+α·prior)/(n+α)`）＋ `build_person_form_features`（騎手/調教師/
  馬主×race_type/開催 の勝率・複勝率 encoding、列欠如は自動スキップ）。
- テスト: `tests/preprocessing/test_target_encoding.py` 6件 PASS（厳密過去/自分不参照/スムージング/
  履歴ゼロ→全体prior/context フィルタ/列欠如スキップ）。
- 結線済み: `_merge_person_target_encoding`（merge の person_te ステップ）。実データで動作・リーク無し確認済み。

**ライブ推論 serve パリティ（P1〜・実データ検証済み）**: 設計 `docs/refs/live_inference_person_form_design.md`。
ShutubaDataMerger は results 履歴を持たず、results 由来特徴が serve で 0 埋めされていた（不足246列）。修正:
- `SERVE_HISTORY_PATH`（serve_history.pkl, 16.5万行×13列）を rebuild 時に `_write_serve_history` で保存。
- person_te serve: `person_te_for_upcoming`（履歴＋出馬表を結合し出馬表行の as-of encoding）。parity テストで
  serve==train を担保（`tests/preprocessing/test_person_te_serve_parity.py`）。
- form-from-results serve: `_merge_horse_results` の再構成元を `_form_history_results`（履歴）に。
- `predict_upcoming` の FE に `add_derived_features` 追加（**単勝_log 最有力**を暫定オッズから復活）。
- **serve merge 順序バグ修正**: person_te / live_ratings(elo) を `_merge_horse_results` の**前**に
  （FE は merged_data を読む＝horse_results で確定するため。elo は以前から serve で 0 だった）。
- person_yearly serve: `RAW_PERSON_YEARLY_PATH` を読み `_merge_person_yearly` を horse_results 前に。
実測: 不足列 246→223→（person_yearly後）約187見込み。残り＝**C.ページ依存**（着差/上り/賞金多窓・speed/pace、
train でも再構成馬は空＝深刻skewでなく backfill で両側回復）、**yoso 18**（要データ源配線）、**D.当日ノート**（intrinsic遅延・0許容）。
診断: `KEIBA_DUMP_MISSING_FEATURES=<path> python predict_upcoming.py ...` で不足列を全ダンプ。
- 追加実装（実データ完走確認済み）: person_yearly serve（jockey_py_*等36列, RAW_PERSON_YEARLY_PATH 読込）、
  yoso consensus serve（`fetch_pro_yoso_marks` で当該レース印取得→`merge_yoso_marks`、guarded・5列＋_z）。
  serve merge 順序: person_te→person_yearly→yoso→live_ratings→horse_results→horse_info→peds。
  serve_history snapshot に馬番追加（14列。yoso スキル実装用の下準備）。
  **実測: 不足列 246→177（69列回復・zscore 69→98）。** 残177の内訳:
  - yoso 予想家スキル/プロフィール 8列（保留・要 履歴印concat＋着順as-of＋馬番snapshot、個々の重要度は低）。
  - **C.ページ依存**（着差/上り/賞金の多窓・speed/pace・クラス別form）＝最大の塊だが train でも再構成馬は空
    ＝深刻skewでなく **backfill 完走で train/serve 両側回復**。
  - **D.当日ノート**（映像/パドック/調教）＝intrinsic 遅延・0許容。
  → **fixable な高価値 serve 欠落は解消済み**。残る fixable は yoso スキル/プロフィールのみ（低優先）。

**form-from-results マイルストーン（実データ検証済み・重要）**: `KEIBA_FORM_FROM_RESULTS=1` で
`build_horse_results_from_results` が未取得馬の form を results 自己結合で補完。実測:
- 未取得馬 22,555頭・156,463行を補完（horse_results 15,222→171,685行、**スクレイピング0件**）。
- 率系 form 非null率: win_rate_5R/place_rate_5R/career_winrate が ~0.04 → **0.860**、
  win_rate_at_course_type 0.80、avg_rank_at_distance 0.66、wet_win_rate 0.60 へ激増。
- debug_leak: 0.9超 0列（率系 form 0.70–0.74）＝リーク無し（date スライスに委譲）。
- 残ギャップ: speed_figure/pace(脚質)/career_earnings はページ固有列のため未取得馬では NaN のまま。

**Phase 2 公正再評価（form あり・2026 OOS）— 結論は不変**: retrain(--holdout-years 2026,
20260702_keibam) 後の stack_eval:
- 市場 logloss=1.9579 / ★合成=1.9604 / clogit=1.9778 / LightGBM=2.0346 / ranker=2.0480。
- 対数プール重み 市場0.737 / clogit0.220 / ranker0.032 / r_hat −0.013。
- form は clogit を改善(1.9854→1.9778)しスタックで0.22の重みを得たが、**★合成は依然 市場を超えない**
  (1.9604≈1.9579)、echo 0.95–0.965。→ **率系 form を86%埋めても「市場効率・独立エッジ無し」は不変**。
  🚨 caveat（form欠損で暫定）は事実上解消し、結論の確度が大幅に上昇。残る未検証は speed/pace のみ。
- 要対応: retrain が28.9秒と速く Optuna 再探索なしの疑い。504特徴で LightGBM が悪化(2.0027→2.0346)。
  ハイパラ再チューニングで改善余地（ただし市場超えは見込み薄）。

**LightGBM 再チューニング（完了・確定）**: 2つの是正を実施。
1. `configs/tuning_config.regularized.json`（正則化前寄り・objective=binary_logloss・200試行）で
   `retrain --holdout-years 2026 --tuning-config ...`。best binary_logloss 0.39296。
2. バグ修正: `--with-tuning` 時に Win ヘッドが tuned params を受け取っていなかった
   （`_train_and_save_win_head` に lgb_params=None が渡り既定パラメータのままだった）。
   `_retrain.run` で Place の `study.best_params` を Win ヘッドへ流用するよう修正（test 2件追加）。
結果（2026 OOS, form+person_te+tuned）: Win ヘッド auc 0.8171→0.8316。
- 市場 1.9579 / ★合成 **1.9602** / clogit 1.9778 / LightGBM 2.0346→**1.9928** / ranker 2.0480。
- 対数プール重み 市場0.706 / r_hat 0.032（−0.013→**プラス転換**＝LightGBMがスタックに貢献）/ clogit0.212 / ranker0.025。
- **LightGBM 過学習は解消**（health回復）。だが **★合成は依然 市場を logloss で超えない**(1.9602≈1.9579)。
  一方 **★合成 ECE 0.0042 < 市場 0.0047＝較正では市場を上回る**（市場併走型の目標達成の小さな証拠）。
- 確定的結論: **form(率系)＋person_te＋チューニング済み3モデルのリークフリー最適合成でも JRA 市場に
  勝てない**＝市場効率は堅牢に確定。到達可能な価値は「市場と同等 logloss ＋ より良い較正の併走型予測器」。
  未検証は speed/pace(ページ固有)のみだが、率系で覆らなかったため反転見込みは小。

**✅35年 seed 検証(2026-07-06・1986-2021)— 市場効率は35年でも不変、むしろ確認強固化**:
  外部 JRA-VAN系 CSV(163万行/121,938レース/1986-2021)を seed_from_csv.py で raw スキーマへ変換
  （レースID=自己整合の race_id、名前→キャリア連続性セグメントの合成 horse_id〔牡→セ去勢は継続〕、
  騎手/調教師/馬主=名前→合成ID、上り/通過/賞金/タイムから本物 horse_results 生成で speed/脚質/賞金/class 有効化）。
  build_seed_featured.py で featured 478列(Elo/person_te 100%、form ~87%、血統 sire 0%=peds無)。
  retrain/backtest に --featured-path 追加。seed35y_ho(2020-21 holdout)で厳密 OOS:
  - **Win AUC 0.8311 / Place 0.8158**＝既存 netkeiba モデル(Win 0.8316)と**ほぼ完全一致**→ seed は本番品質。
  - **echo(r̂,p_mkt)=0.989**（既存0.95より**高い**＝より市場をなぞる）、勝ち馬 logloss モデル1.9718>市場1.9676（**市場に負け**）、
    人気帯別較正 r̂≈実勝率だが市場も同様で上乗せ無し、ΔR²=+0.0011(in-sample・α0.37/β0.64=合成の64%が市場)。
  → **35年に増やしても AUC 不変・logloss 微負け・echo むしろ上昇。市場効率は35年スケールで最強確認**。
    seed 統合(A/B)は予測エッジも較正も改善しないため**見送り**。seed の価値は「本番同等の別コーパス」までで、edge は無い。
  - 補助CSV: corner_passing_order=results自身の per-horse コーナーと冗長で不使用。laptime=レースpace。
    odds.csv は当初「複勝上位人気の事前オッズ」と誤認したが、**実際は複勝/単勝/連系の払戻表（着順≤3の馬とその確定払戻）**。
    → **複勝 overlay はこのデータでは検証不能**（事前の全頭複勝オッズが無い。overlay は賭けなかった馬の事前オッズが要る）。
    誤って overlay 検証すると的中率100%・ROI300%になるが look-ahead（当たった馬だけ選定）で無効(verify_fukusho_overlay_35y.py 冒頭に撤回明記)。
    正当な用途は seed_return_tables 化して複勝 EV バックテスト（Place ヘッド事前選定→実払戻決済）。ただし単勝35年効率＋複勝478行効率より、複勝も効率の公算大。
  - **✅複勝 EV BT(35年・実払戻・2026-07-06)**: odds.csv=複勝/単勝/連系の払戻表と判明。これを seed_return_tables 化し
    (verify_seed_fukusho_backtest.py)、seed35y_ho の Place ヘッドで事前 EV 選定→実払戻決済(look-ahead 無し)。
    2020-21 OOS: 回収率 **83.2%**(2020 86.0/2021 78.9)、除外後82.8=フロック無し、的中11%(中穴選定)。
    → **回収率が控除率ライン(1-0.2=80%)に張り付き 100%未満＝複勝も市場効率・エッジ無し**。
  - **✅特徴エンジニアリング掃討(2026-07-07)**: seed35y に対し (a)クリーンpace (b)ドメイン非線形合成8種(斤量比/パワー×脚/
    closing_bias「負けて強し」/距離×休み明け/実力×クラス 等) を足して 2020-21 OOS 再評価。全て **echo 0.989 不変**、
    model logloss 1.9718(base)/1.9731(pace)/1.9728(comp) で**いずれも市場1.9676に負け**、ΔR²+0.0009〜+0.0011(ノイズ)。
    → **特徴合成は市場効率を破れない。理由: 市場が織り込み済み＋LightGBM が木分割で比/交互作用を既に近似(合成は木に上乗せゼロ)**。
    相性14種(騎手×場/馬場/距離/天候・調教師×場/距離・馬主×種別・馬性×距離・**騎手×馬コンビ** 等)の
    target-encoding sweep(add_affinity_features.py)も実測: seed35y_aff 2020-21 OOS で **echo 0.989 不変**・
    model logloss 1.9730(市場1.9676に負け)・ΔR²+0.0009。→ **相性(相性は市場が最も織り込む領域)でも市場効率不変**。
    **確定: base/pace/合成/相性の全てで echo=0.989(小数3位まで一致)・市場に負け。特徴エンジニアリングでは市場効率を破れない。**
    ツール群: add_pace/add_composite/add_affinity_features(全 leak-safe・self-contained)。
  - **✅較正監査(2026-07-07・verify_calibration_audit.py)**: 本番モデル20260705 の Win 勝率 r̂ の ECE を市場と比較。
    全体 ECE **モデル0.0023 / 市場0.0027**(モデル僅かに良い＝ほぼ完璧に較正済)。cross-fit isotonic 再較正で
    0.0023→0.0013(改善+0.0009・閾値0.002未満＝実質十分)。頭数別は概ね互角(17+頭はモデルが良い)、
    人気帯別は全帯で「モデル劣化」に見えるが p_mkt で帯分けする構造バイアス(市場有利)。本命帯 ECE0.018 が最も甘い(実信号)。
    → **較正は既に完成。再較正は絶対利得が微小で利益化にも無関係(市場効率)。既存 win_calibrator(calibrate-ev/use_ev_calibration)で
    適用は可能だが優先度低**。次は運用側(損失最小化のレジーム選択)が現実的テーマ。
  - **✅連系ΔR²前哨(2026-07-07・verify_dynamics_vs_harville.py)**: 展開/脚質/当日バイアス(無料データ・leak-safe)で
    Harville 複勝残差(actual_top3−p_harv_place)を回帰。99万行OOS前段: 全特徴 |corr|<0.006、多変量 **R²=0.0001(in-sample)=実質ゼロ**。
    → **無料の展開/バイアスは Harville が外す joint を捉えない＝cheap-path は NO-GO**。区間タイム等の重いデータ投資は保留。
    ただし本番テスト(連系事前オッズ vs 市場 p_mkt)は未実施(要蓄積)。odds_watch に馬連(b4)/三連複(b7)捕捉を追加済
    (KEIBA_ODDS_CAPTURE_EXOTIC で有効化)＝受動的に蓄積し数か月後に vs 市場を測る余地は残す(期待は低)。
  - **結論確定: 単勝(35年 echo0.989)・複勝(478行＋35年実払戻 ROI83%)いずれも市場効率、独立エッジ無し。**
    **到達点=市場併走型の較正済み予測器。買い目は単勝一本で運用完成。seed 探索は完了(市場効率を35年で最強確認)。**
    seed 資産: seed_from_csv(合成ID/form強化)・build_seed_featured・retrain/backtest --featured-path は汎用配線として残す。
  - **✅元ネタ動画の「回収率300%」完全解剖(2026-07-07・YouTube知識vault 約405ノート精査)**: ユーザ提供の
    youtube-knowledge vault(チャンネル『競馬予想で始めるデータ分析・機械学習』・動画fQPNBB4p1x4等)を全精査。
    本チャンネルは**本プロジェクトの直接の元ネタ**(LightGBM/isotonic較正/300+特徴/期待値EV=pred×odds/VPS自動投票/IPAT)。
    「回収率300%」の正体を3層のartifactとして確定:
    - **①市場をなぞるモデル**: `tansho_odds`/`popularity` が特徴量重要度**1位**(複数動画で確認)、オッズ除去版(model_odds_removed)で
      logloss 0.205→0.225悪化=作者自ら「オッズが最強シグナル」測定。ロジスティック回帰でも人気係数が支配的。=echo0.989の実測根拠。
    - **②買い目閾値をtest集合で後付け最大化**: `gain(return_func,X,n_samples=100)` で `np.linspace` 閾値スイープ→最大回収率を採用、
      `if n_bets>2` で3点でも記録。独立検証集合で閾値を選んでいない(モデルはtrain/valid/test 3分割で適切、楽観は買い目ルールに純化)。
    - **③極小サンプル**: 315%は総投資額**265円=約3点**、126%は年72枚、293%は三連複。作者自身「少額ベット(1-2枚)は信頼できない、
      安定評価に**5000枚以上必要**(大数の法則)」と明言=サムネ数字を自己否定。バックテスト自体はルックアヘッド無し(6分前オッズ使用)。
    - **作者の正直な全サンプル数字**: モデル回収率=**82%(負け)**、1番人気ベタ買い=**79.8%**(≒控除率20%=市場効率の直接証拠)。300%はどこにもない。
    - **作者の thesis**(動画本人): 「市場は高効率(較正Y=X)、だがMLで市場精度を超える」→ **我々の35年検証が反証**(echo0.989/ΔR²≈0/OOS68.5%/固定額−24万円/較正0.0023)。
    - 作者も「logloss≠回収率」「特徴追加は頭打ち(peds重要度60位圏外)」「silent bugで結果は乖離しうる」を自認。全405ノートに反例ゼロ。
    - **結論**: 入門チュートリアルの300%は再現しない見かけ。本物のエッジは Benter型(独自加工特徴×低効率プール, 控除後101〜103%)か
      Ranogajec型(リベート8〜10%)=予測でなく私的データ/execution/リベート由来で、公開データの趣味実装では入手不能。「市場効率」結論は完全に堅牢。
  - **✅実績手法の再精査(2026-07-07・web research)**: Benter 1994 論文 + Ranogajec 事例 + FLB 学術文献を精査し、
    本プロジェクトの「市場効率」結論と突き合わせた。
    - **Benter(香港1986–93)**: fundamental 多項ロジット(距離適性DP6A/展開/騎手/斤量/luck補正…5人年で構築)を作り、
      **二段目の combining ロジット** `c_i ∝ exp(α·log f_i + β·log π_i)`(f=自モデル確率, π=公開オッズ確率)で融合。
      報告 R²: **公開単独0.1218 / fundamental単独0.1245 / 融合0.1396 → ΔR²=0.0178**。
      ★核心: fundamental 単独は公開とほぼ互角(+0.0027)、**エッジの大半は「融合」で公開の系統バイアスを除いた分**。
      それでも最大期待利益は**ターンオーバーの0.25–0.5%**(控除−19%下)、5季中4季黒字・1季−20%、~470レース/年、計10人年。
      連系(exotic)ほど優位が乗る("the more specific the bet, the higher the advantage")。
    - **Ranogajec(豪)**: エッジは予測ではなく**リベート**。本人証言「you bet to lose … the win comes from the rebates」、
      8–10%バックで回転利益。=予測エッジ0でも黒字化する構造(本プロジェクトには入手不能)。
    - **FLB 学術**: 本命側は「取引コスト内で効率」、非効率は大穴側に僅か。近代・大市場ほど効率。
    - **本プロジェクトとの整合**: 我々の echo=0.989・ΔR²≈0 は Benter の ΔR²=0.0178 と矛盾しない —
      差は(a)Benter の fundamental は公開が**過小評価する**独自加工特徴(5人年)、我々の特徴は公開と同源で市場を echo、
      (b)香港1986–93 の小・閉鎖・低効率プール vs JRA2020年代の大・効率市場、(c)exotic 増幅、(d)10人年の規模。
      Ranogajec の黒字は予測ではなくリベート。→ **「公開データで市場を出し抜く」NO-GO 結論は頑健**。
      ★唯一の未実施ギャップ: **Benter 型 combining ロジット(α·f+β·π)そのものを OOS で回した ΔR² 実測**は未。
      我々の echo は特徴 vs p_mkt の相関であり、combining の二段ロジット OOS ΔR² を明示的に測ってはいない
      (期待は低=特徴が市場同源だが、唯一残る「正攻法の再現テスト」として価値)。
  - **✅元ネタ動画チャンネルの手法解剖(2026-07-07・YouTube知識ノート25本を精査)**: 本プロジェクトの直接の元ネタは
    YouTube『競馬予想で始めるデータ分析・機械学習』。ユーザ提供の分析ノート(youtube-knowledge vault)から手法を完全マッピング:
    - 特徴量・パイプライン(300+特徴/過去6年/血統/脚質/context集計/isotonic較正/walk-forward/学習推論整合チェック)は
      **本リポジトリと同一**。リーク対策も丁寧(odds除去版 model_odds_removed_v3・直前オッズ・リーク回避ノートあり)＝ナイーブリーク無し。
    - ★**echo=0.989 の機械的原因を特定**: デフォルト feature_cols に **`tansho_odds` と `popularity`(人気)が入っている**
      (video BCXjaUtzpiQ)。市場価格を入力に持つ→pred は市場含意確率を再現(echo≈1)→EV=pred×odds が全馬ほぼ一定(≈控除率)
      →そのノイズを閾値で刈るだけ。**echo0.989 は偶然でなく設計上の必然**だった。作者も気づき odds 抜き版を別に作成。
    - ★**「回収率300%」の正体を確定**(作者本人が白状): (a)閾値スイープ `for exp in np.linspace(0.8,2,100): return_rate=payoff/bet`
      で**評価データ上の最大値を後付け採用**(video 80dtDunccf8)＝多重比較の楽観バイアス、(b)高回収率は必ず極小サンプル
      (315%は総投資額**265円=約3点**/126%は**年72枚**)。作者自身「72枚しかベットしない＝サンプリングエラー考慮が必要」と注記。
    - **整合**: 我々の 35年 walk-forward OOS(odds抜き相当)=**68.5%**、echo0.989、ΔR²≈0 と完全一致。
      動画は我々の市場効率結論を**否定でなく独立に裏付け**。→ 「300%は市場なぞりモデル＋買い目閾値の後付け最適化＋極小サンプルの合成artifact」で確定。
    - ★**author本人の記録による2大確証**(2026-07-07 追読): (1)オッズ除去実験(video Puqze70Vx9Q)で
      **`tansho_odds`+`popularity` を抜くと logloss が 0.205付近→0.225〜0.237 に悪化**＝作者自ら「オッズが非常に強力なシグナル」
      と測定＝モデルの予測力の大半は市場の読み直し＝echo論の実測証明。(2)検証は `split_data(test_size=0.3)` の
      **単一70/30ホールドアウト**(walk-forward でない)で、閾値スイープをその同じテスト集合上で最大化＝独立検証集合なしの楽観バイアス。
      → 我々の walk-forward(複数fold・閾値別期間固定)より弱い検証ゆえ高い数字が出る。方法論差で126〜315%を完全説明。
    - (未実施の任意反証) 同一 `np.linspace(0.8,2,100)` を検証期間で最大化→別 held-out に適用すれば「126%が翌期<100%に落ちる」を1枚で示せる(本質は既出)。
  - **③laptime→pace(2026-07-06/07)**: add_pace_features.py で各馬の過去走の相対脚(自上り−レース上がり3F)/前半ペースを
    leak-free(当該走 shift 除外)集計し featured 拡張。**注: CSV 要約列(前半3ハロン/上がり3ハロン)は一部1ハロン目の値で不正確
    → 生ラップ(ラップタイム1..N)から 前半3F=先頭3本和/上がり3F=末尾3本和 に再計算(mean 35/37s で正常)**。
    seed35y_pace(2020-21 holdout OOS): echo 0.989(不変)、model logloss=**1.9731**(pace無 1.9718 より僅かに悪化)・市場1.9676に負け、ΔR²=+0.0009。
    → **pace はバグ版でもクリーン版でも市場効率を動かさない(±0.003 は全てノイズ、buggy版の1.9701改善はノイズと判明)。エッジ無し確定**。
    3補助CSV検討完了: corner=results と冗長/odds=払戻表(overlay 不能・複勝EV は効率)/laptime=pace(微弱・織り込み済)。**全経路で市場効率確定。**
  - **✅Markov/深層RL手法の競馬無効性を実証(2026-07-07)**: 効率的パリミュチュエルでは RL も HMM レジーム検出も
    「状態に無い情報」を作れず市場を破れない＝**最適方策は abstain(賭けない)**、を自己完結の対照実験で固めた。
    実装: `src/simulation/_efficient_market.py`(純numpy・seed制御・hmmlearn非依存) + `verify_rl_markov_futility.py` + テスト14件。
    - 効率/非効率(stale)の2モード市場: 効率では `p_market·odds=1−控除率` が全馬で厳密成立(EV同一=控除率ぶん負け)。
      stale はオッズがレジームを織り込まず優遇馬に `p_true·odds>1`(正EV)が生じる=非効率(=Benterの香港プール相当)。
    - ★4結果(seed0,控除20%): ①素朴ベット −21%(≈−控除率) ②RL粗い文脈=OOS **96%見送り・利益ゼロ**(打たないのが最適)
      ③RL細かい偽文脈=**in-sample +24% → OOS −30%**(動画300%と同じ過学習の機序。文脈の細かさ=モデル複雑度の関数)
      ④HMMレジーム戦略=効率市場 **−25%** → stale市場 **+53%** と反転。同RLも −0.8%→+26%。
    - ★核心: **無効性の原因はアルゴリズムでなく市場効率**。同一手法が市場が非効率になった瞬間に勝つ⇒edge は
      『市場がまだ価格に織り込んでいない情報』からのみ。公開JRA(echo≈0.989, ΔR²≈0)にその余地なし＝高度化で覆らない。
      実データ傍証: `verify_dynamics_vs_harville.py` のレジーム/展開残差 R²≈0.0001。→ docs/rl_markov_futility.md に理論と結果。
  - **✅地方競馬(NAR)の市場効率性を実測(2026-07-08)**: 「特定の地方は流動性が薄く非効率が残るのでは？」を
    netkeiba 実データで検証。大井(44)/高知(54)/佐賀(55) 計475レース(2026-05〜06)を礼儀正しくスクレイプ。
    → `docs/nar_efficiency_pilot.md` + `scripts/nar_pilot/`(スクレイパ2+診断3)。データは第三者由来ゆえ非コミット(gitignore)。
    - ★race_id 12桁 = 年(4)|**場(5-6桁目=競馬場コード)**|回|日|R。中央1-10/地方30-65。`race_id.str[4:6]`で場別に切れる。
    - **単勝**: FLB(本命過小評価)は実在し JRA より強い(本命回収0.858/佐賀0.914 vs JRA≈0.80)。だが控除20%未満で赤字。
      プール475Rでどのオッズ帯も回収率1.0跨がず(最大0.918)。流動性↔効率も確認(大井=最流動=最効率0.765)。
    - **連系**: 三連単プールは単勝Harvilleを**echo 0.94**(log-log相関0.942/中央比0.91)でなぞる=独立情報なし。
      本命連系(馬単/三連単/三連複)は全場・全戦略で回収率<1.0。高控除(25-27.5%)ぶん単勝より悪い。
      ★小標本の嘘の実演: 三連単本命 63R=1.246 → 475R=**0.531** に崩壊(=300%機序)。
    - **モデル比較(対数尤度)**: 割引Harville(Lo,λ0.81/θ0.65)は素Harvilleより改善するが、**市場含意が全4場で最良予測器**
      (素-5.186<割引-5.095<市場-5.018)。薄い連系でも群衆は教科書割引モデルより賢い=強い効率性シグナル。
      天井は単勝そのもの。残る扉=単勝と直交するファンダのみだが JRA echo0.989/joint残差R²≈0.0001 が見込み薄を示す。
    - **リベート**: 楽天/オッズパークの実還元 基本~1%/最大10%(限定) < 控除20-27.5%。官営に機関リベート契約無し。
      Ranogajec式(edge+rebate>控除)は成立せず。実測合成: 全場本命0.858+最大10%=−4.2%(依然赤字)=損失縮小のみ。
    - **総括**: 地方は測定上たしかに JRA より非効率だが「**非効率 < 控除率**」が全プール(単勝/連系/リベート)で成立。
      **公開データで黒字化する経路は地方でも閉じている**。損失最小化フレームワークの前提を地方でも実測で裏づけ。
  - **✅ファンダ模型の「単勝と直交する情報」を実測(2026-07-08・調査の最後の扉)**: Benter 正攻法の combining logit
    (u=α·log f+β·log π)の **OOS ΔR²** を中央+地方で直接測定(これまで未実施だった唯一のギャップ)。
    → `docs/fundamental_orthogonality.md` + `scripts/fundamental_edge/`(スクレイパ2+combining_logit)。データ非コミット(gitignore)。
    - netkeiba から JRA/NAR 各250レース + 計5,481頭の過去走を取得。ファンダ特徴12(過去走相対着順/勝率/経験/間隔/
      賞金/斤量/馬体重/年齢/枠、**市場由来のオッズ・人気は除外**)で条件付きロジット→combining→日付分割OOS。
    - ★結果: JRA **ΔR²=+0.0009**(市場のみ0.320→市場+ファンダ0.321、α(ファンダ)=−0.034≈0)、NAR **ΔR²=−0.0055**(過学習で負)。
      ファンダ単独R²は JRA 0.121=**Benter公開ファンダR²0.1245とほぼ同一**(＝モデルは本物・勝ち馬を当てる)。
    - ★核心: **ファンダは予測できるが単勝と直交しない**(市場が全部織り込み済)。combining は市場を知るとファンダに重み≈0。
      Benter の ΔR²=0.0178 は独自特徴×低効率香港プール由来。公開特徴×効率JRA/NAR では ΔR²≈0＝直交情報ゼロ。
    - **調査の完全閉包**: JRA(単勝echo0.989/ファンダΔR²+0.0009)・NAR(単勝FLB<控除/連系は市場最良/ファンダΔR²−0.0055)・
      リベート(≤10%<控除)——**公開データで JRA/NAR を予測で出し抜く扉は全て閉じた**。唯一の合理的最適化は損失最小化。

---

## 10. 展開×脚質シミュレーション調査（2026-07-12〜13・happy-archimedes ブランチ）

> **一行結論**: 「展開（前傾ペース→差し有利）は薄い物理では再現できない＝NO-GO」という以前の判定は
> **壊れたデータによる偽陰性**だった。データを正し正しい因子を入れると、展開×脚質は**距離勾配まで
> 実測と一致する物理として再現できる**。ただし再現しているのは +0.02〜+0.06 の微小変調で市場が
> 織り込み済み＝**物理忠実度の達成であって市場エッジではない**（既存の「予測で出し抜く扉は閉じた」結論と整合）。
> 総括 Artifact: https://claude.ai/code/artifact/c6f598b2-ae50-465e-8745-87092eb37533

### 10-1. これまでの検討事項（調査のアーク）

- **問い**: モンテカルロ・エージェント sim を「予測器」でなく「複数パラメータ分布の結合による物理現象の
  モデル」として使い、実データの展開を再現できるか。当初の時間箱 sim はペース形が実測と **−0.22 逆相関**で
  「薄い4信号＋物理では再現不能＝NO-GO」だった。
- **★転回点＝データ欠陥の発見**: 展開×脚質の前提の脚質特徴 `leg_type_binary` が、ペースデータのある
  **1986–2021 で全馬同一値（unique=1）に潰れていた**。「現象が無い」でなく「脚質を測れていない」状態。
  2つの独立バグを層まで特定:
  1. **日本語日付パース失敗**: `build_horse_results_from_results` の書式無し `pd.to_datetime` が
     `"1996年01月05日"` を読めず全 NaT → `dropna` で horse_results が 0 行化 → 歴史馬の脚質消失。
  2. **頭数 n_horses の縮退**: race_id でない index への `value_counts` で **全馬 n_horses=1**。
     `_pace_num = 通過/頭数` が 1.0 固着 → 全馬「追込」に潰れた（recent はスクレイプ済 horse_results の
     自前頭数で無傷、archive だけ壊れて見えた）。
  → 両修正＋ `KEIBA_FORM_FROM_RESULTS=1 rebuild-featured` で 1986–2021 の脚質が unique=2/std≈0.25 に復活。
     40年分 133,950 レースでペースと脚質が初めて同時に揃った。
- **★標的の実在確認（純実測・sim不要）**: 各レースを実 `pace_diff` で前傾/後傾に3分位分割し、距離帯ごとに
  `signal = corr(脚質,着順)後傾 − 前傾`（正＝前傾で差し上位）をレース単位ブートストラップ95%CIつきで測定。
  **全距離帯で有意に正**（スプリント+0.053 / マイル+0.045 / 中距離+0.094[最強] / 長距離+0.039、全CIが0を跨がず）。
  → sim が再現すべき標的が確定。
- **ペース事前予測性**: 発走前の隊列構成からペース上振れ/下振れが条件と独立に読めるか（駆け引きの純寄与）を
  前進検証 → `c_resid = +0.248`（実脚質で 0.215→0.248 に上昇）。ペース水準自体も発走前に予測可能。
- **★固定距離エンジンで再構築**: 時間箱（固定時間で距離最多が勝ち＝速いほど有利で符号が逆）を捨て、
  **固定距離を最短時間で走る競争（到達時刻で着順）**に。ユーザが挙げた5因子を物理実装（下記10-2）。
  「前傾→差し有利」の符号は reserve/drafting/kick を振った **24通り全てで robust**（旧 knife-edge/時間箱と決定的に異なる）。
- **★距離勾配の較正一致**: 学習ペースを固定距離エンジンに注入し忠実度を距離帯層別。単一パラメータ較正でなく
  **「スタミナ容量は馬ごとに違う（距離適性）」という物理**（`field.stamina` を D_ref に掛ける）で勾配全体が実測に接近。
  最終: スプリント −0.038（実測≈0）/ マイル **+0.024**（実測+0.046）/ 中距離 **+0.054**（実測+0.065・近似一致）/
  長距離 +0.087（実測+0.061・やや過剰）。**3つのスタミナ律速帯で符号一致、効果が距離とともに増える勾配も再現**。
- **残差の由来（説明済・これ以上は過学習）**: スプリントの小さな負＝D≪容量で枯渇せず注入ペースが先行を利する
  時間箱的残り / 長距離の過剰＝勾配が僅かに急 / 忠実度(1)が負＝**計測アーティファクト**（sim は全馬 v=0 から加速し
  序盤ウィンドウが加速途中を拾う。機構を測る層別 signal は着順ベースで無関係・正常機能）。

### 10-2. 実装済み機能（このブランチで追加）

- **`src/simulation/_agent_race_fixed.py`**（★中核）: 固定距離エンジン `monte_carlo_fixed(field, D, ...)`。
  time-to-D 着順 / 距離連動→**馬固有スタミナ容量**（`B = reserve·v0·A·D_ref·stam`, D_ref=1600m, stam=field.stamina正規化）/
  **drafting**（`_front_gap` 直後で消費軽減）/ **騎手戦略の確率分布**（pos_target・aggr を sim ごとに分布から引く）/
  **逃げの競り合い**（`pressure_gain`: 逃げ志向が多い場ほど序盤ペース創発）。`SimConfigFixed` にハイパラ集約。
- **`src/simulation/_agent_race.py`**: 時間箱エンジンに `pace_intensity`（外生ペース注入口）を追加（旧エンジンとして残置）。
- **`src/simulation/_pace_model.py`**: `PacePredictor`（発走前特徴→pace_diff 回帰→intensity 写像）。
  `pace_features()`（front_ratio/n_front/ability分布/条件、前進安全）。
- **`pace_target.py`**: 距離×ペース層別で「前傾→差し有利」を実測（3分位分割・ブートストラップCI・backness自動選択診断）。
- **`sim_pace_inject.py`**: 学習ペース注入の忠実度A/B。`--engine {timebox,fixed}` 切替、忠実度(2)の**距離帯層別**出力。
- **`pace_forward.py`**: ペース事前予測性の前進検証（条件のみ/構成のみ/残差＝駆け引き純寄与 の分解）。
- **`src/preprocessing/_horse_features.py`**: 2バグ修正（日本語date書式パース救済＋頭数を race_id 実出走数優先）。
  回帰テスト: `tests/preprocessing/test_form_from_results_pace.py`（日本語date・縮退 n_horses で脚質が潰れない）。
- **診断ハーネス**: `diag_legtype.py`（年度別 脚質変動）/ `diag_recon.py`（再構築の中間量）/ `diag_nhorses.py`（n_horses健全性）。
- **テスト**: `tests/simulation/test_agent_race_fixed.py`（符号 robust・ペース形・能力単調・決定論・構成→ペース・
  スタミナ容量勾配）。sim スイート 188 passed / preprocessing 356 passed。

### 10-4. 位置取り物理の拡張（2026-07-13・1D エンジン）— 忠実度検証で 2D が頭打ちと判明し 1D を強化

- **背景（fidelity A/B）**: クリーン rebuild 後の `sim_fidelity.py` で 1D vs 2D を比較 → **2D は 1D をほぼ改善せず**
  ((1)+0.138→+0.169の微差、(3b)位置直接一致は 0.286→0.281 で僅かに悪化)。忠実度(1)の逆相関 artifact は
  クリーンデータで既に解消(1D も +0.14)。**残る最大ギャップ = 位置取りの「決めすぎ」**：corr(脚質,序盤位置)
  が sim=0.61 vs 実=0.37。＝脚質だけで位置が決まりすぎ、現実の散らばり(ゲート/出遅れ/前列争い)が足りない。
- **追加した物理3因子（`_agent_race.py`/`_sim_params.py`、すべて中立既定で後方互換・回帰テスト付き）**:
  1. **ゲート(枠順)**: `RaceField.gate`(馬番/枠番をレース内[0,1]正規化)。序盤フェーズだけ内枠に目標速度優位、
     `gate_fade` で減衰。脚質と独立に序盤位置を散らす。→ **既存 featured の枠番/馬番で即動く(rebuild 不要)**。
  2. **馬体幅/コース幅の定員**: `lane_capacity=course_width/body_width`。前方バンドの頭数が定員超過で前列が
     埋まり上がれない(crowd 比例減速)。全先行馬が前に殺到できず前列争い。既存の単一前方干渉は保持し追加。
     → **rebuild 不要**。
  3. **右/左回り適性**: `Master.PLACE_AROUND`(JRA10場→方向)＋`add_aptitude_stats.around_rel_rank`(同方向の
     過去平均相対着順・leak-safe)＋`RaceField.turn_apt`/`SimConfig.turn_gain`(実効能力を微修正)。
     → **around_rel_rank は新特徴＝rebuild 必要**（無ければ turn_apt 中立でスキップ）。
- **⏭ 検証（ユーザ実行）**:
  1. まず rebuild 無しで `python sim_fidelity.py --engine 1d --limit 8000 --n-sim 400 --max-year 2021` →
     ゲート＋定員で **corr(脚質,序盤位置) が 0.61 から実測 0.37 方向へ緩み、(3b) 位置直接一致が上がるか**。
  2. 回り適性まで効かせるなら `KEIBA_FORM_FROM_RESULTS=1 rebuild-featured`(約2h) → 再度 fidelity。
  規律: パラメータ(gate_early/course_width/turn_gain)は物理的既定で置き、忠実度指標への当て込み(過学習)は避ける。
  すべて sim の**物理忠実度**目的（市場エッジは echo0.989/ΔR²≈0 で閉じている＝§10 結論不変）。

### 10-5. 可変走行距離＋イベント層＋Optuna較正（2026-07-13・設計確定→実装）

**契機**: ユーザ指摘「コース幅・位置取りがある時点で固定距離ではないはず」＝内ラチ(最短)と外を回す馬は
走行距離が違う、という物理的正しさ。加えて 悪天候ダートの砂被り／落馬・巻き込まれ／dt 細メッシュ の要求。

**実装（すべて 1D エンジン `_agent_race.py`・全 knob 既定0＝opt-in・後方互換・回帰テスト付き）**:
- **可変走行距離(lane)**: 縦進行 `x += v·dt·(1−turn_k·lane)`。lane∈[0,1] は枠順で初期化し、前が詰まると外へ
  (`lane_swing_rate`)・空けば内へ(`lane_return`)。**枠順→初期レーン→距離有利／コース幅定員→強制ワイド→距離ロス**
  が1つの物理に統合。「固定距離でない」を解消。
- **砂被り(kickback_k)**: ダートで前に馬がいる後方馬が視界不良で鈍る（重い馬場で増幅・芝無効）＝ダート先行有利。
- **落馬/巻き込まれ(fall_base_flat/jump, fall_congestion_k, brought_down_p)**: 離散イベント。発火馬は DNF(最下位)。
  障害>ダート>芝・混雑で上昇・至近の落馬馬に連鎖。`_crowd_within` で近接判定。
- **dt 不変な積分**（前段）: 加速度ノイズ √dt スケール。dt=1.0 は従来一致。dt 細かくすると収束（一瞬の駆け引き解像）。
- `field_from_featured` が is_dirt/is_jump(race_type)・going(ground_state)・going_apt(wet_rel_rank)・
  turn_apt(around_rel_rank)・gate(馬番)を featured から供給。sim 200+ passed。

**Optuna 較正（`calibrate_sim.py`）— 合意した規律で実装**:
- **目的関数は「集約された創発統計を実測に一致」のみ**（着順予測 logloss/回収率は目的にしない＝物理定数の較正で
  あって予測器フィッティングでない。市場エッジは対象外＝echo0.989/ΔR²≤0 は不変）。
- 目的統計: corr(脚質,序盤位置)を実測≈0.37へ**一致** / corr(枠順,着順)を内枠有利へ**一致** / backness_signal**一致** /
  corr(sim位置,実第1コーナー)**最大化** / pace_shape**最大化**。
- **TPE＋物理prior bounds**（turn_k∈[0,0.03] 等）、**train(≤year)最小化→val(≥year)汎化確認**、
  best＋上位20%幅(不確実性)を `models/sim_calibration.json` に保存。落馬は DNF率データ不足で較正対象外(prior固定)。
- **⏭ ユーザ実行（要データ・重い）**:
  ```bash
  # around_rel_rank 反映に rebuild 済みが前提（§10-4）。race_pace 有れば pace/backness も入る
  python calibrate_sim.py --limit 1500 --n-sim 150 --trials 50 --train-max-year 2016 --val-min-year 2017
  # 細メッシュで忠実度を見るなら（前段）: python sim_fidelity.py --engine 1d --dt 0.25 ...
  ```
  出力の [汎化確認] 表で、val でも style_pos/draw_bias/backness が実測に近く pos_direct/pace_shape が正なら、
  物理定数はデータで裏づけられ汎化（符号一致まで主張・magnitude は幅で報告）。best を SimConfig 既定に反映する。

### 10-6. Optuna 較正の結論（2026-07-13〜14・3回実行）— 物理は方向を再現・汎化するが分散は構造的に未達

`calibrate_sim.py` を規律（目的＝集約統計への一致のみ・time-series val）で3回実行:
1. 初回: draw_bias 過剰(0.12 vs 実0.06)・パラメータが物理天井に張り付き。
2. 重み再調整(draw_bias_match 1→3・最大化項を半減): turn_k は天井離脱したが draw_bias 0.12 不変
   ＝draw_bias は位置取り knob で制御されない大域問題と判明。
3. ノイズ lever 追加(noise_mult＋ability_sigma を較正セットへ): draw_bias 0.12→0.10 に微改善、
   ただし **noise_mult=0.83(＜1)** に収束＝ノイズでは style_pos は下がらないと探索自身が示した。

**確定結論（3回で robust）**:
- **style_pos は構造的に ~0.52 で頭打ち**（実測 0.38 だが、これは**ノイズの多い再構築 leg_type との相関＝減衰値**。
  真値は 0.38〜0.52 の間で、0.38 に合わせるのは誤り）。脚質→目標速度の分離が強く、ノイズでも崩れない。
- **draw_bias ~0.10**（実0.06）、pos_direct **+0.30(val)**、pace_shape +0.22 — 内枠有利・位置一致・pace 形は
  正しい方向で再現。**3回とも train_sim≈val_sim で完璧に汎化**（過学習なし＝規律成功）。
- **物理は「展開・位置取りの方向を再現し汎化するが、分散の大きさは現在のモデル class では完全再現できない」**。
  §10 の既存結論（薄い物理は完全再現しない／市場エッジではない echo0.989・ΔR²≤0）を較正で定量裏づけ。
- 較正済み best（`models/sim_calibration.json`・gitignore）: turn_k≈0.015 / lane_swing_rate≈0.16 /
  lane_return≈0.12 / kickback_k≈0.02 / gate_early≈0.005 / going_speed_k≈0.11 / going_apt_gain≈0.13 /
  turn_gain≈0.15 / noise_mult≈0.83 / ability_sigma≈0.31。`sim_fidelity.py --calibrated` で適用可。
- **停止点**: これ以上（脚質→目標位置の確率化＝2D aptr_sigma 方式の1D移植）は大掛かり・payoff 不確実・
  過学習リスクで、規律的にはここで打ち切り。SimConfig 既定は opt-in(0) のまま（本番 sim/edge 検証を汚さない）、
  較正物理は JSON＋--calibrated で使う設計。**位置取りシミュ拡張の調査アークは一区切り。**
- **最終確認（2026-07-14・`sim_fidelity --calibrated` 全8000レース）**: 較正済みで style_pos 0.593→**0.530**
  （較正 val 0.533 とほぼ一致＝全データ汎化を確認）・(1)ペース形 0.146→0.158・pos_direct 0.279 維持。
  dt=1.0/0.25/0.01 で忠実度不変（dt不変性の実装が正しく、過決定は discretization artifact でなく構造的と確定）。
  ※ `--calibrated` の JSON パスは cwd/models（`sim_fidelity` を `__file__.parent/models` に修正済み）。

### 10-7. 2D化（2026-07-13〜14・context-review-next-tasks ブランチ・PR #18）— 2Dを正統物理エンジンへ格上げ

§10-6 で **style_pos ~0.52 の過決定は 1D では構造的に頭打ち**と確定。残る lever は「脚質→目標位置の
確率化」で、これは 2D エンジン(`_agent_race_2d.py`)が持つ**位置ターゲット制御(pos_gain)**そのもの。
そこで 2D を「1D の実験版」から**1D と同じ物理規約の正統エンジン**へ格上げし、pos_gain を過決定を
下げうる 2D 固有 lever として較正できるよう配線した（実装完了・push 済み。実行検証はユーザ待ち）。

**`_agent_race_2d.py` の物理統一**（1D と同名 knob・既定は中立で無効果＝後方互換）:
- **可変走行距離を arc-distance に統一**: 旧 `swing_cost`（外に出す＝速度ペナルティ近似）を撤去し、
  `x += v·dt·(1 − turn_k·y)` へ。横レーン y（0=内ラチ..1=外）が大きいほど縦進行が減る＝外を回るほど
  距離が延びる、を**物理的に正しい可変距離**として表現（1D §10-4 と同規約）。
- **gate→初期レーン y0**: `y0 = gate_lane·(0.08+0.84·gate) + (1−gate_lane)·0.5`（枠なり発進）。
  詰まり解除で内ラチ復帰 `lane_return`。
- **能力修正**: 回り適性 `turn_gain`、馬場 `going_speed_k`（全馬減速）/`going_apt_gain`（道悪適性）/
  `going_stamina_k`（消費増）。
- **離散イベント**: 砂被り `kickback_k`（ダート限定・前に馬がいる後方馬が鈍る）、落馬 `fall_base_flat/jump`・
  混雑加算 `fall_congestion_k`・巻き込まれ `brought_down_p`（発火馬は v=0・DNF・着順 -inf）。
- **noise_mult**: 加速度ノイズ倍率を大域ばらつき lever として追加。

**配線**:
- `calibrate_sim.py --engine {1d,2d}`。`PARAM_BOUNDS_2D` に **pos_gain(0.2,1.2)**（位置ターゲット強度＝
  style_pos を下げうる本命 lever）と swing_step を追加。エンジン別に out_path/bounds/cfg_cls/run_engine 切替。
  2d は `models/sim_calibration_2d.json` に保存。
- `sim_fidelity.py --engine 2d` は `_load_calibration(SimConfig2D, "sim_calibration_2d.json")`（cfg クラスの
  有効フィールドだけ適用・他エンジン knob は無視）。
- `_agent_race_fixed.py` の既存 ruff エラー(F841 `third`・E702 セミコロン)を解消（CI 通過用）。
- テスト: `tests/simulation/test_agent_race_2d.py` に gate内枠最短・砂被り減速・落馬(障害>平地)を追加。
  sim 全 **207 passed** / `ruff check src/` clean。

**規律は不変**（目的＝集約統計への一致のみ・着順予測/回収率で較正しない）。style_pos が実測に
一致しても市場エッジの話ではない（§10 の echo0.989・ΔR²≤0 は動かない）＝物理忠実度の限界を押す実験。
```
python calibrate_sim.py --engine 2d --limit 1500 --n-sim 150 --trials 50 --train-max-year 2016 --val-min-year 2017
python sim_fidelity.py --engine 2d --calibrated --limit 8000 --n-sim 400 --max-year 2021
```

**結果（2026-07-14・実行済み）— §10-6 の「構造的頭打ち」は 1D モデル class 固有だったと判明**:
- **過決定が解消**: style_pos が 1D 較正の 0.53 →**2D で 0.384（train_sim）/0.383（val）/0.399（全8000）**、
  実測 0.38/0.374/0.373 に一致。**train≈val で汎化**（held-out 年＋8000レース）。1D は何を弄っても 0.52
  から下がらなかったが、2D は位置ターゲット制御(pos_gain)を lever に実測分散へ到達＝**2D モデル class は
  実測の位置分散を表現できる**。§10-6 の「真値 0.38〜0.52・0.38 は誤り」の解釈は 2D で否定的（実測 ~0.38 が
  素直に再現可能）。draw_bias val 0.071(実0.068)・pos_direct +0.21 も維持。
- **トレードオフ（正直に記録）**: **(1)ペース形が +0.158(1D較正)→ −0.014 に退行**。最適化がペース形
  (重み0.3)をほぼ放棄し、**noise_mult=1.85**（1D は 0.83）まで上げて位置分散を作った結果、ペース順位が
  ノイズで洗われた。位置分散 fidelity と pace 形 fidelity は現モデル class で緊張関係にある。
- **切り分けアブレーション（実行済み・決定的）**: `--override noise_mult=1.0` で高ノイズを外しても
  **style_pos は +0.398 のまま（0.5 側へ戻らない）**＝**pos_gain(構造)が主因・noise はブラー役ではない**。
  ```
  python sim_fidelity.py --engine 2d --calibrated --override noise_mult=1.0 --limit 8000 --n-sim 400 --max-year 2021
  # → (3) sim=+0.398(実0.373) 維持 / (1) pace形 −0.011 / (3b) +0.206
  ```
  → **§10-6 の解釈を訂正**: 実測 style_pos ~0.38 は忠実に再現可能で、1D の 0.53 は真の過決定
  （硬すぎる脚質→速度結合）＝label-noise 床ではなかった。0.38 に合わせるのは「誤り」でなく正しい。
  さらに**前回の自分の仮説も訂正**: pace形の退行(+0.158→−0.014)は高ノイズによる洗い流しでなく、
  noise=1.0 でも −0.011 のまま＝**構造的**（noise では回復しない）。noise_mult は style_pos/pace形の
  どちらにも load-bearing でなく、較正では draw_bias/backness 調整に使われていた。

**§10-7 中間結論（style-focused 11次元較正の時点）**:
- 11次元較正（`--w-pace-shape 0.3`）は style_pos 0.38 一致・pace形 平坦(−0.014)。この1点だけ見て
  「pace形は構造的に出ない・両立不可」と早合点していた（下で棄却）。
- **2D は 1D が構造的に不可能だった「実測の位置分散の再現」を達成**（style_pos 0.38・train≈val・全8000で汎化）。

**§10-7 更新結論（探索空間 19次元＋pace重み UP で「両立不可」を棄却・2026-07-14）**:
- **前線はソフト**: pace形を駆動する物理 knob（break_speed/stamina_cost/stalker_*/closer_*/switch/interf_mult）
  を PARAM_BOUNDS_2D に解放し（11→19 次元）、`--w-pace-shape 1.0` で 120 trials 較正 →
  **pace形 (1) が train +0.568 → held-out 全8000 で +0.535 に汎化**（過学習でない）。11次元の「pace形は
  構造的に出ない」は誤り＝**空間が狭く重みが低くて探索が拾えていなかっただけ**。壁は硬くない。
- **ただしトレードオフは実在（ソフト）**: pace形を得た代償に **style_pos が 0.40(一致)→0.453(実0.373 を ~0.08 超過)**。
  前線上に2点が揃い、互いに支配しない:
  | 較正 | style_pos(実0.37) | pace形(1) |
  |---|---|---|
  | 11次元(pace軽視) | 0.40 一致 | −0.01 無 |
  | 19次元(pace重視 w=1.0) | 0.45 過剰 | +0.535 一致 |
- **警戒**: 19次元 best は `noise_mult=2.58`・`ability_sigma=0.685` が上限(3.0/0.80)近くに張り付き
  ＝高ノイズ＋高 pos_gain(0.59) のやや荒い領域で pace を得ている面あり。クリーンな物理解とは断定しない。
**§10-7 最終結論（前線の「膝」を確定＝両立点あり・2026-07-15）**:
- `--w-style-pos 2.0 --w-pace-shape 0.6`（style を締め pace を少し緩める）で 120 trials 再較正 →
  **held-out 全8000 で style_pos +0.391(実0.373) と pace形 (1) +0.505 が同時成立**。しかも
  `pos_gain 0.59→0.43`・`noise_mult 2.58→1.69`・`ability_sigma 0.685→0.40` と**上限張り付きが解け
  クリーンな物理領域**に移った（19次元でも train≈val で過学習なし）。**pace の主駆動は `stamina_cost`(0.027)**
  ＝前が終盤枯れる機構で、pos_gain を下げても pace が残る＝position と pace が別 knob で分離できると判明。
- **調査アークの全軌跡**（style_pos は実測 ~0.37 が目標）:
  | 較正 | style_pos | pace形(1) | 領域 |
  |---|---|---|---|
  | 1D §10-6 | 0.53 過剰 | +0.158 | — |
  | 2D 11次元 | 0.40 一致 | −0.01 無 | クリーンだが pace 無 |
  | 2D 19次元 w=1.0 | 0.45 過剰 | +0.535 | 荒い(noise/absig 上限) |
  | **2D 膝(最終)** | **0.39 一致** | **+0.505** | **クリーン(上限離脱)** |
- **結論**: 前線はソフトで**膝を持つ**。§10-6/11次元時点の「pace形は構造的に出ない・両立不可」は誤りと確定。
  **2D は展開(pace形＝どのレースが前傾か)と隊列(style→序盤位置)を、同時に・実測一致で・held-out 汎化しつつ
  物理再現できる**。位置分散の過決定(1D の宿題)も pace形の欠落(11次元の宿題)も 2D の膝で同時解消。
- **注記（正直に）**: (2)展開機構signalは実測自体が +0.018 と弱い＝「ハイペース→後方脚質が前」の因果は
  実データでも薄く、pace形(1)の再現とは別物（sim の失敗ではなく実データの性質）。`break_speed`(0.785)は
  上限0.80近くで軽い張り付き。knee 定数は **dt=1.0 前提**（2D は dt 不変でない・§dt 注記参照）。
- **規律・エッジ結論は不変**: pace形/style_pos 一致は最適化した集約統計そのもの。展開・隊列を物理再現できる、
  という主張に留まり着順予測・回収率とは無関係（§10 の echo0.989・ΔR²≤0 は動かない）。**市場エッジは無い。**
  较正物理は `models/sim_calibration_2d.json`(gitignore)＋`--calibrated` で使い、SimConfig2D 既定は
  opt-in(中立)のまま本番 sim/edge 検証を汚さない。**位置取りシミュ拡張の調査アークは 2D 化＋膝の確定で完了。**
- **残 follow-up**: ~~(a) 2D を dt 不変化~~ → ✅ **完了（§10-8）**。~~(b) 連系の依存構造を実測と比較~~
  → ✅ **完了（§10-9）**。以下は当初メモ（両方消化済み）——§10 の「市場エッジ無し」結論は動かない前提の、あくまで
  物理忠実度の確認として。

### 10-8. 2D エンジンの dt 不変化（2026-07-15・follow-up (a) 完了）

§10-7 の 2D は dt=1.0 前提（ノイズが ×dt・swing が per-step 固定で dt 依存）だった。ユーザ当初要望の
「dt=0.01 の細メッシュで一瞬の駆け引きを解像」を 2D でも可能にするため、`monte_carlo_2d` の時間積分を
1D と同規約の **dt 不変**へ:
- **加速度ノイズを Wiener 化**: `dv_det=(v_target−v)·accel_k·dt`（決定項 ×dt）と
  `dv_noise=noise_mult·noise·√dt·N`（ノイズ ×√dt）に分離。総時間 T·dt 固定で dt を細かくしても分散が保存。
- **swing_step を横移動「速度」に**: `y += swing_dir·swing_step·dt`（per-step 固定→per-time）。
- 他（x更新・stamina・落馬確率 p·dt・lane_return·dt）は既に per-time。
- **dt=1.0 で従来と数値一致**（√1=1・×1=1・RNG 1ステップ1ドロー不変）＝**既存 knee 較正
  `sim_calibration_2d.json` は再較正不要でそのまま有効**。
- 検証: win ベクトルが **dt=1.0→0.1（10倍細メッシュ）で ±0.01 に収束**（early_speed も 0.783 で不変）。
  回帰テスト2件追加（`test_2d_dt_invariance_finer_mesh_converges`・`test_2d_dt_one_matches_legacy_scaling`）。
  sim 全 **209 passed** / `ruff check src/` clean。`sim_fidelity` の dt 注記を「1d/2d とも dt 不変」に更新。
- これで `sim_fidelity.py --engine 2d --dt 0.01` 等の細メッシュ検証が 2D でも意味を持つ（計算量 ∝ 1/dt）。
- **実データ A/B 確認（同 2,885レース・n_sim=300・dt だけ変える）**: dt=1.0 → (1)0.510 (3)0.390 (3b)0.189、
  dt=0.25 → (1)0.517 (3)0.385 (3b)0.191＝**4倍細メッシュでも全メトリクス ±0.007** に収束。合成 win ベクトル
  だけでなく実測忠実度でも dt 不変を確認。※修正前エンジン(古 print「2dは近似」)では dt=0.1 で pace形が
  0.505→0.362 に落ちた＝ノイズ×dt 縮み＋swing per-step 過剰の artifact で、これが今回の修正で解消。

### 10-9. 連系(馬連・3連複)依存構造の忠実度（2026-07-15・follow-up (b) 完了）— 膝物理は同時分布まで再現

sim_fidelity は**周辺**（勝率・序盤位置）を測るが、連系(exotics)の本質は **joint≠周辺の積**＝共起依存。
膝較正済み 2D 物理が着順の**同時分布**まで再現するかを実測(RANK)と sim(top3)の同一統計で比較。
実装: `monte_carlo_2d(track_exotics=True)` が各試行の上位3頭 index を返す。`sim_exotic_fidelity.py` が
(A)馬連同脚質リフト・(B)1着→2着脚質遷移行列・(C)3連複 top3 同脚質ペア数・(D)1-2着枠距離 を集計。
馬の脚質は sim と同じ `field.style` を使い apples-to-apples。回帰テスト `test_2d_track_exotics_top3_consistent`。

**結果（`--calibrated --limit 6000 --n-sim 400 --max-year 2021`・held-out 5,827レース）**:
| 軸 | 実測 | sim | ヌル/独立 | 判定 |
|---|---|---|---|---|
| (A) 馬連 同脚質リフト L | 1.069 | 1.113 | 1.0 | 同符号・Δ0.044 ✓ |
| (B) 1着→2着 遷移行列 | — | — | — | Frobenius 0.050（ほぼ一致）✓ |
| (C) 3連複 top3 同脚質ペア数 | 1.678 | 1.727 | 1.613 | 両方ヌル超・Δ0.049 ✓ |
| (D) 1-2着 枠距離 | 0.379 | 0.390 | 0.389 | sim が実の弱依存を欠く △ |

- **(A)(B)(C) が一致＝膝物理は連系の共起依存（同時分布）まで再現**。特に (B) の構造——逃先1着なら2着は
  逃先(実0.61/sim0.63)か追込(実0.39/sim0.36)で差しはほぼ来ない(実0.005/sim0.017)——を正しく写す。
- **正直な2点**: ① sim が一貫して**わずかに過剰クラスタリング**（L 1.113>1.069・trio 1.727>1.678）＝§10-7 の
  style_pos 軽過剰(0.39>0.37)と同根で整合的。② **(D)枠の共起依存は sim が欠く**（実 0.379<ヌル 0.389 の
  極小 signal を sim は 0.390≒ヌルで拾えず）——ただし実の signal 自体 Δ0.010 と微小で「再現すべき依存が
  ほぼ無い」に近い。
- **規律・エッジ結論は不変**: これは sim の joint が**実測の経験的 joint**に一致した話。連系オッズは市場が
  織り込んだ joint で値付けされ、公開・構造的な依存の物理再現では優位性は生まれない。§10 の「エッジ無し」
  （回収0.696・echo0.989・ΔR²≤0）は不変。**市場エッジは無い・ライブ張り禁止。**
- **調査アークの到達点**: 周辺(勝率/位置)→ペース形 → **同時分布(連系依存)** まで、膝較正の 2D 物理が
  実測に一致することを held-out で確認。位置取り×展開シミュの物理忠実度は「周辺から joint まで」到達し、
  §10 の調査アークはここで**完了**（follow-up (a)(b) とも消化）。市場エッジは一貫して否定されたまま。

### 10-3. 次に着手すべきタスク

- ✅ **n_horses=1 の根本修正（コード完了・2026-07-13, context-review-next-tasks ブランチ）**:
  `ResultsProcessor._preprocess` の `df.index.map(df.index.value_counts())` を `_add_n_horses` に切り出し、
  race_id を明示取得して `groupby(race_id).transform("size")` で実頭数を算出（`_horse_features.py` と同規約・
  race_id が列/index どちらでも動作）。生 pickle が RangeIndex（race_id は通常列）形状のとき各行 index が
  一意になり全馬 n_horses=1 に潰れていたのが根本原因。回帰テスト `tests/preprocessing/test_results_n_horses.py`
  追加（RangeIndex / race_id index 両形状で縮退しない・1頭立てのみ 1）。preprocessing 359 passed / ruff・mypy 0。
  - ✅ **再ビルドで表面化した2つの潜在バグを連鎖修正（2026-07-13、実データ 177万行で確認）**:
    ユーザ環境で `rebuild-featured`（成功・133950 レース）→ `retrain`/`walk_forward` を実走した際、
    §10 で results に `通過` を追加して以降 retrain 未実行だったため、以下が初めて表面化:
    1. **`通過`（コーナー通過順）が学習特徴に混入** → `ValueError: could not convert string to float: '7-8-3-3'`。
       着順確定後の post-race 情報＝リーク列。`_DROP_FOR_TRAIN`/`_DROP_FOR_TEST`/`_DROP_FOR_PREDICT` に
       `HorseResultsCols.CORNER` を追加して除外（`tests/training/test_drop_corner_leak.py`）。
    2. **`best_class_won`/`best_class_won_z` が object dtype** → `pandas dtypes must be int, float or bool`。
       `race_class_level` が旧年代の分類不能名に None を返し `_past_level`(→groupby.max) が object 化するのが原因。
       (a) 根本: `_horse_features.add_race_class_stats` の `_cur_level`/`_past_level` を `to_numeric(coerce)` で数値化。
       (b) セーフティネット: `DataSplitter.__coerce_object_features` が学習入力の object 特徴量列を数値化
       （予測側 `_coerce_for_predict` と対称。date/horse_id は保護、category は非対象）。**既存 parquet を
       再ビルドせず学習可能**にする。回帰テスト追加。preprocessing+training+policies 813 passed / ruff・mypy 0。
  - ✅ **walk-forward 再確認 完了（2026-07-13・クリーンデータで「エッジ無し」確定）**:
    form-from-results（147,075 頭補完）+ n_horses 修正済みの 40年 featured（177万行）で `python walk_forward.py`
    を実走（retrain 不要 — fold ごと単一 LightGBM を `KeibaAIFactory.create`→`DataSplitter` で内部学習）。
    結果（単勝 OOS・5分割拡張窓）:
    | 評価fold | 買い目 | 的中率 | 回収率 |
    |---|---|---|---|
    | 1993–2001 | 0 | – | – |
    | 2001–2009 | 0 | – | – |
    | 2009–2017 | 0 | – | – |
    | 2017–2026 | 83,533 | 0.167 | **0.666** |
    | **通算プールOOS** | 83,533 | 0.167 | **0.666**（損益 −27,904）|
    → **回収率 0.666 < 1 = エッジ無しを、フォーム完全・n_horses 正常なクリーンデータで確認**。
    **これで 2026-06-29 の大保留（「エッジ無し結論はフォーム 93–96% 欠損で confound・無効」）が解消** —
    正しいフォームを入れても回収率は上がらず（従来 0.685 と同水準）、フォームは公開情報として市場に
    織り込み済みの結論が確定。
    - ✅ **fold 1〜3 の「買い目 0」の真因を特定（2026-07-13・`diag_tansho_odds.py`）— 単勝オッズ欠損は誤り**:
      当初「旧年代の単勝オッズ欠損」を疑ったが診断で**否定**。単勝オッズは featured 全年代 100%・raw 99.6% 存在。
      真因は**払戻テーブル（return_tables）が旧年代でほぼ空**:
      | 年代 | 払戻あり% |
      |---|---|
      | 1986–2016 | **0.0%** |
      | 2017–2026 | **43.1%** |
      （return_tables は全 12,012 レースのみ／featured は 133,950 レース）。`_betting_tickets._SingleStrategy.place`
      は race_id が払戻テーブルに無いと `(0,0,0)` を返し `_simulator` が n_bets=0 で除外するため、旧 fold は
      「賭けなかった」でなく「決済できなかった」だった。**上記 0.666 は払戻のある約 12k（≒2017年以降の43%）だけの値**。
    - ✅ **正しい実行法（コード修正不要・ツールに自己完結モードが既存）**: walk_forward には
      **`--by-odds`（+ 既定 bet_type=tansho）で `_tansho_oos_walk_forward` が起動**し、単勝払戻を
      featured の「勝ち馬 × 単勝オッズ」から決定論的に復元（return_tables 不要・line 345-348 に設計意図明記）。
      → **`python walk_forward.py --by-odds`** で全 40年・133,950 レースを決済した真の通期 OOS が出る（実走待ち）。
      当初の `--by-odds` 無し実行が Simulator 経路（return_tables 依存）に入ったのが買い目0の原因。
      （代替の本質対処: 旧年代 return_tables を ingest すれば汎用 Simulator 経路も使えるが ~12万ページの
      重いスクレイプで、単勝 OOS には不要。連系馬券まで OOS 測るなら必要。）
    - ✅✅ **【解決済・修正検証完了】`walk_forward --by-odds` の回収率 1.324 = 人物統計の同一レースリークが原因、修正後 0.696 に是正（2026-07-13）**:
      当初 全40年決済で **1.324**（全fold>1）が出たが、これは旧年代で jockey/trainer/owner_id が単一定数に潰れ、
      `_attach_jockey_trainer_stats` の位置ベース shift(1).rolling が同一レース他馬を取り込むリークの artifact。
      **date-strict 集約（(id,date)集約→日付shift(1).rolling）で修正 → rebuild 後は回収率 0.696・全fold<1・
      モデル logloss>市場 に是正され、リーク消滅を確認**（詳細は下記）。**「エッジ無し」が 40年通期クリーンデータで確定。**
      切り分け診断 **`diag_betall_recovery.py`** で払戻機構は健全と確定: 全馬買い回収率 **0.72**（<1・控除どおり）/
      勝ち馬数/R **1.001**（着順重複なし）/ overround **1.25**（オッズは正しい最終値）。→ **>1 は払戻でなく
      EV 選択＝モデル側**。払戻が健全なのにモデルの選ぶ馬が公正の 1.6倍で勝つ＝**モデルに特徴リーク**。
      **最有力容疑 = §10 の form-from-results 再構築（`KEIBA_FORM_FROM_RESULTS=1`）の時間リーク**:
      2026-06-29「エッジ無し」はフォーム 93–96% 欠損データ、今回フォーム全補完で初めて >1。フォーム集計が
      当該レース自身/未来走を「過去成績」に含めていれば自分の結果を見て当てられる（3–7倍の買い目 72→32,517 の激増も符合）。
      **次アクション**: `python debug_leak.py --top 40` でリーク列を名指し（held-out 単独 AUC≈1.0 の列）→
      該当する form/best_class_won 系集計の時間フィルタ（当該レース date 未満のみ集計）を修正 → rebuild-featured →
      walk_forward --by-odds が <1 に戻ることを確認。**この解決までエッジ主張は保留**（従来結論=エッジ無しが正の公算大）。
      - **debug_leak 結果（2026-07-13）— グロスな単一列リークは無し**: 直近スライスも旧年代(1993-2001)も
        **AUC>0.9 は 0 列**。最上位は 単勝_log 0.83（市場オッズ＝既知）、form 系は 0.70 前後（正常な因果範囲）。
        → 「form 再構築が当該レースを含む」タイプの明白なリークは**否定**（含んでいれば AUC≈1.0 になる）。
      - **旧年代で発見した データ異常（要調査・リークとは別問題の可能性）**: 1993-2001 で
        `jockey_avg_rank_z == trainer_avg_rank_z == owner_avg_rank_z == 0.8787`（小数4桁一致＝**3列が完全同一値**）、
        `jockey/trainer/owner_win_rate_z == 0.8220` も同様。旧年代で jockey_id/trainer_id/owner_id が同一値に
        潰れ、人物統計が実質「その馬自身の過去成績」に縮退している疑い。ただし `_attach_jockey_trainer_stats` は
        `shift(1)` で自レース除外済みなので**リークでなく冗長**の公算（過去form の重複）。要 ID 健全性の実測。
      - **✅ 決着（2026-07-13・`walk_forward.py --quality`）— 1.324 は旧年代リークの artifact で確定**:
        fold 別 勝ちlogloss（モデル vs 市場）:
        | fold | 市場 | モデル | 判定 |
        |---|---|---|---|
        | 2001–2009 | 1.950 | **1.767** | モデルが市場を 0.18 下回る（異常に良い）|
        | 2009–2017 | 1.988 | **1.720** | モデルが市場を 0.27 下回る（異常に良い）|
        | **2017–2026** | 1.936 | **2.087** | **モデルが市場より悪い＝正常（エッジ無し）** |
        決定的理由: (1) **近代 fold(2017-2026)はクリーン**（実スクレイプ horse_results）で**モデル<市場＝エッジ無し**、
        従来結論と一致し信頼できる。(2) 公開情報 GBDT が締切オッズを **0.2 nat** 上回るのは現実にあり得ない
        （本物の非効率はせいぜい数千分の1 nat）。(3) **市場 logloss は全年代でほぼ一定(~1.95)**＝市場のシャープさは
        今も昔も同等。なのにモデルだけ旧年代で急に良くなる ⇒「旧年代市場が非効率」では説明できず、**旧年代のデータ
        処理経路のリーク**でしか説明できない。リークが「どの年代の処理を使うか」で出没する時点で本物のエッジではない。
        → **walk_forward の 1.324 は旧年代限定リークの artifact。プロジェクトの「エッジ無し」結論はクリーンな近代 fold で
        逆に再確認された。実戦投入は引き続き禁止。**
      - **✅ リーク源を特定・修正（2026-07-13・`diag_person_ids.py` → `_data_merger` 修正）**:
        **機序確定**: 旧年代 raw の jockey_id/trainer_id/owner_id が**各1つの定数に潰れている**
        （diag_person_ids: 1986–2016 で全て uniq=1／null 0%／近代は 277・304・2093）。→ `_attach_jockey_trainer_stats`
        の位置ベース `shift(1).rolling(N)` で **groupby(id) が全馬1グループ**になり、日付ソート内で**同一レース他馬**が
        ウィンドウに入る。rel_rank はレース内総和固定なので同レース他馬の着順を混ぜると自分の着順が漏れる
        → 単独 AUC 0.879・logloss 0.2nat 改善。3者バイト一致（featured で jk==tr==ow rank 100%）は grouping が
        同一（全行1グループ）だから。
        **修正（コミット f8b016d）**: `_recent` を **(id, date) 集約 → 日付方向 shift(1).rolling → 各行へ broadcast** に変更し、
        同一レース(=同一 id×date)を構造的に除外。旧年代の潰れ id は「当日より前の開催平均」＝非リークの near-const になる。
        **近代の「同一レース複数頭の調教師/馬主」の潜在リークも同時解消**。real jockey(1頭/レース)は従来と同結果で
        既存テスト不変。回帰テスト `test_same_race_multiple_horses_no_leak` 追加。preprocessing 361 passed。
        **✅ 検証完了（2026-07-13・rebuild → walk_forward 再走）— リーク消滅を確認**:
        修正後 `KEIBA_FORM_FROM_RESULTS=1 rebuild-featured` → 再評価で、旧年代の異常が**完全に消滅**:
        | 指標 | 修正前 | 修正後 |
        |---|---|---|
        | logloss 2001-2009（モデル vs 市場1.950）| **1.767**（リーク）| **2.283**（市場より悪い＝正常）|
        | logloss 2009-2017（vs 市場1.988）| **1.720**（リーク）| **2.337**（正常）|
        | 通算 モデル AUC | 0.865（リーク）| **0.790**（市場0.836より下＝正常）|
        | `--by-odds` 通算回収率 | **1.324**（全fold>1）| **0.696**（全fold 0.690–0.703・全オッズ帯<1）|
        → **モデル単独は全 fold で市場より悪化（logloss>市場・AUC<市場）＝エッジ無しに戻り、回収率も 40年通期で
        0.696（控除どおり）に収束**。リーク artifact は完全消滅。**これで「エッジ無し」が 40年通期のクリーンデータで確定**。
        companion は logloss 1.9555 ≤ 市場 1.9578・ECE 0.0014 ≤ 0.0017 で「market-companion（市場同等の較正済み予測器）」を
        多時点で達成（§0 の北極星と一致）。**実戦投入は引き続き禁止**（市場を超える独立情報は無い）。
  - ⚠️ **retrain の `Killed`（OOM）は別問題（コードではなくメモリ）**: featured 177万行×583列をスタッキングで
    扱うと数〜十数 GB 要し WSL のメモリ上限で殺される。本番モデルが要る場合のみ対処:
    `retrain --no-stacking`（単一 LightGBM・大幅減）を第一選択、または `.wslconfig` で WSL メモリ増設。
    walk-forward 再確認には retrain 自体が不要なので、当面はスキップ可。
- 🟢 **（任意・興味が続けば）固定距離エンジンの残差詰め**: スプリントの負を消す（枯渇しない帯で注入ペースを減衰）、
  長距離の過剰を緩和。ただし band 数値への当て込みは過学習——独立較正データ（各馬の**区間ラップ**＝ハロン毎タイム）が
  無い限り out-of-sample 符号一致以上は主張しない規律を維持。
- 🟢 **忠実度(1)の計測修正**: sim を v=0 でなく巡航速度付近から始める or 序盤ウィンドウを加速後にずらして、
  ペース形の見かけの逆相関（アーティファクト）を解消（機構の結論には影響しない）。
- 🔵 **市場エッジは依然閉じている**: 本調査は「展開の物理再現」の達成であって、公開データで JRA/NAR を
  予測で出し抜く扉（echo0.989/ΔR²≈0）を開けるものではない。エッジ探索としては**損失最小化フレームワークが正**
  （既存 §結論と整合）。展開因子を賭けに使うには「市場がまだ織り込んでいない情報」が必要で、公開出走構成にはそれが無い。

---

## 2026-07-21 分析モデルの 6 分割管理（全国/地方 × 芝/ダート/障害）+ カテゴリ別 LightGBMTuner

**目的**: Place ヘッドを主催者区分（全国=JRA中央 / 地方=NAR）× 馬場種別（芝/ダート/障害）の
6 グループに分割し、グループごとに Place ヘッドを個別学習・保存・推論する（②(a) Place のみ。
Win ヘッドは従来どおり 1 本）。統合 Place ヘッドも併存させ、該当カテゴリが無い/データ不足の
レースは推論時に統合へフォールバックする。

**分類ロジック（`src/constants/_model_category.py`・純粋）**:
- 主催者: `race_id[4:6]` の場コード（中央 01〜10 / それ以外=地方）。馬場: `race_type`（芝/ダート/障害）
- slug: `central_turf` … `local_hurdle` の 6 種 + `combined`。`categorize(race_id, race_type)` で導出
- `src/training/_category_split.py`: featured の `race_type__*` ワンホットから馬場を復元し
  `split_featured_by_category(df)` で 6 分割

**学習（`RetrainJob` / `KeibaAIFactory.save(..., suffix=)`）**:
- 統合 Place（`<version>.pickle`）+ Win（`<version>__win.pickle`）に加え、データが
  `min_category_races`（既定 300）以上のカテゴリを `<version>__<category>.pickle` に保存
- `RetrainConfig.train_categories`（既定 True）/ `min_category_races` / `tune_categories`
- CLI: `retrain --no-category-split` / `--min-category-races N` / `--tune-categories`
- **`--tune-categories`**: 各カテゴリ Place ヘッドを LightGBMTuner で個別探索して学習。
  探索結果は `tuning_history.json` に `(version, category)` 単位で保存
  （`_tuning_history` に category フィールド・`get_params_by_rank(category=)` を追加）
- カテゴリ分割は gbdt DataFrame 上で行い、NN base は自動で外す（`_strip_nn`）

**推論ルーティング（`app/_data_loader.py` / 予測ページ）**:
- `resolve_place_model_path_for_race(place_path, race_id, race_type)` で該当カテゴリの
  Place ヘッドを解決（無ければ統合へフォールバック）。Win ヘッド解決（`win_head_path_for`）と同型
- 予測ページ: 「レース種別で自動選択（推奨）」で選択レースのカテゴリ別 Place ヘッドを自動適用
- モデルラボ Tab1: 「対象モデル」セレクタで統合/カテゴリ別の探索結果を切替表示

**⚠️ データ制約**: netkeiba スクレイパは中央のみ対応で地方(NAR)レースは未収集。実運用で学習される
のは中央 3 カテゴリ（データ十分なもの）+ 統合。地方 3 カテゴリは NAR スクレイパ追加後に同じ基盤で自動学習される。

**検証**: 実 `KeibaAIFactory`/`RetrainJob` で `--tune-categories` を E2E 実行し、中央芝/中央ダ/地方ダを
LightGBMTuner で個別探索→ `<version>__<category>.pickle` 生成、各カテゴリの探索履歴が category タグ付きで
保存されることを確認。回帰テスト: `test_model_category.py`(12) / `test_category_split.py`(7) /
`test_tuning_history.py`(+5) / `test_retrain.py`(+5)。全 1821 passed・ruff/mypy/import-linter 0 エラー。

---

## 2026-07-21 開催/クラスの standalone ターゲットエンコーディング（One-Hot 併用）

**背景**: One-Hot 化している列のうち高カーディナリティで TE が効きそうなのは **開催(57種)** と
**race_class(11種)**（sex/around/ground_state/race_type は低カーディナリティで TE 不要、race_type は
6分割で定数化）。jockey/trainer/owner の TE は既に `_merge_person_target_encoding` で導入済み。

**実装**（既存の `expanding_target_encode`＝時系列 expanding＋スムージング・リーク無しを再利用）:
- `_target_encoding.py` に `DEFAULT_CONTEXT_SPECS`（開催×_place/_win、race_class×_place/_win の4列）
- `DataMerger._merge_context_target_encoding`（merge ステップ `context_te`・person_te の直後）で
  学習/backtest 特徴に付与。`_shutuba_data_merger._merge_context_target_encoding_shutuba` で
  ライブ推論も同一計算（`person_te_for_upcoming`・train/serve skew ゼロ）
- One-Hot（開催__*/race_class__*）は**残す**ので A/B 比較可能。env `KEIBA_DISABLE_CONTEXT_TE=1` で無効化、
  `KEIBA_TE_ALPHA`（既定20）でスムージング強度
- 生成列: `place_place_te` / `place_win_te` / `race_class_place_te` / `race_class_win_te`

**注意**: featured_data の**スキーマが変わる**ため、`rebuild-featured` → `retrain` で反映が必要。
ライブ serve 履歴に race_class が無い場合は race_class TE のみ自動スキップ（開催 TE は有効）。
リーク検証は `test_target_encoding.py`（既存＋context 2ケース追加）で担保。全 1823 passed。

---

## 2026-07-21 交互作用ターゲットエンコーディング（天候×馬場, エンティティ交互作用）

**追加**（既存 `expanding_target_encode` の複数キー対応をそのまま利用・リーク無し・train/serve一致）:
- **context 交互作用**（`DEFAULT_CONTEXT_SPECS` に追加）: `weather × ground_state1` の複勝率/勝率
  （`weather_gs_place_te` / `weather_gs_win_te`）。雨→重馬場 等、単独 One-Hot では表せない組合せ効果。
- **エンティティ交互作用**（新 `DEFAULT_ENTITY_INTERACTION_SPECS`, 新ステップ `entity_te`, env
  `KEIBA_DISABLE_ENTITY_TE`）: 騎手×調教師（win/place）・馬×騎手（win/place・手替わり効果）・
  騎手×馬主（win）。単独は person_te/_horse_features でカバー済みなので組合せ効果のみ狙う。
  退化的な組（馬×調教師・馬×馬主＝ほぼ 1:1）は除外。
- **serve 履歴に `weather` を追加**（`_SERVE_HISTORY_COLS`）— ライブ推論で weather×馬場 TE を同一計算するため。
- shutuba 側にも `_merge_entity_target_encoding_shutuba` / context に weather×馬場を追加（as-of・train/serve skew ゼロ）。

**注意**: 高カーディナリティのエンティティ交互作用は大半が初出＝prior へ縮小され、AUC 改善は限定的な
見込み（前回の 開催/クラス TE も AUC フラットだった）。効果検証は AUC でなく backtest 回収率で。
featured スキーマ変更のため `rebuild-featured → retrain` が必要。テスト: test_target_encoding に 3 ケース追加。全 1826 passed。

---

## 2026-07-21 オッズ帯別 ROI 最適化（OOS 検証つき買い目フィルタ）

**背景**: 「的中率でなく回収率を最大化したい」。市場効率が高く（echo≈0.989）、`_loss_minimization.py`
は後付けしきい値フィッティングを禁止。素朴な「ROI が高い帯だけ買う」は過学習になるので、
**train/val 2 期間の両方**で採算が合う帯だけを採用する OOS 規律を課す。

**実装**:
- `src/policies/_odds_band_policy.py`（新・policies レイヤ）: `DEFAULT_ODDS_BANDS`
  =（1-3, 3-7, 7-15, 15-50, 50-∞）、`odds_band_label`、`select_profitable_bands`
  （train と val の**両期間**で ROI≥floor かつ信頼水準の的中数、さらに最大払戻 1 本除外後の
  `roi_ex_top`≥floor＝万馬券依存を排除）、`OddsBandPolicy`（frozen DTO）＋ save/load（inf→null）。
  ※ policies は simulation を import できないため帯定義はこちらに置き、simulation が import する。
- `src/simulation/_backtest.py`: `_settle_detailed`/`_aggregate_stats` に精算を分解、
  `roi_by_odds_band`・`format_odds_band_report` を追加。`run_backtest` は結果に `by_odds_band` を付与、
  `odds_band_policy=` で選定後の候補を帯フィルタ。
- CLI: `optimize-odds-bands --train-years … --val-years … [--roi-floor 1.0]`（帯を選定して policy 保存）、
  `backtest --by-odds`（帯別 ROI 診断）、`backtest --odds-band-policy <path>`（採用帯のみ購入）。
- 運用: `optimize-odds-bands` で policy を作り、`backtest --odds-band-policy` で **OOS** 回収率を確認。

**現実的な見立て**: 高効率市場ゆえ policy 適用後も ROI>1 が安定して出る保証はない。目的は
「回収率を正直に OOS 計測し、採算帯があれば絞る」こと。効果は backtest 回収率で判定。
テスト: `tests/policies/test_odds_band_policy.py`（7 ケース）＋ cli_smoke 更新。全 1833 passed・
ruff/mypy/import-linter 0。

---

## 2026-07-22 回収率最大化の OOS 検証（オッズ帯選定＋EV較正）＜結論＞

**目的**: 的中率でなく回収率(ROI)を最大化したい。手段としてオッズ帯別 ROI 最適化を実装・検証。

**検証データの制約**: featured は 2022 欠損・2021 部分的（28k）・確定オッズ snapshot は 2026(予定)のみ。
よって帯 ROI はオッズ境界のみ Harville 推定・**払戻は実データ(return DB)**＝回収率は本物。train/val は
払戻DBが揃う 2023/2024/2025 で組む。

**検証結果（すべて OOS：学習と別期間で再現確認）**:
1. **帯スライス単独では勝てない**。2024(train)/2025(val) のどの帯も回収率 100% 未満
   （最良 3-7 で train84.1%/val91.5%、最悪 50-∞ で train45.7%/val24.9%）。`select_profitable_bands`
   は roi_floor=1.0 で「採用帯なし＝買わない」を返す（＝後付け最適化を排除した正しい答え）。
2. **無較正 EV 方策は病理的**。全買い目の ~92% を 50-∞ の大穴帯（三連単/三連複）に垂れ流し、
   全体回収率 ~27%（2023）。EV=prob×odds が「小確率×巨大オッズ」を過大評価するため。
3. **EV 較正が最大の改善**（calibrate-ev を 2024 で fit→2025 で評価＝OOS）:
   全体回収率 **27%→76.0%**、50-∞ 帯 24.9%→74.1%、買われる券種は複勝・ワイド中心に健全化
   （三連単/三連複/馬連/馬単は 0 件に）。→ **本番は較正を必ず適用すべき**
   （backtest なら --corrected-harville --calibrate --blend）。
4. **較正後も赤字**。最良帯 ~80%・全体 76%。残り ~24% ロスは概ね**控除率(takeout ~20%)そのもの**。
   「モデルは正しく較正されているが控除を超える genuine edge は無い」＝効率市場(echo≈0.989)の帰結。

**実装**: `optimize-odds-bands` は較正を既定適用に修正（無較正だと帯 ROI が病理的に歪むため。
`--no-calibration` で無効化可）。`_odds_band_policy.py`/`select_profitable_bands`（両期間 floor 以上＋
roi_ex_top で万馬券依存を排除）。CLI: optimize-odds-bands / backtest --by-odds / --odds-band-policy。

**較正あり optimize-odds-bands 実測（train=2024 / val=2025・OOS）**:

| 帯 | train ROI(的中) | val ROI(的中) |
|----|----------------|---------------|
| 1-3   | 80.6% (3738) | 78.6% (3612) |
| 3-7   | 77.1% (1885) | 79.8% (2005) |
| 7-15  | 73.5% (773)  | 78.7% (844)  |
| 15-50 | 71.5% (306)  | 74.5% (285)  |
| 50-∞  | 69.9% (454)  | 74.1% (502)  |

→ 較正で全帯 70〜81% にフラット化（無較正で病理的だった 50-∞ が 24.9%→74.1%、的中も大穴の
数百→複勝/ワイドの数千に健全化）。両期間とも floor(1.0) 未達＝**「買わない」が OOS の正答**。

**到達点**: 「較正を入れて損失を最小化する（全体 27%→76%）」が現実的な上限。ROI>1 を狙うならスライスでなく
**モデルの edge そのもの**（新特徴/別ターゲット/オッズ動学）か**控除の低い場面に限定**する方向。
次テーマ候補: B=較正を本番/ライブ既定に組込み、C=edge 探索。

---

## 2026-07-22 EV較正を本番/ライブ推論の既定に組込み（B）

A の検証で EV 較正が全体回収率を 27%→76% に改善し無較正の大穴垂れ流しを解消したため、
ライブ推論の既定を較正 ON に変更:
- `src/operation/_config.py`: `OperationConfig.use_ev_calibration` 既定 `False`→**`True`**。
  models/*.json が無ければ `_load_ev_artifacts` が全 None を返し従来挙動へフォールバックするので
  未 fit 環境でも無害。無効化は `use_ev_calibration: false`。
- `config.yaml`: `use_ev_calibration: true` を明示追記（運用既定を可視化・上書き可能に）。
- `app/_prediction_service.py`: `getattr(..., "use_ev_calibration", True)` に更新＋docstring 修正。
- テスト: `test_disabled_by_default_does_not_load`→`test_enabled_by_default_loads`（既定で呼ぶ）に更新、
  `test_disabled_when_off_does_not_load`（明示 OFF）を追加。全 1834 passed・ruff/mypy/import-linter 0。

**運用注意**: 較正物は必ず OOS で fit すること（`calibrate-ev --years <学習年より後>`）。本番で使う前に
最新の calibrate-ev を実行して models/{place_exponents,win_calibrator,blend_weights}.json を用意する。
残タスク候補: C=edge 探索（オッズ動学/新特徴/別ターゲット）。

---

## 2026-07-22 edge 探索（C）＜結論：genuine edge なし・危険な誤記述を訂正＞

**目的**: ROI>1 を狙える edge を探す。

**オッズ動学は現状検証不能**: `evaluate-odds-dynamics` は各レースに複数フェーズ
（締切直前 T0＋それ以前・共通2頭以上）のスナップショットが必要だが、現状は各レース単一フェーズ＝
オッズ変動が未捕捉。前向きに odds_watch を締切30分前〜直前まで複数フェーズ蓄積して初めて作れる。

**単勝 edge の再監査（validate_edge.py・プラセボ＋リーク監査・直近26,790レース）**:
- 全オッズ: 本番回収率 0.68 < プラセボ（レース内で予測勝率シャッフル）0.75、かつ EV閾値↑で悪化
  （0.695→0.573）。edge があれば逆に上がるはずで、完全に逆。
- **オッズ≤15**（config の主張条件）: 本番≈0.80 ≈ プラセボ≈0.80（全閾値でほぼ同一、高閾値では
  プラセボが上）。→ モデルに genuine edge なし。回収率≈控除率ぶんの負け。
- 原因: モデルが特徴量に現在の単勝オッズ（単勝_log/単勝_z）を使い**市場をほぼ模写**（echo≈0.989）。
  EV>1 の買い目＝市場よりわずかに穴寄りの評価＝人気薄バイアスで負ける馬を選ぶ。
- リーク監査: 単勝_log/単勝_z は結果リークでなく市場オッズ（合法だが市場模写の主因）。
  owner_avg_rank/avg_rank_same_class 等は過去走集計で false positive。

**訂正**: `config.yaml` の「検証済み単勝戦略・回収率1.9–2.1・エッジ本物」は**誤り**（実弾で ~20% 負ける
危険な記述）。証拠つきで訂正済み。max_odds=15 / tansho_ev_threshold=1.1 は「損失最小化の足切り」で
あって利益戦略ではない旨を明記。`validate_edge.py` に `--max-odds` を追加（≤15 スライス検定用）。

**残る唯一の望み（未実施）**: 市場を模写しない独立信号＝**オッズ特徴を使わないモデル**
（`retrain --no-odds-features`）が市場と profitable に食い違うか。効率市場では大抵これも負けるため
期待値は低いが、edge を狙うならこれが本命実験。C の次アクション候補。

## 2026-07-22 edge 探索（C）追補＜no-odds モデルでも edge なし・最終決着＞

**独立信号テスト（本命実験）**: `retrain --no-odds-features`（オッズ由来12列を除外・市場を模写しない
基礎モデル。AUC 0.8023 vs odds版 0.8184）を validate_edge で検定。
- 全オッズ: 本番 0.705→0.547（閾値↑で悪化）< プラセボ 0.810→0.864。本番 << プラセボ。
- ≤15: 本番 ≈0.79 横ばい、プラセボ 0.76→0.89（高閾値で大幅に上）。本番 ≤ プラセボ。
- → 市場非模写の独立信号でも本番がプラセボを下回る＝EV 選定はランダム以下。モデルが自信を持つ
  買い目＝過剰人気の穴馬で系統的に負ける（favorite-longshot bias）。

**C 最終結論**: 3方向（全オッズ/≤15/較正backtest）＋独立信号テストの**すべてで genuine edge なし**を確認。
効率市場（echo≈0.989）と JRA 控除 ~20% は、現行の特徴量・券種・スタッキング構成では超えられない。
edge を狙うなら「市場に上乗せする真に新規な情報源」（未取得の調教/血統/バイオメカ等）か、
控除の低い場面（前向きのオッズ動学データ収集後）が必要。現行データでは打ち止めが正直な判断。

**運用注意（重要）**: no-odds は実験用の使い捨てモデル。`models/<新日付>/noodds_keibam.pickle` として
保存されると find_model_paths が最新として拾い本番既定が化けるため、**実験後は必ず削除**すること。
`validate_edge.py` に `--max-odds` / `--no-odds-features` を追加済み（対市場エッジ A/B 検定用）。

---

## 2026-07-22 feature harm 分析＜生 ID カテゴリが過学習で AUC を害＞

**目的**: 入力に混入していて AUC を悪化させている項目を特定。

**手法**: `feature_harm.py`（新）— permutation importance（AUC 基準・shuffle で AUC が上がる列＝害）
＋ `--ablate-groups`（各領域を drop 再学習した ΔAUC＝相関冗長を排した確定判定）。単一 LGBM・
--since-year 2016・baseline AUC 0.7879。

**結論（drop 再学習の ΔAUC が権威。permutation は相関で誤誘導）**:
- **生 ID カテゴリ（jockey_id/trainer_id/owner_id/breeder_id・4列）を落とすと AUC +0.011 改善**＝
  最大の害。permutation では jockey_id +0.005 等「有用」に見えたが、それは同一モデルが訓練データを
  丸暗記して shuffle で下がるだけ（暗記＝非汎化）。新規 fit すると ID 抜きの方が汎化して AUC 上昇。
  高カーディナリティ・カテゴリの典型的な過学習。汎化信号は TE・py 年度成績・集計で既に捕捉済みで
  生 ID は冗長かつ有害＝「高カード列は TE、生 ID は入れない」の定石通り。
- TE 群は permutation では害に見えたが drop 再学習では -0.0008（落とすと悪化＝有用）。誤検出だった。
- リーク監査フラグ列（owner_avg_rank/avg_rank_same_class/単勝_z 等）は全て無害 or 有用（false positive）。
- オッズ/人気群を落とすと -0.026（市場模写の中核）。

**実装**: `HIGH_CARD_ID_FEATURE_COLS`（定数）＋ `retrain --no-id-features` / `backtest --no-id-features`
（--no-odds-features と同じ配線）。`feature_harm.py --ablate-groups` で確定判定、人物群は
ID/py/着順集計に細分化。全 1834 passed・ruff/mypy/import-linter 0。

**次アクション**: 本番パイプライン（stacking＋tuning）で A/B 確認 → 効果が残れば生 ID を恒久除外。
注意: 実験モデルは version-name を付け、検証後は削除（find_model_paths が最新を拾い本番既定が化けるため）。

## 2026-07-22 feature harm 追補＜本番 A/B で「生 ID 害」は否定・訂正＞

直前セクションの「生 ID を落とすと AUC +0.011＝害」は **単一 LGBM proxy の値**だった。
本番 stacking パイプラインで A/B（--since-year 2016・同条件）を実走した結果:
- base2016（生 ID あり）  auc_test = **0.819**
- noid2016（生 ID 除外）  auc_test = **0.819**  → **差なし＝生 ID は本番では無害**

**理由**: feature_harm の proxy は正則化の弱い単一 LGBM（n_estimators=300/num_leaves=63）で高カード ID を
丸暗記 → 落とすと +0.011。一方 **本番 stacking は正則化が効き**（基底 LightGBM の used features=88＝
大半の列を使わない）、ID の過学習を最初から吸収。よって除外しても AUC 不変。

**教訓 & 最終結論**: permutation も単一 LGBM の drop 再学習も harm を誤誘導/過大評価しうる。
**最終権威は本番パイプライン上の A/B**。それに照らすと **本番モデルに AUC を悪化させる混入項目は無い**
（生 ID・TE・リーク監査フラグ列すべて無害。市場模写のオッズ群は AUC には有用）。
→ 生 ID の恒久除外は**不要**。`--no-id-features` は診断用に残置（既定 OFF・本番挙動は不変）。
feature_harm.py の proxy は harm を過大評価しうる旨を留意（確定は必ず本番 A/B で）。

---

## 2026-07-22 較正運用の堅牢化＋実験モデルの本番乗っ取り防止

**① 較正鮮度チェック（doctor）**: EV 較正（27%→76% の実利）が再学習で古いまま使われて劣化するのを
防ぐ。`_doctor.check_calibration` を追加し run_doctor に配線:
- models/*.json が無い → WARN「未fit。calibrate-ev を実行」
- 較正物が現行本番モデルより古い → WARN「モデルより N.h 古い。再fit 推奨」
- モデルと同鮮度 → OK。運用: 再学習のたびに `calibrate-ev --years <直近年>`。

**② 実験モデルの本番乗っ取り防止**: `--version-name` の使い捨て実験（noodds_keibam/base2016/noid2016）が
最新日付で保存され find_model_paths[0]＝本番既定を乗っ取る事故を3回踏んだため根絶。
`app/_data_loader.find_model_paths` を **日付接頭辞（YYYYMMDD_）必須**に厳格化
（`_keibam.pickle` 接尾辞＋`^\d{8}_`）。実験は日付接頭辞を持たない名前を使えば自動除外。
doctor 側も `_newest_production_model` で同基準を使い、実験モデルを本番扱いしない。

テスト: find_model_paths が実験名を除外・check_calibration の 未fit/古い/新鮮/実験除外 の4ケース追加。
全 1839 passed・ruff/mypy/import-linter 0。

---

## 2026-07-22 6分割モデルの効果検証＜結論：分割は予測を改善せず・統合で運用＞

**当初タスク**「分析モデルを全国/地方×芝/ダート/障害の6種類に分割管理したい」の効果を定量化。
`eval_category_models.py`（新）で、カテゴリ専用 Place ヘッド vs 統合ヘッドを同一カテゴリレース上で
AUC/logloss 比較（in-sample の相対比較）。

**結果（全カテゴリでカテゴリ専用が統合と同等〜僅かに劣る）**:
| カテゴリ | ΔAUC(全data) | ΔLL | ΔAUC(2025) |
|---|---|---|---|
| 全国・芝   | -0.0026 | +0.0083 | -0.0007（~同等） |
| 全国・ダート | -0.0018 | +0.0075 | +0.0003（~同等） |
| 全国・障害  | -0.0045 | +0.0315 | 2025は0レース |

**結論**: カテゴリ専用モデルは統合モデルを AUC・logloss とも上回らない。分割で1カテゴリの学習量が減る
不利＋統合モデルが芝/ダート/障害を跨いで共通信号（馬・騎手の調子・クラス等）を学べる利点が勝る。
障害ですら専用が不利（4,824レースと少なくデータ分割の不利が最大）。
→ **6分割インフラは完成・保持するが、予測では統合が最適。本番は統合フォールバックで運用**
（resolve_place_model_path_for_race のカテゴリ切替は現状 価値なし）。将来データ増／カテゴリ固有の
新特徴投入時に再評価の余地。ツール `eval_category_models.py`（--years/--roi）で随時再検証可能。

---

## 2026-07-22 地方競馬(NAR)データ取得 — Phase 1: ライブ系ドメインの organizer 分岐

**目的**: NAR の出馬/レース情報/オッズ/結果・払戻を、中央(JRA)用スクレイパを転用して取得。
調査結果、スクレイパは2系統: **DB系(履歴: 結果/情報/払戻/馬/血統)=db.netkeiba.com は JRA/NAR 共通で
変更不要**、**ライブ系(カレンダー/一覧/出馬表/オッズ)=race.netkeiba.com が JRA ハードコード**。

**実装(Phase 1: ライブ系ドメインを organizer で切替。JRA 挙動は不変)**:
- `_model_category.py`: `LIVE_NETKEIBA_DOMAIN`(central=race./local=nar.)＋`live_netkeiba_base(org)`／
  `live_netkeiba_base_for_race_id(race_id)`。既存 `organizer_of_race_id`(場コード[4:6]、≥30=地方)を再利用。
- `_scrape_shutuba.py`: URL をパス定数＋`_shutuba_url(race_id)`/`_race_list_sub_url(kaisai_date, organizer)`
  に置換。`scrape_race_id_race_time_list(kaisai_date, organizer="central")` に organizer 追加。
- `_odds_snapshot.py`: `build_odds_url` が race_id からドメイン解決。
- `_data_source.py`: `resolve_race_ids(date, organizer="central")`(全実装)。
- `ingest --organizer {central,local,both}` を追加。`--post-date` で NAR(または両方)の race_id を発見。
  `--race-id` 指定は race_id から自動判定。
- ポライトネス(`_rate_limiter.py`: 1〜3秒+時間上限1000+指数バックオフ)は全ドメイン共通で自動準用。

**残(要実ページ検証)**: NAR のオッズ `type` コード/DOM セレクタ・出馬表列クラスは実 nar ページで
要確認(枠連が無い場等)。履歴系は `ingest --race-id <nar_id>` が db.netkeiba.com 経由でほぼ動く見込み(要検証)。
テスト+6(ドメイン解決/URL ルーティング central↔nar)。全 1845 passed・ruff/mypy/import-linter 0。

---

## 2026-07-22 NAR 追補: ポライトネス 4 秒化 ＋ 取得可能性の事前検証ツール

**ポライトネス強化（ユーザー要望「間隔は少なくとも4秒」）**:
- `_rate_limiter.py`: `MIN_INTERVAL_SEC` 1.0→**4.0**、`KEIBA_SCRAPE_DELAY` 既定 1.0→**4.0**。
  既定で 4〜6 秒間隔（+jitter 2.0）。`base<=0` の無効化（テスト用）は維持。上げるのは env で可。
- テスト2件を新既定に更新。全 1845 passed。

**取得可能性の事前検証（ユーザー要望「多量件数の前にデータ項目の取得可能性を検証」）**:
- `verify_nar_scrape.py`（新）: NAR レースを **1件だけ**取得し、結果/レース情報/払戻の各データ項目が
  中央用パーサで取れるかを表示（列・行数・先頭行例）。**pkl 非破壊・馬/血統/featured 非触**。
  パーサは summary="レース結果" 等のテーブル属性依存なので NAR で DOM が違えば例外を表示（DOM 差の手掛かり）。
- 使い方: `python verify_nar_scrape.py --race-id <確定済みNAR_race_id>`。3項目 ✓ なら bulk へ。

**注意（別問題）**: `ingest --race-id` は結果パース後に「未取得の馬」を全件取得しようとする
（今回 147,486 頭・~229h 見積り→中断）。これは NAR 固有でなく horse_info カバレッジ不足の
全件バックフィルが毎回走る既存挙動。検証時は `KEIBA_SKIP_HORSES=1 KEIBA_SKIP_PEDS=1` で回避。
未確定レース(202630072201=本日)は結果テーブルが無く正しくスキップされた（＝正常）。

---

## 2026-07-22 NAR 検証結果: 履歴系は db.netkeiba.com＋既存パーサで動作（無改変）

**verify_nar_scrape.py で 202630072101（門別07/21R1・確定済）を検証 → 3/3 ✓**:
- results 12行×25列（着順〜馬体重＋各ID）/ race_info 33列（place_id=30 門別・ダート・1000m・稍重）/
  return 8行（単勝/複勝…）すべて既存 db.netkeiba.com パーサで完全取得。
- 先日 202630072201 が空だったのは「未確定」でなく **db.netkeiba.com の当日インデックス遅延**（翌日には載る）。

**判明した重要事項**:
- **NAR 履歴（結果/情報/払戻）は db.netkeiba.com＋現行パーサで無改変で取れる**。転用ほぼ完了。
- **NAR race_id 規則 = 年(4)+場コード(2)+MMDD(4)+レース番号(2)**（例 2026+30+0721+01）。
  → 日付＋場＋R から race_id を機械生成でき、バルク履歴はレース一覧スクレイプ不要で回せる。
- **オッズ type コードは中央と一致**（b1単複/b3枠連/b4馬連/b5ワイド/b6馬単/b7三連複/b8三連単）。
  ライブオッズは URL を nar.netkeiba.com/odds/?race_id=..&type=bN（/odds/・index.html なし）にするだけ。
- ポライトネス 4〜6s 表示・実測 6.67s で正常。

**軽微な要調整（非ブロッカー・後日）**: NAR の race_info で sex=None/age=0/race_class unknown。
中央の条件文前提の抽出が NAR 表記に未対応の取りこぼし（結果・払戻は完全）。特徴量に効くので調整候補。
**残**: (a) 当日分は nar.netkeiba.com/race/result.html のモダン DOM パーサが要（db 反映前に取るなら）、
(b) ライブオッズ URL パス調整＋DOM 確認、(c) NAR race_id 生成/一覧ヘルパでバルク収集。

---

## 2026-07-22 NAR バルク収集 稼働＋race_class 抽出を NAR 対応

**collect_nar.py 稼働確認**: 7日分312レース→results +3175行、2025門別+大井 limit200→+2520行。
発見(nar一覧)→取得(db)→マージ→dedup→中断再開すべて動作。ポライトネス 4〜6秒。

**race_class 抽出を NAR 対応に修正**（大量の unknown race_class 警告＝全NARで格未取得だった）:
- 原因: NAR は格が p[0] 末尾の「過去の◯○」に入る（例「過去の2歳未勝利」「過去のC3十 11」）。
  中央は text2[2] に条件が入るが NAR には無く、race_condition/race_class/sex/age が全 NaN だった。
- `_raw_parsers.create_raw_race_info`: text1 から `過去の(.+)` を抽出し classify_race_class の
  第3ソースに追加（中央/地方どちらの体系でも取りこぼさない）。
- `_master.py`: NAR 格 A1/A2/B1/B2/B3/C1/C2/C3 を RACE_CLASS_NAR_LIST・RACE_CLASS_LEVEL(1〜6)・
  _RACE_CONDITION_RULES に追加。Jpn重賞は既存グレード正規表現で G1/2/3 に写像済み。
- 検証: 2歳未勝利→未勝利, C3十→C3, C2一→C2, A1→A1, JpnIII→G3。JRA 回帰なし。全 1845 passed。
- 既知の残: NAR「一般/特別」は未対応(None)・sex/age は NAR で未抽出（格テキストから将来対応可）。
  既収集分は cached bin から race_info 再パースで race_class を後追い充填できる（get_rawdata_info）。

---

## 2026-07-22 NAR B: sex/age 抽出・ばんえい除外・オッズURL（データ項目の完成）

**発見**: 再パース後の残り警告は全て場コード65＝**帯広ばんえい**（輓馬）。ground_state が
"3.7"等＝馬場水分%で平地と別体系。→ 平地モデルに不適のため除外方針。

- **sex/age（NAR）**: `create_raw_race_info` で「過去の◯○」の格テキストから sex/age も補完。
  "2歳未勝利"→age2。"C3" 等 歳を含まない格から数字を誤抽出しないよう「歳」有無でソース切替
  （合成テスト: NAR 未勝利 age2/C3 age0、JRA 3歳牝 は不変）。
- **ばんえい除外**: `collect_nar` で場65を既定除外（`--include-banei` で任意）。残り race_class
  警告が消える。
- **オッズ URL**: NAR は `/odds/?type=bN`（index.html なし・実 URL 準拠）に修正。中央は従来
  `/odds/index.html`。type コードは共通（b1〜b8）。build_odds_url が organizer で切替。

**残（ライブオッズ DOM）**: オッズ URL は正した。ただし NAR オッズページ HTML の実際の DOM
（parse_combo_odds_html の odds-<type>-<umaban> id 体系）は実ページでの検証が必要（前向き作業）。
履歴の単勝オッズは results に、払戻は return に既に取得済み。全 1845 passed。

---

## 2026-07-22 NAR 収集基盤の堅牢化（dedup修正・ばんえい除外・掃除）

- **dedup バグ修正**: results.pkl は RangeIndex＋race_id 列（index は race_id でない）と判明。
  index を見る existing_race_ids では既存を拾えず毎回全件新規扱い＝再取得していた。collect_nar を
  race_id 列ベースの _existing_race_ids に修正。検証: 07/15-21 再discover で「既存276/新規0」＝dedup 正常。
- **ばんえい除外**: collect_nar が場65(帯広ばんえい)を発見段階で除外（312→276）。--include-banei で任意。
  `purge_banei.py`（新）で既収集の帯広を results/race_info/return・bin から除去（dry-run/--apply）。
- NAR 取得の仕組みは完成（発見/結果/情報[格・年齢・性別]/払戻・ポライトネス4-6s・dedup・ばんえい除外）。
  実データはサンプル約512レースのみ。全NAR履歴の本格収集・馬/血統・featured再生成・再学習は未実施。

**次(A)の見積り**: NAR は 1 年 ~3.5万レース×5秒 ≈ 48h/年。全史は多日級。まず 1 年スライス(例 2025 全NAR)で
collect→馬/血統 backfill→featured 再生成→NAR カテゴリにモデルが載るか、まで下流を検証してから拡大が安全。

---

## 2026-07-24 卍（Manji）ハイブリッド予測補正の設計・実装・検証（総括）

馬券裁判男「妙味度名鑑」の卍ロジックを、①Raw前処理→②LightGBM→③期待値→④補正 の
パイプラインに組み込む設計を実装し、実データで検証した。**結論から言うと、JRA公開データでは
回収率エッジは出ない（市場効率＋控除の壁）。次はNAR（地方）での再検証に着手中。**

### A. 検討した設計（ハイブリッド採用）
- ①(featured)は不変。①.5で卍補正を作り②の学習データに足す。②は較正済み勝率、③EV=P×odds、
  ④選定/サイジング、という層分離。
- 卍は「馬王の予測を使わない」＝**補正は結果(回収率)由来でモデル非依存**。②の較正済みPに
  加減点を掛けると脱較正して③EVが壊れるため、掛け算補正は不採用。
- 更新の入れ子（3層）: 外側=補正シナリオ/Optuna trial（②を作り直す・唯一重い）／中間=
  walk-forward fold／内側=as-ofブロックのベイズ更新（①.5内で完結・②非依存）。
- ①.5のベイズ更新の評価関数＝**観測補正回収率の尤度**（②不要）。シナリオ選抜の評価関数＝
  その①.5を食べた②のOOS回収率（baseline/placebo対照）。

### B. 実装済み機能（全て claude/model-6split-mainline に push 済み）
- **①.5a ファクター事前計算層** `src/tuning/_manji_factor_store.py`
  - `build_recent_form_features`: 馬×時刻の履歴依拠 mf_* を forward-only 算出（近走3/5走の
    平均着順・勝率・回収、通算、**前走着順mf_prev_rank・間隔mf_interval・距離変更mf_dist_change**、
    近走詳細mf_recent_deokure/trouble/close・トラック別/道悪別最高着、対戦履歴mf_h2h_score）。
  - `build_factor_table`(with_h2h/attach_peds)：全53因子のバケット表を1回生成しキャッシュ。
    CLI `python -m src.tuning._manji_factor_store --force --with-h2h --coverage`。
- **①.5b ベイズ事後分布ストア** `src/tuning/_manji_posterior.py`
  - Normal-Normal 共役事後で各(factor,bucket)のエッジを較正。**妙味度=100×post_mean（卍基準100）**、
    加減点=100×(post_mean−1)。σ²窓プール収縮、忘却割引(半減期)、均等払戻(recovery_mode=implied)、
    普遍性フィルタ、**情報事前(卍の方向性ルール)** prior_offsets。前進安全(as_of)。
- **卍の方向性加減点ルール（情報事前）** `src/policies/_manji_priors.py`
  - FACTOR_PRIORS: 22因子×バケットに妙味度オフセット（人気/馬体/斤量/枠/脚質/前走特記/
    パドック/基準乖離/季節性別/馬齢ローテ/近走出遅れ不利着差/逆トラック逆馬場/対戦履歴/生月2歳）。
    小標本は事前へ収縮、n増で実測が上書き。
- **シナリオcompile** `src/tuning/_manji_scenario.py`
  - Scenario/SCENARIOS(recent_form/pedigree_class/pace_dev/market_value/value_jinba/alive_value/all)。
    prepare_shared/build_block_posteriors（as-ofブロック・全シナリオ共有）。
    ①.5出力=①features⊕manji_score⊕**補正ファクター毎の妙味度列manji_myoumido_***(高カードも1列)。
- **Step4 ②学習＋OOS選抜** `src/tuning/_manji_scenario_eval.py`＋`manji_scenario_select.py`
  - walk_forward_roi/evaluate_scenario(s)（baseline=manji列除外・placebo=manji列行シャッフル）。
    **tune_lgb_optuna**：①.5成熟後(posterior_ready)に②のLightGBMをn_trials(既定100)＋
    Early termination(LightGBM早期停止＋Optuna MedianPruner枝刈り)で探索。
- **卍因子 53種** `src/policies/_manji_factors.py`
  - 基底: umaban_parity/sex/age/season/season_sex/weight_rank/rotation/dist_change/kinryo_rank/
    track_sex/career/sire_line/popularity(13番人気以下long13plusも)/waku/body_weight/weight_diff/
    kinryo_per_weight/age_rotation/dist_age/prev_finish/paddock/ground/race_class/leg_type/
    pace_pressure/kijun_gap/prev_trouble/prev_deokure/jockey/trainer/sire/race_type/dist_band/
    place/turn/birth_month_2yo/foreign_bred/prev_kanto/prev_kansai/prev_overseas/pedigree_2gen/
    recent3/5_form/recent3/5_recovery/career_form/career_recovery/recent_deokure/recent_trouble/
    recent_close/offsurface_form/offground_form/head2head。クロス"A*B"は自動生成。
- **Optuna汎用化**: `optimize_manji_config(points_fn=)` で100基準ベイズ点も探索可。
  `manji_walk_forward.py --bayes`／**`--organizer central/local/both`**（NAR特化検証）。
- **棚卸し** `data_inventory.py`：中央/地方×データ種別×年度の取得状況を集計。

### C. 検証結果（実データ・確定）
- **②feature注入(hybrid)は効かない**: value_jinba lift −0.010、alive_value lift −0.004、placebo90%。
  買い目≈全馬（EV>1.0で全馬選択）＝控除の壁0.66に張り付き。featured既存集計と重複で限界情報0。
- **卍の"選定"は効く（が壁の内側）**: manji_walk_forward（中庸ゾーン+top_k）
  - 単勝・Optuna・8fold・全期間: 通算0.870 vs baseline(ゾーンのみ)0.824、最良帯15-50=0.906、placebo90%点。
  - 複勝・固定config・全期間(121,938R): 0.806 vs 0.803、**placebo8%点（fit点がランダム以下＝過学習）**。
  - どの構成・帯も**1.0未満（利益未達）**。改善はあるが控除+市場効率の壁を越えない。
- **確定事実**: JRA公開データの卍妙味は回収率エッジなし（大標本で一貫）。出力の自己診断
  「JRDB相当の直交情報が不足」。エッジが出るとすれば①複勝×選定×サイジング②JRDB直交情報
  ③市場効率の低い市場(NAR)、のいずれか。

### D. データ取得状況（data_inventory.py 結果）
- 中央(JRA): 結果/レース情報 1986–2026（〜134k、2022欠）。払戻archive payoffs 中央1986–2021(121,938)。
- **地方(NAR): 2025–2026 の 1,309レースのみ**＝卍検証に全く不足。NAR払戻(複勝)はpayoffsに無し(0)。
- **血統 peds: 824頭のみ＋horse_id別形式(featured8桁 vs peds10桁)でoverlap0** → 血統因子は使用不可
  （sire/sire_line/pedigree_2gen 0%）。`_attach_peds`は非破壊修正済み（peds充実時に効く）。
- 発火因子（factor_table --coverage）: 90-100%＝umaban_parity/sex/age/season/season_sex/weight系/
  kinryo系/career/popularity/waku/ground/rotation/dist_change/age_rotation/dist_age/prev_finish/
  jockey/trainer/race_type/dist_band/place/turn/recent系/career_form/recovery。部分＝head2head60%/
  offsurface33%/offground22%。0%＝血統3種/JRDB4種/生月/遠征3種/近走出遅れ不利着差（元データ欠）。
  8-9%スパース＝race_class/leg_type/pace_pressure（featuredで8%しか無い・要調査）。

### E. 次に着手すべきタスク
1. **【進行中】NAR(A')パイロット収集**: 2024全NAR場（Phase0実測=1月952R→年間≈11,400R、単勝&複勝）。
   `python collect_nar.py --from 20240101 --to 20241231`（ポライトネス4-6s・resume安全・約12-16h/一晩）。
   → backfill-horses → rebuild-featured → factor_table --force →
   `manji_walk_forward.py --bayes --placebo 50 --folds 6 --organizer local --factors jockey,...`。
   判定: NAR単勝で回収率>1.0◎かつplacebo≥95%点が出るか。出れば複勝→Phase2(複数年)へ、出なければ壁確定。
2. **NAR複勝の決済配線**（Phase1完了後）: collect_nar が return_tables に貯めるNAR複勝を
   manji_walk_forward --bet-type fukusho が読めるよう return_tables→複勝lookup を実装（payoffs.pkl は
   中央archiveのみのため）。
3. **（NARで効かなければ）撤退 or JRDB取得**: JRA公開データ卍は「利益エッジなし」で撤退が誠実。
   JRDB(IDM/基準オッズ/パドック/出遅れ不利)を入れれば kijun_gap/paddock/prev_trouble/prev_deokure/
   recent_deokure が即発火＝市場と直交する私的情報で再検証可能（インフラは完成済み）。
4. **（任意）スパース因子調査**: race_class/leg_type/pace_pressure が featured で8%しか無い理由。
5. **（データ充実時）血統復活**: backfill-peds ＋ horse_id 突合（featured8桁 vs peds10桁の crosswalk）。

### F. 重要な設計原則（維持）
- 目的は**回収率最大化**（的中率ではない）。閾値の後付け最適化は禁則（_loss_minimization）。
- 補正の有意性は**必ずOOS＋placebo対照**で判定（小標本のまぐれに注意＝--limit 2000の0.88は幻だった）。
- 全作業は `claude/model-6split-mainline` に commit/push（footer: Co-Authored-By Claude Opus 4.8 /
  Claude-Session）。テストは合成データで、全スイート pass を維持（現在 331+）。

---

## 2026-07-25 NAR検証の最終判定：公開データ卍はエッジなし（JRA/NAR両方で確定）

NAR(A')パイロット完了（2024全場≈13,776R収集 → rebuild-featured 1,928,877行/148,935R →
factor_table再生成 → manji_walk_forward --organizer local）。

### 結果
- NAR単勝(14,985R・6fold): 通算0.798 vs baseline 0.787（lift+0.011）、placebo70%点、
  オッズ帯別 全て0.78-0.82（NAR控除の壁〜0.75-0.80に張り付き）。
- NAR複勝: 通算0.752 vs baseline 0.757（lift**−0.005**）、placebo38%点（ランダム以下＝過学習で悪化）。
- NARはJRAより控除が重く壁が低い。卍補正はどちらも壁を越えない。

### 全検証の総括（確定）
| 市場 | 券種 | 回収率 | placebo | 判定 |
| JRA | 単勝(Optuna全期間) | 0.87 | 90% | 壁未達 |
| JRA | 複勝(全期間121,938R) | 0.81 | 8%(過学習) | 無効 |
| NAR | 単勝(14,985R) | 0.80 | 70% | 無効 |
| NAR | 複勝 | 0.75 | 38% | 無効 |
→ **公開データの卍妙味は JRA/NAR・単勝/複勝いずれでも市場効率＋控除の壁を越えない**（大標本で一貫）。
  唯一の残路は JRDB相当の直交(私的)情報。

### 副次的成果（基盤は完成・検証済み）
- 遠征ファクター(prev_kanto/kansai/overseas)が履歴復活で90.6%発火。leg_type 8.3→10.4%、
  pace_pressure 8.6→14.8%（NAR分＋form-from-results）。sireは0.5%（血統backfill未実施・peds824＋
  horse_id 8桁 vs netkeiba10桁の突合問題は未解決）。
- data_inventory.py（棚卸し）/ diagnose_sparse_factors.py（二層構造診断）追加。
  featured の二層構造確定: 1986-2021=アーカイブ(horse_results無し・格無し・horse_id 8桁)、
  2023-2026=netkeiba生。race_class/leg_type が最近層でしか埋まらない根本。

### 次に着手すべきタスク（更新）
1. **判断待ち**: 公開データ卍は「利益エッジなし」で確定。選択肢は
   (A)撤退しEV較正+損失最小化運用に戻す（卍コードはJRDB用に温存）、
   (B)JRDB(IDM/基準オッズ/パドック/SKB不利/SED出遅れ)を取得し同じハーネスで再検証
      （kijun_gap/paddock/prev_trouble/prev_deokure/recent_deokure が即発火＝市場と直交）。
2. （B採用時）JRDBデータ取込（src/jrdb/ 既存・scripts/jrdb_build_features.py）→ featured付与 →
   factor_table再生成 → manji_walk_forward --bayes で再検証。
3. （任意・低優先）血統: backfill-peds＋horse_id突合（8桁 vs 10桁 crosswalk）。JRA archive race_class
   再パース（race_info に格があれば）。いずれもエッジ無しのため優先度低。

### 確定した設計原則（維持）
- 目的は回収率最大化。OOS＋placebo対照で有意性判定（--limit小標本のまぐれに注意）。
- 卍ハイブリッド基盤（53因子・ベイズ100基準・情報事前・シナリオ選抜・Optuna・NAR/複勝配線・
  organizer分離）は完成・全スイートpass。JRDB投入で即再検証可能。

---

## 2026-07-27 レーティング Phase 2-5 の隔離検証＜結論：Elo に対し冗長・本配線せず＞

`confident-hypatia-ruibla` ブランチ由来のレーティング拡張 **Phase 2-5**（TrueSkill /
条件別 TrueSkill / 能力 Kalman / 階層ベイズ）を、**production の core を一切改変しない
隔離検証ハーネス**として main へ移植し、実データ A/B でエッジの有無を判定した。

**前提（既知）**: Phase 1（ペアワイズ Elo）は main 統合済みで、実データ A/B（2026-07-15・
434,655 行）の結果すでに「**冗長（AUC ±0.001）**」と判明済み。未統合は Phase 2-5 のみで、
いずれも Elo と同じ「着順履歴からの潜在地力」前提の拡張。

**移植物（core 非配線・ハーネスからのみ参照）**:
| 種別 | 物 |
|---|---|
| 定数 | `_feature_cols.py` に TS_*/COND_*/KF_*/HB_* + `PHASE25_RATING_FEATURE_COLS`（24列） |
| 純粋モジュール | `src/preprocessing/_trueskill.py` / `_conditional_trueskill.py` / `_ability_kalman.py` / `_hier_bayes_trueskill.py` |
| 単体テスト | `tests/preprocessing/test_{trueskill,conditional_trueskill,ability_kalman,hier_bayes_trueskill}.py`（72件・as-of リーク無し等） |
| A/B ハーネス | `scripts/rating_ab_check.py`（featured から Phase2-5 特徴を生成→On/Off で AUC/logloss 比較。`data_merger`/`feature_engineering`/`retrain` は非改変） |

**実データ A/B 結果（`--since-year 2016 --target rank_win`・46,308 レース・586,913 行）**:

| 条件 | AUC | logloss |
|---|---|---|
| ベースライン（Elo あり） | 0.8505 | 0.5229 |
| +Phase25（24列・全6ファミリ有効） | 0.8505 | 0.5210 |

- **ΔAUC = −0.0001**（±0.001 以内）→ **Phase 2-5 は Elo に上乗せ価値なし＝冗長で確定**。
- 被覆率は全ファミリ有効（ts 0.888 / cond-surface 0.838 / cond-distance 0.777 /
  cond-around 0.483 / kf 0.888 / hb 1.000）。**Phase 3（条件別TS）は featured で dummify 済みの
  `race_type`/`around` をダミー列から argmax 復元して populate**（`recover_condition_columns`）。
  復元前は surface 被覆率 0%＝未検証だったが、復元後も結論は不変（ΔAUC −0.0005 → −0.0001）。
- Δlogloss = −0.0019 は**確率較正の微改善**であって順位付け（AUC＝馬券選定/EV に効く軸）は非改善。

**結論・方針**:
- **本配線（data_merger 等への Phase 2-5 組込み）は行わない。** main 自身の Elo A/B「冗長」・
  プロジェクト全体の「公開データにエッジ無し」と完全整合。レーティングは着順履歴＝公開情報の
  再要約なので Elo で頭打ちなのは理論的にも自然（エッジ源泉は市場と直交する JRDB 系のみ）。
- 純粋モジュール・テスト・`scripts/rating_ab_check.py` は**将来の再検証用に保持**（特に JRDB 等の
  直交データが featured に入ったとき、同ハーネスで即 On/Off 再検証できる）。
- 検証優先アプローチが機能: **core を一切汚さずに「配線する価値なし」を確定**できた。

**この作業ブランチ（`claude/review-context-and-tasks-0bkff7`）の他の成果**:
- `fix(preparing)`: bin 保存を常に上書き化し HTML 連結によるデータ破損を解消（`_data_loader.py`）。
- `refactor(leak)`: 目的変数リーク列を `TARGET_LEAK_COLS` に一元化 + retrain 警告（rank_win 除去
  自体は main 済み・挙動不変の DRY 化）。

## 2026-07-30 回収率重視の要求 → オッズ軌跡/ベイズ路線の理論・realizable実証で決着

**発端の要求**（ユーザ）: 「現行モデルが的中率重視なら回収率重視に変えたい」＋「卍と良いとこどり」。
さらに「オッズ変化の時系列予測（ディリクレ状態空間＋ベイズ更新）で最終確率を予測し bet時に活かせるはず」。

### 現状整理（コードで確認）
- 現行モデル(①)は二値 rank/rank_win で **AUC/logloss を最適化＝的中率(確率精度)重視**。ROIは学習に不在。
- 回収率(②)は既にポリシー層に存在: `_pnl_objective.evaluate_pnl`（"回収率を目的関数に"、log資産成長・
  市場アンカー Kelly）＋ `app/_model_eval.compute_confidence_sweep`（EV閾値スイープ）。
- つまり「回収率重視」= ①再学習ではなく **②運用点の切替＝損失最小化**。

### 新規ハーネス（本ブランチ push 済み・純ロジック単体テスト付き）
- `scripts/roi_vs_hit_operating_point.py`（+`--verify`）: 的中率重視(本命買い) vs 回収率重視(EV>閾値)。
  **決済は必ず真の1着(着順==1)**。"rank" 列は top3 二値目的変数なので使うと単勝配当×複勝的中で膨張(初期バグ)。
  検証: オッズ帯分解 `ev_band_breakdown` + p̂プラセボ `shuffle_within_race`。
- `scripts/roi_prerace_odds_check.py`: OZ前売り選定 vs 最終選定（精算は常に最終）。
- `scripts/roi_trajectory_check.py`: **前売りOZ→直前TYB(realizable)→最終** の3点。TYB=`raw_jrdb_tyb`（DB取込済・
  2015-2026、PK(race_id,umaban)、tansho_odds）。格納スケールは最終オッズ比で自己校正(`infer_odds_scale`、実測×1.0)。

### 実データ結果（baseline_jrdb_seirei・JRA・holdout直近20%）
- 運用点(全holdout): 的中率重視 return 0.841/hit 0.338。回収率重視 EV>1.0〜1.5 で **見かけ return 1.076→1.278**。
- **精査で棄却**: (A)帯分解＝利益は中穴〜大穴に集中(本命帯は0.96で負け)＝FL-bias否定だが最終オッズ依存。
  (B)p̂プラセボ＝実測1.076→0.67に崩壊＝モデル依存(オッズ構造単独ではない)。→ gate1通過だが最終オッズ先読み疑い。
- **決定的(OZ被覆10,437レース、精算は常に最終)**:
  - 前売りOZ 0.84–0.875 / 直前TYB 0.84–0.91 / 最終(先読) 0.74–0.77。**全て<1**。
  - 最終選定が一貫最悪＝**オッズ情報↑で選定悪化**（drift-out大穴に集中）。直前TYBは前売りを系統的に超えず。

### 中心定理（ユーザのディリクレモデルを閉形式で分解）
- 賭け条件: p_true/q^fin > Z（=オーバーラウンド, JRA単勝≈1.25）。
- 事後 p̂=(1-ρ)p_ML+ρ·q̄_mkt。発走直前 q̄_mkt→q^fin。よって **êdge ≈ (1-ρ)(p_ML−q^fin)**。
  ⇒ ① **追跡精度↑(ρ→1) ⇒ エッジ↓**（直交トレードオフ、最終選定0.74が実証）。
  ⇒ ② エッジの唯一源は市場非効率残差 (p_ML−q^fin)＝edge_diagnostic で ≈0(市場エコー)。
- **不変量**: 入力が {ML特徴, 市場オッズ軌跡} のみなら p̂∈span{p_ML,q_mkt}、エッジ⊆(p_ML−q_mkt)≈0。
  **天井 1−控除 はどんなベイズ/時系列/RL 再重み付けでも不変。** 破るには両者に直交な第3源(前走不利/出遅れ/映像)。

### 確定した方針
- **オッズ軌跡/ベイズ路線はエッジ源として決着（理論＋realizable実証）。** PyMC/SMC/RL をエッジ目的で組むのは非推奨。
- ディリクレ逐次モデルの**正しい使い道＝較正/損失最小化（＝回収率重視の実装本体）**。realizable最良点は
  TYB EV>1.5≈0.91 / OZ EV>1.2≈0.875（盲目本命0.84を数pt改善）。**天井<1 は不動**を明言した上でなら価値あり。
- 卍(妙味度)も同カテゴリ(ROIベース選択＝損失最小化)。良いとこどり＝①較正p̂ × ②EV/妙味選択。

### 追試（同日）: 「市場の動き×ML残差」も realizable エッジ無し
`scripts/odds_movement_edge_check.py`（OZ×TYB×holdout 142,414頭/10,437R）:
- (I) 動きΔR² blend(q_OZ,q_TYB) in-sample **+0.0029**（連系+0.009 より小・多重比較ノイズ域）、
  OZの年別被覆薄で **OOS両方向は算出不可**＝未検証。
- (II) セグメントROI（realizable・精算最終）: 全象限<1。最良=市場↑×ML↑(一致)0.876、
  **Case2「市場↑×ML↓(逆行)」は0.759で最悪級＝不一致エッジ仮説は反証**。
→ 「追従は負け／動きのズレに情報」仮説も棄却。市場効率の確認（第5経路）。realizable ROI 天井は
  依然 TYB EV>1.5≈0.91 / OZ≈0.875（損失最小化）で、**1−控除を超える経路は公開+半公開データに無し**。

### 2026-07-30(続) 市場残差エッジの精密検定 — 単勝決着・複勝で直交信号14倍だが控除未満（公開探索完全終了）
ユーザ方法論(残差モデル: target=win−q, ROIより先にE[Δ]binning)を実装。真OOS(2024+)・年分割。
`scripts/residual_edge_check.py`(単勝) / `scripts/place_residual_edge_check.py`(複勝, Phase C)。
直交JRDB指数=deokure_rate/pace_idx/chokyo_idx/gekiso_idx/start_idx/ten_idx/agari_idx/manken_idx（raw_jrdb_kyi, 被覆86%）。

**単勝**（q=最終単勝 within-race正規化, ROI=確定オッズ全張り）:
- Test1: ΔlogLoss −0.00013 / ΔAUC +0.00023（僅少）。
- Test2: トップビン E[Δ]=+0.0132（realized 0.2304 vs q 0.2172, ~3.5σ 実在）。**帰属: full +0.0132 / baseline(q再較正) +0.0070 → 直交寄与 +0.0062（エコーではない・実在アルファ）**。
- ROI: 全ビン・極端テール(上位10/5/2/1/0.5%)とも realized/q≈1.06 でフラット（集中せず）、ROI≈0.85。控除Z≈1.25 の1/4。**単勝は完全決着=tradeableでない**。

**複勝**（target=着順≤3, q=直前複勝オッズ Σ=3正規化, ROI=確定複勝払戻/100）:
- Test1: **ΔlogLoss −0.00181（単勝の14倍）/ ΔAUC +0.00205（9倍）、chokyo_idx係数 0.194**。→ ユーザ仮説「P(rank≤3)は調教/展開依存が大」を実証。
- Test2: bin9 E[Δ]=**+0.1162**（realized 0.624 vs q 0.508）だが複勝ROI 0.858。favorites(配当~1.37)に偏るため入着62%でも控除に負ける。
- 極端テール上位1% ROI 0.889（**セッション最接近**）。EV-value選択(中穴): 全閾値<0.83（favoritesより悪い）。

**確定結論**: 公開+半公開データは**全券種・全選択で控除(~20-25%)を越えない**。ただし**直交シグナルは実在**し、
複勝=展開依存券種で単勝の14倍強い（調教指数が主）。realizable最良=複勝本命上位1% ROI 0.889。
→ 天井を破る唯一の道は、この直交信号の上位互換＝**映像由来の展開/不利/出遅れ*事象*（JRDBの事前予測指数より遥かに強い版）**。
それでも~20%控除を越えるには JRDB指数の数倍の説明力が要る。**回収率重視の実務解=損失最小化**（盲目0.80→複勝本命選択0.89）。

### 2026-07-30(続2) 非線形GBM残差＋プラセボ＋年別再現＝公開データ探索の最終決着
`place_residual_edge_check.py --nonlinear`(LightGBM+脚質, 交互作用自動学習) / `--placebo`:
- 非線形: ΔlogLoss −0.00244(線形−0.00181より35%増・交互作用は効くが小)、chokyo/ten/pace 主。複勝ROI 上位1% 0.900。
- **プラセボ(特徴レース内シャッフル): ΔlogLoss 実測−0.00244 → +0.01249(悪化)＝信号は実在・過学習でない。**
- **年別再現: 上位5%複勝ROI 2024/25/26 = 0.871/0.874/0.867(プラセボ 0.80-0.81)＝安定・偶然でない・だが全年<1。**

**最終確定**: 公開+半公開データの対市場アルファは【実在・安定・複勝で最強(調教/展開)・線形も非線形も同じ】だが、
全券種・全選択・全手法で控除(~20-25%)に~10-13%届かない＝tradeable でない。プラセボと年別再現で bulletproof。
天井突破は【映像由来の展開/不利/出遅れ*事象*(JRDB事前指数の数倍強い版)】のみ。回収率重視の実務上限=損失最小化 複勝本命 ~0.89-0.90。

### 2026-07-30(続3) 最小ABM遷移ゲート＝Route C 決着（公開データ探索の理論的最終部分）
`scripts/abm_transition_place.py`: corner1-4 から start/trans(プール)/finish の3GBMを学習→事前状態から
決定論ロールアウト→P_ABM(rank≤3)。構造プライア(逐次Markov)が flat GBM を超えるか。
- OOS logloss: 市場q 0.4253 / flatGBM 0.4185 / **ABM 0.5124(大幅悪化)**。
- 複勝ROI上位1%: **ABM 0.851 < GBM 0.909(差−0.057)**、年別も一貫 ABM<GBM、プラセボ版も同様。
- 敗因=決定論ロールアウトが全事前特徴を低次元位置に圧縮する**情報ボトルネック**（誤差も3回複利）。
→ 含意: 競馬予測の律速は「構造」でなく「情報アクセス」。**フル物理MCでも GBM の直接特徴アクセス超えは困難**。
**Route C(構造プライア)決着。公開+半公開データは線形/非線形/相互作用/ABM構造まで全て探索し ROI<1 確定。**
残る道: (B)映像由来の新情報のみ（唯一 p_true-q を正にしうる）／実務は運用層=損失最小化(複勝本命~0.90)。

### 2026-07-30(続4) 評価関数の修復＝払戻を JRDB SED に統一（回収率計測の正確化）
運用層(BetPolicyFukushoHonmei)を配線後、バックテスト払戻源 netkeiba raw_return_tables が複勝
カバレッジ~46%で回収率を過小評価していた（本セッション冒頭の netkeiba 破損の残存）。
`src/simulation/_jrdb_return_source.py::JrdbReturnSource`（ReturnProcessor 互換・.preprocessed_data のみ）
を追加し SED の tansho/fukusho_payoff を (race_id,umaban) 100% 供給。simulate_model は
return_processor を DI 受けするため差し替えのみで配線。win_* は object dtype 保持
(.loc の float 格上げ→_match int(str) 破綻を回避)。end-to-end は BettingTickets 経由テスト。
- 結果: n_covered 46%→**100%**。複勝本命(top_n=1) 真の return_rate=**0.862**(netkeiba欠損版0.855の過小評価を解消)。
  min_score は弱レバー(0.862フラット/強見送り2.0で0.866)。top_n=1 が主レバー。盲目~0.80→本命0.862、<1 不変。
- **これで「予測→意思決定→評価」が揃い、評価関数が正確な完全検証環境が完成**（以降の ABM/映像/ポリシー改善を正しく測れる）。
- 運用層(回収率重視=損失最小化)の実装＋正確計測が完結。既定運用点: BetPolicyFukushoHonmei(top_n=1, min_score=0)。

### 2026-07-30(続5) 卍再検討: 購入ゾーン(#1・2D edge×odds 両側打ち切り walk-forward)も控除未満
`scripts/manji_purchase_zone.py`: (残差edge帯10 × 複勝オッズ帯5) セルを学習期間で ROI≥1.0&n≥300 採用→
翌年OOS固定適用・毎年ウォークフォワード。edge=place GBM−市場複勝確率、精算=JRDB SED確定払戻。
- 結果: **walk-forward 全体 OOS_ROI=0.782(n=49,579)、全年<1**。学習でROI≥1のセルもOOSで0.78に回帰
  (in-sample→OOS regression)。購入ゾーン(0.78)は全張り(0.73)より良いが**単純複勝本命(0.862)より悪い**。
→ **#1(卍購入面)棄却。** ユーザ3分割: (1)当時2005-09にROI>1は真、(2)今に適用でROI>1は否＝アルファ減衰/環境差実証。
残る卍角: #3独自スピード指数−IDM(要 馬場差/ペース補正の本格build・公開データ最後の筋)、#2ペアワイズ直接対決、
#4三連系(控除27.5%↑・多重検定リスク大)。takeout の壁は全角度で不変。回収率重視の実務解は本命0.862のまま。

### 2026-07-30(続6) 卍購入ゾーン v2（OOF・形状制約・除上位5）で#1を厳密棄却
`manji_purchase_zone.py` を4critique対応で厳密化: ①ゾーン選択edgeをOOF化(評価期間を手前fitモデルで
予測)②オッズ帯事前固定・edge帯OOF分位固定③50セル自由+連結矩形(方式C)④7分前≠TYB明記。軸=rank/abs。
- 結果: OOFにすると購入ゾーンがほぼ形成されない(多年 zone=0)。形成された稀ゾーンも n極小(117-267)・ROI<1。
  最良 2022 rank A=0.9811(n=127) も **除上位5払戻=0.5016** ＝数件の高配当依存(単一配当依存検査で崩壊)。
  abs軸 0.3410(除上位5=0.00)。walk-forward全体 rank A0.98(n127)/C0.74、abs0.34。
→ **#1(卍中央購入帯)を OOF+形状制約+多重選択抑制+単一配当依存検査で厳密棄却。** v1の0.78は in-sample楽観だった。
  限定命題を確定(現GBM残差指数×T-15複勝オッズ×直近OOF 2D中央帯は翌年移植せず・単一配当依存)。卍固有45factor指数/別券種/7分前オッズ/2005-09市場/独自スピード指数は未否定。残る#2ペアワイズ/#3独自スピード指数/#4三連系は
  未検証だが build大・期待低・takeout壁不変。回収率重視の実務解は複勝本命0.862(JRDB100%計測)で確定済み。

### 2026-07-30(続7) 卍購入ゾーン#1 感度診断で正式クローズ（限定命題）
`manji_purchase_zone.py --sweep`（oof窓2/3/5 × min_n300/1000 × n_eb5/10、最良採用せず）:
- **min_n=1000 では全設定でゾーン形成ゼロ**＝十分標本で OOF ROI≥1 の(edge×odds)セルは存在しない。
- ゾーン形成は min_n=300(小標本ノイズ)のみ。短窓 OOF1.016→OOS0.679、5yr OOF1.08→OOS0.78-0.81(ギャップ大)。
- 唯一 OOS>1(3yr 1.042)は n=73・2022単年・除上位5=0.50(単一高配当依存)。
→ 設定を振っても標本頑健・移植可能な購入ゾーンは無い。**#1(現GBM残差指数×T-15複勝×2D中央購入帯)を正式クローズ**。
未否定(別命題): 卍固有45factor指数/別券種(三連系)/7分前実オッズ/2005-09市場/独自スピード指数-IDM/ペアワイズ。
残る公開データ本命は #3独自スピード指数-IDM(購入面でなく情報表現・要 馬場差/ペース補正の本格build)。

### 2026-07-31 Δ_ability ゲート: 市場直交した過去能力残差は「簡易市場モデル」へは増分を持つ（第一の陽性）
`scripts/market_ability_residual.py`: 公開データ最後の筋＝#3独自スピード指数(市場と別座標系)を最小構成で検証。
Δ_ability = MySpeed − ĝ(市場能力写像)。MySpeed=過去 soten 群(S_last/mean3/max5/ewm λ0.7/trend, 全て当該走除外
leak-safe)を小型GBMで place へ圧縮(fold内<Y学習)。g=Ridge(IDM,IDM²,log基準オッズ,基準人気レース内百分位,距離)
を fold内学習し残差化(水準差の誤直交化を防ぐ)。3モデル真OOS: Base(市場q複勝) / Orth(q+8直交IDX) / Orth+Δab。
- 結果(2024+真OOS, JRDB SED確定精算): **ΔlogLoss(Orth+Δ−Orth)=−0.00277**（本格build閾値−0.001を突破）。
  年別 2024 −0.00293 / 2025 −0.00332 / 2026 −0.00156（全年改善・符号一致）。
  プラセボ(Δabレース内シャッフル) +0.00495（実測より悪化＝本物）。permutation悪化 +0.00786。**LGBM gain Δab全10特徴中1位**。
  複勝ROI 上位1/2/5/10%＝Orth 0.920/0.896/0.883/0.862 → +Δab 0.934/0.913/0.906/0.884（**全4帯改善**+0.014〜0.023）。
  効果形状: Orth残差平均 下位−0.025/中央−0.005/上位+0.027（単調＝Δab上位でOrthの過小評価を正しく補正）。
→ **クリーンGO。約20テストで初の陽性シグナル**。ただし比較対象は簡易市場baseline([q+8直交])であり、
  本番全モデル(~600特徴、豊富な過去成績)が既に同情報を持つ可能性は**未検証**（＝重複懸念）。

### 2026-07-31(続) 最終ゲート `--vs-production`: Δab が本番全モデルへ増分を持つか（重複検証）
`market_ability_residual.py --vs-production`: 陽性の帰属先を確定する最終ゲート。本番 baseline_jrdb_seirei の
place確率を featured 全体で算出(BasicScorePolicy)し (rid,uma) で te(2024+)へ結合、te内 GroupKFold-OOF で3比較:
  A=[logit(prod_p)] / B=[+raw MySpeed] / C=[+Δability]。prod_p/myspeed/dab は全て<cutoff写像から生成(leak-safe)。
  本番予測が te で in-sample でも A過大評価＝Δab増分過小評価の**保守方向**なので GO判定は安全側。
指標: ΔLL(C−A, Δab増分)・ΔLL(B−A, raw増分)・Δab固有(C−B, 直交の価値)・ΔAUC・年別≥2/3改善・プラセボ・
  複勝ROI上位1/2/5/10%(A vs C) **＋除上位5**(単一高配当依存検査)。純ロジック roi_top_pct/roi_excl_top をテスト追加(5 pass, ruff clean)。
事前固定Go: ΔLL(C−A)≤−0.0005 & 年別≥2改善 & ΔAUC≥0 & プラセボ死 & ROI≥3帯改善（強GO=≤−0.001 & C<B）。
No-Go解釈: 「Δabは簡易市場モデルに欠けた過去能力を効率表現するが、本番全特徴（豊富な過去成績）とは重複し追加情報は限定的」。
context記録方針: GO→「GO（本番全特徴との重複否定＝独立増分確認）」、No-Go→「（増分無し確認）」。実行はローカル(要 featured+SED+本番pickle)。

### 2026-07-31(続2) 最終ゲート結果: Δab は本番全モデルへ増分なし＝NO-GO（ただし過去能力自体は実在）
`market_ability_residual.py --vs-production`（te内 GroupKFold-OOF, 2024+, JRDB SED精算）:
  A=本番のみ logloss 0.41800/AUC 0.80315 ／ B=本番+raw MySpeed 0.41443/0.80871 ／ C=本番+Δab 0.41779/0.80412。
- **ΔLL(C−A, Δab増分)=−0.00021**（GO閾値−0.0005未達）。年別 全3年わずかマイナス(−0.0004/−0.0001/−0.0001)・
  プラセボ死(+0.00223)・ROI全4帯改善(上位1/2/5/10% +0.006〜0.018, 除上位5も0.88-0.94で単一配当依存なし)だが、
  肝心の logloss増分が閾値の半分未満＝実質ゼロ。→ **Δab は本番全モデルへ独立増分を持たない＝NO-GO**。
- 決定的内訳: **ΔLL(B−A, raw MySpeed増分)=−0.00357**（簡易baselineの−0.00277より大）。**Δab固有(C−B)=+0.00336**
  ＝市場直交すると価値が消える。∴ 簡易baselineでの陽性は「弱い市場モデルに欠けた過去能力を Δab が代理」しただけ。
  本番は市場側(~600特徴)を既に持つため、MySpeedから線形市場写像ĝを引く直交は**有損・冗長＝誤操作**だった。
- **結論(帰属確定)**: 陽性の正体は市場非効率エッジではなく**過去素点能力の実在情報**。しかし(1)本番モデルは既にそれを
  概ね内包(Δab増分≈0)、(2)残った raw MySpeed の増分(−0.00357)も **AUC 0.803→0.809 の確率品質改善であって回収率ではない**
  (ROI全帯0.87〜0.94<1・控除未満不変・EV線形性どおり)。→ **公開データの市場直交エッジ探索を正式クローズ**。
- 副次リード(未確定): raw MySpeed(過去SED素点履歴)は本番へ logloss/AUC を確かに足す＝本番が JRDB SED素点履歴を
  過小利用の可能性。ただし B 単独の年別/プラセボ/ROI 未検証・かつ的中率改善で回収率非改善のため、
  **エッジ探索としては打ち切り**。モデル品質改善リードとして別途1回確認する価値のみ（回収率重視の実務解は本命0.862で不変）。

### 2026-07-31(続3) 方針確定＋本格MySpeed Phase1 段階ゲート harness
最終ゲート結果を受けユーザ確定: **回収率エッジ探索＝正式クローズ**／本格MySpeed＝**予測モデル品質改善の別プロジェクトとして条件付きGo**。
理由: 粗いMySpeedの確率改善(ΔLL−0.00357)が上位帯ROI 0.87〜0.94(控除未満)に留まった＝「精緻化でROI>1」の投資仮説は弱い。
ゆえに回収率目的ならNo-Go・予測品質目的ならGo候補。目的は勝率/複勝率精度・校正・能力表現安定化・他券種/分析再利用基盤。
- `scripts/myspeed_staged_gate.py` 新規: 一括buildせず段階ラダーで**本番への純増分だけ**を測る。
  M0本番のみ / M1+raw MySpeed / M2+距離コース横断正規化(素点→条件内z, バケット統計<cutoffのみ・未知は全体fallback) /
  M3+ペース非対称(正規化速度＋ペース/位置履歴: asym=agari−ten, posr=corner1/頭数, pace_idx, uma_pace)。全て素点履歴を
  shift≥1 で当該走除外(leak-safe)、条件バケット=芝ダ×距離帯(固定境界)×クラス。te内 GroupKFold-OOF で
  ΔLL_k=LL(M_k)−LL(M_{k-1}) と本番比を算出。純ロジック(build_hist/dist_band/bucket_stats/condition_zscore/roi_*)テスト5 pass・ruff clean。
- 各段 継続ゲート(事前固定): ①ΔLL(前段比)≤−0.0005 ②年別≥2/3同方向 ③プラセボ(MySpeed_kレース内シャッフル)で消失
  ④過去≥3走部分集合でも増分(薄履歴依存でない) ⑤上位1%複勝ROIが前段以上(除上位5併記)。増分が止まった段で終了。
- **区間ラップ/馬場内時間変化/位置取りコーナーロスは Phase1(M2/M3)が本番へ増分を出した場合のみ後続**（高コスト・本harness対象外）。
  実行はローカル(要 featured+raw_jrdb_sed+本番pickle): `python scripts/myspeed_staged_gate.py --jra-only --db data/keibam.db`。
- SED が Phase1 全入力を保持確認済み(soten/kyori/shiba_dirt/class_code/pace_idx/ten_idx/agari_idx/uma_pace/corner1/toushuu, layouts.py)。

### 2026-07-31(続4) Phase1 初回結果＝設計欠陥判明→分岐(factorial)設計へ改修
初回(累積置換ラダー)実測: **M2(距離コース正規化)−M1=+0.00432(悪化)**／過去≥3走+0.00401／ROIも1%以外悪化 → No-Go。
**M3(ペース非対称)−M2=−0.00172**(2024−0.00131/2025−0.00210/2026−0.00177 全年改善・プラセボ+0.00116で死・過去≥3走−0.00129)
＝ペース信号は本物。ただしM3−M1≈+0.00260＝**M3は raw M1 に負けている公算**。
- 診断(ユーザと独立に一致): 累積で素点を条件内zに**置換**したためM2で絶対水準を破壊→その悪化がM3へ伝播し帰属が混在。
  「正規化は有害・ペース非対称は有望だが raw への純増分は未帰属」。gate⑤(top1%ROI hard stop)も単一帯で厳しすぎた。
- **改修(ユーザ指定の分岐設計)**: 累積一本線を廃し M1 からの factorial 分岐に。
  M0本番 / M1+raw / M2n=M1+条件正規化 / M2p=M1+ペース非対称(★) / M2np=M1+両方。各精緻化を**別軸スカラーで追加**(置換しない)。
  **最重要比較 LL(M2p)−LL(M1)**＝ペース非対称が raw MySpeed の上に直接足すか。
  M2p採用ゲート(確率品質): ①ΔLL(vsM1)≤−0.0005 ②≥2/3年改善 ③プラセボ(ペース軸シャッフル)消失 ④過去≥3走でも改善
   ⑤AUC・ECE(校正)非悪化。**ROIは hard gate から除外→ガードレール**(複数帯極端悪化/除上位5崩壊のみ監視・単一1%帯で決めない)。
- z-score置換版の正規化は破棄。将来 条件情報を使うなら raw⊕条件平均⊕(soten−条件平均)を**並列**(置換せず)。ただし今回の分岐には足さない(探索を広げない)。
- 分岐結果の判断: M2p採用→区間ラップ(ペース表現の精緻化・優先1)→馬場内時間変化→位置取りコーナーロス。M2p非採用→M3改善は正規化誤差の修復と判断し**最良=M1 raw**でPhase1終了・高コスト段へ進まない。
  実行: `python scripts/myspeed_staged_gate.py --jra-only --db data/keibam.db`。純ロジックテスト5 pass・ruff clean。

### 2026-07-31(続5) Phase1 分岐結果＝ペース非対称も raw への純増分なし。Phase1 終了(最良=raw MySpeed)
factorial 分岐(M1基準)の実測 te内 GroupKFold-OOF:
- **M2p(M1+ペース) − M1 = −0.00009**（閾値−0.0005に遠く未達）。プラセボ−0.00005＝実測と区別不能。AUC−0.00002(横ばい)。→非採用。
- **M2np(M1+両方) − M1 = −0.00055**（閾値ぎり超）だが **プラセボ−0.00061 が実測−0.00055 より良い**＝シャッフルが本物を上回る＝ノイズ。
  年別2/3(2026 +0.00001横ばい)・ECE +0.0005(校正悪化)。→非採用。
- M2n(正規化)も非採用(z-score置換版は破棄済)。ROIガードレールは全モデルOK(M2p上位1% 0.936→0.955 等)だが**採否非依存**。
→ **帰属確定: 前回の M3−M2=−0.00172 は正規化(M2)の水準破壊の修復であって raw への純増分ではなかった**。
  分岐で正しい基準(raw M1)に対して測ると、ペース非対称・条件正規化とも純増分なし。プラセボが本物を上回る点が決定的。
- **Phase1 終了**: 現時点の最良MySpeed = **M1 raw(過去SED素点履歴の圧縮)**。区間ラップ/馬場内時間変化/コーナーロスへは**進まない**
  (最も基礎的で安価な2精緻化が raw を超えない以上、高コスト段の期待値は低い＝ユーザ事前固定の分岐判断どおり)。
- **確定した唯一のモデル品質の勝ち = raw MySpeed を本番へ追加**(--vs-production: ΔLL−0.00357, AUC0.803→0.809, 全年改善, プラセボ死, 薄履歴非依存)。
  ただし ROI は控除未満不変(的中率/AUC改善であって回収率ではない)。回収率重視の実務解は複勝本命0.862のまま。
- 総括(本セッションの探索全終了): ①回収率エッジ=公開/半公開データに無し(約20テスト+最終ゲートで帰属まで確定・正式クローズ)。
  ②モデル品質=raw過去素点履歴の追加のみが本番へ純増分(精緻化は不採用)。次アクション候補は raw MySpeed 特徴を本番pipelineへ配線(別作業)。

### 2026-07-31(続6) Phase1 正式終了＝M1 raw MySpeed 採用。M1本番採用前の validation チェックリスト
ユーザ最終判定に合意: **M1(=production + raw historical MySpeed)を Phase1 の勝者として残す。M2n/M2p/M2np 非採用。
区間ラップ/馬場内時間変化/位置取りコーナーロスは不着手。MySpeed モデル品質プロジェクトは一旦完了。**
- M2p 実質null: ΔLL(M2p−M1)=−0.00009(閾値−0.0005に遠い)・実測−プラセボ=−0.00004のみ・AUC−0.00002。上位ROI改善はtail順位の入替で採用根拠にならず。
- M2np 採用不可: ΔLL−0.00055で閾値ぎり超だが**プラセボ−0.00061が実測を上回る**＝ペース軸へ帰属不可(正則化/分散/再学習揺らぎの範囲)。2026改善なし・ECE悪化。
- 累積版 M3−M2=−0.00172 の正体＝M2のz-score置換が作った誤差の後段修復(ペース信号そのものではない)と確定。→高コストなペース精緻化へ進む実証的な橋は無い。
- 研究知見: 市場直交化/条件内z置換/粗いペース非対称圧縮より、**過去SED素点の絶対能力水準を保持したまま本番へ渡す**方が有効(ΔLL−0.00357)。
  本番が距離/クラス/コース/過去成績を既に持つため、条件付け・相互作用はモデル内部に任せる方が良い。
- **M1本番採用前の validation(エッジ探索でなく通常の production validation。採用条件)**:
  ① raw MySpeed単独の年別改善(--vs-production で全年マイナス確認済) ② raw MySpeedのレース内シャッフル(プラセボ死・確認済)
  ③ 欠測率と履歴数別性能(未・要確認) ④ 校正への影響(ECE・部分確認) ⑤ 学習/推論パイプライン再現性(未・要確認)。
  ①②は --vs-production で同等厳密に確認済。③⑤が残(必要なら M1 単独 validation pass を1本)。
- 技術メモ: staged_gate 実行時の sklearn "X does not have valid feature names" は fit/predict のコンテナ型不整合による cosmetic(判定値は無効化しない)。
  本番配線時は学習時と同一列名・同一順序の DataFrame を推論へ渡し将来の列順ずれを防ぐ。
- 次アクション候補(任意): raw MySpeed 特徴(raw_last/mean3/max5/ewm/trend, leak-safe)を本番 featured pipeline へ配線＋③⑤の validation。ROIは控除未満不変(的中率改善)。

### 2026-07-31(続7) 研究フェーズ 正式クローズ → 実装フェーズへ（失敗マップ＝設計図）
研究課題「公開データから回収率エッジは作れるか？」= **終了（NO・帰属まで確定）**。以降は工学課題「production modelを少しでも良くする」のみ。
探索は事前仮説の検証段階を終え、これ以上は仮説を探し続ける段階に入る＝多重検定リスク。ゆえにここで研究ノートを閉じる。
規律の記録（自動判定/事前予想を実測へ修正した履歴・本プロジェクトの核心的価値）:
  ・「中央購入帯は残っているかも」→ OOF・walk-forward・感度分析で否定。
  ・「Δability が最後の本命」→ production比較で帰属を切り分け（簡易baselineの陽性は過去能力の代理）。
  ・「距離コース正規化＋ペース非対称へ進む」→ factorial分岐で帰属を再分離。
  ・「M3が効いた」→ M2(z-score置換)の副作用修復だったと確認。

失敗マップ（何を試し・何が生き残り・なぜ止めたか）:
  公開情報
    ├─ raw MySpeed ────────────── 採用（本番へ ΔLL−0.00357 / AUC0.803→0.809）
    ├─ Δability ─ × 市場直交化で情報喪失（本番全モデルへ増分なし）
    ├─ 距離コースz ─ × 絶対能力水準を破壊（M2−M1=+0.00432）
    ├─ ペース非対称 ─ × 単独では増分なし（M2p−M1=−0.00009・プラセボと区別不能）
    │                     累積改善(M3−M2=−0.00172)は M2 修復効果だった
    └─ 区間ラップ以降 ─ 着手せず（実証的な橋が無い）

核心的知見: 正規化/残差化/直交化/ペース補正を足すほど悪化した。**本番モデルが十分強いなら、特徴側で意味付けし過ぎるより
  生の情報を渡してモデルに条件付き関係を学ばせる方が良い**（GBDT系で既知の現象を、丁寧な帰属分離で確認した価値）。

→ 研究ノート クローズ。新Issue「Integrate raw historical MySpeed into production pipeline」を工学タスクとして起票。
  以降の評価項目に **ROIは含めない**（回収率エッジは終了済／目的は確率品質）。

### 2026-07-31(続8) 券種別「買い方(戦略)」ROIバックテスト＝物理シム着順標本の同時頻度で決済
目的: 券種の選び方・買い方で変わる回収率を、物理シムのモンテカルロ着順標本から評価する枠組みを実装。
核心の差異化: 連系(馬連〜三連単)の確率を**周辺勝率/複勝率の積(Harville)ではなく着順標本 top3_orders の同時頻度**で
  直接推定する。物理シムが内生する着位間の相関(先行総崩れ→差し1-2-3・展開一括逃げ残り)を確率へそのまま織り込める。
  Harville は「各馬の強さ→周辺確率」から連を組むため、この従属を構造的に表せない。ここが本モジュール固有の価値。

再発明の回避(既存資産をフル再利用・ユーザ指摘「8券種の支払情報もダウンロード済み」に従う):
  ・着順標本 = `monte_carlo(return_orders=True)["top3_orders"]`（既存物理シム）
  ・決済 = `BettingTickets.settle_one`（8券種・厳密照合・BOX非展開／既存）
  ・集計/ファットテール指標 = `_backtest.BetTypeStats`（roi_ex_top=除最大1件・reliable・top_share／既存）
  ・払戻データ = `raw_jrdb_hjc`（全8券種・100%・既にDB取込済）＋`normalize_combo`（既存パーサ）

新規は2つの橋渡しのみ:
  ① `src/simulation/_jrdb_return_source.py` に `JrdbHjcReturnSource`＋`build_hjc_table`/`_combo_to_ints` 追加。
     HJC(1レース1レコード・8券種の(当選組合せ,払戻)) → ReturnProcessor互換の win_i/return_i 形へ。
     従来 `JrdbReturnSource` は単複のみで連系が空→券種別ROIが測れなかった穴を塞ぐ(全8券種100%供給)。
  ② `src/simulation/_ticket_backtest.py`(新規): `aggregate_ticket_probabilities`(着順標本→券種確率・同時頻度),
     買い目生成器(trifecta_top2_reverse 等), 戦略テンプレ S1〜S6＋単勝/複勝, `build_candidates`(→BetCandidate),
     `settle_per_race`/`roi_by_year`/`race_bootstrap_ci`(既存に無い年別・レース単位CI＝三連単ファットテール頑健評価)。
  ・`scripts/sim_ticket_strategy_roi.py`(新規): sim1回/レースを全戦略で共有→券種別ROI表(除最大/CI/年別)＋前進検証(過去年で戦略選択→翌年評価)。

リーク規律: 券種確率・買い目は as-of sim 由来のみ。購入時オッズを持つのは単複だけ→EV選別は単複限定・連系は固定購入、
  決済は確定払戻(後決済で購入判断に使わない)。rank_bonus(単一スナップ全期間=leak)使用時のROIは過去探索用でliveに非transfer。
判定規律: 三連単の素ROIは万馬券1本で激変→採否は「除最大ROI・レース単位bootstrap CI下限>1・年別+の多さ」で。単一年の高ROIは不採用。
テスト: tests/simulation/test_ticket_backtest.py(7)＝同時頻度≠周辺積の回帰ガード含む。既存 backtest/betting_tickets/hjc parser 全緑。
次アクション(ユーザ実行): `python scripts/sim_ticket_strategy_roi.py --db <DB> --walk-forward --n-sim 800`（要 raw_jrdb_hjc 取込済DB）。
  効率的市場の壁の再確認になる公算が高いが、「買い方」の自由度で素ROIがどう動くか＋CI下限で黒字が統計的に言えるかを測る初の枠組み。

### 2026-07-31(続9) 騎手＋厩舎ランク＝① live専用プライアに確定（保守的固定・過去へは当てない）
方針変更: ③（単一スナップ全期間・リーク承知で過去 ROI を sweep）→ **①（live 予測のみ・保守的固定スケール）** にユーザ確定。
核心の論理: 単一「現時点スナップ」ランクは **これから走るレース** には as-of 情報＝リークなしの妥当なプライア。
  リークは同スナップを **過去レース** に当てて評価/較正するときだけ生じる。ゆえに live 適用はクリーン・backtest 適用は禁止。
実装:
  ・`_rank_bonus.py`: `RANK_GAIN_LIVE=0.05`(保守的固定・過去データで調整しない。rank_bonus≈±1.4σ→ability±0.07=能力spread≈0.2の1/3)。
    `build_live_field(race_df, rank_gain=RANK_GAIN_LIVE 既定)`＝① 唯一の live 適用経路。
    `assert_live_only(rank_gain, context)`＝backtest で rank_gain!=0 を大警告（方針違反=リーク検知）。
  ・`sim_walk_forward.py` / `scripts/sim_ticket_strategy_roi.py`: 起動時に assert_live_only で --rank-gain!=0 を警告（既定0）。
  ・`scripts/sim_rank_gain_sweep.py`: 見出しを[リサーチ専用]に。過去 ROI を動かすか診るリーク承知の診断で本番機構でないと明記。
  ・module docstring を ③→① へ全面改訂（build_rank_z/attach_rank_bonus 自体は不変＝スナップ z 生成器）。
判断根拠: 過去データへの gain 最適化はリーク値で live に transfer しない＝自己欺瞞。よって過去最適化を放棄し保守的固定値に据える。
テスト: test_rank_bonus.py に build_live_field(固定gain一致・保守レンジ)＋assert_live_only(0で遵守/非0で違反) 追加（計8緑）。

### 2026-07-31(続10) 券種ROIバックテストに 市場1番人気対照＋TOTAL(仮想ポートフォリオ) を追加
ユーザ指示で厳密化。全期間8000レース実測（n_sim800・rank_gain0）= 全戦略赤字・合算TOTAL ROI51.9%(除最大42.6%)・年+0/2・
全戦略CI下限<1 ＝市場効率の壁の再確認（単勝1位78.5%≒JRA単勝控除≒シムは市場に純増分なしの疑い）。これを決定化する2機能を実装:
- [市場対照] Market-W/Sim-W/Market-P/Sim-P の4本。**市場1番人気は購入時点(前売りOZ)の単勝オッズで決定**（確定オッズは精算のみ＝
  リーク回避を厳守。OZ無指定時は「測定不能」を明示し確定での代用はしない）。一致率(Sim1位=市場1番人気,≥90%で再表現)・
  的中率差・平均オッズ差・**レース単位 paired bootstrap の ΔROI 95%CI**(同一レースで差を再標本化＝検出力↑)を出力。
- [ALL TOTAL] 「全8戦略を同時に全購入した仮想ポートフォリオ」=投資額加重ROI=Σreturn/Σstake。除最大1/除上位5払戻・
  購入レース数・総点数・1レース平均投資・最大DD(レース単位資金曲線)・年別TOTAL ROIを併記。S0_skipも対照として表示。
- [券種グループ別TOTAL] 単複/馬連馬単ワイド/三連複/三連単に分割（三連系の大量投資が全券種合算を支配するのを切り分け）。
- 解釈修正(ユーザ指摘): 控除率は投資額割合で「点数増自体が1円あたり控除率を上げるのではない」。ROI低下の主因は複合
  （2・3着順位付けが弱い/組合せ確率未校正/低EV券の追加/三連系の高分散/高控除/最大払戻依存）。出力注記に反映。
実装: _ticket_backtest.py に portfolio_metrics/settle_tickets_detailed/market_favorite/paired_delta_roi_ci/BET_GROUP・
  _max_drawdown 追加。scripts/sim_ticket_strategy_roi.py に --oz-dir・_market_control/_print_total/_load_oz_win_odds 追加。
テスト: test_ticket_backtest.py 計9緑（portfolio TOTAL/除最大/群別・market_favorite・paired ΔROI CI）。既存backtest系70緑。
次: ユーザが `--oz-dir <OZ前売り.txtフォルダ>` 付きで再実行→市場対照ΔROI CIとTOTALを確認（OZが無ければ市場対照は測定不能表示）。

### 2026-07-31(続11) 戦略セット拡張＝rank vs joint 対照・馬単追加・S4整合性・p1校正（ユーザ精査反映）
ユーザ精査で5点の調整を実装。核心は「現行は周辺勝率順位しか使っていない→MCの同時確率を直接使う joint 版を同点数で対照」。
1. S4点数不足の真因を確定: **重複馬番ではなく小頭数**（third_slots(2..5)が頭数超）。5頭レースはS4=6点/S5=12点で
   S5に不足が出ない事実と整合（34不足=17×5頭レース×2）。`validate_ranking`(重複検出assert・sim毎に実行)＋
   `s4_point_audit`＋harnessの[データ整合性]内訳出力で実データ確認可能に。当方の当初「候補重複除外」説明は誤りと訂正。
2. 戦略を (rank, probs) シグネチャへ一般化。追加:
   S3b_ワイド1軸→2-5(4点,1位の複勝圏信頼) / S7_馬単1↔2(2点) / S8_馬単1軸→2-4(3点) /
   S9_三連単1着固定_p1≥0.50(条件付き12点/見送り) / J1_三連複joint4 / J2_三連単joint8 / J3_馬単joint2。
   S2/S3改名(上位3頭ペア_馬連/ワイド＝買い目同一のvenue対照を明示)。枠連は後回し(集約処理・多重比較増で優先度低)。
3. RANK_JOINT_PAIRS[(S1,J1)(S4,J2)(S7,J3)]＝同点数の rank↔joint を paired ΔROI CI で直接比較→harnessの[rank↔joint]。
   「MCの順位依存構造を使う価値」を測る中核。ΔROI(joint−rank) CI が有意に正なら2・3着の同時構造がROIに効く。
4. [校正] Sim1位のp1帯別実勝率を出力＝S9の0.50閾値の妥当性確認（未校正なら過信で『強い軸』判定にならない）。
5. 固定買い(Sim順位そのものの券種別価値の基準)は維持。EV版は購入時オッズある券種のみ後で分離（確定払戻は購入判断に使わない）。
実装: _ticket_backtest に validate_ranking/s4_point_audit/exacta_top2_reverse/exacta_single_winner/wide_axis_flow/
   joint_topk/RANK_JOINT_PAIRS・全_s*を(rank,probs)化。scripts に _print_s4_audit/_print_calibration/_print_rank_joint。
テスト: test_ticket_backtest.py 14緑（S4小頭数/重複検出/馬単・ワイド軸/joint_topk/S9条件）。simulation全218緑(matplotlibのみenv欠)。
次: ユーザ実データ再実行で ①S4内訳=小頭数か ②p1校正が0.5近傍か ③S4↔J2等のΔROI CI符号 を確認。

### 2026-07-31(続12) 評価系の立て直し＝TOTAL金額単位バグ修正＋確率校正(temperature scaling)
ユーザ精査で実測が明確化: 固定買い全滅・joint≦rank(CI 0跨ぎ)・**p1が大幅過信(予測≥0.5→実勝率18.6%)**。
「戦略追加を止め評価系を先に立て直す」方針に合意。本コミットで2点:
1. TOTAL金額単位バグ修正: 決済 amount=1(1点=1単位=100円)なので stake/return は100円単位。旧表示は単位数を円と
   誤表記(投資=250,064/1レース67円)。→×PAYOUT_UNIT_YEN で円表示(投資25,006,400円/総点数250,064点/1レース平均点数・円)。
   ラベル「全8戦略」も実際は買い目を出した戦略数を動的表示(S0除く実15)に修正。ROIは比率で不変だが資金管理評価が正しくなる。
2. 確率校正モジュール `src/simulation/_prob_calibration.py`(新規): レース単位 temperature scaling
   p~_i=p_i^(1/T)/Σp_j^(1/T)。fit_temperature(勝者NLL最小・黄金分割・T∈[0.3,12])/apply_temperature/nll/
   reliability_top/ece_top。harnessの[校正]を walk-forward化(過去年でT fit→翌年固定→校正前後のNLL/ECE/帯別実勝率)。
   合成過信データで ECE0.492→0.056・予測0.33≈実0.27 と確認。確率ベース戦略(S9/EV/joint閾値)は校正後にのみ有効と明示。
研究結論(合意): 「Sim周辺順位は単勝で市場控除相当の予測力の可能性はあるが、絶対確率は著しく過信、2・3着依存にも収益価値なし。
   固定買いで券種を複雑化すると低品質組合せへの投資増でROI大幅低下」。次に戦略追加せず、市場対照(OZ/TYB)と校正を先に決着。
テスト: test_prob_calibration.py 4緑・test_ticket_backtest 14緑。
次アクション: ①ユーザがOZ/TYB前売りを --oz-dir で渡し市場1番人気 vs Sim1位の paired ΔROI CI を確定
   ②校正はharnessで自動walk-forward表示(データ渡すだけ) ③校正後も市場に増分がある場合のみ確率戦略を再評価。
   購入時点の連系オッズが無い限り固定買い探索は診断で終了。

### 2026-07-31(続13) 実データ確定＝固定買い/券種変換はクローズ・残る価値は市場対照カバレッジ増だけ
実データ(3,765レース)で決着。ユーザ合意の限定命題:「物理シムの周辺勝率順位を固定的に券種へ変換しても
安定した控除超過は得られない。sim確率は大幅過信、joint(順位依存)にも周辺順位超の収益価値なし」。ただし
「Simは市場を超えない」は市場比較148件では未確定→唯一価値ある作業=市場対照カバレッジ増(148→1000-3000+)。
決定的数値:
 ・S4整合性クリーン: 8点未満44件=全て小頭数(2点=3頭21/4点=4頭6/6点=5頭17)。重複ゼロ。
 ・校正: 生ECE0.287(予測0.87→実0.167)。walk-forward T=12(上限張付=もっと過信)。校正後ECE0.021だがp1≥0.5消失。
 ・市場対照(N=148): **一致率わずか10.1%**・Sim1位平均オッズ23.09倍(ロングショット誤選択)・単勝的中Sim11.5% vs市場29.1%。
   単勝ΔROI+21.3%だがCI[-48%,+132%]=未判定。→Simは市場の再表現ですらなく人気薄を誤選択。
 ・単勝1位的中9.9%(全3738)≒ランダム(14頭で7%)。シムは勝ち馬をほぼ順位付けできていない(全データで確定)。
 ・rank↔joint: 全ペアCI0跨ぎ(S4/J2 joint-27.7%)。2・3着同時構造にROI価値なし。
 ・ALL TOTAL(単位バグ修正後): 投資2500万/払戻1248万/損益-1253万/ROI49.9%/最大DDほぼ投資額(終始出血)。診断値であり採用指標にしない。
凍結/廃止(合意): S9(p1≥0.5)・固定買い追加・joint追加・新券種・ALL TOTAL最適化・現段階のn_sim大幅増。
本コミットの実装(新戦略は追加せず市場対照分析のみ):
 ・fit_temperature 探索上限12→30(T=12境界張付の確認用)。
 ・[代表性監査] _print_coverage_audit: 市場比較可能レースが全評価を代表するか(年/場/頭数/Sim1位的中の分布比較)。
 ・[市場×選択差] _print_market_subgroups: Sim1位の市場人気順位でA(=1番人気)/B(≠1番人気)/C(4番人気以下)に分け
   単勝複勝ROI/的中/平均O/除最大/年別を出す(一致率10%の中身を見る)。
次: カバレッジ増が本質(raw_odds_snapshotsは148/3765=3.9%＝going-forward収集で薄い)。JRDB OZ前売り(--oz-dir)が
   歴史的に広いカバレッジを持つ可能性→OZファイルがあれば数千レースで市場対照を決着できる。無ければ将来収集分でのみ。

### 2026-07-31(続14) 市場対照カバレッジ問題を解消＝TYB直前単勝(≈T-15)を市場源に
ユーザ提示: JRDB TYB(直前情報・年度パック1999-2025)取込済。TYB.tansho_odds(73,6・ZZZ9.9・直前単勝)は
「発走15分前頃更新」＝購入時点(T-15)・リーク無で全レースをカバー。raw_odds_snapshots(going-forward・3.9%)の
カバレッジ問題を解消し、市場対照を148→全3,765レースで決着できる。決済は HJC 確定払戻(後決済)のまま。
実装: scripts に _load_tyb_win_odds(raw_jrdb_tyb→{race_id:{馬番:直前単勝}})＋ --tyb フラグ。
市場源の優先順位 --tyb > --oz-dir > --odds-db。in-memory sqlite で reader 検証済(0除外・favorite=min)。
次: ユーザが `--tyb` 付きで再実行→[市場対照](全レース)・[代表性監査](カバレッジ↑)・[市場×選択差A/B/C]を
   十分件数で確認。単勝ΔROI(Sim−Market)のCIが0跨ぎか・B/Cの人気薄選定が偶然かを決着。

### 2026-07-31(続15) 確率源を本番LightGBMに差替え可能に（--prob-source lgbm）＝市場対照の本命化
ユーザ「差し当たりの確率計算はMCでなくLightGBM結果の解釈でOK」。MC sim は勝ち馬順位付けがほぼランダム
(単勝1位的中9.9%)で市場対照が無意味だったため、本番LightGBM較正勝率を確率源に差替える経路を追加。
これで市場対照が「本番モデルは市場を超えるか（＝効率的市場の壁の直接検証）」の本命になる。
実装(既存資産を再利用・新規予測ロジックなし):
 ・_lgbm_probs(model, featured, order): ExpectedValueScorePolicy.calc(model,X)(特徴量整合込みの
   per-horse較正勝率PROB)→ top_by_race(予測1位)＋calib(勝率ベクトル・勝者index)。app._data_loader.
   load_latest_model / load_model_by_version でモデルロード。
 ・--prob-source {mc,lgbm}＋--model-version。lgbm は [校正][市場対照][代表性監査][A/B/C] のみ実行
   （MC専用の券種戦略グリッド/TOTAL/rank↔joint は joint 確率が要るのでスキップ）。
 ・print 関数群に pick_label を配線（Sim1位→LGBM1位 等ラベル差替え）。
期待: LightGBMは勝ち馬順位付けが強い(AUC~0.80)ので一致率は市場と高く、単勝ΔROI CIは0跨ぎ＝
   「本番モデルは市場の再表現・控除超えなし」を全カバレッジ(TYB)で決着する見込み。逆にB/CでΔROI有意正なら
   walk-forward＋最低的中件数で要再検証。tests 18緑(既存)。
次: ユーザ実行 `python scripts/sim_ticket_strategy_roi.py --db data/keibam.db --tyb --prob-source lgbm`。

### 2026-07-31(続16) LightGBM確率源の silent feature mismatch を検出・是正（strict契約＋正常性スモーク）
前回の --prob-source lgbm 出力は無効: booster特徴名が汎用(Column_N・numpy fitのため)で、実列名 featured を
名前reindexすると全588列0補完→全ゼロ入力→予測ランダム(LGBM1位的中7.8%≒1/頭数)。ROI/A-B-C/校正は本番モデル
評価として採用不可。ユーザ指摘(=以前のJRDB二重マージと同種のsilent mismatch)に完全同意し是正:
 ・_training_feature_order(model,eff): booster汎用名でなく KeibaAI feature_names_/feature_contract_ の
   実列名から、モデル n_features_in_ に一致する候補を選ぶ（605契約/596契約-meta ではなく 588 実特徴量）。
 ・[契約診断] model_class/n_features_in_/学習列数/共通/不足/余分/先頭列 を出力。
 ・0補完を禁止＝不足列があれば ValueError で停止（市場検証で逃げ道なし）。学習列に厳密reindex(fillなし)。
 ・[正常性スモーク] Place/Win AUC を算出。保存時 Place≈0.807-0.809 と一致しなければ特徴破損→AUC<0.65で中止。
   これを通るまで市場対照/ROIを読まない（ユーザ規律）。合成 numpy-fit モデルで strict raise・AUC算出・
   実列名整合(prob非定数)を確認。
 ・校正表示の矛盾を修正: p1≥0.50 行が勝者index合計(→実勝率5.000の不整合)だったのを reliability_top と
   同一定義(トップ馬の1着一致0/1)に。temperature採用ゲート追加(ECE有意改善かつT非境界のときだけ採用、
   さもなくば非採用＝生確率評価)。前回の T=30境界・ECE悪化は非採用が正しい。
次: ユーザ再実行で [正常性スモーク] Place AUC を確認。≈0.807なら特徴整合OK→市場対照が本番モデルの
   決定的検証に。その後の精緻化(共通母集団=JRA×TYB×HJC固定・市場人気帯 1/2-3/4-6/7+・的中数併記)は
   AUC正常確認後に実施。tests 23緑。

### 2026-08-01 本番LightGBMで初の正常な市場対照＝「わずかに市場超えの方向・ただし控除内・統計的null」
featured↔model 版ずれを再学習(20260801・605特徴量・契約あり)で解消。契約診断=不足0・Place AUC 0.826(保存0.807近傍)・
LGBM1位的中32.1%・市場1番人気一致率78.3%＝正常な本番モデル評価として採用可。実測(全3,738 JRA×TYB):
 ・単勝 Market 74.0% vs LGBM 77.3%(ΔROI+3.2% CI[-0.4,+6.9]=境界だが0跨ぎ=null)/複勝 83.9 vs 84.8(Δ+0.9 CI[-1.2,+2.8])
 ・的中率 単30.9→32.1 複64.2→65.3(全て小幅にLGBM優位)。絶対ROIは控除未満。
 ・校正: 生ECE0.081・**過小信頼(実勝率>予測)**・T=0.73<1(確率差を強める)でOOS改善(NLL1.99→1.96/ECE0.084→0.029)。
 ・A/B/C: B(市場と違う選択812)単勝83.2%・C(市場4番人気以下43)102.8%だがN小・最大払戻依存・年別不安定=採用不可。
結論(合意): 「本番LightGBMは市場コンセンサスを主に再現しつつ市場と異なる選択で小幅な予測改善を示すが、
  統計的に確定せず控除超えエッジではない」。「市場の写経」より前進だが「市場超え」とは言えない。
ユーザ精査反映の実装(script):
 ・校正表示を方向判定に(過信/過小信頼を実測 実−予 で判定・T<1=強める/T>1=平坦化を明示)＋(T,adopted)を返す。
 ・[市場×選択差]を **同一群レースでの paired ΔROI(LGBM−市場1番人気)＋95%CI＋的中数** に(市場全体74%と直接比較しない)。
 ・[高信頼単複] 校正後 p1≥0.50 レースで まず単勝/複勝を市場と paired 比較(三連単1着固定へ広げる前の関門)。
 ・共通母集団を JRA×TYB×HJC×モデル予測可 に固定し 校正/市場対照/A・B・C/高信頼 を全てこの集合で評価。
 ・代表性監査を「別母集団(全評価は地方/少頭数含む)なので適格集合へ揃える」に是正。
 ・_lgbm_probs が prob_vec_by_race を返す・calib に race_id 付与(母集団restrict用)。
次(ユーザ): 再実行(retrain不要)で B/C の paired ΔROI CI・高信頼単複ROIを確認。新バンド追加は不可(多重検定)。
  C群を追うなら 2024以前で条件定義→2025固定→2026評価・最低的中数事前固定 の walk-forward が必須。

### 2026-08-01(続) 研究テーマ転換＝「市場とモデルが食い違う~22%」へ。B群が唯一の生存シグナル
今回の本番LightGBM市場対照が本調査で最も価値ある結果（市場×モデルの関係を初めて定量分解）。合意:
 ・全体: 単勝Δ+3.2%(CI[-0.4,+6.9] null)・複勝Δ+0.9%(null)＝符号+だが「市場超え」未証明。
 ・**B群(LGBM≠市場1番人気・812レース)**: 同一レース paired Δ単勝 **+14.9%(CI[-1%,+31%])** ＝あと一歩。除最大82.5%・年別82/84%安定。N大きく累積で有意化の余地＝**唯一の生存シグナル候補**。
 ・C群(市場4番人気以下・43): paired +94%(CI[+35,+157]*)だがN=43・10的中・年別125/78%不安定＝運用不可・ケーススタディ対象。
 ・高信頼 p1≥0.50(91): 単勝81.9%=市場81.9% 完全一致＝一致帯では市場を超えない→「市場と違う所」だけ研究価値。
 ・校正成功: ECE0.080→0.029・T=0.73(過小信頼＝もっと自信持ってよい・将来Kelly/EVで重要)。
方針転換(ユーザ確定): 新規特徴量探索は終了。新主テーマ=「市場とモデルが対立したときどちらを信じるか」。
実装(P1/P2/P4・新特徴量なし・採否用バンド追加なし):
 ・P1 --dump-disagreement CSV: 予測≠市場1番人気レースを race_id/両本命/オッズ差/確率差/払戻 で保存(累積5000件目標)。
 ・P2 _print_disagreement_patterns: 不一致時に市場が何番人気か(2/3/4-6/7+)の記述的分布＝『僅差入替』か『大穴指名』か。
 ・P4 kelly_log_growth/paired_log_growth_ci: 固定比率f=.05のlog成長率でLGBM vs市場をpaired比較(ROI僅差でもlogで差が出うる)。市場対照に単勝Δlog成長CIを追加。
 ・市場×選択差を同一群paired ΔROI+CI+的中数、校正を方向判定(過小信頼)、共通母集団固定は前コミットで実施済。
次(ユーザ): --dump-disagreement で B群CSVを毎回蓄積→将来5000件でB群 paired CI再検定。C群43件は人手ケーススタディ
  (ダート/新馬/騎手/脚質/休養明け)。閾値・バンドは事前固定しwalk-forward(2024定義→2025固定→2026評価)以外で動かさない。

### 2026-08-01(続2) 【最重要】市場対照は in-sample＝未採用。完全OOS walk-forward の基盤を実装
ユーザ最重要指摘: model 20260801 の data_period=('2015-01-04','2026-07-26)＝2025-2026を学習に含む→現B群結果は
in-sample・採用不可(Place AUC0.826>保存0.807も in-sample楽観と整合)。完全OOS(評価年より前だけで学習＋温度校正)が必須。
合意した記録の分離: 「LGBMは市場1番人気より良い(相対)」≠「LGBMを買えば儲かる(絶対)」。B群も絶対ROI83.2%=控除未満。
不一致パターン確定: B群のズレは75%が市場2番人気・20%が3番人気・大穴5%＝「大穴探索でなく市場上位の僅差順位改善(false-favorite検出)」。
Kelly: LGBM絶対log成長 -0.0133・市場-0.0149 とも負→f*=0が妥当(差が有意でも絶対負なら運用外)。
実装(OOS基盤・条件追加なし):
 ・retrain --until-year YYYY: 指定年以前(<=)だけで学習＝完全OOSモデル生成(wf_2025=<=2025で2026評価用 等)。
 ・[市場×選択差]に年別 paired Δ単勝CI(同一レース比較のOOS複製確認・年で同符号か)。in-sampleモデルでは無効と明記。
 ・Kelly: 単一群 kelly_log_growth_ci(絶対log成長 vs 0)＋LGBM−市場Δを別表示(絶対負なら運用外)。
 ・高信頼単複を OOS(校正の評価年=共通最新年)に限定(T=0.73は2025fit→2026の65件だけがOOS)。
 ・--dump-disagreement を累積安全化: model_version/schema_hash/生成時刻/calibration_T 列＋既存CSVへ追記＋
   race_id×model_versionで重複排除(同一過去レース再生成では増えない)。
採用条件(B群): 年で同符号＋paired ΔROI CI下限>0＋LGBM絶対ROI CI下限>1＋除最大後ROI>1＋log成長>0。
  最初の2つ=市場順位改善シグナル / 全部=運用可能エッジ。C群(N43)=ケーススタディ限定・購入条件化しない。
次(ユーザ・完全OOS walk-forward): 
  1) python -m src.pipeline.run_pipeline retrain --until-year 2024 --version-name wf_2024  (2025評価用)
  2) python -m src.pipeline.run_pipeline retrain --until-year 2025 --version-name wf_2025  (2026評価用)
  3) sim_ticket_strategy_roi ... --prob-source lgbm --model-version wf_2025 --max-year 2026 → 2026行=OOS
  で B群 年別paired ΔROI CI を再確認。条件(閾値/バンド)は一切追加しない。
tests 19緑。

### 2026-08-01(続3) wf_2025がstacking fallback＝契約未保存で評価停止→学習の全経路で契約保存に修正
ユーザ指摘の根本原因を特定: wf_2025 は use_stacking:False/stacking_fallback:True（--until-yearで データ減→
stacking split退化→非stacking LightGBMへfallback）。feature_names_/feature_contract_ は train_with_stacking
だけが設定していたため、fallbackモデルは契約未保存→評価が0補完せず正しく拒否(strict成功)。20260801が動いたのは
stacking(契約あり)だったため。修正(upstream):
 ・_keiba_ai: _set_feature_contract(X) を新設し train_with_tuning/train_without_tuning(=非stacking fallback)
   でも呼ぶ。train_with_stacking も同helper経由に統一。全経路で feature_names_/feature_contract_ を保存。
 ・併せて training_data_period_(学習日付min,max) を保存＝OOSの時間重なりを機械assert可能に。
 ・harness: --eval-year YYYY(評価を厳密に単一年へ＝完全OOS。<=max-yearだと前年混入)。
   [OOS assert] 学習終端>=評価年開始 なら in-sample として中止。training_data_period_ 無い旧モデルは警告。
 ・_select_usable_lgbm_model: 明示版名(wf_2025.pickle等・_keibam接尾辞なし)を models/** glob で発見(Place本体優先・__win/__category除外)。
合意した設計(ユーザ): 相対(市場より良い)≠絶対(儲かる)。B群はrank定義なので温度校正不要。高信頼p1やEV/Kellyのみ
  nested-OOS温度(wf_2024で2025からT推定→wf_2025で2026適用)が必要。C群は購入条件化せずケーススタディ。
次(ユーザ・要 wf再学習で契約付与): 
  retrain --until-year 2024 --version-name wf_2024 ; retrain --until-year 2025 --version-name wf_2025
  → sim_ticket_strategy_roi ... --model-version wf_2025 --eval-year 2026 で完全OOSのB群 paired Δ単勝CIを確認。
tests: training 270緑・ticket 15緑。

### 2026-08-01(続4) 【決着】完全OOSでB群は複製せず＝市場を超える収益エッジは無い（研究クローズ）
初の完全OOS walk-forward（wf_2024→2025・wf_2025→2026、両方 ✓学習終端<評価年 を機械assert通過）。決定的:
 ・2025 OOS: 全体単勝ΔROI −1.4%[−5.8,+3.2] / B群Δ単勝 **−6.6%**[−30,+16] / Kelly絶対 −0.0145(負)
 ・2026 OOS: 全体単勝ΔROI +3.3%[−0.8,+7.8] / B群Δ単勝 **+16.4%**[−6,+38] / Kelly絶対 −0.0135(負)
 ・**B群Δ単勝の符号が年で反転**（−6.6% / +16.4%）＝事前登録条件「年で同符号かつCI下限>0」両方不成立→B群は本物でない。
 ・全体ΔROIも符号反転(−1.4/+3.3)、一致率は両年79.5%で安定＝市場の再表現。Kelly絶対log成長 両年負＝f*=0賭け対象なし。
 ・in-sample(20260801)で見えたB群+14.9%は評価年を学習に含んだ楽観。C群(N18/30)も年不安定で複製せず。
 ・不一致パターンは両年「市場2番人気への僅差入替」(78/72%)で一貫だが、その入替はOOSで儲けにつながらない。
正式結論: **本番LightGBMは市場と~80%一致し、不一致B群でも完全OOSでは市場本命を安定して上回らない（符号が年で反転）。
  市場を超える収益エッジは完全OOSでは確認されない。** 効率的市場の壁は「食い違う22%の部分集合」でも成立。
  → 「市場とモデルの不一致」研究テーマも、規律的OOS検証でエッジ無しと決着。研究フェーズ完全クローズ。
残す価値: data/disagreement.csv(累積874件・model_version/schema/日付付)は将来の新年度foldでB群を再検定する種。
  ただし2 OOS foldが既に符号反転＝事前確率は強くエッジ否定。運用は f*=0（賭けない）が合理。
方法論の成果(本セッションの核心的価値): TYB購入時点オッズ×HJC確定払戻の時点分離・共通母集団固定・
  同一レースpaired ΔROI CI・完全OOS(学習終端<評価年assert)・strict特徴量契約(silent mismatch再発防止)・
  過小信頼の方向判定・Kelly絶対vs差の分離。評価基盤が繰り返し in-sample/leak/列不整合を機械的に捕捉した。

### 2026-08-01(続5) 研究テーマ確定＝「市場×モデル不一致の説明」。①記述分析＋③メタ判断の種を実装
ユーザ最終評価: プロジェクトは「市場超え特徴量探し」→「市場とモデルの不一致を説明する研究」へ自然進化。
終了(合意): 新特徴量探索/外厩/MySpeed/TARGET追加/券種最適化/MC順位改善/B群だけROI探索。
新テーマ: 不一致の発生機構の説明・分類(ROIでなく説明可能性)。①B群特徴量解析(最優先)②SHAP③disagreement.csvを
  「市場が勝った/モデルが勝った」二値のメタ意思決定モデル化④市場差の時系列。
実装: scripts/analyze_disagreement.py（disagreement.csv 自己完結・任意で featured join）:
  ①誰が勝ったか年別／市場人気順位・オッズ差・確率差・モデル本命オッズ帯 別の モデル勝率/市場勝率/単ROI。
  ③メタ判断の種: モデルor市場の本命が勝ったレースで『モデルの勝ち(lgbm_hit)』を各特徴が予測するか＝
    年別 単変量AUC(sklearn非依存 _auc)。両年でAUC>0.55なら『信じる条件』候補・全て≒0.5なら判別不能。
  --featured 指定時 芝ダ/距離/クラス/頭数 別(列auto-detect)。記述統計であり買い目化は多重探索と明記。
合成CSVで動作確認(AUC≒0.5=無情報が正しく出る)。②SHAPは model+shap 要で次段。
次(ユーザ): disagreement.csv を年度で貯め、analyze_disagreement で ①③ を実データ確認。
  『信じる条件』候補が両年AUC>0.55で出たら、事前登録して完全OOS(wf_2024→2025/wf_2025→2026)で検証。

### 2026-08-01(続6) 不一致の説明を効果量二群比較で強化＝勝率差を主指標に・メタ識別の可否を別角度で確認
実データ(disagreement.csv 874件・2 OOS fold)の所見（ユーザ合意）:
 ・**モデル勝率>市場勝率が両年で安定**(2025 23.7>22.4 / 2026 25.4>17.1)＝順位付けは不一致集合で僅かに優勢。
   だが ROI は逆転(2025 モ76<市83 / 2026 モ79>市63)＝ROIはオッズ由来の分散大→**主指標を勝率差へ**。
 ・「どちらでもない」54-58%＝市場もモデルも外す共通難レースが過半（将来の難易度推定テーマ）。
 ・不一致の75%は市場2番人気との僅差入替（発生機構は僅差順位交換）。
 ・③メタAUC 全特徴 両年≒0.5＝『モデルを信じるべき不一致』は現情報で判別不能（綺麗な負の結論）。
実装(analyze_disagreement.py 強化):
 ・主指標を勝率差と明示。--featured で race-context 数値列(頭数/距離/コース/クラス)を join し breakdown・効果量・メタに供給。
 ・_effect_sizes: 片方の本命が勝ったレースで A=モデル勝ち/B=市場勝ち を Cohen's d ＋ Cliff's δ(=2·AUC−1・sklearn非依存)
   で二群比較。|δ|降順で『最も分ける特徴』。全て小→AUC≒0.5を別角度で裏付け／中以上→事前登録し完全OOS検証すべき仮説。
 ・_breakdown を連続値は分位6区分に自動ビン化(空表バグ修正)。
方針(ユーザ): テーマは「市場コンセンサスとMLモデルの意思決定差の説明」(株/スポーツ/需要予測に汎用)。
  次: ②SHAP(B群でモデルが市場と割れた根拠・説明用)／説明モデルの目的変数=市場順位(2/3/4+)の入替種別。
  規律継続: リーク防止・完全OOS・多重探索回避。効果量が大の特徴が出たら事前登録→OOS。
合成で動作確認(全δ無視=分離不能を正しく表示)。

### 2026-08-01(続7) 【三重確定】不一致は現特徴で分離不能＝メタ意思決定モデルもNo（edge探索クローズ）
効果量二群比較(実データ・片方本命が勝った388レース)で、市場×モデル不一致の分離不能を第3の角度から確定:
 ・全特徴 |Cliff δ|<0.33。最大 breeder_py_芝勝率 δ+0.192/d+0.339=「小」のみ。他 race-context 37列含め全て無視域。
 ・breeder最大は約42特徴のmax-of-many＝多重比較のノイズ(A/B平均0.08/0.07で実質差なし)。事前登録に値せず。
三重確定: ①paired ΔROI 完全OOSで符号反転(−6.6/+16.4・両CI0跨ぎ) ②メタAUC両年≒0.5 ③効果量全て小。
→「モデルが市場に勝つ場面を現情報で事前識別できない」＝メタ意思決定モデルもNo。効率的市場の壁は不一致22%部分集合でも成立。
研究結論(確定): 公開/半公開データ＋本番LightGBM＋物理MC＋TYB購入時点オッズ×HJC確定払戻の完全OOSで、
  市場を一貫して超える収益エッジも『信じるべき不一致』の事前識別も確認されない。edge探索は完全クローズ。
生き残った知見(edgeでない価値): (a)不一致でモデル勝率>市場勝率が両年安定=false-favorite弱検出(条件化不可)
  (b)共通難レース54-58%=難易度推定という別テーマの芽。
残タスク(任意・説明/理解用): ②SHAP(B群でモデルが市場と割れた根拠の説明・edgeでなく理解)。
  説明モデル(目的=市場順位2/3/4+の入替種別)。難易度推定。いずれも収益目的でなく understanding。
方法論の成果(本プロジェクトの核心価値・再掲): TYB×HJC時点分離・共通母集団固定・同一レースpaired CI・
  完全OOS(学習終端<評価年assert)・全学習経路で特徴量契約保存・過小信頼の方向判定・Kelly絶対vs差分離・
  効果量(Cohen d/Cliff δ)での記述。評価基盤が in-sample/leak/列不整合/多重探索を繰り返し機械的に捕捉した。

### 2026-08-01(続8) 優先度リスト実装＝二頭差分join監査・race_class是正・勝率差Wilson常設・SHAP二頭差分・難易度推定
ユーザ最終synthesis(勝率差を主指標にモデルは不一致で僅かに順位改善／メタ識別は不能／未収益化)を受け、
提示された優先度1-5を実装(featured/model実データは gitignore の巨大成果物でこのコンテナに無く、実行はローカル。
純関数は合成データで単体テスト済み・9件green):
 1. featured結合を馬単位の二頭差分に修正(analyze_disagreement.py):
    旧 groupby(level=0).first() は無関係な先頭馬の値を付けていた(バグ)。(race_id,LGBM馬番)/(race_id,市場馬番)の
    二系統で本命を引き当て、馬固有特徴の lgbm−market を差分列 d_* に。レース内一定(距離/クラス/頭数)は差分0で
    文脈列(実値)へ自動振り分け→効果量は差分列で二群比較。_two_horse_diff/_horse_feature_cols(純関数・テスト済)。
 2. race_class全ゼロ是正: 疎な one-hot ダミー race_class__<cat>(個別ほぼ全ゼロ=無情報)を除外し、ordinal
    race_class_level を採用。文脈列が全レース単一値なら「特徴未取得の疑い」を警告(沈黙して"効果なし"と誤報しない)。
    ＝ユーザの「クラス特徴を正しく取得できていない可能性」を選択の是正で解消。
 3. 勝率差の常設 paired 検定: _winrate_test/_wilson。片方の本命が勝った決着レースでモデル勝率の Wilson 95%CI を
    年別・通算で出し 0.5 を跨ぐか判定(単一の事前指標=多重探索でない)。ユーザ手計算 214/388=55.2% [50.2,60.0] と整合。
    併せて _effect_sizes_by_year(年別 Cliff δ＋同符号判定)で breeder_py_芝勝率 が多重探索の産物でないか年再現性を確認。
 4. SHAP二頭差分(scripts/shap_disagreement.py): 各不一致で LGBM本命/市場本命 2頭の TreeSHAP 寄与差
    ΔSHAP=SHAP(LGBM本命)−SHAP(市場本命) を LightGBM pred_contrib(shapパッケージ不要)で計算。
    出力=①全体 平均|ΔSHAP|上位 ②年別 ③モデル勝利群−市場勝利群の平均ΔSHAP差 ④各レース上位5理由＋年跨ぎ符号一致。
    モデル選択/列名解決は sim_ticket_strategy_roi の既存ヘルパ再利用(0補完なし・契約整合)。要約は純関数
    summarize_delta_shap に分離しテスト済。SHAPは根拠説明であり抽出条件で同2025-2026に買い目化は多重探索と明記。
 5. 難易度推定(scripts/difficulty_estimation.py・別研究): 「どちらでもない」= P(モデル本命も市場本命も負ける)=neither を
    完全OOS(walk-forward: 学習=評価年より前のみ)で予測できるかだけを問う。① 多変量 OOS AUC/Brier(LightGBM)
    ② 特徴ごと walk-forward 単変量AUC(学習年で向き決定→評価年で測定)。特徴は購入時点観測可能のみ(確定オッズ/払戻不使用)。
    合成データ(neither に odds を注入)で OOS AUC≒0.82 と leak-safe walk-forward を確認。純ヘルパ(neither_label/
    walk_forward_splits/_oriented_auc)テスト済。収益化前に「予測できるか」のみ・条件化は事前登録し将来年度で検証。
規律継続(全実装): リーク防止・完全OOS・多重探索回避・sklearn非依存 _auc 再利用・純関数分離で単体テスト。
push先: claude/review-context-and-tasks-1vxigl(本作業ストリームの継続先。セッション見出しの designated branch
  claude/jrdb-target-downloads-docs-xw8of3 は analyze_disagreement.py を含まない別タスク線=SED照合docs)。

### 2026-08-01(続9) 実データ二頭差分の是正一巡＋未取得ファミリ調査（owner_py/sire/damsire/開催）
実データ(874件・wf_2024/wf_2025)で二頭差分が稼働。ユーザレビューを受けハーネスを是正し、
最大の発見＝「特徴が欠損している」ことを機械検出した。
是正(analyze_disagreement.py):
 ・目的変数リーク除外: d_rank_win(AUC1.000/δ+1.000=勝敗そのもの)を _TARGET_POST_COLS＋|δ|≈1自動ガードで除外。
 ・coverage監査: 全件定数の文脈列(days/times/teiryo…)を内訳から除外。候補は各年 非欠測≥50/非ゼロ≥20/unique≥3必須。
 ・AUC方向補正: 識別力=max(AUC,1−AUC)。yoso_*skill の逆張り(LGBM−)を拾えるように。
 ・_candidate_scan: coverage＋方向補正AUC(両年≥0.55)＋方向一致 を満たす候補のみ抽出＋coverage不足の「保留」欄。
 ・Wilson文言: 0.5基準・「弱い証拠/時間安定性未確定」に緩和。
実データ候補(完全母集団): d_kinryo_per_weight(LGBM−,0.583/0.581・非市場・最有力), d_yoso_honmei/profile_skill_sum(LGBM−・逆張り),
 d_単勝系(市場情報=別枠)。保留(coverage不足): d_wet_rel_rank(δ0.377だが道悪実績馬の部分集合依存)。
wet_rel_rank リーク監査: _data_merger は searchsorted(side=left)で厳密過去のみ＝leak-safe を確認。
未取得ファミリ調査(点①・最優先): owner_py_*/sire_*/damsire_*/開催__*/guide_* が featured で全レース定数＝
 「効果なし」でなく FE段で値が入っていない(race_class 全ゼロと同種)。機構追跡:
  - owner_py: jockey/trainer/breeder と同じ _merge_person_yearly。breeder_py は生存(horse_info.breeder_id 由来)、
    owner_py のみ死＝backfill(_backfill.py:159)が owner_id を raw_results から引くが、owner_id は下流の
    entity-resolution で materialize されるため raw_results に無く owner scrape が skip される疑い(owner_win_te は
    merged results 由来で生存)。→ 直接証拠は raw_person_yearly の owner 行数=0 で確定できる。
  - sire/damsire: add_pedigree_stats が peds(peds_0/peds_32)を _ped_key で結合。全NaN＝peds 未整備/キー不一致の疑い。
  - 開催__*: venue one-hot が全ゼロ＝race_class 同様 get_dummies のカテゴリ不一致。
成果物: scripts/audit_feature_coverage.py(featured のファミリ別充足＋生ソース突合で根因locate・純関数テスト済)、
 _merge_person_yearly に _py_ 全欠測の永続 warning を追加(本番ビルドで未取得を可視化)。テスト計13 green。
次(ユーザ優先): ①FE欠損修正(owner/sire/damsire/開催)→ 候補順位が変わりうる ②wet coverage 精査 ③SHAP実行 ④2027完全OOS事前登録検証。

### 2026-08-01(続10) FE欠損の根因を全系統で確定＋監査ツール精緻化（guide 生成確認・race_class は JRDB変換失敗）
実データ監査(669034行)＋コード追跡で、DEAD 90列の根因を系統別に確定（ユーザ精緻ラベル採用）:
 ・race_class 一族(level/place_te/win_te)全DEAD＝TRANSFORM_FAILURE。mapping(race_class_level)は正しい
   (500万下→2等 legacy 対応済)ので、元 race_class が全NaN。JRDB経路 _adapter.py:248
   out["race_class"]=_col(d,"joken").map(JOKEN_TO_CLASS) で joken 未読み or JOKEN_TO_CLASS が
   graded/listed 欠落。→ 元 joken 値と race_class value_counts で確定要（最優先・モデルに効く文脈）。
 ・owner_py=ID_NAMESPACE_MISMATCH。生ソース owner 4683行在るが results owner_id(000031…) と
   person_yearly entity_id(100800…) が別コード体系＋部分scrape(255<<3060)＝一致5.7%。単純ゼロ埋めでは
   直らず、名前照合で対応表を確定してから変換 or 再scrape。owner情報は owner_win_te(99.6%)で概ね代替済み。
 ・sire/damsire=SOURCE_PARTIAL_OR_KEY_MISMATCH。ソースは peds.pkl(RAW_PEDS_PATH)で peds_0/peds_32 在り、
   ただし824頭のみ(featured 669034行)＝血統 coverage 僅少。824が全データなら全期間再取得案件。
 ・guide=DERIVED_MASTER_MISSING。course_guide.csv(committed)在り・course_guide_master.csv 未生成が原因。
   _generate_guide はオフライン純変換＝`python scripts/scrape_course_master.py --stage analyze` で生成可
   (本セッションで生成確認: 84行。run_style_bias/time_bias は充填、corner/drainage/upset は prose 語彙不足で疎)。
 ・開催__/around__/race_class__=UNSEEN_CATEGORY(one-hot未出現・多くは正常)、条件フラグ=TRUE_CONSTANT。
監査ツール精緻化(audit_feature_coverage.py):
 ・classify_dead を対処直結ラベルへ(ID_NAMESPACE_MISMATCH/SOURCE_PARTIAL_OR_KEY_MISMATCH/
   DERIVED_MASTER_MISSING/TRANSFORM_FAILURE/UNSEEN_CATEGORY/TRUE_CONSTANT_OR_SCOPE_CONSTANT/UNKNOWN)。
 ・owner join診断を [1]ID一致 [2]year一致 [3]名前照合 の3段に分離。peds は featured horse_id∩peds で
   partial/keymismatch を判定。テスト計14 green。
次(ユーザ推奨順): Step1 race_class変換監査(joken/race_class value_counts・最優先) → Step2 guide master生成
 → Step3 owner名前照合でID空間解読 → Step4 peds coverage確定 → Step5 rebuild-featured後 再監査で DEAD減確認
 → Step6 baseline/+guide/+owner/+pedigree の個別ablation を完全OOSで(一括投入せず帰属可能に)。

### 2026-08-02 race_class 真因是正(fill 保持)＋grade overlay＝DEAD 90→77・base クラス復活。残: 2024-25 SED 疎
overwrite 再fill＋rebuild-featured で race_class TRANSFORM_FAILURE 解消を実測確認:
 ・真因は _fill.FILL_RACE_INFO_KEEP が race_class を除外し NA 化していたこと（adapter の joken→class は
   実データ 100% 被覆で正常だった）。KEEP に race_class 追加＝是正。--mode fill(追加のみ)では既存行が
   書き換わらず無効、--mode overwrite で 40,012 race を JRDB 置換して初めて反映（手順が要点）。
 ・結果: DEAD 90→77。race_class_level/place_te/win_te が生存、base one-hot(新馬/未勝利/1-3勝/オープン)populate。
   guide も 5→1 DEAD（course_guide_master 生成＋rebuild で反映。残 guide_drainage_good 等は prose 語彙不足）。
 ・grade overlay 追加(_adapter.GRADE_TO_CLASS): grade 1/2/3/6→G1/G2/G3/リステッド、5×オープン→オープン特別。
   標準JRDBコード表＋実分布(G1<G2<G3)整合。次の overwrite+rebuild で graded one-hot が populate。
【新規重要】race_info の race_class 非欠測は年で大きく偏る: 2019-2023=100% だが 2024=20%/2025=24%/2026=88%。
 ＝raw_jrdb_sed が 2024-2025 で疎（164,288 race 中 JRDB 置換は 40,012 のみ）。モデル評価年(2024-25)の
 race_class が大半 NaN のままなので、2024-2025 の JRDB SED を追加 ingest しないと recent 年の race_class は不完全。
owner_py=ID空間不一致(owner_win_te で代替・park)、sire/damsire=peds.pkl が行番号キー＋824頭のみ(park)。
次: ①2024-2025 SED 追加ingest→overwrite再fill→rebuild で race_class 全年化＋grade one-hot 反映
 ②再 audit で DEAD 減確認 ③analyze_disagreement 再走(race_class_level が新規に効く)で候補再評価。

### 2026-08-02(続) 2024-2025 race_class 疎はデータ取得課題（名前backfill不可・0%）
audit の回復可能性測定: race_class NaN 124,276行の race_condition から classify_race_class で復元可=0.0%。
＝race_info の名前列に格テキストが無く、コード側 backfill は不可（race_condition は格を持たない/空）。
year別 race_class 非欠測: 2019-2023=100%（JRDB SED カバー）、2024=20%/2025=24%/2026=88%、pre-2019 は NaN。
→ NaN 124,276 の大半は pre-2019 の netkeiba 履歴（学習古年）＋2024-2025 の JRDB 未取得分。
結論: 2024-2025 の race_class を埋めるには **JRDB SED(または KYI joken_class) を 2024-2025 分 追加 ingest** し
overwrite 再fill→rebuild が唯一の道（KYI も 559,700 行で SED と同カバレッジのため既存では不足）。pre-2019 は
歴史 JRDB を入れるか、古年として許容。名前 backfill・既存データでの回復は不可と確定。
FEギャップ調査は code 側で完了: race_class(base+grade)是正・guide是正・監査/警告/分類ツール整備。
残る 3 件は全てデータ取得タスク（ユーザ駆動）: ①2024-2025 SED追加 ②血統 peds 全期間再取得+horse_id再キー
③owner 年度成績の別ID空間 crosswalk（owner_win_te で代替済みのため優先度低）。

### 2026-08-02(続2) 研究フェーズへ移行＝条件付き相互情報量(edge源泉)ツール。grade overlay は実装済
ユーザ戦略チェックポイント: データ基盤は code 側で完成。grade overlay は既に実装済(a3cecdd・GRADE_TO_CLASS)
＝次の overwrite+rebuild で G1/G2/G3/リステッド/オープン特別 が populate（未実装ではない）。2024-2025 の
race_class 疎は SED 追加 ingest のみの問題でモデル影響小（学習 2019-2023 は 100%）。owner_py/血統は park。
研究ピボット実装: ユーザ提案の I(X;Y|市場) を実装。
 ・src/simulation/_information.py: mutual_information / conditional_mi / edge_decomposition（plug-in・分位ビン・
   sklearn非依存）。I(X;Y)=生情報、CMI=I(X;Y|市場)=市場後に残る情報(edge候補)、edge_ratio=CMI/MI。
   Residual Learning と一致（市場が知る情報は edge にならない）。冗長=MI−CMI。小標本ストラタムは落とす。
 ・scripts/audit_feature_information.py: featured 全数値特徴で Y=勝(着順1)・M=市場含意(1/単勝) に対し
   edge_decomposition を計算し CMI 降順(edge源泉順)＋市場冗長列を出す。「特徴が在るか」でなく「情報を持つか」監査。
 ・テスト: 完全依存/独立、冗長(M=X=Y→CMI≈0)/直交(M⊥X,Y=X→CMI≈MI)、NaN/低カーディナリティ、小ストラタム落とし（4件）。
次の研究優先(ユーザ): ①P(z)予測器→Mixture-PL本番 ②JRDB特徴のVOI(ΔNLL/ΔECE/ΔKL/ΔlogW)＋CMIで edge源泉評価
 （調教→ペース→ラップ→厩舎→脚質 をカテゴリ別 ablation・完全OOS・帰属可能に一括投入しない）。

### 2026-08-02(続3) P1: P(z)予測器→Mixture-PL を完全OOSで配線＋P(z)モジュール単体テスト
研究フェーズ P1。P(z)予測器(_pace_state.py)は実装済だが未テスト・未配線だったため、テスト＋
rolling-origin 配線で「本番」化:
 ・tests/preprocessing/test_pace_state.py(6件): parse_pace_string / label_pace_states(rankで3分割ほぼ均等・
   グループ内相対) / build_race_features(発走前集計・着順=結果列を使わない) / race_pace_balance(horse_id経路) /
   evaluate_pz(一様比 Δlogloss)。P(z)モジュールの純関数を検証。
 ・scripts/run_pace_mixture.py: 完全OOS評価ハーネス。baseline=市場(uniform P(z)・β=0→q退化)、
   challenger=学習P(z)＋fit β(residual=0でpace-mixture寄与のみ分離)。rolling_origin_compare で
   ΔNLL/ΔECE/Bootstrap/LRT＋fold別ΔNLL。ラベル z=結果由来(教師可)・特徴=発走前のみ・fitは各foldの過去のみ
   ＝機構でリーク遮断。成功判定=ΔNLL<0 & CI上限<0 & ECE非悪化(事前定義)。build_race_records は純関数で
   単体テスト(tests/scripts/test_run_pace_mixture.py・2件)。
 ・条件付き相互情報量 I(X;Y|市場)(_information.py・前タスク)も揃い、VOI+edge源泉評価の道具が完備。
実行はローカル(featured/horse_results 巨大成果物)。純部は計18件 green。
次: ローカルで run_pace_mixture 実行→P(z)品質(Δlogloss)と Mixture-PL採否を確認。採用可なら P(z) を
 本番 prob 経路へ。続いて P2=JRDB特徴のVOI(ΔNLL/ΔECE/ΔKL/ΔlogW＋CMI)をカテゴリ別 ablation。

### 2026-08-02(続4) 【正式クローズ】pace-mixture = NO-GO（完全OOSで市場超え情報なし・きれいなnegative）
run_pace_mixture 実測（データ経路・ラベル・rolling-origin 全て正常動作の上での帰無）:
 ・SED race_pace(H/M/S) から 38,381 レースに z 付与（データ不足failでない）。
 ・ΔNLL=-0.00009（2018-2026 全9年で負・符号一貫だが幅 -0.00002〜-0.00015 と極小）、ΔECE=0、LRT_p=0.954。
正式結論: 事前予測ペース状態の混合PLは市場確率に対し一貫してごく微小なNLL改善方向を示したが、効果量は
 実質ゼロ・校正改善なし・統計的支持なし。**採用しない（NO-GO・終了）**。実装ミスでなく、正しいデータと
 時系列評価で「ペース状態混合に市場超えの追加情報なし」と確定できた negative result。
台帳整備（再開でなく完成のため）: 開催日 block bootstrap で ΔNLL の 95%CI を出す機構を追加
 (_model_compare.block_bootstrap_ci・同日相関を保存＝iid より正直)。加えて **実用最小効果量ゲート(--mes,
 既定0.001 bit/race)**: CI上限<0 でも |ΔNLL|<MES なら不採用（統計的有意性だけで救済しない）。-0.00009≪0.001。
 P(z) 自体の OOS 品質(Δlogloss/acc)も report に追加（β採否と独立に予測器の情報量を可視化）。
禁止（多重探索回避・ユーザ指示）: H/M/S境界変更・状態数追加・ラベル再定義・混合方式探索は行わない。
residual head は pace-mixture の延長でなく `baseline market vs market+preregistered residual head` の独立比較でのみ評価。
LRT 注意: 有限混合は境界/識別不能で χ²近似が厳密でないが、ΔNLL極小・ΔECE0 で判定は不変（追加研究の価値低）。
テスト: block_bootstrap_ci 4件（定数=幅0・平均を挟む・同日相関でiidより広い・空）green。

### 2026-08-02(続5) pace-mixture 最終台帳＝🟡(統計的positive・実用未達)で確定クローズ
実測（pull 88a0eed 後・完全動作）:
 ・P(z)品質: Δlogloss平均 -0.0776（全9年負）・acc 0.469（偶然0.333）＝ペースは事前予測可能（分類器成功）。
 ・Mixture-PL 増分 ΔNLL=-0.000086・ΔECE=0・全9年同符号。
   paired CI: レース単位iid[-0.000138,-0.000033] / 開催場×日block[-0.000140,-0.000029]（372ブロック）＝両方0未満だが
   ほぼ同幅（同日相関の影響は小＝効果が本質的に微小）。
 ・LRT/CI 監査: n=29,732 LL_market=-57228.687 LL_mixture=-57226.125 LR_stat=5.122 df=12 p=0.9538。
   CI<0(平均の検定・df非依存) と LRT_p≈0.95(12βを罰する別仮説) は両立＝実装齟齬でない。主判定=paired block、LRTは参考。
最終判定: 🟡 統計的改善のみ・shadow限定（|ΔNLL|=0.000086 ≪ MES 0.001＝実用効果量未達・本番NO-GO）。
確定解釈: H/M/Sペースは予測可能・しかし市場が既に織り込み済み＝pace情報は market-orthogonal でない。
pace-mixture は事前登録どおりクローズ。状態数/境界/混合式の再探索は禁止。residual head は独立事前登録比較のみ。
次候補(未着手・道具は整備済): (A) residual head 独立比較 baseline market vs market+preregistered residual head、
 (B) P2 JRDB特徴 VOI+CMI カテゴリ別 ablation（調教→ペース→ラップ→厩舎→脚質・ΔNLL/ΔECE＋I(X;Y|市場)）。

### 2026-08-02(続6) 生DB完全性監査＝JRA results は全年 clean・24.6%は NAR 混在(分裂でない)と確定
audit_db_completeness で 1着1頭率24.6% の主因を確定切り分け:
 ・JRDB raw(SED/KYI/HJC/TYB): PK重複0・2015-2026欠落なし・SED=KYI一致・TYBのみ18行(anti-join)。構造健全。
 ・results JRA限定(場01-10): 2015-2026 全年 99.7-99.95%・約3,454races・14 rows/race＝完全に clean（2024/25/26含む）。
 ・24.6%の内訳: (a)1986-2014 は 1行/race の stub(JRDB前・horse結果なし・29年)、(b)2024-2025 に NAR(場30-88=
   園田50/名古屋48/佐賀55/大井44…)が race_info/results へ stub 混在(rows/race~3.7・着順欠落)。
 ・race_id は全年 12桁で形式変化なし＝「1実レースが複数race_idへ分裂」ではない（ユーザ仮説は否定・NAR混在が真因）。
   14506×24%≈3464 の一致は JRA が NAR膨張総数の約24%だから。
影響: pace-mixture/disagreement は SED/HJC 由来の正しい JRA 粒度(2025=3464)を使用＝結論は有効。
運用是正: NAR stub は学習に有害(race文脈崩壊)。--jra-only(既定off・central_index_mask で場01-10)で除外可。
 既定offのまま学習すると 2024-2025 の NAR stub が混入するため、_train に NAR混在>2%で warning を追加(log-only)。
 → 中央のみ学習なら --jra-only 必須。TYBの18行は要itemize(取消/発売差)・元ファイル1:1照合はローカル別途。
認定: 生DB(JRA)は概ね正常認定可。残: NAR除外の運用徹底・source1:1照合・派生特徴(owner/血統/guide)完成。

### 2026-08-02(続7) B=残差ヘッド 事前登録比較＝🟡（pace より強い・LRTも有意だが実用MES未達）
run_residual_head --features jrdb_idm,jrdb_kishu_idx,jrdb_joho_idx,wet_rel_rank,kinryo_per_weight --l2 1.0:
 ・ΔNLL(pooled)=-0.000307（pace の 3.4倍）、開催場×日 block CI[-0.000429,-0.000190]（0を明確に除外）、
   ΔECE≈0、LRT_p=0.0027（df=5 の複雑さ penalty 下でも有意＝pace の 0.95 と対照的）、8/9 fold 負（2019のみ+）。
 ・総LL改善 ≈ n×|ΔNLL| ≈ 9.1（29,649レース）。|ΔNLL|=0.000307 は MES 0.001 の 0.31＝実用未達。
判定: 🟡 統計的改善のみ・shadow限定。**pace と違い LRT でも有意＝JRDB能力/適性指数に市場非依存の情報が
 “ある”ことは確か**。ただし効果量は運用閾値未達で本番採用しない。
重要な留保（admissibility）:
 (1) 特徴集合は先の disagreement 効果量(2015-2026)から一部選ばれた＝完全な事前登録でない。wet_rel_rank/
     kinryo_per_weight はデータ駆動選択。方向を凍結し 2027 完全未見年で確認するまで確定不可。
 (2) ΔNLL の edge ≠ ROI の edge（控除~20%）。金銭優位は未検証（NLL改善は控除を超えない可能性大）。
 (3) 状態/特徴の追加探索は多重探索＝禁止（この事前登録結果で確定）。
次候補: (a) 方向凍結→2027 で確認、(b) 事前登録ROI検査（残差ヘッド1位 vs 市場本命の paired ΔROI・購入時
 オッズ・確定払戻／既存 sim_ticket_strategy_roi の market_control 再利用）、(c) shadow ログ運用、(d) クローズ。

### 2026-08-02(続8) B残差ヘッド 確定クローズ＝🟡（正式文言・ECE明文化・8/9・LRT参考）
ユーザ最終確認を反映して確定:
 ・判定 🟡 統計的改善のみ・shadow限定（ΔNLL=-0.000307・block CI[-0.000429,-0.000190]・MESの30.7%）。
 ・年安定性: 「8/9年度で再現・2019のみ逆行(+0.000136)」。2019除外の加重平均≈-0.000365で pooled は1年依存でない。
   ただし「全年同符号」ではない。
 ・ECE: ΔECE=+0.000003＝符号上は微悪化だが、事前固定の許容 ΔECE≤+0.005（_model_compare 標準・run前から
   確定）内＝採用ゲート通過。後知恵で許容幅を決めていない旨を台帳明記。harness verdict にも許容値を明示表示。
 ・LRT_p=0.0027（LR≈2×9.09≈18.2・df=5 と算術整合）だが rolling-origin OOS 尤度差の合算のため参考値。
   主判定は開催場×日 block CI（十分）。
確定文: 事前登録5特徴の市場残差ヘッドは 2018-2026 rolling-origin OOS で 9年中8年 市場単独を上回り、
 開催場×日 block CI も 0未満＝市場未反映の残差信号の存在を支持。ただし改善幅は MES の30.7%・校正も実質不変で、
 本番採用せず shadow 限定。同一OOS期間での特徴追加・L2調整は行わない。🟡で固定・クローズ。
次: (a)同特徴を無調整で2027確認 / (b)完全独立の P2 VOI+CMI 仮説へ。特徴追加/L2調整は多重探索で禁止。

### 2026-08-02(続9) P2 VOI+CMI カテゴリ・アブレーション ハーネス実装（事前登録・完全OOS）
ユーザ指示「Choose P2 VOI+CMI now, keep frozen residual-head queued for 2027」を実装。
実装物:
 ・src/simulation/_model_compare.py: block_bootstrap_ci に p_improve（片側p=bootstrap平均≥0の割合・
   H0「改善なし」）を追加。holm_correction(pairs,alpha) を新設＝Holm–Bonferroni 逐次棄却（p昇順×(m−i)・
   単調非減少強制・1でクリップ）で5カテゴリ多重比較の FWER 制御。
 ・scripts/run_voi_cmi.py: 5事前登録カテゴリ 調教/ペース/ラップ/厩舎/脚質 をキーワードで a-priori 帰属
   （first-match で相互排他・市場由来[単勝/odds/impl/kijun_odds/人気]と outcome[rank_win/払戻/実現ペース/
   実現ラップ/着順]を除外・実行前に membership を必ず印字＝pre-registration 記録）。各カテゴリを
   market vs market+category-residual で rolling-origin OOS 比較（fit_residual_head 再利用）。
   Primary=カテゴリ別 開催場×日(race_id[:10]) block bootstrap の ΔNLL 片側p → Holm補正。
   Secondary=CMI I(残差_category;勝敗|市場)[bit]（edge_decomposition・説明のみ・採用トリガでない）。
   Secondary=累積前進系列 ΔNLL（市場+調教+…+脚質・参考のみ）。共通固定: JRA限定(provenance
   assert_jra_only fail-closed)・L2=1.0・MES=0.001・ECE許容+0.005・同fold/同z-score/同モデルクラス。
 ・境界を出力に明記: P2勝者から 2018-2026 上で合成モデルをチューニングしない（新仮説=新期間・frozen 2027）。
   中間結果を見て特徴追加/閾値変更/再検定するのは事前登録違反と明示。
テスト: tests/simulation/test_block_bootstrap.py に p_improve（強改善→0/悪化→1/対称ノイズ→~0.5）と
 holm_correction（昇順・段階Bonferroni値・単調性・1クリップ）を追加。tests/scripts/test_run_voi_cmi.py 新設
 （市場/outcome除外・キーワード帰属・相互排他first-match・非該当drop・空入力）。657 passed。
残: 実行はローカル(featured成果物必須)。frozen 2027 確認は residual-head features=jrdb_idm,jrdb_kishu_idx,
 jrdb_joho_idx,wet_rel_rank,kinryo_per_weight L2=1.0 を無調整で（membership/L2/MES/ECE許容は再調整しない）。

### 2026-08-02(続10) P2 VOI+CMI 実行結果（40,012 JRAレース・2018-2026 rolling-origin OOS）
Primary（開催場×日 block ΔNLL・Holm補正・実施4検定 m=4／m=5でも結論不変）:
 ・脚質(3feat): ΔNLL=-0.000145 CI[-0.000264,-0.000026] p_Holm=0.028✓棄却・8/9年負(2020のみ+微)
   ＝**直交情報の唯一の堅い局在**（実在・年安定・多重補正後生存）だが MESの14.5%＝🟡 sub-MES。
 ・調教(1feat 調教評価_score): ΔNLL=-0.000014 CI<0 p_Holm=0.028✓ だが fold別で2018-2023が厳密0・
   2024-2026のみ負＝**特徴が2024+しか無いカバレッジ artifact**。脚質と同格に扱わない。
 ・厩舎(21feat): ΔNLL=-0.000142 CI[-0.000292,+0.000015]0跨ぎ p_Holm=0.075✗・2026逆行+0.00040。
   CMI I(X;Y)=0.00106 vs CMI=0.00006＝94%市場冗長＝**市場は厩舎の強さを既に織込済**＝確たる❌。
 ・ラップ(1feat course_lap_length): ΔNLL=厳密0。**レース内定数**（コース属性）→z-score≡0→市場退化。
   馬単位ラップ特徴が featured に無い＝否定でなく**未検定(source不足)**。
 ・ペース(0feat): SOURCE_MISSING（KYI pace_yosou 未取込）＝**未検定(source不足)**。
CMI(副次): 全カテゴリ edge_ratio≈2-6%＝**市場が各カテゴリ情報の94-98%を吸収済**。
 orthogonal順: 脚質(0.037)>厩舎(0.058は冗長大)≈調教(0.020)≫ラップ/ペース(source不足)。
累積(参考・非トリガ): 市場+4カテゴリ=-0.000298 CI[-0.000486,-0.000108]・個別和(-0.000301)とほぼ一致
 ＝カテゴリ間冗長小・加法的。B残差ヘッド(-0.000307)とほぼ同値＝独立2手法が同じ「MESの約30%」着地。
確定: **採用候補ゼロ**（全カテゴリ<MES）。市場に無い直交情報は「在るが極小」で脚質に最も局在。
 厩舎=冗長で確たる負。ラップ/ペース=source不足で未検定＝唯一の actionable FE-gap。
次候補: (a)P2をこの帰属で確定クローズ / (b)ペース(KYI pace_yosou)・ラップ(過去走3F集約=発走前の
 馬単位ラップ)の**馬単位発走前特徴**を新規FEしてから、この2カテゴリだけ探索的に再測（confirmは2027）/
 (c)B frozen 2027 確認（features=jrdb_idm,jrdb_kishu_idx,jrdb_joho_idx,wet_rel_rank,kinryo_per_weight
 L2=1.0 無調整）。境界厳守: P2勝者から2018-2026上で合成モデルをチューニングしない。

### 2026-08-02(続11) P2 正式クローズ＝仮説別記録＋結果不変の診断ハーネス追加（ユーザ査読反映）
ユーザ査読の訂正を反映:
 (1) 累積-0.000298 を B残差ヘッド-0.000307 と**直接比較しない**（特徴集合も推定対象も異なる・
     続10の「ほぼ同値＝一貫性」は撤回）。累積は Secondary で採用/調整の根拠にしない。
 (2) 脚質は `jrdb_kyakushitsu` を数値線形投入＝脚質間に**単調順序を仮定**。「脚質カテゴリ全体」でなく
     **現在の数値化された脚質軸**の検定である旨を明記（完全な脚質検定には one-hot 等の別仮説が要る）。
 (3) P2 を単一の成否にまとめず**仮説別**に台帳化する。
P2 正式クローズ（仮説別）:
   P2-脚質: 🟡 市場条件付き情報あり（Holm後も有意・block CI<0・8/9年）だが MESの14.5%。ただし
            現状は数値化脚質軸の検定（単調順序仮定）。
   P2-調教: 🟡 極微小。`調教評価_score`1列のみ・2024+限定の coverage 依存（下の診断で確定）。
   P2-厩舎: ❌ Holm補正後 非支持（p_Holm=0.075）・CI0跨ぎ・2020/2026逆行。21特徴でも市場に冗長。
   P2-ペース: NOT TESTED / SOURCE_MISSING（0特徴）。
   P2-ラップ: NOT TESTED（course_lap_length=レース内定数のみ・z-scoreで消滅＝情報なしでなく検定不能）。
 最も堅い科学的結論: 現特徴表現では脚質軸に市場条件付きの微小追加情報を確認・調教評価に更に小さい
   近年依存信号の示唆・厩舎は多重補正後 非支持・ペース/ラップは適切な馬別特徴が無く検定不能。
追加実装（結果不変の診断＝ΔNLL/Holm/CMI を一切変えない）: run_voi_cmi.py に
   ・source_diagnostics: カテゴリ×年度の 非欠測率/最大unique/レース内分散あり率（raw featured 由来）。
   ・_effective_rank(Gram経由・数値rank/有効rank=exp(entropy(特異値))/cond): raw/z 重複=共線性を可視化。
   ・OOS係数ノルム ‖θ‖（fold平均/最終）: L2下で多特徴が効かない厩舎の説明。
   これで「調教の近年限定性・ラップの構造的ゼロ・厩舎の raw/z 冗長」を**再検定せず**確定できる。
   純ヘルパー(_nonmissing_rate/_within_race_var_fraction/_effective_rank)は単体テスト済(665 passed)。
次候補: (a)診断出力で上記3点を確定しP2完全クローズ / (b)ペース(KYI pace_yosou)・ラップ(過去走3F集約=
   発走前の馬別)を新規FE→**探索的**に再測（confirmは2027・2018-2026での再検定は新仮説）/ (c)B frozen 2027。

### 2026-08-02(続12) p_improve 帰無分布の厳密化＋診断の推定構造整合（ユーザ査読反映）
ユーザ査読（片側bootstrap p の定義・診断の計算位置）を反映。**Primary の数値解釈は不変**（脚質/調教
🟡・厩舎❌・ペース/ラップ未検定）だが根幹の p 定義をコードで厳密化:
 (1) block_bootstrap_ci の p_improve を **percentile型 mean(t*≥0)** から **中心化帰無分布の ASL** へ:
     t*_null=t*−obs を作り p=(1+#{t*_null≤obs})/(B+1)=(1+#{t*≤2·obs})/(B+1)（Davison–Hinkley）。
     対称時は従来値と一致（脚質0.007等は概ね維持）だが非対称でも正しく、+1/(B+1) で p=0 を回避。
     手計算可能な固定ケース（1ブロック負→1/(B+1)厳密・正→1.0厳密）＋同一seed手再現一致で単体テスト固定。
 (2) 有効rank/条件数を **全期間 pooled でなく fold毎の学習行列** で算出し 中央値[最小,最大] 表示
     （古い年で欠測・近年のみ存在の特徴が混ざるのを排除＝実際の推定問題を表す）。cond は全特異値で
     smax/smin を取り **rank落ちは inf**（微小特異値を除外して有限に見せない＝正直な条件数）。
 (3) 係数解釈を強化: ‖θ‖ はカテゴリ間で n_feat が違い比較不可 → **‖θ‖/√nf・std(Xθ)・mean|Xθ|** を追加。
     とくに **std(Xθ)=市場logitを実際どれだけ動かしたか**。厩舎が21特徴でも std(Xθ) 小なら L2下で縮退＝
     「多特徴でも効かない」を直接説明。
 (4) 診断の計算位置: 非欠測率/unique/レース内分散あり率=**raw featured（JRA抽出後・0埋め前）**、rank/cond=
     **変換後(z-score後)の学習行列・fold毎**、係数ノルム/std(Xθ)=**fold学習係数のOOS適用**。
台帳上の位置づけ訂正: この診断は **事前登録 Primary とは別の post-hoc explanation**。「信号を確証した」でなく
 **Primary結果が生じたデータ構造・推定構造を説明した**と記録する（採用判定は Primary のみ）。
テスト672 passed（p_improve厳密化6件・cond=inf/‖θ‖/std診断・_minmedmax nan除外inf保持 等を追加）。
次: 実データ再実行（Primary数値は不変・診断が想定通りか＝調教2018-23非欠測≈0/ラップ分散率0/厩舎有効rank≪21・
 std(Xθ)小）を確認してP2完全クローズ。以降は (b)ペース/ラップの馬別発走前FE→探索的(confirm2027) or (c)B frozen2027。

### 2026-08-02(続13) 退化ケース拒否＋estimand明記＋SVD直接化（ユーザ査読・完全クローズ前提条件）
ユーザ最終査読の残点を全対応（Primary解釈は不変・実装の頑健性/正確性の訂正）:
 (1) **1ブロックの退化を「最小pで正しい」と扱わず拒否**: block_bootstrap_ci は nb<2 で ValueError
     （再標本化分布が退化＝推論不能。obs<0 でも最小pは強い証拠でない）、nb<30 で警告。単体テストも
     「1ブロック→小p期待」を撤回し ValueError 期待へ変更。手計算テストは複数ブロック固定リサンプル
     （同一seed手再現一致）を採用。実データは2,472ブロックで主推論に影響なし。
 (2) **estimand を race-weighted と明記**: 抽出ブロック内の全レースを連結し「レース差総和/レース総数」
     ＝全レース等重み平均（大開催日はレース数ぶん強く効く／block-weighted＝ブロック平均を等重み化する
     別estimand は採らない）。obs=v.mean() も同じで、中心化 t*−obs は「各レース差を obs で引いてから
     ブロック再標本化」と代数的に同一。docstring/コメントに明記。
 (3) **"assumption-free" は言い過ぎ→訂正**: 「非対称なbootstrap分布にも対応する、中心化帰無分布に基づく
     片側ASL」と表現。前提（開催場×日が妥当な依存単位・ブロック間弱依存・ブロック集合が期間を代表・
     bootstrapが平均ΔNLLの標本分布を近似）を docstring に明記。
 (4) **有効rank/cond を Gram でなく直接SVD**: Gram(X.T@X)は条件数を二乗し微小特異値の数値誤差を拡大。
     QR で nf×nf 縮約後 svd(compute_uv=False) で X の特異値を直接取得（QR安定・特異値不変）。rank は
     matrix_rank 既定 tol（smax·max(m,n)·eps）、rank<min(m,n) なら cond=inf。exact collinear テストで
     numerical_rank=1/cond=inf を確認（＝厩舎 raw+_z が within-race z-score 後に厳密一致する機構と一致）。
再実行時の正式値: 新ASLの raw p / Holm補正p / 診断を正式採用（旧percentile値と同一前提でクローズしない）。
 脚質/調教の Holm 判定が変われば新結果へ更新（再探索でなく検定実装の訂正）。
最終合格条件（ユーザ提示・再実行で確認）: 2ブロック未満拒否✓・race/block重み明記✓・中心化ASL再計算✓・
 Holmを新pから再計算✓・調教の古年度coverage不足・ラップのレース内分散率0・厩舎のfold別rank低下/高cond・
 脚質のcoverage/rank/std(Xθ)全年安定。テスト672 passed。

### 2026-08-02(続14) Holm family を事前登録5カテゴリに固定＋bootstrap再現性メタ（クローズ前最終）
ユーザ査読の最後の残点を対応:
 (1) **Holm family=事前登録5カテゴリ（m=5）に固定**。SOURCE_MISSING（ペース＝0特徴）は「検定した」
     ことにせず、多重補正の帳簿上のみ **p=1.0** を置いて非棄却で family を保つ（事前登録との対応が明瞭・
     最も保守的）。従来 m=4 で計算していたのを m=5 へ。ラップは1特徴で active のまま走り p≈1.0（ΔNLL=0）。
     m=5 でも 脚質(0.007×5=0.035)・調教(0.009×4=0.036) は棄却、厩舎(0.0375×3=0.11) は非棄却＝結論不変
     の見込み（正式値は新ASL再実行から）。Primary表に NOT TESTED 行も表示し family と対応を明記。
 (2) **bootstrap 再現性メタを出力**: B（全カテゴリ共通）・seed・ブロック数・最小到達可能 p=1/(B+1)・
     estimand=race-weighted 平均ΔNLL。最小 p が大きい（B小）と p≈0.007-0.01 帯に Monte Carlo 誤差が乗る
     ため、最終台帳は --n-boot を増やし固定B/seed で一度だけ再計算（特徴/モデル不変）と注記。
 (3) --seed を追加し全カテゴリ共通で block bootstrap / rolling-origin に伝播（再現性）。
最終採用値は全て今回の修正版から: 中心化ASLのraw p / m=5維持のHolm p / block CI / 年別ΔNLL /
 post-hoc診断。脚質・調教のHolm判定が変われば新結果へ更新（再探索でなく検定実装の訂正）。テスト673 passed。
確認すべき診断（再実行時）: 調教=2018-23の非欠測/分散率≈0・2024+のみ有効／ラップ=非欠測高だが分散率0・
 std(Xθ)=0／厩舎=rank≪21・cond=inf・std(Xθ)小・2020/2026逆行／脚質=coverage/rank/std(Xθ)全年安定・新Holm<0.05。

### 2026-08-02(続15) P2 完全クローズ＝正式値(B=20,000/seed=0/2,472ブロック/中心化ASL/Holm m=5)
再実行の正式値（旧percentile値でなく中心化ASL・事前登録5カテゴリHolm）を採用。**結論は前回と不変**。
Bootstrap: B=20,000 seed=0 開催場×日ブロック=2,472 最小到達可能p=5.0e-5 estimand=race-weighted 平均ΔNLL。
Primary（正式）:
   脚質(3feat): ΔNLL=-0.000145 CI[-0.000262,-0.000028] ASL_p=0.0084 Holm_p=0.0336<0.05✓棄却・8/9年負
     ＝**直交情報の唯一の堅い局在**（m=5でも生存・CI<0・年安定・std(Xθ)=0.0113非ゼロ）。だが MESの14.5%＝🟡。
     注: 3特徴のうち leg_type_binary と _z が within-race z-score 後に一致→有効rank 2（cond=inf）。実質
     「jrdb_kyakushitsu(数値=単調順序仮定) + leg_type_binary 軸」の検定。
   調教(1feat 調教評価_score): ΔNLL=-0.000014 CI[-0.000025,-0.000003] ASL_p=0.0052 Holm_p=0.0262<0.05✓
     だが MESの1.4%＝🟡。診断で **coverage が 2023頃開始・効果は2024+のみ**（訓練に入る1年遅れ）＝近年限定。
   厩舎(21feat): ΔNLL=-0.000142 CI[-0.000293,+0.000011]0跨ぎ ASL_p=0.0333 Holm_p=0.0999>0.05✗非棄却。
     診断で **21→数値rank14(=7冗長・raw/_z対が within-race z-score 後に一致)・有効rank10.32・cond=inf**、
     std(Xθ)=0.0129小、fold で2020(+0.00018)/2026(+0.00040)逆行＝**共線・年度不安定で確たる❌**。
   ラップ(1feat course_lap_length): ΔNLL=0・ASL_p=1.0。診断で **非欠測率=0.00(全年)＝当該列は featured で
     全欠測**（続10-13の「レース内定数」を訂正：定数でなく空列）→残差モデルで検定不能＝NOT TESTED。
   ペース(0feat): SOURCE_MISSING（KYI pace_yosou 未取込）＝NOT TESTED。Holm家系では p=1.0 帳簿で m=5 維持。
Secondary CMI: edge_ratio 脚質0.037>厩舎0.058(冗長大)≈調教0.020≫ラップ/ペース0＝**市場が各カテゴリ情報の
   94-98%を既に吸収**（脚質が相対最大だが大部分市場冗長）。
Secondary 累積(非トリガ・B比較しない): 市場+4カテゴリ=-0.000298 CI[-0.000486,-0.000108]。
診断scope注記: source_diagnostics は raw featured(1986-2026)を走査、Primary推定の records は完全な
   オッズ/結果を持つ年（テスト fold 2018-2026・訓練2015+）。旧年(pre-2015)は stub で records から脱落済で
   Primary に不参加（診断の古年行は推定に無関係）。
**P2 完全クローズ（仮説別・正式）**:
   脚質: 🟡 現在の数値化脚質軸に微小な市場条件付き追加情報（Holm m=5 通過・MES未達）
   調教: 🟡 近年coverage限定の極微信号（2024+のみ・Holm通過だがMESの1.4%）
   厩舎: ❌ 多重補正後不支持(Holm_p=0.0999)・共線(21→rank14,cond inf)・年度不安定(2020/2026逆行)
   ペース: NOT TESTED / SOURCE_MISSING（Holm家系は p=1.0 帳簿）
   ラップ: NOT TESTED / course_lap_length 全欠測で馬間残差モデルに載らない
最も堅い結論: 現特徴表現では市場に無い直交情報は「在るが極小」で**脚質に最も局在**（唯一 Holm m=5 生存の
   実効カテゴリ）。調教は近年coverage限定の極微。厩舎は市場に冗長・年度不安定で不支持。ペース/ラップは
   適切な馬別発走前特徴が無く検定不能＝唯一の actionable FE-gap。**採用候補ゼロ**（全カテゴリ<MES）。
次候補: (a)P2 これでクローズ / (b)ペース(KYI pace_yosou)・ラップ(過去走3F集約=発走前の馬別)を新規FE→
   探索的に再測（confirm は 2027・2018-2026 再検定は新仮説）/ (c)B frozen 2027 確認
   （features=jrdb_idm,jrdb_kishu_idx,jrdb_joho_idx,wet_rel_rank,kinryo_per_weight L2=1.0 無調整）。

### 2026-08-02(続16) P2台帳の表現訂正（ユーザ査読）＋H3=新規独立仮説の起票
P2はクローズ確定。台帳表現を3点厳密化:
 (1) 脚質は「唯一のHolm生存カテゴリ」ではない（調教もHolm通過）。正確には
     **全年に実効coverageがあり年度安定性も伴ってHolmを通過した唯一のカテゴリ**（調教はHolm通過だが
     2024+のcoverage限定・年度実効が近年のみ）。
 (2) 厩舎の std(Xθ)=0.01285 は脚質 0.01133 より**小さくない**（むしろ大）。厩舎は縮退したのでなく
     **実際に市場logitを動かしたが年度不安定で改善につながらなかった**。「多特徴でも縮退」は誤り＝訂正。
     厩舎不採用の根拠は「Holm非棄却＋CI0跨ぎ＋年度逆行(2020/2026)」であって std(Xθ) の小ささではない。
     （共線性=21→rank14/cond=inf は独立自由度が21未満という事実で、これは有効。）
 (3) CMI は「市場が94-98%吸収」を厳密化: **使用した情報分解推定器では、カテゴリの周辺情報量のうち
     市場条件付きで残った割合が 2-6%(edge_ratio) だった**、と表現（推定器依存を明示）。

### 2026-08-02(続16) H3 起票＝馬別・発走前 適性の新規独立仮説（P2再開でなく別仮説）
P2の結果を見て発案＝2018-2026 では confirmatory と呼べない。**feature-development 期間の探索的OOS推定**
として扱い、confirm は未見期間。B frozen 確認は無変更で待機（H3結果を見ても B5特徴に pace/lap を足さない
＝合成はさらに別仮説）。H3 は2仮説に分離（寄与の切り分けのため）:
   H3a 馬別ペース適性: x_i = Σ_z P_r(z)·E[perf_residual_i | z, strictly prior]。P_r(z)=発走前ペース予想
     (KYI pace_yosou 等)、z=H/M/S、馬別残差=その馬が過去に各ペース状態で市場期待をどれだけ上回ったか
     （現レースより前の走歴のみ・初出走/履歴不足は事前固定の縮約）。脚質の再符号化でなく過去成績由来の
     ペース状態別・馬固有反応。
   H3b 馬別ラップ適性: course_lap_length 修復でなく過去走から発走前に作る馬別特徴（前半速度/終い(上り)速度/
     前後半差/同距離・馬場・コース基準からの残差/ペース補正の脚使用効率/直近履歴の縮約）。現レースの実現
     ラップ・着順・払戻・レース後確定値は不使用。
実施順（性能評価より前）: (1)ソース契約確定（source table/column・発走前取得可否・coverage開始年・更新時刻・
   horse_id/race_id結合キー・strictly-prior条件・欠測処理／KYI pace_yosou は「予想値」かつ賭け時点前に確実
   観測可能かも確認）→ (2)リーク防止を機械テスト（source_date<target_date・target自身を集計に含めない・
   同日後続から逆流させない・現レース実現pace/lapを見ない・JRA filterを特徴生成前に適用／日付だけでは同日
   混入し得るので日時 or 開催日×R で厳密順序）→ (3)outcome前に特徴定義freeze（H3a/H3b各小固定集合）。
評価条件（後日・継承）: market vs market+category residual head・L2=1.0・MES=0.001・ECE許容+0.005・
   JRA-only nar_rows=0・rolling-origin・開催場×日 block bootstrap・race-weighted ΔNLL・Holm family m=2。
三層確認: (1)2018-現末=探索開発 (2)freeze後の直近=prospective shadow (3)2027=正式長期。artifact に
   feature_freeze_commit/timestamp・max_training_race_date・first_prospective_race_date・feature hash・
   source coverage を保存。B frozen(jrdb_idm,jrdb_kishu_idx,jrdb_joho_idx,wet_rel_rank,kinryo_per_weight,
   L2=1.0)は完全別レーンで無変更維持。
次: 性能評価より先に H3a/H3b の source contract・strictly-prior 生成・coverage 監査を実装。

### 2026-08-02(続17) H3 監査①の結果＝SED中心へ再設計（horse_results の race_id 欠如を回避）
audit_h3_source_contract 初回実行(ユーザ環境)の判定:
 ・KYI pace_yosou: populated(559,700行・H313895/M184915/S43343/空17547)・race一意率0.998。ただし
   **hard label(H/M/S)で確率でない**・空3.1%・0.998は空文字混在の見かけの可能性。
 ・過去走の市場残差材料(人気/着順/頭数): ほぼ完全✅。
 ・過去走のペース状態: horse_results.ペース は 2015-2019 coverage≈0(0-9%)・2022=62%/2023=74%＝疎🔴。
 ・過去走の上り: 2015+ で 0.96-0.99🟡（数値parse/基準化の監査要）。
 ・strictly-prior 実データ検証: **サンプル馬0頭**＝horse_results が index名None・race_id列なし・
   horse_id不明で検査が対象を取得できず🔴（リーク検出でなく検査未実行）。
**再設計（重要）**: horse_results は race_id 列を持たず ペース も疎＝H3 の過去走z結合に不適。一方
   **JRDB SED は per-(race,horse) で ketto(血統登録番号=安定horse id)+race_id+ymd(日付)+race_pace/
   uma_pace(実測z)+chakujun/kakutei_ninki(市場超過)+mae3f_time/ato3f_time(前後3F)+kyori/shiba_dirt/
   baba_state(層)+ten_idx/agari_idx を全て持つ**。⇒ H3 は horse_results を使わず **SED を主ソース**に、
   ketto で馬履歴を束ね ymd で strictly-prior、race_id で「同走比較(race相対上り残差)」まで可能。
   これで「race_id欠如」「ペース疎」「サンプル馬0頭」の3ブロッカーが同時に解ける（SED に ketto/race_id/
   ymd が揃うため）。現レースの P_r(z) は KYI pace_yosou、過去走の z は SED race_pace/uma_pace。
監査②(強化版・要ローカル再実行)で確認する gate:
 (1) SED ketto 同定統計(非欠測/unique/走数中央値・最大/2日付以上の馬数/同一馬×同一日重複=0)→
     strictly-prior を100頭以上でPASS（SED ketto 履歴・ymd順）。
 (2) SED race_pace/uma_pace の H/M/S 変換成功率＋年別coverage（過去走zの本命ソース）。
 (3) pace_yosou を**空文字正規化後**の race単位coverage＋非空値の真の競合数（0.998の実体確認）。
 (4) SED ato3f/mae3f の数値parse率・min/med/max・年別 finite/total（分母効果）。
 (5) target(featured)↔SED (race_id,馬番) 一致率（H3特徴を target に載せられるか）。
freeze前の未決定（性能前に一つ選ぶ・両方を2018-2026で試して勝った方を採らない）:
 ・H3a の P_r(z): **hard-label(H→P(H)=1)** か **fold内calibrated P(z_actual|pace_yosou)**（学習期間のみで
   calibration規則/平滑化/欠測を先に固定）のどちらかを主仮説として freeze。
freeze候補（gate通過後）:
 H3a = 現レース pace_yosou(hard or calibrated) × strictly-prior SED実測ペース状態別の馬別市場残差＋事前縮約。
 H3b = strictly-prior 過去走の race相対 上り残差(＋必要なら前後半差1特徴)＋事前縮約。
正式結論(現時点): **H3b の上りソースと H3a の現レースpace予想は存在。だが過去ペース状態の完全結合と実データ
   strictly-prior 保証が未完のため、H3a/H3b の特徴定義はまだ freeze せず、性能評価にも進まない。**
   → SED中心の監査②を実行して上記5 gate を通してから freeze。B frozen は無変更で待機。

### 2026-08-02(続18) H3 監査②（SED中心・強化版）＝5 gate 全PASS＋H3b range 要件が判明
audit_h3_source_contract 強化版(ユーザ環境・SED 559,700行 2015-2026)結果:
 gate1 horse_id+strictly-prior: ketto 非欠測1.0000・unique 61,276・走数中央6/最大84・2日付以上57,093・
   **同一馬×同一日 重複=0**（date<target で安全）・strictly-prior **300/300 PASS**。✓
 gate2 結合: KYI↔SED ketto 共通=61,276/61,276(100%)・SED は race_id+ymd をネイティブ保持。✓
 gate3 pace_yosou: 空正規化後 **非空値の真の競合race=0**（0.998は空文字混在の見かけ）・非空race率0.9636・
   全空race率0.0364・混在race94。＝pace_yosou は真にレース単位＝P_r(z) 妥当。✓
 gate4 parse/range: race_pace H/M/S 変換0.965(M205709/H182909/S151292/空19790)・uma_pace 0.960(S偏り
   376164)・全列coverage≈1.00。**ただし数値に sentinel/garbage**: mae3f_time min=-89.0(負)・
   ato3f_time max=984(98.4s)・agari_idx/ten_idx min=-99.9(欠測sentinel)。⇒ H3b は妥当域clip＋
   -99.9/負を欠測化する仕様を freeze に含める必須。⚠️
 gate5 target↔SED (race_id,馬番) 一致=0.8297（pre-2015 featured 希釈込。SEDは2015-2026なので評価窓
   2018-2026 では実質満杯。評価窓限定の再測は軽微follow-up）。
判定: **5 gate 実質PASS**。過去zは race_pace（レース環境＝pace_yosou と整合。uma_pace は脚質寄りなので
   H3a本命には race_pace を採る）。残る freeze前決定は H3a の P_r(z) 方式（hard-label vs fold内calibrated）
   を性能前に一方 freeze すること。H3b は race相対上り残差＋前後半差＋妥当域clip/-99.9欠測化を freeze 予定。
次: ユーザに H3a P_r(z) 方式を確認 → 両仕様 freeze → 特徴ビルダ実装（性能評価はその後）。B frozen 待機。

### 2026-08-02(続19) H3 特徴定義を仕様確定＝H3a calibrated/H3b percentile（gate A/B 通過で最終freeze）
ユーザ追加査読を反映。**freeze は残2 gate 通過後**（性能評価はさらにその後）:
 ・H3a P_r(z) 方式: chip回答は「Hard-label」だが詳細メッセージが最終的に「fold内calibrated pace適性1列」と
   明示 → **calibrated を採用**（入力 pace_yosou は hard label、その誤差を fold内 P(z_actual|pace_yosou)
   3×3混同・Dirichlet α=1・学習期間のみ で確率化）。hard one-hot は並行実施しない。要ユーザ最終確認。
実装（frozen 仕様・src/features/_strictly_prior.py）:
 ・fit_pace_calibration(pace_yosou, race_pace, α=1): 3×3 P(z_actual|pace_yosou)・各行正規化・学習期間のみ。
 ・sed_market_perf: r=(人気−着順)/(N−1)∈[−1,1]・N=race行数(頭数)・ijo_kubun≠0(取消/除外/中止/失格/降着)
   と範囲外(人気/着順∉[1,N])を除外。
 ・sed_race_percentile_ato3f: race内 上り昇順percentile→ s=0.5−pct（正=同走より速い終い）。
 ・clip_3f: 物理域[280,450]=28.0-45.0秒・0/負/999/空→NaN（監査の mae3f=-89/ato3f=984/idx=-99.9 sentinel対応）。
 ・h3a_pace_aptitude(hist, pace_yosou, calibration, k=5): x=Σ_z P_r(z)·shrink(mean r_z,n_z,λ=5)。
   履歴なし→0・予想欠測→0（中立＝そのレースは市場のまま）。
 ・h3b_lap_aptitude(hist, k=5): shrink(mean s, n, λ=5)。履歴なし→0。**ato3f 1特徴のみ**
   （mae3f/前後半差/距離別/近走窓は足さない＝別仮説 H4）。
監査③(audit_h3_source_contract 追加 gate・要ローカル再実行):
 ・Gate A: eligible(year>=2015 & JRA場01-10 & 有効馬番)での target↔SED 一致率・年別・未一致 年×場分布・
   未一致race_id例・SEDキー重複（0.8297 の希釈を排し 99%台後半を確認）。
 ・Gate B: 意味的validity（空文字を非欠測に数えない）: race_pace 年別H/M/S有効率、市場残差の有効率＋
   ijo_kubun分布、ato3f 物理域valid率（年別・単位0.1秒明記）。経験quantile不使用・spec/ijo準拠。
freeze前チェックリスト（性能前）: Gate A 99%台後半 / Gate B semantic-valid 率確定 / H3a calibrated 確定。
本番生成時のリーク全件assert（設計要件）: 各target行で max(source_ymd)<target_ymd・source race_id≠target・
   同日履歴=0 を全件検査し違反1件で停止、artifact に max_source_ymd/監査集計を保存。
性能評価の固定条件（gate通過後）: H3a vs market / H3b vs market・Holm family=2・JRA-only nar_rows=0・
   rolling-origin・L2=1.0・MES=0.001・ECE許容+0.005・開催場×日 block bootstrap・race-weighted ΔNLL・
   B=20,000/seed=0。2018-2026は探索的OOS。結果を見て hard化/λ/窓/前後半差/合成 を同期間で試さない。
   B frozen は無変更で待機。tests/features 25 passed。

### 2026-08-02(続20) H3 gate A/B 全通過→仕様 freeze＋生成/評価ハーネス実装（性能はローカル実行待ち）
監査③(ユーザ環境)結果＝**両 gate PASS**:
 Gate A: eligible(year>=2015 & JRA & 有効馬番)=555,128・target↔SED 一致率=**1.0000(全年)**・未一致0・
   SEDキー重複0。0.8297 は pre-2015 希釈のみと確定。
 Gate B: race_pace H/M/S 有効率0.938-0.971(~96%)・市場残差 r 有効率0.9918(ijo≠0/範囲外除外後)・
   ijo分布 0=555,128完走/3=2533/2=1135/1=882/5=21/4=1(非完走~0.8%を除外)・ato3f 物理域valid 0.959安定。
⇒ freeze 条件充足。H3a=calibrated P(z)（要ユーザ最終確認・chip「Hard-label」vs詳細「calibrated」は
   詳細を採用）、H3b=race内上りpct。両者 λ=5・履歴/予想欠測→0。
実装: src/features/_strictly_prior.py に assert_strictly_prior（本番生成の全件リークassert＝
   max(source)<target・race_id≠target・同日=0／違反1件でRuntimeError・max_source_ymd返す）。
   scripts/run_h3.py（生成＋探索的OOS評価）: SED per-row 中間量(_z/_perf/_agari_pct/_d)→ketto時系列で
   strictly-prior スライス→fold内 calibration(学習年のみ KYI×SED)→各target全件assert→records→
   market vs market+h3a / market+h3b を Holm family=2・B=20,000/seed=0・開催場×日 block・race-weighted
   ΔNLL・L2=1.0・MES=0.001・ECE許容+0.005 で評価。JRA-only fail-closed。生成監査(leak-assert通過数・
   max_source_ymd・非ゼロ数)を出力。tests/features 26 passed・全体706 passed。
次: ローカルで `python scripts/run_h3.py`（探索的 2018-2026）。結果は探索的OOSで、見て hard化/λ/窓/
   前後半差/合成を同期間で再試行しない。採用候補は未見期間で確認。B frozen は無変更で別レーン待機。

### 2026-08-02(続21) H3 監査と評価を分離＝feature-only監査ゲート→manifest一致検証で一度だけ評価
ユーザ手続き修正を反映（性能を見る前に監査を確定するゲートを機構化）:
 ・H3a は calibrated 正式確定（hard-label は未実施の別仮説として封印）。
 ・run_h3.py を二段化: 既定 --audit-only（特徴生成＋feature-only監査＋manifest保存のみ・ΔNLL/ECE/p を
   計算しない）／--evaluate --audit-manifest（manifest の3ハッシュ一致検証後に一度だけ ΔNLL・Holm m=2）。
 ・manifest(artifacts/h3_feature_audit.json・gitignore): feature_definition_commit=cef95ac・
   feature_definition_hash(_strictly_prior.py)・source_contract_version・data_fingerprint・
   generated_feature_hash・lambda=5・alpha=1・ato3f域[280,450]・strictly_prior_violation=0・
   max_source_ymd・history_volume・feature_effectiveness_var_frac・calibration・checks・audit_result。
   評価時に feature_def/data/feature の3ハッシュが不一致なら STOP（監査後の改変で古いPASSを流用不可）。
 ・feature-only 監査合格条件: target(race_id,馬番)重複0・NAR0・時系列違反0（生成完走=全件assert通過）・
   NaN/inf0（欠測は中立0）・完全性(全eligibleに特徴)・calibration行和=1・H3a/H3b が全ゼロ/全年race内分散0
   なら構造的検定不能でFAIL。履歴量分布(0/1/2-4/5+)と年別 race内分散>0率を表示。
 ・_race_pace_yosou: 非空の真の競合 race は fail-closed（レース単位一意化不能を検出）。
 ・rolling-origin 時間仕様を明文化: residual-head係数と pace calibration は test年より前のみ学習・test年中
   固定／馬別 pace/lap 履歴は各target発走時点まで逐次更新可（オンライン逐次特徴・年次固定モデル）。
合成 end-to-end で audit(PASS+manifest)→evaluate(3ハッシュ一致→ΔNLL 一度)を確認。tests/scripts/
   test_run_h3.py 追加（feature hash 決定性・監査 pass/fail 各種）。touched suites 982 passed。
次: ローカルで (1) `python scripts/run_h3.py --audit-only --seed 0` → manifest PASS を確認、(2) PASS 後
   `python scripts/run_h3.py --evaluate --audit-manifest artifacts/h3_feature_audit.json --n-boot 20000
   --seed 0` を一度だけ。監査出力を先に貼れば評価前にゲート判定する。B frozen 無変更待機。

### 2026-08-02(続22) H3 評価の検定力バグ発見→fold内 global 標準化を freeze（無スケールは L2 潰れ）
ユーザがstale(分割前)run_h3を実行し ΔNLL h3a=+0.000000/p=0.99・h3b=-0.000000/p=0.23（CI[0,0]）を得た。
診断: **無スケールの H3a/H3b(r∈[-1,1]縮約→std~0.05-0.1・percentile縮約)を L2=1.0 の残差ヘッドへ直入れ→
θ≈0 に潰れ→ΔNLL≈0（情報の有無に関わらず0）＝検定力ゼロ**。B/P2 は build_residual_records で
within-race z-score 済（unit スケール）だったが run_h3 は未スケールだった＝conformance バグ。
H3 の特徴スケーリングは明示 freeze されていなかったため事前登録 fork として確認 → ユーザ選択=
**fold内 global 標準化**（学習期間のみ mean/std・test へ適用・leak-safe）。理由: H3a/H3b は母平均≈0
（市場残差は対称・percentile 中心0）ゆえ raw 0(履歴なし)→標準化後も≈0＝**neutral-0 保持**しつつ unit
スケールで L2 に対し検定力確保。within-race z-score(B/P2一致案)は neutral-0 を壊すため不採用。
実装: run_h3._eval_feature を fold内 global 標準化(train のみ mean/std)へ改修（ΔECE/ΔNLL は手計算・
block bootstrap は標準化後 per-race ΔNLL）。manifest に feature_scaling="fold_internal_global" 追加
（標準化は feature_hash の下流＝raw特徴は不変・評価は決定的）。合成検証: 小スケール(std0.08)の情報あり
特徴が標準化後 ΔNLL=-0.576 CI<0 p=0.0005＝**検定力回復**（無スケールなら≈0）。stale の 0.000000 は無効。
注意: ユーザは分割前の run_h3 を実行した＝gated flow(--audit-only→--evaluate)未使用。要 git pull。
次: ローカルで git pull → `run_h3.py --audit-only --seed 0`(manifest PASS 確認)→ PASS 後
`--evaluate --audit-manifest artifacts/h3_feature_audit.json --n-boot 20000 --seed 0` を一度だけ。

### 2026-08-02(続23) H3 確定クローズ＝H3a/H3b とも ❌（検定力ありの genuine null・P2と整合）
gated 評価(--audit-only PASS→--evaluate・manifest 3ハッシュ一致・records=29,649・B=20,000/seed=0):
  H3a: ΔNLL=+0.000000 CI[0,0] p_Holm=0.9858 ❌（僅かにノイズ）
  H3b: ΔNLL=-0.000000 CI[0,0] p_Holm=0.4585 ❌（非有意）
**識別上の重要点**: 結果が stale(無スケール)と**完全一致**(p=0.9858/0.2292)。これはバグでなく genuine null
  の最強証拠。理由: 残差ヘッドは within-race softmax ゆえ **global 標準化は model 出力不変**（global mean
  は race 内定数で softmax でキャンセル・global scale は θ が吸収）＝標準化は L2 罰則域のみ変える（raw は
  有効係数を λ=1.0、標準化は λσ²≈0.0064＝~150倍弱）。**150倍違う正則化が同一 ΔNLL** ⇒ 最適係数が
  penalty-insensitive＝**無正則化(MLE)最適でも改善ゼロ**＝sub-MES より強い null。検定力は合成で独立確認
  （同 _eval_feature に std0.08 の signal あり特徴→ΔNLL=-0.576＝un-crush）。real は検出すべき signal 無し。
結論: **馬別ペース適性・終い適性は市場に対し検出可能な直交情報を持たない**。P2(市場が各カテゴリ情報の
  94-98%吸収)と完全整合＝P2 が残した唯一の actionable FE-gap(ペース/ラップ)も、馬別・発走前で正しく作り
  検定力あるハーネスで測ると市場が既に効率的に織込済。多重探索禁止に従い hard化/λ/窓/前後半差/合成の
  同期間再試行はしない。H3 クローズ。
研究フェーズ総括（P1/A/B/P2/H3）: 市場に対し MES(0.001 bit/race)を超える直交情報は**どこにも無い**。
  最良でも B残差ヘッド/脚質の -3e-4/-1.4e-4（MESの15-31%・shadow限定🟡）。H3 は null。
残: B frozen 2027 確認（features=jrdb_idm,jrdb_kishu_idx,jrdb_joho_idx,wet_rel_rank,kinryo_per_weight・
  L2=1.0・無調整）を未見期間で。それ以外の探索は打ち止め候補（市場効率が高く NLL 優位が控除を超えない）。

### 2026-08-02(続24) H3 台帳の期間表記訂正＋内部値科学表記（ユーザ査読・再探索なし）
ユーザ査読の2点を反映（H3クローズは確定・hard化/λ/窓/前後半差/合成の再探索はしない）:
 (1) **評価期間表記の訂正**: fold出力の test は 2021-2026。正しくは「2018-2020 を初期学習/履歴 warm-up、
     2021-2026 を test folds とした探索的 rolling-origin OOS」。29,649 は 2018-2026 の records pool 全件で
     あり全てが test 観測ではない。run_h3 の境界表示を warm-up/test/pool を動的に出す形へ修正。
 (2) **内部値を科学表記で追記**（±0.000000 の下の実値・表示精度の修正のみ）: run_h3 に ΔNLL/CI/ΔECE を
     %.3e で出す行を追加。再現の正本は p 値: H3a raw=Holm=0.9858・H3b raw=0.2292/Holm=0.4585。
     |ΔNLL| は 6桁表示で 0.000000＝ <5e-7（MES 0.001 から桁違い・~2000倍遠い）。
H3 最終台帳（確定）:
   H3a 馬別ペース適性: ❌ 改善なし。calibrated P(actual|pace_yosou)×strictly-prior ペース状態別市場残差・
     λ=5・L2=1.0・raw=Holm p=0.9858・ΔECE≈0。active feature だが市場追加情報なし（僅かに悪化方向）。
   H3b 馬別終い適性: ❌ 支持せず。strictly-prior race-relative ato3f percentile・λ=5・L2=1.0・raw p=0.2292・
     Holm p=0.4585・ΔECE≈0。active feature だが Holm 後に支持なし。
   評価: 2018-2020 warm-up / 2021-2026 test folds・開催場×日 block bootstrap・race-weighted ΔNLL・
     B=20,000/seed=0・manifest の feature_def/data/feature hash 一致。
科学的結論（確定）: 馬別の過去ペース状態別市場超過と過去レース内終い順位を単純全履歴縮約に落とした本表現に、
   市場確率を上回る追加予測情報は無し。P2 の数値化脚質軸の微小信号は pace/終い適性へ一般化しなかった。
   MES から桁違いに遠く将来確認へ回す採用候補なし。**H3 完全クローズ**。今後の未見確認は別レーン凍結済の
   B residual head のみ。

### 2026-08-02(続25) 総括の厳密化＋B-2027 confirmation を完全事前登録→研究フェーズ休止
ユーザ査読の表現訂正:
 (1) **MES 単位訂正**: race_nll は自然対数(−ln P)ゆえ ΔNLL/MES=**nats/race**（bit/race は誤り）。
     過去記述の「bit/race」は nats/race に訂正。CMI(_information)のみ log2=bit（別物・正しい）。
 (2) **市場効率の主張を scope 限定**: 「JRA単勝市場は極めて効率的に織込済」→「**2015-2026 JRA データ・
     今回検証した特徴族/表現/残差モデルの範囲では、市場確率を大きく上回る直交情報は確認されなかった**」。
     未検証の映像・板時系列・馬体挙動等まで否定する結果ではない。

研究台帳（現時点・production 採用: なし）:
   P1 pace-mixture: 🟡 統計/微小改善・MES未達
   B residual head: 🟡 最強 positive（-3e-4 nats/race・MESの~30%）→ 2027 frozen confirmation 待ち
   P2 attribution: 脚質🟡 / 調教🟡(近年限定・極小) / 厩舎❌ / ペース・ラップ=未検定だった(source)
   H3 新規馬別 pace/lap: H3a❌ / H3b❌（検定力あり genuine null）

### B-2027 confirmation 完全事前登録（scripts/run_residual_head_2027.py・凍結）
Hypothesis: B_RESIDUAL_HEAD_2027_CONFIRM。features=jrdb_idm,jrdb_kishu_idx,jrdb_joho_idx,
  wet_rel_rank,kinryo_per_weight。model=market+linear residual head・L2=1.0。**変更なし**（特徴追加/除外・
  変換・L2・欠測処理・interaction・2027再較正 いずれもしない）。前処理=within-race z-score(build_residual_
  records・B原仕様)。学習=2026-12-31までのJRAデータで**一度だけ**（2026完結後に確定）・係数は2027中固定。
  test=2027 JRAのみ。
  データ完全性ゲート(性能前): year=2027・JRA場01-10・NAR=0・(race_id,馬番)重複0・1着1頭率>=99.5%・
    training max year<test・凍結特徴 coverage>=0.5・2027 records>=100。
  Primary: baseline=市場 / candidate=市場+凍結残差。estimand=race-weighted 平均 ΔNLL(nats/race・負=改善)。
    依存単位=開催場×日 block。centered-null 片側 ASL・B=20,000・seed=0。**m=1（B単独・Holm不要）**。
  Secondary(採用判定でない): ΔECE・会場別ΔNLL(月別は ymd 要)・std(Xθ)・mean|Xθ|・係数・race数・block数。
  事前判定: 🟢 ΔNLL<=-0.001 & CI上限<0 & ΔECE<=+0.005 ／ 🟡 CI上限<0 & -0.001<ΔNLL<0 & ΔECE<=+0.005 ／
    ❌ CI0跨ぎ or ΔNLL>=0 or ΔECE>+0.005。🟢 でも NLL 上の採用候補であり ROI/控除超過/サイジングは別新仮説。
  interim looks=0（2027 全開催終了後に一度だけ）。manifest(artifacts/b_2027_audit.json・gitignore)に
    freeze_commit/feature_definition_hash/training_code_hash/data_fingerprint/generated_feature_hash/
    data_contract_version と gate_checks・audit_result を保存。--evaluate は4ハッシュ＋特徴集合一致で STOP-gate。
  H3 同型の audit-only→evaluate 分離。純部(verdict規則・feature hash)は test_run_residual_head_2027.py 9件。
  **現状は 2027 データ未整備ゆえ --audit-only は gate FAIL（想定どおり）。2027 完結後に audit→evaluate を
    一度だけ。それまで同系統の探索は休止（研究フェーズ (a) 締め）。**

### 2026-08-02(続26) B prospective shadow を今 freeze（層2）＋2027 は層3で併存・共有評価コア
ユーザ指摘: 今日=2026-08-02 で 2027 完結は約17か月先＝遠い。続16 の三層確認どおり層(2) prospective shadow
は freeze 日以降のレースを未見窓に使え、**今すぐ蓄積開始できる**。ユーザ選択=「prospective shadow を今 freeze」。
実装:
 ・run_residual_head_2027.py の評価コアを **fit_and_eval(train,test,feat_cols,l2,n_boot,seed)** へ切出し共有化
   （market vs market+残差・race-weighted ΔNLL・venue×日 block・centered ASL・ΔECE/θ/std(Xθ)/会場別）。
 ・run_residual_head_prospective.py 新設＝**B_RESIDUAL_HEAD_PROSPECTIVE_CONFIRM**（層2）を凍結:
   features/L2/within-race z-score/欠測 は B 原仕様のまま無変更。**freeze_date=2026-08-02** で train(<=)/
   test(>) を **SED ymd を race_id に結合して暦日 split**。**min_test_races=5000（~1.5年）trigger** 未満は
   --evaluate を gate 拒否＝interim looks=0（trigger 到達時に一度だけ）。--audit-only はデータ完全性のみで反復可。
   Primary/判定規則/verdict は 2027 と同一(m=1・B=20,000・seed=0・🟢/🟡/❌)。manifest-bound(4ハッシュ+特徴一致 STOP)。
 ・検定力の事前明記: B の −3e-4 は小さく小窓では block CI が広い→真に −3e-4 でも CI上限<0 に届かない可能性＝
   shadow は早期監視。~1.5年(9年→半幅1.2e-4 から years∝1/half² で ~1.44年)で CI上限<0 が見込める水準に trigger 設定。
 ・層(3) 2027 カレンダー確認(run_residual_head_2027.py)は別レーンで併存・両窓を混同しない。
テスト: test_run_residual_head_prospective.py（freeze split・unmapped除外・trigger gate・凍結spec）＋2027 9件。
   touched 727 passed。現状 test 窓 ≈0（freeze 当日）で gate FAIL＝想定どおり蓄積中。
運用: 定期的に `run_residual_head_prospective.py --audit-only`（データのみ・性能見ない）で trigger 到達を監視、
   到達したら --evaluate を一度だけ。それまで同系統探索は休止。

### 2026-08-02(続27) B 確認ゲートの coverage バグ修正＝絶対閾値→train/test 相対（wet_rel_rank 誤ブロック解消）
prospective --audit-only 実行で判明: coverage ゲートが wet_rel_rank=0.4985 を「不足」と誤ブロック。原因は
 (1)全 featured(pre-2015/NAR 込)を分母にした絶対閾値0.5、(2)wet_rel_rank は wet-track 相対順位で**自然に
 ~50%**（B で検証済・欠測は残差ヘッドが z-score→0 処理）。絶対閾値では B を trigger 到達後も永久ブロック。
修正: 両ゲート(prospective/2027)の coverage を **train窓 vs test窓の相対**へ。新期間の coverage が学習期の
 **0.5倍未満**なら「取込断絶」として blocker、それ以外は自然な部分特徴を罰しない。prospective は record の
 train/test race_id で featured 窓を切り coverage 比較、2027 は 2027 vs pre-2027(JRA)で比較。time-order は
 test 非空時のみ blocker 化。テスト追加(自然部分→非ブロック / 0.8→0.1 断絶→ブロック)。13→計 test 通過。
現状(freeze当日・test=0)は trigger blocker のみで FAIL＝正しく蓄積待ち（coverage 誤ブロックは解消）。

### 2026-08-02(続28) 時系列3分割 standing protocol を明文化（新仮説の検証規律）
ユーザ選択=standing protocol 明文化。dev2015-22/val2023-24(ここで freeze)/burned2025-26(既存仮説で観測済＝
clean test 不可)/test2027+(未接触・一度だけ) を standing 規律として確定。2025-2026 が使えない理由: P1/A/B/
P2/H3 が 2018-2026 を rolling-origin 横断済（B は 2025/2026 fold を明示観測・B 特徴の一部は 2015-2026 から
データ駆動選択）→ 循環になるため。既存仮説(B)の未接触窓は 2027+、加えて freeze 日以降の新規レースは同暦年でも
未接触ゆえ prospective(date粒度)で 2026 後半を回収可。
実装: src/training/_temporal_split.py（phase_of/split_records/phase_counts/assert_clean_final_test=
  test は>=2027のみ・train重複なし・max(train)<min(test)/assert_freeze_before_test=freezeはval まで）。
  run_residual_head_2027 の evaluate に assert_clean_final_test を belt-and-suspenders 追加。
  docs/temporal_split_protocol.md に規律・2025-26不可の理由・新仮説手順(dev探索→val freeze→2027 一度だけ)を明文化。
テスト: tests/training/test_temporal_split.py 9件（境界・split・assert 各種）＋harness 回帰。24 passed。
研究フェーズは引き続き休止。新仮説はこの protocol で dev→val→2027 test。既存 B は 2027/prospective 待ち。

### 2026-08-02(続29) standing protocol の3点修正（ユーザ査読・証拠状態と学習可否を分離）
続28 の protocol を3点訂正（方向性は正しいが厳密化）:
 (1) **burned は「学習禁止」でなく「証拠再利用禁止」**。2025-2026 は評価/選択/独立再現に使えないが、仕様を
     完全 freeze 後の**最終 refit（係数学習）には使える**。→ B の 2027 確認は最終 fit=2015-2026/test=2027 でよい
     （2025-2026 を fit から外さない。ハーネスは既に train=year<2027＝burned 含む refit で正しかった）。
 (2) **2023-2024 は clean validation でない**（P1/A/B/P2/H3 で観測済）。内部選択には使えるが独立一般化証拠に
     しない。2015-2024 を一括 development_known に。複数仮説を同じ 2023-2024 で選び続けると validation 過適合。
 (3) **reserved_test(2027+)は永久 holdout でない**。一度評価すると reserved→evaluated→consumed＝以後 burned。
     次仮説の test は次の未接触窓(2028+)。同じ 2027 を複数仮説に使うなら 2027 を見る前に一括事前登録。
実装訂正: src/training/_temporal_split.py を「証拠状態(phase_of: excluded/development_known/
 burned_for_evidence/reserved_test/consumed_test)」×「学習可否(refit_allowed=test前は全て可・burned含む)」
 の別軸へ再設計。selection_allowed=development_known のみ。assert_clean_final_test は train に burned を許可し
 test は reserved 窓のみ・consumed 再利用不可・時系列。assert_selection_only_on_known / assert_test_after_cutoff
 (日付粒度・freeze cutoff 後のみ prospective) を追加。run_residual_head_2027 に reserved_test_start/
 consumed_test_years/test_tranche_status を manifest 記録＋assert に渡す。docs/temporal_split_protocol.md を
 5区分表(証拠×refit)＋状態遷移＋日付粒度＋assert要件＋B具体形(refit 2015-2026/test 2027/評価後consumed)へ改稿。
 コードは「研究者が窓を見た事実」を自動判定できないため reserved/consumed は ledger/manifest 運用規約とする。
テスト: test_temporal_split.py 10件（証拠状態・refit は burned 含む・selection は known のみ・consumed 再利用
 拒否・次仮説は次tranche・日付 cutoff）＋harness 回帰。25 passed。研究フェーズは引き続き休止。

### 2026-08-02(続30) 取得項目の妥当性・網羅性 監査（3層 + scripts/audit_acquired_items.py）
ユーザ依頼「取得項目の妥当性・網羅性を確認」。3層で監査:
 L1 仕様妥当性(データ不要・本env で判定済): JRDB 15形式すべて **オフセット境界 OK（record 長内・overrun なし）**。
   取得 byte 率: SED99/SRB99/KTA98/UKC97/BAC96/CYB95/KKA94/KSA91/CSA89/KAB88/CHA86/KYI59/TYB51/**SKB9%**。
   SKB は tokki×6/bagu×8/ashimoto_sougou のみ取得＝残りは未 parse（要否確認）。KYI/TYB も約半分未 parse。
 L2 store: 15形式すべて RECORD_TYPES で保存＋parser 対応（ingest は完全 15/15）。行数/年範囲はローカルで。
 L3 featured 実体化（**最重要・定義≠実体化**）: KYI_FEATURE_MAP は 34 の jrdb_* を定義するが、P2/H3 の証拠上
   featured に実在するのは jrdb_idm/kishu_idx/joho_idx/kyakushitsu/kijun_* 程度で、jrdb_ten_idx/pace_idx/
   agari_idx/chokyo_idx/kyusha_idx/pace_hms/gekiso/manken/ninki_idx 等は **ABSENT の疑い**（P2 で
   ペース=SOURCE_MISSING・調教=netkeiba 調教評価_score のみ＝jrdb_chokyo_idx 不在）。attach 未適用/古い
   featured の可能性＝owner_py/race_class と同型の DEAD 再発。要ローカル認定。
 L4 未ブリッジ（保存済だが featured 未橋渡し＝latent）: TYB(直前オッズ/パドック/馬体重)・CYB(追切/仕上/調教
   評価)・CHA(本追切ハロン)・KKA(条件別着度数/父母父連対率)・SRB(ハロンタイム/バイアス)・KAB(馬場差/天候)・
   UKC/KSA/CSA/KTA/BAC。＝ingested-but-unused の大きな伸びしろ。
妥当性リスク: idx/3F の sentinel(-99.9/負) が中央で未クレンジング→materialize した jrdb_ 数値に混入し得る
   （H3 で SED 3F に -89/984/-99.9 を確認・clip は H3 内のみ）。HMS(S=-1/M=0/H=+1)・脚質は単調順序仮定。
実装: scripts/audit_acquired_items.py（L1 純・L2 store・L3 featured 実体化+sentinel・L4 未ブリッジ）。
   L1 は本env で全形式 OK を確認。L2-L4 はローカルで認定（DEAD/ABSENT を最優先）。
結論(現時点): **ingest（parse/store）は妥当かつ網羅的（15/15・境界OK）。網羅性の実質ギャップは (a)featured への
   実体化（定義された jrdb_ 指数の大半が未実体化の疑い）と (b)rich テーブルの未ブリッジ**。次アクションは
   ローカルで audit_acquired_items.py を実行し L3/L4 を数値認定。

### 2026-08-02(続31) 取得項目 監査の実データ結果＝featured 実体化 5/43・根本原因確定（2経路乖離）
audit_acquired_items.py 実行(ユーザ環境):
 L1 妥当性: 全15形式 境界OK(overrun なし)。
 L2 store: KYI/SED/SKB/TYB/CYB/CHA/KKA≈559,700(2015-2026)・HJC/SRB=40,012・BAC=39,977・UKC=61,272・
   KAB=3,342＝良好。**異常: KTA=0行(登録馬・未取得)・KSA=120/CSA=187(2026のみ＝今週分だけ・履歴無し→
   KZA/CZA 全騎手/全調教師が必要)**。
 L3 featured 実体化＝**5/43 のみ OK**（jrdb_idm/jrdb_kishu_idx/jrdb_joho_idx/jrdb_kyakushitsu/
   jrdb_kijun_odds）。**38 ABSENT**: jrdb_ten_idx/pace_idx/agari_idx/chokyo_idx/kyusha_idx/gekiso_idx/
   manken_idx/kishu_kitai_rentai/ninki_idx/sougou_idx/start_idx/deokure_rate/kokyu_flag/nyukyu_days/
   **jrdb_pace_hms/jrdb_kijun_gap/全MySpeed(jrdb_ms_*)/prev_deokure/prev_trouble** 等。
根本原因（確定・2経路乖離）:
 (1) 本線 featured は _adapter→_results_processor:119 が **固定5サブセットのみ注入**（jrdb_idm/kijun_odds/
     kyakushitsu/joho_idx/kishu_idx＝_KYI_INDEX_COLS）。
 (2) 完全38特徴の augment(build_kyi 全KYI_FEATURE_MAP＋build_history prev_*＋build_soten_history MySpeed＋
     attach kijun_gap)は **scripts/jrdb_build_features.py（standalone・別pickle data/featured_jrdb.pkl 出力）
     にのみ存在し本線に未配線**。→ 研究 featured(featured_data.pkl)には 5 しか無い。
**重大な含意（過去結果の再解釈）**: B/P2/H3 は 5 JRDB特徴だけの featured で実施。B の🟡(-3e-4)は
   idm/kishu_idx/joho_idx のみ由来。**JRDB 独自の pace/調教/厩舎/上がり指数(ten_idx/pace_idx/agari_idx/
   chokyo_idx/kyusha_idx/gekiso)と MySpeed＝コードが「市場直交」と重視する当の指数群は featured に一度も
   実体化されず未検証**。P2 の「ペース/ラップ SOURCE_MISSING」「市場が94-98%吸収」もこの5特徴 featured 上の話。
   H3 は SED/KYI store から pace/lap を直接読んで null だったが、JRDB の**自前 pre-race 指数**は別途未検証。
妥当性リスク: sentinel(-99.9/負)の中央未クレンジング（materialize 時に混入し得る）。KSA/CSA 今週分のみ。
結論: ingest は妥当・網羅的(15/15)。**網羅性の実質ギャップは featured 実体化の2経路乖離(5/43)＋rich
   テーブル未ブリッジ＋KTA未取得/KSA・CSA履歴無し**。修正= 完全 augment を本線に配線し38特徴を実体化。
   これは data-basis 修復であり、実体化後は standing protocol(dev2015-24 で選択→2027 confirm)で
   JRDB独自指数を初めて検証できる（未観測ゆえ新仮説として clean）。

### 2026-08-02(続32) 監査 taxonomy 修正＋fail-closed ガード＋lineage doc（ユーザ査読の action plan 着手）
ユーザ査読（結論同意＋厳密化）を反映:
 ・L1「取得byte率」→「**定義byte被覆率(parsed-byte span ratio)**」に改名（取得率/正解率でない）。SKB9%は
   必要な特記のみ意図parseなら異常でない旨明記。
 ・L4「ブリッジ済/未ブリッジ」→ **7状態＋時点クラス**へ改修（MATERIALIZED/IMPLEMENTED_NOT_APPLIED/
   INGESTED_NOT_BRIDGED/SOURCE_EMPTY/INGESTION_MISSING/HISTORICAL_ONLY/OUTCOME_ONLY・時点=direct_current/
   bet_time_contract/historical_only/outcome_only）。KYI/SED/SKB は「ブリッジ済」でなく IMPLEMENTED_NOT_APPLIED。
   KTA=INGESTION_MISSING、KSA/CSA=HISTORICAL_ONLY(2026のみ)。同名 ten_idx/agari_idx が KYI(予想)/SED(実測)
   両在＝列名でなく source×timestamp で時点判定し contract に持たせる。
 ・fail-closed ガード実装: src/training/_feature_materialization.py（assert_features_materialized＝required
   欠落で RuntimeError・optional 欠落はリスト返却で warn）。REQUIRED_JRDB_MIN=現行5(退行検知)・
   EXPECTED_JRDB_FULL=全43(配線後に実体化すべき)。build/train の featured ロード直後に呼ぶ想定。tests 4件。
 ・docs/jrdb_feature_lineage.md: 監査結論・2経路乖離の根本原因・38実体化の lineage 表・修復手順(1 fail-closed→
   2 attach 本線配線→3 feature-only 再監査→4 standing protocol)・妥当性リスク(sentinel/KSA-CSA/KTA/行数差
   anti-join)・優先順位を明文化。
既存結論の限定: 「JRDB探索完了・残るは映像のみ」は**撤回・限定**。正しくは「現 featured 実体化分は探索済・
   定義38特徴＋取得済複数ソースは未実体化/未ブリッジ＝探索空間が実装上閉じていなかった」。B/P2/H3 は使用列に
   ついては有効。次工程は attach/build 経路修復→feature-only 再監査（性能はその後・standing protocol で 2027）。

### 2026-08-02(続33) 既存 standalone augment の実データ検証ツール（本線統合の前段）
ユーザ選択=「既存 standalone をまず実データ検証」。scripts/verify_jrdb_augment.py 新設:
 --augmented PATH（jrdb_build_features.py 出力 pickle を直検証）or --jrdb-dir DIR（その場 augment）。
 EXPECTED_JRDB_FULL(43) 各について 実在/非欠測(JRA2015+)/年別/sentinel率(-99.9,負)/race内分散有率/NaN・inf、
 join 一致率、asof 特徴(jrdb_ms_npast)の leak spot-check、base(現5列)との差分(5→43)を認定。判定=非欠測>=0.3&
 sentinel<0.2&分散有率>0.1。ABSENT 0＆大半 OK なら standalone は 38 を正しく materialize＝本線統合へ。
 性能(ΔNLL)は見ない（standing protocol の 2027 で別途）。fail-closed(REQUIRED充足)も同時確認。tests 29 passed。
次: ローカルで (1) `python scripts/jrdb_build_features.py --jrdb-dir <dir> --out data/featured_jrdb.pkl`
 → (2) `python scripts/verify_jrdb_augment.py --augmented data/featured_jrdb.pkl`。ABSENT/薄い が無ければ
 本線統合(Task#22)へ。それまで研究フェーズ休止・B 2027/prospective 待機は不変。

### 2026-08-02(続34) augment を df-native 化＋verify に --from-store（raw txt 不要で実データ検証）
ユーザが placeholder <JRDB txt dir> をそのまま実行しシェルエラー。実データは既に store 取込済ゆえ raw txt
不要。対応:
 ・src/jrdb/_augment.py を **df-native 化**: build_kyi_from_df / build_history_from_dfs / build_soten_from_df
   を新設（パース済 DataFrame を受ける純transform）。txt 版 build_kyi/build_history/build_soten_history は
   parse→df 版へ委譲。これで store(JrdbStore.read)経路でも本線統合でも同ロジックを再利用可。
 ・scripts/verify_jrdb_augment.py に **--from-store**（JrdbStore から KYI/SED/SKB を読み df-native builder＋
   attach で augment 構築→38特徴を feature-only 認定）。raw txt 不要。--augmented/--jrdb-dir も併存。
 テスト: build_kyi_from_df(jrdb_ten_idx 等 従来ABSENT指数も df から出る＋pace_hms)、build_history_from_dfs
   (deokure/tokki TROUBLE)、build_soten_from_df(MySpeed/npast)。tests/jrdb/test_augment.py 7 passed。
次: ローカルで **`python scripts/verify_jrdb_augment.py --from-store`**（raw txt 不要）→ 38特徴の
   実在/coverage/sentinel/分散/join/leak を認定。ABSENT0＆大半OKなら本線統合(Task#22)へ。性能は 2027 で別途。

### 2026-08-02(続35) --from-store の attach バグ2件修正（umaban dtype／ketto 補完）
verify --from-store で attach が2件で失敗→修正:
 (1) store の umaban は str・featured の _uma は Int64→merge dtype 不一致。build_kyi_from_df で結合キーを
     正準化(race_id→str・umaban→Int64)。txt/store 両経路で統一。
 (2) history/soten は ketto 結合だが netkeiba featured は ketto を持たない→verify に _ensure_ketto
     (KYI race_id,馬番→ketto で base を補完)。本線統合でも要る配線と明記。
 進捗: from-store で KYI jrdb列=34/history 559,700/soten 536,270 まで構築成功。合成 smoke で umaban str→
 attach 通過・jrdb_ten_idx 等が流れることを確認。augment 7 passed。
次: ローカル再実行 `python scripts/verify_jrdb_augment.py --from-store` で 38 の実体化認定（実在/coverage/
 sentinel/分散/join/leak）。ABSENT0＆大半OKなら本線統合(Task#22)へ。

### 2026-08-02(続36) store 由来 KYI 数値の str→数値化＋値一致検証（scale/parse）
verify --from-store で jrdb_kijun_gap 除算が str で失敗→原因: store は SQLite 往復で KYI 数値を
str(arrow large_string)化。build_kyi_from_df に KYI_FEATURE_MAP 全列の to_numeric(coerce) を追加
（txt 経路は parse で数値済＝冪等・store 経路を修正）。smoke で kijun_gap=0.5(3.5/7.0) 確認。
加えて verify に **_agreement_vs_base**: store 由来 kyi と base(adapter経路・既知良好5列)を (race_id,馬番)で
突合し corr/median比を表示（corr≈1&比≈1 で整合・比~10/~0.1 は ZZ9.9 スケール差＝統合前要修正）＝妥当性検証。
augment 7 passed。次: `verify_jrdb_augment.py --from-store` 再実行で 38 実体化＋値一致を認定。

### 2026-08-02(続36-b) 9 DEAD列の lineage 修復＋学習側 allowlist 関門
standalone augment 実データ検証で列実体化は成功(33 OK/0 ABSENT/既知5列 corr=1.0)と認定後、残る
DEAD 10列を**性能でなく lineage**で修復（Task#22・新規性能評価はしない）:
 ・history/soten 8列(prev_deokure/prev_trouble/jrdb_ms_last/mean3/max5/ewm/trend/npast)=HISTORY_
   ATTACH/PIPELINE_FAILED。根本原因は attach の asof 左キー pd.to_datetime(featured.date) が netkeiba
   和暦 'YYYY年MM月DD日' を**全 NaT** 化→空結合。src/jrdb/_augment.py に _to_race_datetime(サンプルで
   明示フォーマット検出・ISO 回帰維持)を追加し両 asof で使用。ketto は _ensure_ketto で補完済。
 ・jrdb_kakutei_bataijuu=WRONG_SOURCE(確定馬体重は発走約15分前確定・KYI では 0/空)。KYI_FEATURE_MAP
   から除去し TYB(bet_time_contract)へ移譲。KYI map 33→32・EXPECTED_JRDB_FULL 43→42。
 ・jrdb_pace_hms=MATERIALIZED_RACE_CONTEXT(場の展開予想=race内定数)＝DEAD でなく正常。audit L4/lineage
   の分類を訂正(MATERIALIZED_RACE_CONTEXT/WRONG_SOURCE を taxonomy に追加)。
学習側 allowlist 関門 src/training/_feature_materialization.py::assert_training_allowlist を追加。本線は
denylist(_DROP_FOR_TRAIN 以外全採用)ゆえ attach 配線後に新規 JRDB 列が silent 混入する→(1)allowlist
未実体化=RuntimeError (2)allowlist 外 jrdb_*/prev_* 混入=RuntimeError で本線統合前に明示決定を強制。
B 凍結は 5特徴のまま不変。tests: 和暦 asof 回帰・kakutei 非KYI・allowlist 4件を追加。全 pass(142)。
push 済(8adda50)。次: 本線統合(Task#22)は allowlist 関門付きで別工程。B 未見確認(Task#21)はデータ待ち。

### 2026-08-02(続36-c) history/soten DEAD の真因は ketto 列衝突（asof スキップ）
verify --from-store 再実行で date有効率 既定=robust=1.000・ketto∩(base,hist)=61,120 なのに prev_*/
jrdb_ms_* が 0.000。前提は満たすのに DEAD＝attach 内バグと切り分け。真因: base が _ensure_ketto で
ketto を持ち、build_kyi_from_df 出力も ketto を持つため KYI merge が **ketto_x/ketto_y に分裂**→
後段 asof の `"ketto" in f.columns` が False→prev_*/jrdb_ms_* を全 NA にしていた。合成再現で確認。
修復: src/jrdb/_augment.py の attach で KYI merge 後に ketto を単一列へ coalesce(base優先→kyi補完)。
回帰テスト test_attach_ketto_collision_still_joins_history を追加(prev_trouble=1・jrdb_ms_last=55)。
続36-b の _to_race_datetime は防御的堅牢化として残置（この実データでは date は元々 OK）。augment 18 passed。
次: ローカルで verify --from-store 再実行→ prev_*/jrdb_ms_* が非欠測>0（初出走のみ NaN）を確認して
history/soten を実体化認定。以降 本線統合(Task#22)は allowlist 関門付きで別工程。

### 2026-08-02(続36-d) history 修復を実データ確認＋verify を3契約に分離
ketto coalesce 修復(4b57018)後の verify --from-store(JRA2015+ 555,128)で 8 history 列すべて実体化:
prev_*=0.891, jrdb_ms_*≈0.887(trend0.693), npast min=1/median=6。npast canary 単調増加＝as-of 行復元健全。
DEAD 完全解消。正確な内訳: 新規37列=ACTIVE28(+既存5=33)/CONTEXT1(pace_hms)/HISTORY8（EXPECTED=42）。
「38 materialize」旧表記は撤回。verify_jrdb_augment.py を刷新: (1)列を3群に割当て群別基準で判定
（ACTIVE=presence+coverage+race分散 / CONTEXT=presence+coverage・分散不問 / HISTORY=semantic coverage・
全欠測は fail-closed）(2)3契約 CURRENT_ACTIVE/CONTEXT/HISTORY を個別 PASS/FAIL 表示 (3)pace_hms は
CONTEXT で「薄い」除外 (4)ketto有効率を all/JRA2015+ eligible に分離表示 (5)jrdb_ms_npast canary
（3走以上馬で target 各走 npast を位置整列で表示）(6)非連番index回帰テスト追加。
_feature_materialization に CURRENT_ACTIVE_JRDB(33)/CONTEXT_JRDB(1)/HISTORY_JRDB(8) を追加(partition=42)。
本線統合判定: current-only ✅(allowlist付) / 完全augment も実データ ✅。B frozen 5特徴のまま。augment19 passed。
次: Task#22 本線配線（allowlist 関門付き）。性能評価は行わない。B 未見(Task#21)はデータ待ち。

### 2026-08-02(続36-e) 42-feature contract 認定確定＋strictly-prior 全件 manifest
ユーザが 42-feature augment contract を正式認定（33 current active / 1 context / 8 history・ABSENT/DEAD 0・
既存5列 corr=1/median比=1・行数不変）。canary の per-horse 確認に加え、本線 artifact 用の**全件 leak 証跡**を追加:
src/jrdb/_leak_audit.py::strictly_prior_join_report（attach と同じ merge_asof backward/exact不可を正規化日付で
再現し future/same_day/exact 参照=0・max_source_date・target/feature_rows・key_duplicates を集計）＋
assert_strictly_prior(fail-closed)。verify --from-store が history/soten 双方で manifest 表示＋未来/同日参照で停止。
test_leak_audit.py 4件 pass。docs/jrdb_feature_lineage.md に確定文＋認定範囲限定（CYB/CHA/TYB/KKA/UKC/SRB/
KSA・CSA/KAB は INGESTED_NOT_BRIDGED のまま＝未網羅）＋学習側 freeze 規則（既存B 5固定・新規 config限定・
欠測は一律0埋めせずモデル別規則を freeze）を明記。jrdb 138 passed。
研究結論: 特徴生成基盤の完成であり新規列の予測価値は未評価。既存 B/P2/H3 不変。新規検定は別仮説で開封前一括登録。
次の分岐: (A) Task#22 本線配線（確定42を allowlist+manifest 付きで統合） / (B) 別ソース(CYB/CHA/TYB/…)の
新規ブリッジ契約に着手。ユーザ確認待ち。性能評価は行わない。

### 2026-08-02(続36-f) Task#22 本線配線: allowlist opt-in＋manifest（既存モデル不変）
ユーザ選択「Task#22 本線配線」。本線の特徴選択が denylist(_DROP_FOR_TRAIN 以外全採用)のため、37新規列を
default featured へ入れると既存 stacking へ silent 混入し性能が変わる（禁止）。既存 denylist を保ったまま
不変にする唯一策＝「新規列を default featured に載せず消費を明示 allowlist の opt-in にする」を採用:
 ・DataSplitter に feature_allowlist=None を追加。None=従来 denylist（既存 B/P2/H3 byte 一致・不変）。
   指定時は構築時に featured を allowlist＋保護列(date/rank/着順/単勝/horse_id/CORNER/leak列)に絞り、
   以降の drop で残るのは allowlist のみ＝新規列 silent 混入なし。未実体化列は assert_training_allowlist
   で fail-closed。tests/training/test_data_splitter_allowlist.py 3件（denylist 不変/allowlist 限定/欠落停止）。
 ・完全 augment(42列)は opt-in build(scripts/jrdb_build_features.py)で生成し、そこに strictly_prior_join_report
   ＋assert_strictly_prior の全件 manifest を出力・fail-closed 検査を配線（history/soten 未来/同日参照=0）。
   default 本線 featured の schema は不変(既存5のみ)。
docs/jrdb_feature_lineage.md 続36-f に設計判断を明記。全テスト 419 passed。Task#22 完了。
新規列の予測価値は未評価＝別仮説として未接触 test tranche 開封前に事前登録の後、性能検証（今は行わない）。
次: Task#21(B 未見確認・データ待ち)、または別ソース(CYB/CHA/TYB/…)の新規ブリッジ契約（ユーザ指示で）。

### 2026-08-02(続36-g) 最後の fail-closed: augment artifact＋allowlist なしを拒否
ユーザ指摘の穴: feature_allowlist=None は denylist のため、完全 augment artifact を従来経路へ渡すと
新規37列が silent 混入する（「denylist で jrdb_ten_idx も入る」smoke がまさにそれ）。修正:
 ・_feature_materialization に LEGACY_JRDB_COLUMNS(既存5)/JRDB_AUGMENT_ONLY_COLUMNS(残り37)＋
   assert_no_unguarded_augment(columns, feature_allowlist): augment 専用列が在るのに allowlist 未指定なら
   RuntimeError("Refusing denylist-based training")。純関数＝将来の非DataSplitter入口にも設置可。
 ・DataSplitter.__init__ で構築時に発火（allowlist 分岐の前）。legacy 5列のみなら従来どおり通す。
 ・artifact 消費契約: build_training_provenance に requested_feature_allowlist / resolved_training_features /
   resolved_training_features_hash / input_feature_schema_id を追加（設定でなく実消費列を後日監査）。
   DataSplitter は resolved_feature_columns / resolved_feature_hash を公開。
 ・docs 修正: 「byte一致」は hash 実比較時のみ→「legacy schema に対する既存挙動を維持」に訂正。ガード行列表も追加。
tests: ガード行列6ケース（legacy通過/augment拒否/allowlist限定/欠落停止/B5厳密/hash安定）＋provenance新規。全423 passed。
Task#22 完全クローズ。方針どおり **研究フェーズ休止**。次は B の reserved tranche 到着時のみ再開（Task#21・データ待ち）。
別ソース(CYB/CHA/TYB…)ブリッジは今行わない（未評価の28+8探索空間が既にあり、開封前登録仮説数を増やさない）。

### 2026-08-02(続37) 「現時点の性能を見たい」→新規42特徴の予測価値を joint 事前登録
ユーザ「現時点の性能を見たい」→対象=「新規42特徴の予測価値」を選択。重要: 今 clean 数値は出せない
（2025-26 burned・2027 reserved 未発生・prospective 未達）。今 burned/reserved で評価すると証拠が焼ける。
→プロトコル順守で **freeze＋事前登録** し tranche 到着時に一度だけ評価する道を実装:
 ・scripts/run_jrdb42_confirm.py（JRDB42_RESIDUAL_2027_CONFIRM）: B と同じ market residual head コア
   (fit_and_eval/verdict/block bootstrap を import 再利用)を特徴集合だけ差し替え。features=CURRENT_ACTIVE(33)
   ＋HISTORY(8)=41（CONTEXT jrdb_pace_hms は within-race softmax で相殺のため除外）。gated audit→evaluate・
   data gate・manifest 一致検証・assert_clean_final_test/assert_jra_only・holm_reject(m=2)。l2=1.0(既定)。
 ・docs/jrdb42_preregistration.md: B と 2027 を共有するため joint 一括登録(Holm family m=2)。MES=0.001 nats/race・
   ECE+0.005・interim=0。freeze 前にユーザ確定2点(features=41でよいか / L2=1.0 か dev-CV 選定)。
 ・tests/scripts/test_run_jrdb42_confirm.py 5件(features 構成/登録不変/Holm 2件/verdict)。全 307 passed。
Task#23 追加。現時点で見られるのは in-sample/development(2015-2024)内部CV の参考値(非証拠)のみ＝希望あれば別途。
次: ユーザが freeze 2点を確定→最初の --audit-only で freeze_commit 固定。数値は tranche 到着時。

### 2026-08-02(続37-b) JRDB42 freeze 2点をユーザ確定＝features=41全 / L2=1.0 事前コミット
ユーザ確定: features=41列すべて(CURRENT_ACTIVE 33＋HISTORY 8・部分集合にしない)、L2=1.0 事前コミット
(development CV 選定はしない)。両方 run_jrdb42_confirm.py の FROZEN 既定と一致＝コード変更不要。
docs/jrdb42_preregistration.md の「未確定2点」を「freeze 決定(確定済み)」へ更新。凍結の時刻印は最初の
--audit-only 実行時の commit を freeze_commit として manifest に刻む（ローカル）。数値は 2027 tranche 到着時に一度だけ。
希望あれば in-sample/development(2015-2024) 内部CV の参考値(非証拠)を別途出せる（未着手・要望次第）。

### 2026-08-02(続37-c) JRDB42 in-sample 参考ハーネス（非証拠・development 2015-2024）
ユーザ要望で「今すぐ見られる」非証拠の参考値ハーネスを用意: scripts/run_jrdb42_insample_reference.py。
development_known(2015-2024)の rolling-origin CV（train=[2015,eval)/test=eval・過去→未来のみ・同年混在なし）。
2025+ は assert_selection_only_on_known で fail-closed。features=41/l2=1.0 は凍結一致。fit_and_eval を再利用
（返り値に生 dnll/blocks を追加＝後方互換）し fold 横断でプールし block_bootstrap_ci を1度。出力は fold別ΔNLL＋
pooled ΔNLL(nats/race)+CI+p_improve+改善fold数。大きな⚠非証拠バナー（selection域・過適合含みうる・見て特徴/L2
を選び直さない）。tests/scripts/test_run_jrdb42_insample_reference.py 3件(leak安全/first_eval/2025排除)。全17 passed。
docs/jrdb42_preregistration.md に実行法を追記。採否判定は 2027 で一度だけ(Holm m=2)＝この参考値では変えない。

### 2026-08-02(続37-d) データ項目監査の残2点: L4 二軸表示＋jrdb_kokyu_flag 診断
ユーザ査読で「ほぼ合格・残るは L4 表示整合と jrdb_kokyu_flag 1列」。対応:
 (1) L4 を二軸化: SOURCE_STATE を (artifact_status, default_status, timing, note) の4項へ。selected artifact
     (完全augment 42列)と default 本線 featured(既存5列)を別列表示＝矛盾解消。KYI=MATERIALIZED(33+1)/既存5、
     SED・SKB=MATERIALIZED_HISTORY/NOT_APPLIED、TYB等=INGESTED_NOT_BRIDGED 両軸。
 (2) scripts/diagnose_feature.py 新設: 1列を年別(value_counts/global unique/非ゼロ率/race内分散有率)＋
     saved artifact vs fresh augment(store) の (race_id,馬番) 一致率・dtype で診断。jrdb_kokyu_flag は
     audit(2020+)vf=0 だが verify(2015+)vf=0.129＝古い年のみ変動の疑い→TEMPORALLY_DEAD/CONTEXT/DEAD/
     SCHEMA_DRIFT/ACTIVE を証拠で確定するため。分類指針を出力。要ローカル。
現状の正式判定: L1✅/L2🟡(KTA=0・KSA/CSA=2026限定は別スコープ)/L3 列存在✅42/42・意味的契約 32/33(kokyu_flag 保留)/
L4 二軸修正済。42-feature augment は認定・未ブリッジsourceは別スコープ・新規特徴の予測価値は未評価。
次: ユーザが diagnose_feature.py --col jrdb_kokyu_flag を実行→年別 vf で確定→CURRENT_ACTIVE を 32 or 33 に確定。

### 2026-08-02(続37-e) jrdb_kokyu_flag=TEMPORALLY_DEAD 確定＝データ項目監査クローズ
diagnose_feature.py --col jrdb_kokyu_flag で確定: 2015-2018 は{0,1,2}・race内分散≈0.34、2019 減衰、
2020-2026 は全行 0(global_unique=1)。saved vs fresh parity 値一致率=1.0000＝SCHEMA_DRIFT でなくデータ自体が
2020 で定数化＝TEMPORALLY_DEAD。test(2027)期は定数ゆえ residual head で θ 寄与≈0＝inert(リーク/有害でない)。
→freeze は 41 維持（データを見て凍結列を削ると selection 汚染）。_feature_materialization に
TEMPORALLY_DEAD_JRDB={jrdb_kokyu_flag} を登録、audit L3 は TDEAD として別計上(ACTIVE 未達に数えない)。
docs/jrdb42_preregistration.md に注記。data 項目監査クローズ: L1✅ / L2🟡(KTA=0・KSA/CSA=2026限定は別スコープ) /
L3 列存在✅42/42・意味的 32 active+1 tdead+1 context+8 history / L4 二軸✅。総括: parser・store・42-feature
augment 認定・未ブリッジsourceは別スコープ・新規特徴の予測価値は未評価。全 8+ tests pass。

### 2026-08-02(続37-f) JRDB42 in-sample 的中率/回収率 参考ハーネス（非証拠）
ユーザ「現時点の取得データによる的中率/回収率を見たい」→選択「新規42特徴の in-sample ROI 参考を新規作成」。
scripts/run_jrdb42_roi_insample_reference.py 作成: NLL 参考の ROI 版。development(2015-2024) rolling-origin で
41特徴 market residual head の勝率 P から2運用点を測る: 本命(argmax P・1点/レース→的中率/回収率＋race block
bootstrap CI)、EV妙味(EV=P·odds>閾値 1.0/1.1/1.2/1.3 の全馬→bets/的中率/回収率)。selection 域限定
(2025+ は assert_selection_only_on_known で fail-closed)、features=41/l2=1.0 凍結一致。_bet_metrics は純ロジック。
限界明記: 非証拠(過適合含みうる)・最終単勝で選択も精算する近似(購入時オッズでない)・市場半強効率で回収率は
≈1−控除率に張り付くのが帰無(負で当然)・採否は ROI family を別途事前登録し 2027 で一度だけ。
tests/scripts/test_run_jrdb42_roi_insample_reference.py 4件(本命=市場favorite/hit_rate/ROI/EV集計/fold域)。全12 pass。
次: ユーザが run_jrdb42_roi_insample_reference.py --featured data/featured_jrdb.pkl を実行→出力を読み解く。

### 2026-08-02(続37-g) ROI 参照の査読対応: paired 比較新設＋CI/用語/帰無表現の修正
ユーザ査読（本命 ROI=0.793 全年赤字・EV 最良 0.943 も赤字・単調でない＝高閾値はノイズ・増分未測定）に対応:
 (1) scripts/run_jrdb42_paired_roi_reference.py 新設: 市場本命/B5(5特徴)/J41(41特徴)を同一fold・同一レースで
     本命1点対置。絶対 ROI＋paired Δ的中率/ΔROI(J−market, J−B)・選択馬一致率・市場本命から変更したレースのみ
     の ROI＋venue×日 block bootstrap の paired ΔROI CI(1レース1点ゆえ per-race 差で厳密)。増分が宿るのは
     変更レースのみと明示。登録条件=ΔROI CI 下限が安定して>0 のときだけ、でなければ ROI 仮説は登録せず閉じる。
 (2) run_jrdb42_roi_insample_reference.py 修正: EV に _ratio_block_ci(venue×日 block 単位で ROI=Σ払戻/Σ購入を
     再計算・bet 独立 bootstrap 禁止)を追加、「in-sample」→「development rolling-origin OOF reference」、
     帰無表現を「既検証経路では実用的控除超過を確認していない＝回収率≈控除後を作業帰無参照(半強効率の証明でない)」へ。
 (3) kokyu_flag parity は解決済み(値一致率=1.0000・旧0.129 は 2015+ 集計 vf)＝freeze blocker なし。表記
     「41-column(artifact 上 40 変動)」。docs/jrdb42_preregistration.md に ROI 位置づけ・登録条件・bet-time 契約
     (feature_available_at<=bet_decision_at/selection_odds=bet時点/settlement=確定払戻)・用語を追記。
tests: paired 2件＋ratio_block_ci 1件追加。全 pass。現状結論: 41列の絶対 ROI は赤字・市場/5特徴への増分は
paired で要測定・実運用は最終オッズゆえ未評価。次: ユーザが paired 比較を実行→ΔROI CI で ROI 登録可否を判断。

### 2026-08-02(続37-h) ROI 仮説=登録せずクローズ（paired 診断で増分なし）
run_jrdb42_paired_roi_reference.py 実行結果(development OOF・24,184レース・本命1点): 絶対 ROI market 0.790/
B5 0.789/J41 0.793(全て控除後・赤字)。paired ΔROI: J41−market=+0.0026 CI[-0.0024,+0.0076]、J41−B5=+0.0039
CI[-0.0007,+0.0085]、変更レースのみ(n=596)+0.1057 CI[-0.0965,+0.3040]＝**全て 0 を跨ぐ**。選択馬一致率
vs market 0.975/vs B5 0.976(J41 はほぼ市場本命追従・L2 が θ 縮約)。事前登録ルール(ΔROI CI 下限が安定して>0
のときだけ登録)により **ROI 仮説は 2027 family に登録せずクローズ**(ユーザ推奨どおり)。
これは NLL の JRDB42_RESIDUAL_2027_CONFIRM(2027 queued)を変えない(ROI と NLL は別問題)。非証拠ゆえ
「ROI ゼロの証明」でなく「clean ROI 試験を登録するに足る in-sample 増分なし」。将来 bet-time(TYB)契約/
未ブリッジ source で見込みが出れば別途事前登録。docs/jrdb42_preregistration.md に判定表を記録。
現状の全体: データ項目監査クローズ・42-feature 実体化認定・ROI 登録せず閉・NLL(B/JRDB42)は 2027 queued・
研究フェーズ休止。次アクションは 2027 tranche 到着時のみ(Task#21/#23)。

### 2026-08-02(続37-i) 券種ROI: 全戦略赤字なら S0(賭けない)を選ぶよう walk-forward 修正
ユーザ指摘「一番多いのは S0 では？」＝全15戦略が ROI<100%・CI下限<1（黒字ゼロ）ゆえ、正解は「賭けない
(S0_skip)」。純損益で S0=0 > あらゆる賭け(負)。従来 walk-forward は elig を n_bets>=100 で絞り S0 を除外し
max(roi_ex) で「負けの中で最良」の券を必ず選ぶ欠陥→単勝1位(train79.3%)を選び test 72.6% も赤字。修正:
select_bet_or_skip(純関数)を新設し「点数十分な賭ける戦略のうち除最大ROI最大が、除最大ROI≥1.0 かつ CI下限>1.0
(統計的に黒字)のときだけ採用。無ければ S0(賭けない)」。walk-forward はこれを使用。要約文も「proven edge が
無いため S0 が選ばれるのが正しい挙動＝資金保全が最適」に修正。tests/scripts/test_sim_ticket_strategy_roi.py 4件
(全赤字→S0/proven edge→採用/高ROIだがCI無有意→S0/点数不足→S0)。全 pass。
実データ結果の解釈: 単複~0.75-0.79・馬連馬単ワイド0.56・三連複0.46・三連単0.47・全ポートフォリオ0.499＝全て控除
負け。walk-forward 2025→2026 も単勝1位 test 72.6%(CI[0.56,0.90])で赤字→修正後は S0(賭けない)を選ぶ。市場効率の
壁の再確認。ROI 仮説は既に登録せずクローズ済(続37-h)と整合。

### 2026-08-02(続38) 目的関数比較 実験の設計固め（未実装・design only）
ユーザがコンペ上位手法の総説を提示。照合結果: 大半は既実装（レース内z-score=build_residual_records、
market logit offset=residual head、walk-forward/purge=_temporal_split、EWMA=jrdb_ms_*、calibration、
block bootstrap、LogLoss改善だがROI控除未満=本プロジェクト所見）。真に新規は (1)ランキング目的関数
(LambdaRank/rank_xendcg・現行は全て objective=binary)、(2)時系列OOF Target Encoding のみ。
ユーザ選択「まだ開始しない（設計のみ先に固める）」→ docs/objective_comparison_design.md を作成:
 ・問い: 目的関数を listwise/ranking に変えると binary GBDT よりレース内 LogLoss/較正/NDCG が
   development で一貫改善するか。位置づけ=selection(2015-2024・tranche 非消費)、勝者は 2027 別途事前登録。
 ・固定軸: LightGBM 単一・purged walk-forward(rolling_folds)・特徴=production数値+レース内z/rank/top差+log q・
   OOF isotonic・共有ハイパラ・固定seed。primary=objective∈{binary/lambdarank/rank_xendcg}（同一lib・目的だけ差替）。
 ・指標: LogLoss(nats/race)/ECE/レース内NDCG@1,@3/Top-1/edge分位別ROI(非証拠)/年別再現性。
 ・判定: binary比 ΔLogLoss の venue×日 block bootstrap paired CI 上限<0 & |Δ|≥MES0.001 & ΔECE≤+0.005 &
   年別符号過半 & NDCG矛盾なし。多重性 Holm m=2({lambdarank,xendcg} vs binary)。ROI は判定に使わない。
 ・スコープ外: TE(別軸)・ROI採否(非証拠)・NN。再利用資産と実装チェックリストを明記。
未実装。次: ユーザが設計を承認/修正→凍結→run_objective_comparison.py 実装（development のみ）。
