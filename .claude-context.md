# Claude コンテキスト — keibam プロジェクト

最終更新: 2026-06-11

---

## 1. プロジェクト概要

netkeiba のレースデータを収集・前処理し、LightGBM × ニューラルネットのスタッキングで
勝率を予測、期待値ベースで馬券を選定・資金配分するシステム。

---

## 2. 作業ブランチ

```
claude/festive-dijkstra-7bccs9
```

（前ブランチ `claude/trusting-noether-xJD0m` は main にマージ済み）

---

## 3. 実装済みの修正・機能

### 3-1. ライブ予想パイプライン（出馬表→特徴量→スコア）

- `src/preparing/_scrape_shutuba.py` を新規実装
  - `scrape_race_id_race_time_list(kaisai_date)` — 開催日の race_id と発走時刻
  - `create_active_race_id_list()` — 本日開催で馬体重発表済みのレースだけ抽出
  - `scrape_shutuba_table(race_id, date_str, filepath)` — DataFrame を pickle 保存
- `ShutubaDataMerger.__init__` に `_separated_hr_with_sire_dict` の初期化を追加
- `_merge_horse_info` / `_merge_peds` で `horse_id` の dtype 不一致（str/int64）を `astype(str)` で解消
- `FeatureEngineering.dumminize_kaisai` の `place`/`time` 列 drop を `errors="ignore"` に変更
- `FeatureEngineering` に `dumminize_ground_state()`（単一列版）を追加
- `__label_encode` のラベルエンコードマスタで重複インデックスを drop して `InvalidIndexError` を解消

### 3-2. スコア計算（バックテスト・ライブ共通）

- `src/policies/_score_policy.py` の `_calc` を、race_id がカラムでもインデックスでも動作するよう改修
- LightGBM モデルが generic 名 (`Column_0..Column_N`) で保存されている場合のフォールバック対応

### 3-3. 払戻データ（旧フォーマット対応）

- 2020 年以前の払戻 raw データは行区切りが `br` ではなく半角空白
- `src/preprocessing/_return_processor._build_bet_df` の先頭に正規化処理を追加

### 3-4. race_id_list のキャッシュ破壊問題

- `_filter_by_years` を多段フォールバック付き `_filter_requested` に置き換え
- フィルタ後の縮小データを `to_pickle` で書き戻していた行を削除し、全件 pkl を保護

### 3-5. ノートブック改善（main.ipynb）

- 各シミュレーション保存セルの保存先を `models/<today>/...` に動的化
- モデルロードを `glob` で最新ディレクトリ自動検出に変更

### 3-6. Phase 1: raw データの SQLite 永続化（`src/storage/`）**完了**

pickle が消えると全件 re-scrape が必要という問題を解消するため、
SQLAlchemy + SQLite (`data/keibam.db`) で raw データを永続化する。

**設計方針**:
- `INSERT OR IGNORE`（冪等 upsert）で既存行を上書きしない
- pickle はキャッシュとして残す
- pickle が消えた場合は DB から再生成
- 初回起動時に既存 pickle を DB へ自動移行（`auto_migrate_if_empty`）

**スキーマ（7テーブル）**:

| テーブル | 主キー | index_col |
|---|---|---|
| `raw_results` | (race_id, 馬番) | race_id |
| `raw_race_info` | (race_id,) | race_id |
| `raw_return_tables` | (race_id, row_idx) | race_id |
| `raw_horse_results` | (horse_id, 日付) | None |
| `raw_horse_info` | (horse_id,) | None |
| `raw_peds` | (horse_id,) | None |
| `raw_odds_snapshots` | (race_id, captured_at, bet_type, combo) | None |

**テスト確認**: pytest 590 件 pass、ruff clean、lint-imports 4 contracts kept

### 3-7. Phase 2: featured_data DB 永続化 **完了**

- `src/storage/_db.py` の `TABLE_SPECS` に `featured_data` テーブルを追加
  - PK: `(race_id, 馬番)`
- `src/constants/_local_paths.py` の `PICKLE_PATH_TO_ALIAS` に `FEATURED_DATA_PATH → "featured_data"` 追加
- `src/pipeline/run_pipeline.py` / `_ingestion.py` で `featured_data.to_pickle(...)` 直後に upsert 追加
- `src/preprocessing/_abstract_data_processor.py` に `repo` / `alias` keyword-only 引数を追加
  - 既存サブクラスは後方互換（`filepath` 位置引数はそのまま）

### 3-8. バックテスト可視化ページ（`app/pages/5_backtest.py`）**完了**

- タブ 1: フルシミュレーション — EV 閾値スライダー + 累積損益折れ線グラフ + per_race テーブル
- タブ 2: 確信度スイープ — 閾値 1.0〜2.5 での回収率/的中率/最大ドローダウン多軸グラフ
- `app/_model_eval.py` に `compute_full_backtest()` / `compute_confidence_sweep()` を追加
- `app/_data_loader.py` に `load_db_stats()` を追加
- `app/pages/1_dashboard.py` に DB テーブル統計セクションを追加
- `src/simulation/_plot.py` に `plot_confidence_sweep()` を追加

### 3-9. 過去データ一括取得スクリプト（`scripts/fetch_history.py`）**完了**

月単位または年単位で過去レースデータをスクレイピングするスクリプト。

```bash
# 月単位（推奨）
python scripts/fetch_history.py --from 2008-01 --to 2008-03
python scripts/fetch_history.py --from 2008-01 --to 2008-01 --no-tables  # 取得のみ

# 年単位（後方互換）
python scripts/fetch_history.py --from-year 2000 --to-year 2025
```

- 失敗月のロールバック（その月の bin ファイルを削除して再試行を促す）
- `--stop-on-error` で即座に停止
- テーブル生成は全月完了後に一括実行（二重取込防止）

### 3-10. スクレイピングパイプライン多数のバグ修正 **完了**

**修正した主なバグ**:

| ファイル | 修正内容 |
|---|---|
| `src/preparing/modules.py` | `Unnamed:` 列除去（storing_process / csv_reader / transfer_temp_file）|
| `src/preparing/modules.py` | `copy_files` が既存 pkl を上書きしてデータ消失 → マージに変更 |
| `src/preparing/modules.py` | `get_kaisai_date_list` の月ゼロ埋め（`01` → `1`）と空ページ検出 |
| `src/preparing/modules.py` | `scrape_race_id_list` が kaisai_date をインデックスに保存 → 名前付き列 `{"kaisai_date":..., "race_id":...}` に変更 |
| `src/preparing/modules.py` | `scrape_html` でブロック時に 39 バイト空 bin が作られる問題を修正 |
| `src/preparing/modules.py` | 古い temp CSV が再利用される stale 問題 → 処理開始時に削除 |
| `src/preparing/modules.py` | race_info パースの `NoneType.group()` / `"障害" in None` クラッシュを修正 |
| `src/preparing/_scrape_kaisai_date.py` | キャッシュから日付フィルタなしで全件返す問題 → `_filter_range` を両コードパスに適用 |
| `src/preparing/_scrape_race_id_list.py` | `_normalize_date` の NaN/float クラッシュを修正 |
| `src/preparing/_scrape_race_id_list.py` | `_kaisai_date_column` で kaisai_date 列を確実に識別 |
| `src/constants/_master.py` | 2019 年以前のレースクラス `500万下` / `1000万下` / `1600万下` を追加 |

**レート制限対策**:

- `is_blocked_html(html)` — 39 バイト空ページを検出
- `_fetch_with_retry()` — ブロック検出時に指数バックオフ（10→20→40→80s）でリトライ
- 環境変数による調整:
  - `KEIBA_SCRAPE_DELAY` — リクエスト間隔（秒）
  - `KEIBA_SCRAPE_MAX_RETRY` — 最大リトライ回数
  - `KEIBA_SCRAPE_BACKOFF` — バックオフ倍率
  - `KEIBA_SCRAPE_ABORT_AFTER` — ブロック検出後の即時停止回数

---

## 4. 既知の未解決事項

### 4-1. `test_horse_id_in_peds` のテスト失敗（pre-existing）

- `tests/preparing/test_scrape_html_ped.py::test_horse_id_in_peds` が失敗
- 原因: `results.pkl` の `horse_id` 5855 件が `peds.pkl` に存在しない（peds の scrape が不足）
- 対処: 不足している horse_id の血統データを re-scrape する必要がある

### 4-2. mypy エラー（pre-existing、8 ファイル）

- `src/training/_data_splitter.py` の `lgb.Dataset` など LightGBM 4.x 変更に伴うエラー

### 4-3. `ingest` CLI の動作確認未完了

- Playwright を使う scrape がメモリを大量消費する可能性
- ローカル環境（Playwright 導入済み）での確認が必要

### 4-4. 月次 race_id_list.pkl の確認未完了

- netkeiba のレート制限中に `data/html/race_id_list/race_id_list.pkl` に 6958 行（2008 年全体）の誤データが残った状態
- **次の実行前に削除が必要**:
  ```bash
  rm -f data/html/race_id_list/race_id_list.pkl
  rm -f data/html/race_id_list/race_id_list.pkl.bak
  ```
- 39 バイト空 bin も削除してからリトライ:
  ```bash
  find data/html/race -name "*.bin" -size 39c -delete
  ```
- ブロック解除後（数時間〜1日程度）に再実行して月単位フィルタが正しく機能するか確認

---

## 5. 次に着手すべきタスク

### 優先度 高

1. **過去データ取得の完走確認**（レート制限解除後）
   - `python scripts/fetch_history.py --from 2008-01 --to 2008-01 --no-tables`
   - 期待: 約 200〜400 件の race_id（2008 年 1 月分のみ）
   - kaisai_date 列が race_id_list.pkl に保存されていること
   - bin ファイル数が race_id 数と概ね一致すること

2. **2000 年〜2025 年の全データ取得**
   - 確認が取れたら年単位で順次実行
   - `python scripts/fetch_history.py --from-year 2000 --to-year 2025`

### 優先度 中

3. **特徴量生成・モデル学習**
   - 過去データが揃ったら `run_pipeline.py` で特徴量 pkl と DB を更新
   - バックテストページで実際のシミュレーション確認

4. **DB 初期移行（既存 pickle → SQLite）**
   - 既存の `data/raw/*.pkl` を DB に取り込む（Phase 1 完了の恩恵を受けるため）

5. **血統データ補完**（4-1 の解消）
   - `horse_id` 5855 件の peds を re-scrape

### 優先度 低

6. **mypy エラーの解消**（4-2）
7. **docs の整備**

---

## 6. ダッシュボード（Streamlit）の注意点

- `特徴量データ ✗` → `data/raw/featured_data.pkl` が存在しない（FeatureEngineering を実行する必要あり）
- `オッズスナップ ✗` → `data/raw/odds_snapshots.pkl` が存在しない（段階オッズ収集 cron が未実行）
- ダッシュボードはデータ収集・学習のコントロール機能を持たない（CLI / ノートブック経由で実施）
- `5_backtest.py` ページが新規追加済み（モデル pkl があれば動作する）

---

## 7. テスト・CI

```bash
python -m pytest tests/ -q          # 590+ 件
python -m ruff check src/ app/
python -m mypy --config-file pyproject.toml
lint-imports
```

---

## 8. よく使うコマンド

```bash
# ブランチ確認
git branch --show-current

# 過去データ取得（月単位・推奨）
python scripts/fetch_history.py --from 2008-01 --to 2008-01 --no-tables  # テスト
python scripts/fetch_history.py --from 2008-01 --to 2008-03              # 本番
python scripts/fetch_history.py --from-year 2000 --to-year 2025          # 全件

# ネットワーク前の事前確認
find data/html/race -name "*.bin" -size 39c | wc -l    # 空 bin 数
find data/html/race -name "*.bin" -size 39c -delete    # 空 bin 削除
rm -f data/html/race_id_list/race_id_list.pkl           # stale pkl 削除

# 日次取込
python -m src.pipeline.run_pipeline ingest --race-id 202401010101

# 強制上書き取込（誤情報修正時）
python -m src.pipeline.run_pipeline ingest --race-id 202401010101 --force

# 週次再学習
python -m src.pipeline.run_pipeline retrain

# ダッシュボード起動
streamlit run app/Home.py

# 初期 DB 移行（既存 pickle → DB 一括取込）
python -c "
from src.storage import RawDataRepo
from src.constants._local_paths import LocalPaths
repo = RawDataRepo()
for alias, path in [
    ('raw_results', LocalPaths.RAW_RESULTS_PATH),
    ('raw_race_info', LocalPaths.RAW_RACE_INFO_PATH),
    ('raw_return_tables', LocalPaths.RAW_RETURN_TABLES_PATH),
    ('raw_horse_results', LocalPaths.RAW_HORSE_RESULTS_PATH),
    ('raw_horse_info', LocalPaths.RAW_HORSE_INFO_PATH),
    ('raw_peds', LocalPaths.RAW_PEDS_PATH),
]:
    print(alias, repo.auto_migrate_if_empty(alias, path))
"

# featured_data DB 移行
python -c "
from src.storage import RawDataRepo
from src.constants._local_paths import LocalPaths
repo = RawDataRepo()
n = repo.auto_migrate_if_empty('featured_data', LocalPaths.FEATURED_DATA_PATH)
print('migrated rows:', n)
"

# import テスト（修正後の動作確認）
python -c "
from src.preparing.modules import is_blocked_html, _fetch_with_retry
from src.preparing._scrape_kaisai_date import scrape_kaisai_date
from src.preparing._scrape_race_id_list import scrape_race_id_list, _filter_requested
from src.constants._master import Master
m = Master()
print('500万下 in RACE_CLASS_LIST:', m.RACE_CLASS_500 in m.RACE_CLASS_LIST)
print('is_blocked_html:', is_blocked_html('<html><head></head><body></body></html>'))
print('all imports OK')
"
```

---

## 9. プロジェクト構成（レイヤ依存）

```
src/
├─ constants/       定数・列名・閾値・馬券種・ログ設定（最下層）
├─ storage/         raw データ SQLite 永続化（Phase 1 完了）
├─ preprocessing/   前処理（結果/払戻/血統/特徴量エンジニアリング）
├─ preparing/       データ収集アダプタ（Playwright スクレイパ・オッズ取得）
├─ policies/        馬券/スコア戦略・Harville 確率・オッズ供給
├─ training/        学習器（KeibaAI / スタッキング / sample weights）
├─ portfolio/       資金配分（ケリー）
├─ simulation/      バックテスト・指標・可視化
├─ operation/       運用モード・発注アダプタ・設定
└─ pipeline/        継続学習 CLI（ingest / retrain）
```

レイヤ依存の方向:
```
constants → storage → preprocessing / preparing → policies → training → portfolio → simulation → operation → pipeline → ui
```

---

## 10. スクレイピング実装メモ

### netkeiba URL 構造

| 用途 | URL |
|---|---|
| カレンダー（開催日一覧） | `race.netkeiba.com/top/calendar.html?year=YYYY&month=M` |
| レース一覧（開催日指定） | `race.netkeiba.com/top/race_list.html?kaisai_date=YYYYMMDD` |
| レース結果 | `db.netkeiba.com/race/RRRRRRRRRRRR` |

### race_id の構造

```
年(4桁) + 場コード(2桁) + 開催回(2桁) + 日目(2桁) + R番号(2桁) = 12桁
例: 200801060101 = 2008年, 場06(中山), 1回, 6日目, 1R
```

### レート制限の挙動

- 多数リクエスト後に 39 バイト空 HTML `<html><head></head><body></body></html>` が返る
- ブロック解除まで数時間〜1日程度かかることがある
- 対策: `KEIBA_SCRAPE_DELAY` 環境変数でリクエスト間隔を広げる（推奨: 3〜5 秒）

### 月ゼロ埋め注意

カレンダー URL の `month` パラメータはゼロ埋めなし（`month=1`、`month=01` は NG）
→ `modules.get_kaisai_date_list` で `str(int(month))` に変換済み
