# Claude コンテキスト — keibam プロジェクト

最終更新: 2026-06-10

---

## 1. プロジェクト概要

netkeiba のレースデータを収集・前処理し、LightGBM × ニューラルネットのスタッキングで
勝率を予測、期待値ベースで馬券を選定・資金配分するシステム。

---

## 2. 作業ブランチ

```
claude/laughing-ramanujan-qsb9ot
```

---

## 3. 実装済みの修正・機能

### 3-1. ライブ予想パイプライン（出馬表→特徴量→スコア）

- `src/preparing/_scrape_shutuba.py` を新規実装
  - `scrape_race_id_race_time_list(kaisai_date)` — 開催日の race_id と発走時刻
  - `create_active_race_id_list()` — 本日開催で馬体重発表済みのレースだけ抽出
  - `scrape_shutuba_table(race_id, date_str, filepath)` — DataFrame を pickle 保存
- `ShutubaDataMerger.__init__` に `_separated_hr_with_sire_dict` の初期化を追加
- `_merge_horse_info` / `_merge_peds` で `horse_id` の dtype 不一致（str/int64）を `astype(str)` で解消
- `FeatureEngineering.dumminize_kaisai` の `place`/`time` 列 drop を `errors="ignore"` に変更
- `FeatureEngineering` に `dumminize_ground_state()`（単一列版）を追加
- `__label_encode` のラベルエンコードマスタで重複インデックスを drop して `InvalidIndexError` を解消

### 3-2. スコア計算（バックテスト・ライブ共通）

- `src/policies/_score_policy.py` の `_calc` を、race_id がカラムでもインデックスでも動作するよう改修
  - `groupby` のキー選択 / `wakuban_flag` の結合を条件分岐
- LightGBM モデルが generic 名 (`Column_0..Column_N`) で保存されている場合の対応
  - try/except で直接 `predict_proba(X_pred)` を試し、shape 不一致時のみ位置/名前ベースでフォールバック

### 3-3. 払戻データ（旧フォーマット対応）

- 2020 年以前の払戻 raw データは行区切りが `br` ではなく半角空白
- `src/preprocessing/_return_processor._build_bet_df` の先頭に正規化処理を追加
  - `' - '` → `-`、`' → '` → `→`、残った ` ` → `br` に置換

### 3-4. race_id_list のキャッシュ破壊問題

- `src/preparing/_scrape_race_id_list.py` の `_filter_by_years` を多段フォールバック付き `_filter_requested` に置き換え
- フィルタ後の縮小データを `to_pickle` で書き戻していた行を削除し、全件 pkl を保護

### 3-5. ノートブック改善（main.ipynb）

- 各シミュレーション保存セルの保存先を `models/<today>/...` に動的化
- モデルロードを `glob` で最新ディレクトリ自動検出に変更
- `BetPolicyTanshoFukusho` を追加
- `race_id_list[0]` → `race_id_list.iloc[0]["race_id"]`（戻り値が DataFrame のため）

### 3-6. README.md 更新

- `## 8. 最近の主要修正` セクションを追加（8-1〜8-6 を記載）

### 3-7. Phase 1: raw データの SQLite 永続化（`src/storage/`）**完了**

**目的**: pickle が消えると全件 re-scrape が必要という問題を解消。
SQLAlchemy + SQLite (`data/keibam.db`) で raw データを永続化する。

**設計方針**:
- `INSERT OR IGNORE`（冪等 upsert）で既存行を上書きしない
- pickle はキャッシュとして残す（既存 Processor が `pd.read_pickle()` で読んでいるため）
- pickle が消えた場合は DB から再生成 → 再ダウンロード不要
- 初回起動時に既存 pickle を DB へ自動移行（`auto_migrate_if_empty`）
- 誤情報修正時は `--force` フラグで該当行を削除してから再 scrape

**スキーマ（7テーブル）**:

| テーブル | 主キー | index_col |
|---|---|---|
| `raw_results` | (race_id, 馬番) | race_id |
| `raw_race_info` | (race_id,) | race_id |
| `raw_return_tables` | (race_id, row_idx) | race_id |
| `raw_horse_results` | (horse_id, 日付) | None |
| `raw_horse_info` | (horse_id,) | None |
| `raw_peds` | (horse_id,) | None |
| `raw_odds_snapshots` | (race_id, captured_at, bet_type, combo) | None |

### 3-8. `ingest` CLI のバグ修正（2026-06-10）

`python -m src.pipeline.run_pipeline ingest --race-id <race_id>` の動作確認で
複数のバグを発見・修正した。修正コミットは `claude/laughing-ramanujan-qsb9ot` ブランチに積み上げ済み。

#### 修正一覧

| # | ファイル | 内容 |
|---|---|---|
| 1 | `src/preprocessing/_results_processor.py` | `_select_columns` で race_id がインデックスのみの pickle を読むと `df[["race_id"]]` が KeyError → `reset_index()` で列に復元 |
| 2 | `src/pipeline/run_pipeline.py` | `_ScrapingFetcher.fetch_results/fetch_race_info` が `preprocessed_data` を返して pickle を上書き → raw スキーマ（`馬体重` 等）が消える。`load_raw` で直接読み `race_id` をインデックスに正規化して返すよう変更 |
| 3 | `src/preprocessing/_data_cleaner.py` | pandas 2.x で `Float64` 列に `.astype(str).str.split(".")` が `AttributeError` → `.fillna(0).astype(float).astype(int).astype(str).str.zfill()` に変更 |
| 4 | `src/preprocessing/_data_merger.py` | `_merge_race_info` で `results`（str）と `race_info`（int64/float64）の race_id インデックス型不一致でジョイン全 NaN → 両インデックスを `astype(str).str.replace(r'\.0$', '')` で正規化 |
| 5 | `src/storage/_repo.py` | `upsert()` で RangeIndex を `spec.index_col` にリネームしてから実際の race_id 列を drop → DB に行番号が race_id として保存。`index_col` が既に列にある場合は `reset_index(drop=True)` で RangeIndex を捨てる3分岐に変更 |
| 6 | `src/pipeline/_ingestion.py` | `IngestJob.run()` で `existing_results` が RangeIndex のまま `existing_race_ids` を返すため `find_new_race_ids` が常に全レースを新規判定し `append_idempotent` が全行追記 → race_id を index に正規化してから比較 |

#### pickle の正しい形式（重要）

- `results.pkl` / `race_info.pkl` はスクレイピング直後: **RangeIndex + race_id 通常列（int64 or float64）**
- `ResultsProcessor` / `RaceInfoProcessor` が `set_index("race_id")` して加工済みデータを返す
- `upsert()` はこの RangeIndex + race_id 列形式を正しく扱えるよう修正済み
- DB からの `read()` は race_id を index + 列の二重構造で返す（`ResultsProcessor._select_columns` 互換）

#### DB データの汚染について

- `auto_migrate_if_empty` を `upsert` バグ修正前に実行した場合、DB の `raw_results` に race_id = 0, 1, 2... （行番号）が保存されている
- DB 復元（`RawDataRepo().read('raw_results')`）は信頼できない状態
- `results.pkl.bak`（2026-06-10 12:10 作成）が正常なバックアップ → DB 復元より .bak からの `cp` を優先すること

---

## 4. 既知の未解決事項

### 4-1. `test_horse_id_in_peds` のテスト失敗（pre-existing）

- `tests/preparing/test_scrape_html_ped.py::test_horse_id_in_peds` が失敗
- 原因: `results.pkl` の `horse_id` 5855 件が `peds.pkl` に存在しない（peds の scrape が不足）
- 対処: 不足している horse_id の血統データを re-scrape する必要がある

### 4-2. mypy エラー（pre-existing、8 ファイル）

- `src/training/_data_splitter.py` の `lgb.Dataset` など LightGBM 4.x 変更に伴うエラー
- Phase 1 の変更とは無関係

### 4-3. `ingest` CLI の動作確認未完了（ローカルで継続中）

- ローカル（WSL2/Ubuntu-26.04）で `python -m src.pipeline.run_pipeline ingest --race-id 202401010101`
  を実行中だが WSL2 が不安定（接続切断）で完了確認できていない
- `_FullPipelineBuilder.build()` の DataMerger 処理（horse_results の分離・マージ）が重く数分かかる
- 上記バグ修正（3-8）を適用した状態での最終確認が必要

### 4-4. `race_info.pkl` のスクレイピングが不完全

- `results.pkl`: 11774 行 / 11672 unique race_id
- `race_info.pkl`: 858 件のみ（results の約 7%）
- 858 件以外のレースは `date` が NaN → `DataMerger._separate_by_date` でスキップ
- `featured_data.pkl` の対象レースが 858 件相当に限定される
- 対処: `race_info` の再スクレイピングが必要（全 race_id 分の HTML を取得）

### 4-5. DB の `raw_results` データ汚染

- `upsert` バグ（3-8 修正 #5）修正前に `auto_migrate_if_empty` を実行した場合
  DB に race_id = 0, 1, 2... が保存されている
- DB をクリアして `results.pkl.bak` から再移行が必要
  ```bash
  rm data/keibam.db
  python -c "
  from src.storage import RawDataRepo
  from src.constants._local_paths import LocalPaths
  repo = RawDataRepo()
  repo.auto_migrate_if_empty('raw_results', 'data/raw/results.pkl')
  repo.auto_migrate_if_empty('raw_race_info', LocalPaths.RAW_RACE_INFO_PATH)
  "
  ```

---

## 5. 次に着手すべきタスク（優先順）

### 5-1. `ingest` CLI の動作完全確認（最優先）

ローカルで以下を実行して `status: ok` かつ `featured_data build failed` なしを確認する：

```bash
cp data/raw/results.pkl.bak data/raw/results.pkl
python -m src.pipeline.run_pipeline ingest --race-id 202401010101 2>&1
```

期待ログ:
```
INFO separating horse results by date
100it [...]
INFO merging horse_results
100it [...]
INFO [ingest] {'status': 'ok', 'n_new': 1, 'n_total': ..., 'ingest_time': '...'}
```

### 5-2. DB の再移行（`upsert` バグ修正後）

```bash
rm data/keibam.db
python -c "
from src.storage import RawDataRepo
from src.constants._local_paths import LocalPaths
repo = RawDataRepo()
for alias, path in [
    ('raw_results', 'data/raw/results.pkl'),
    ('raw_race_info', LocalPaths.RAW_RACE_INFO_PATH),
    ('raw_return_tables', LocalPaths.RAW_RETURN_TABLES_PATH),
    ('raw_horse_results', LocalPaths.RAW_HORSE_RESULTS_PATH),
    ('raw_horse_info', LocalPaths.RAW_HORSE_INFO_PATH),
    ('raw_peds', LocalPaths.RAW_PEDS_PATH),
]:
    print(alias, repo.auto_migrate_if_empty(alias, path))
"
```

### 5-3. `race_info.pkl` の再スクレイピング

858 件しかない race_info を全 race_id 分補完する。
`results.pkl` の全 race_id に対して HTML をダウンロード → `TableCreator.create_race_info_table()` で再生成。

### 5-4. Phase 2（将来）

- 前処理済み特徴量データも DB に保管（`featured_data` テーブル）
- `AbstractDataProcessor` を DB 直読み対応に拡張（`filepath=None, repo=None, alias=None`）
- ダッシュボードからの DB 統計表示

---

## 6. ダッシュボード（Streamlit）の注意点

- `特徴量データ ✗` → `data/raw/featured_data.pkl` が存在しない（`ingest` or `FeatureEngineering` を実行する必要あり）
- `オッズスナップ ✗` → `data/raw/odds_snapshots.pkl` が存在しない（段階オッズ収集 cron が未実行）
- ダッシュボードはデータ収集・学習のコントロール機能を持たない（CLI / ノートブック経由で実施）

---

## 7. テスト・CI

```bash
python -m pytest tests/ -q          # 590 件（storage 12 件 pass 確認済み）
python -m ruff check src/ app/
python -m mypy --config-file pyproject.toml
lint-imports
```

期待: pytest pass（storage 12 件以上）、mypy clean（Phase 1 対象モジュール）、import-linter 4 contracts kept

---

## 8. よく使うコマンド

```bash
# ブランチ確認
git branch --show-current

# 日次取込（ingest）
python -m src.pipeline.run_pipeline ingest --race-id 202401010101

# 強制上書き取込（誤情報修正時）
python -m src.pipeline.run_pipeline ingest --race-id 202401010101 --force

# 週次再学習
python -m src.pipeline.run_pipeline retrain

# ダッシュボード起動
streamlit run app/Home.py

# ノートブック
jupyter lab

# pickle 構造確認
python check.py

# DB から pickle 復元（upsert バグ修正後のみ信頼可能）
python -c "
from src.storage import RawDataRepo
df = RawDataRepo().read('raw_results')
df.to_pickle('data/raw/results.pkl')
print(df.shape, 'race_id sample:', df['race_id'].head(3).tolist() if 'race_id' in df.columns else df.index[:3].tolist())
"
```

---

## 9. プロジェクト構成（レイヤ依存）

```
src/
├─ constants/       定数・列名・閾値・馬券種・ログ設定（最下層）
├─ storage/         raw データ SQLite 永続化（Phase 1）
├─ preprocessing/   前処理（結果/払戻/血統/特徴量エンジニアリング）
├─ preparing/       データ収集アダプタ（Playwright スクレイパ・オッズ取得）
├─ policies/        馬券/スコア戦略・Harville 確率・オッズ供給
├─ training/        学習器（KeibaAI / スタッキング / sample weights）
├─ portfolio/       資金配分（ケリー）
├─ simulation/      バックテスト・指標・可視化
├─ operation/       運用モード・発注アダプタ・設定
└─ pipeline/        継続学習 CLI（ingest / retrain）
```

レイヤ依存の方向:
```
constants → storage → preprocessing / preparing → policies → training → portfolio → simulation → operation → pipeline → ui
```
