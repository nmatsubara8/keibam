# Claude コンテキスト — keibam プロジェクト

最終更新: 2026-06-11

---

## 1. プロジェクト概要

netkeiba のレースデータを収集・前処理し、LightGBM × ニューラルネットのスタッキングで
勝率を予測、期待値ベースで馬券を選定・資金配分するシステム。

---

## 2. 作業ブランチ

```
claude/sweet-dijkstra-mtre06
```

（Phase 1 のブランチ `claude/trusting-noether-xJD0m` は main にマージ済み）

---

## 3. 実装済みの修正・機能

### 3-1. ライブ予想パイプライン（出馬表→特徴量→スコア）

- `src/preparing/_scrape_shutuba.py` を新規実装
  - `scrape_race_id_race_time_list(kaisai_date)` — 開催日の race_id と発走時刻
  - `create_active_race_id_list()` — 本日開催で馬体重発表済みのレースだけ抽出
  - `scrape_shutuba_table(race_id, date_str, filepath)` — DataFrame を pickle 保存
- `ShutubaDataMerger.__init__` に `_separated_hr_with_sire_dict` の初期化を追加
- `_merge_horse_info` / `_merge_peds` で `horse_id` の dtype 不一致（str/int64）を `astype(str)` で解消
- `FeatureEngineering.dumminize_kaisai` の `place`/`time` 列 drop を `errors="ignore"` に変更
- `FeatureEngineering` に `dumminize_ground_state()`（単一列版）を追加
- `__label_encode` のラベルエンコードマスタで重複インデックスを drop して `InvalidIndexError` を解消

### 3-2. スコア計算（バックテスト・ライブ共通）

- `src/policies/_score_policy.py` の `_calc` を、race_id がカラムでもインデックスでも動作するよう改修
  - `groupby` のキー選択 / `wakuban_flag` の結合を条件分岐
- LightGBM モデルが generic 名 (`Column_0..Column_N`) で保存されている場合の対応
  - try/except で直接 `predict_proba(X_pred)` を試し、shape 不一致時のみ位置/名前ベースでフォールバック

### 3-3. 払戻データ（旧フォーマット対応）

- 2020 年以前の払戻 raw データは行区切りが `br` ではなく半角空白
- `src/preprocessing/_return_processor._build_bet_df` の先頭に正規化処理を追加
  - `' - '` → `-`、`' → '` → `→`、残った ` ` → `br` に置換

### 3-4. race_id_list のキャッシュ破壊問題

- `src/preparing/_scrape_race_id_list.py` の `_filter_by_years` を多段フォールバック付き `_filter_requested` に置き換え
- フィルタ後の縮小データを `to_pickle` で書き戻していた行を削除し、全件 pkl を保護

### 3-5. ノートブック改善（main.ipynb）

- 各シミュレーション保存セルの保存先を `models/<today>/...` に動的化
- モデルロードを `glob` で最新ディレクトリ自動検出に変更
- `BetPolicyTanshoFukusho` を追加
- `race_id_list[0]` → `race_id_list.iloc[0]["race_id"]`（戻り値が DataFrame のため）

### 3-6. README.md 更新

- `## 8. 最近の主要修正` セクションを追加（8-1〜8-6 を記載）

### 3-7. Phase 1: raw データの SQLite 永続化（`src/storage/`）**完了**

**目的**: pickle が消えると全件 re-scrape が必要という問題を解消。
SQLAlchemy + SQLite (`data/keibam.db`) で raw データを永続化する。

**設計方針**:
- `INSERT OR IGNORE`（冪等 upsert）で既存行を上書きしない
- pickle はキャッシュとして残す（既存 Processor が `pd.read_pickle()` で読んでいるため）
- pickle が消えた場合は DB から再生成 → 再ダウンロード不要
- 初回起動時に既存 pickle を DB へ自動移行（`auto_migrate_if_empty`）
- 誤情報修正時は `--force` フラグで該当行を削除してから再 scrape

**作成・修正ファイル**:

新規:
- `src/storage/__init__.py` — `RawDataRepo`, `TABLE_SPECS`, `TableSpec`, `PICKLE_PATH_TO_ALIAS`, `alias_to_pickle_path`, `get_engine` を re-export
- `src/storage/_db.py` — `get_engine()` シングルトン（WAL mode）+ `TABLE_SPECS` + `PICKLE_PATH_TO_ALIAS` + `_create_tables`（PK 列不足時に自動 DROP+再作成）+ `ensure_columns`
- `src/storage/_repo.py` — `RawDataRepo`（upsert / read / delete / delete_by_index / has_rows / auto_migrate_if_empty）
- `tests/storage/__init__.py`
- `tests/storage/test_repo.py` — 12 ケース（roundtrip / 冪等 / delete / return_tables PK / auto_migrate / unknown alias）

修正:
- `src/constants/_local_paths.py` — `DB_PATH` 追加
- `src/preparing/_get_rawdata.py` — `update_rawdata` 末尾で SQLite に冪等 upsert（失敗は warning で握り潰し）
- `src/pipeline/run_pipeline.py` — `ingest` サブコマンドに `--force` フラグ追加
- `src/pipeline/_ingestion.py` — `IngestConfig.force` 追加、`IngestJob.run` 冒頭で対象 race_id の DB 行を DELETE
- `requirements.txt` — `sqlalchemy>=2.0` 追加
- `.gitignore` — `data/keibam.db*` 追加（WAL/SHM 含む）
- `.importlinter` — layers 契約に `src.storage` を `preprocessing` と `constants` の間に追加
- `pyproject.toml` — mypy `files` に `src/storage` を追加

**スキーマ（7テーブル）**:

| テーブル | 主キー | index_col |
|---|---|---|
| `raw_results` | (race_id, 馬番) | race_id |
| `raw_race_info` | (race_id,) | race_id |
| `raw_return_tables` | (race_id, row_idx) | race_id |
| `raw_horse_results` | (horse_id, 日付) | None |
| `raw_horse_info` | (horse_id,) | None |
| `raw_peds` | (horse_id,) | None |
| `raw_odds_snapshots` | (race_id, captured_at, bet_type, combo) | None |

**重要な実装上の注意事項**:

1. `raw_results` / `raw_race_info` の pickle は `race_id` を index **かつ** 通常列として持つ二重構造。
   - `upsert()` では reset_index 前に `spec.index_col` 列を drop して重複エラーを回避。
   - `read()` では `set_index(spec.index_col)` した後に `df[spec.index_col] = df.index` で列も復元。
   - これにより `ResultsProcessor._select_columns` が `df[["race_id", ...]]` で読める。

2. `raw_return_tables` は払戻 raw の int 列名（0, 1, 2）を持つ。
   - `df_norm.columns = [str(c) for c in df_norm.columns]` で文字列化してから `ensure_columns` を呼ぶ。
   - `auto_row_idx_col=True` で race_id 内の連番を自動付与して PK を構成。

3. `raw_horse_results` / `raw_horse_info` / `raw_peds` は index に名前がなく、horse_id は通常列として存在する。
   - `TableSpec.index_col=None` とし、PK 列（horse_id 等）を通常列として扱う。

4. スキーマ移行: `_create_tables` で PK 列が既存テーブルに不足していたら DROP して再作成。
   - データは消えるが `auto_migrate_if_empty` で pickle から再取込できる。

**テスト確認**:
- pytest: tests/storage/ 12 件 pass、全体 590 件 pass
- ruff: All checks passed（src/storage/ 含む）
- mypy: src/storage/ にエラー無し（他 8 ファイルに pre-existing エラーあり）
- lint-imports: 4 contracts kept

---

### 3-8. Phase 2: featured_data の DB スナップショット永続化（`src/storage/`）**完了**

**目的**: 前処理済み特徴量データ（featured_data）を DB に保管し、pickle 揮発時の
高速復元・バージョン管理・ダッシュボード統計を実現する。

**設計方針（Phase 1 と異なる点）**:
- featured_data は数百列の数値/category/bool/date が混在し、学習が dtype をそのまま消費する。
  → 列ごと TEXT 保存（Phase 1 方式）ではなく **1 スナップショット = 1 行の BLOB**
    （parquet バイト列、pyarrow 不在/非対応 dtype 時は pickle にフォールバック）で dtype を完全保持。
- `version` 単位で保存（省略時 `YYYYmmdd_HHMMSS` 自動採番）。同一 version は `INSERT OR REPLACE` で上書き。
- 別途 `featured_meta` 表に n_rows/n_races/n_features/date_min/date_max/columns/created_at を保存し、
  ダッシュボード統計に利用。

**作成・修正ファイル**:

新規:
- `src/storage/_featured_repo.py` — `FeaturedDataRepo`（save / load / latest_version / get_meta / list_meta / delete）
  + `_serialize`/`_deserialize`/`_extract_meta`/`_row_to_meta`（純粋関数）
- `tests/storage/test_featured_repo.py` — 18 ケース（roundtrip / dtype 保持 / version / meta / delete / pickle fallback / raw 表との共存）
- `tests/preprocessing/test_abstract_data_processor_db.py` — 4 ケース（filepath/repo経路・エラー）

修正:
- `src/storage/__init__.py` — `FeaturedDataRepo` を re-export
- `src/preprocessing/_abstract_data_processor.py` — `__init__(filepath=None, *, repo=None, alias=None)` に拡張。
  filepath か (repo, alias) のいずれかで raw を読む。repo は duck-typed（storage への import 依存なし）。
- raw processor 群（results/race_info/horse_info/horse_results/return/peds）の `__init__` に `repo=`/`alias=` を追加し
  デフォルト alias を設定。pickle 揮発時に DB から直接前処理を再開できる。
- `src/pipeline/_ingestion.py` — `IngestConfig.save_featured_to_db`（既定 True）追加。
  featured 再生成後に `FeaturedDataRepo().save()` で DB スナップショット保存（non-fatal）。
- `src/pipeline/run_pipeline.py` — `retrain` で pickle 揮発時、まず DB スナップショットから復元を試みる。
  再計算した場合も DB に保存。
- `app/pages/1_dashboard.py` — 「🗄️ 特徴量スナップショット（SQLite）」セクション追加（list_meta を表示）。

**重要な実装上の注意事項**:

1. `FeaturedDataRepo` は Phase 1 の `TABLE_SPECS`（列ごと TEXT）に乗らず、自前で
   `featured_snapshots`(version PK, format, data BLOB, created_at) と `featured_meta` を
   `CREATE TABLE IF NOT EXISTS` する。raw 表と同一 DB ファイルに共存する。
2. parquet は列名が全て文字列である必要があり、非文字列列名や非対応 dtype では pickle に自動フォールバック
   （format 列に記録し load 時に分岐）。
3. IngestJob 内部の `FeaturedDataRepo()` は db_path 省略で `LocalPaths.DB_PATH` を引く。
   pipeline テストは実 DB を汚さないよう既定 `save_featured_to_db=False`、DB 保存検証は
   `monkeypatch.setattr(LocalPaths, "DB_PATH", tmp)` + `_reset_engine_for_testing` を使う専用テストで行う。

**テスト確認**:
- pytest: 全体 622 passed / 5 skipped、storage 46 件 pass
- ruff: 自身の変更ファイルは All checks passed（既存 `src/preparing/_scrape_shutuba.py` の B905 は pre-existing）
- mypy: src/storage/ にエラー無し（他 8 ファイルに pre-existing エラーあり）
- lint-imports: 4 contracts kept
- DB 汚染なし（テスト後に data/keibam.db が残らないことを確認）

---

## 4. 既知の未解決事項

### 4-1. `test_horse_id_in_peds` のテスト失敗（pre-existing）

- `tests/preparing/test_scrape_html_ped.py::test_horse_id_in_peds` が失敗
- 原因: `results.pkl` の `horse_id` 5855 件が `peds.pkl` に存在しない（peds の scrape が不足）
- Phase 1 の変更とは無関係
- 対処: 不足している horse_id の血統データを re-scrape する必要がある

### 4-2. mypy エラー（pre-existing、8 ファイル）

- `src/training/_data_splitter.py` の `lgb.Dataset` など LightGBM 4.x 変更に伴うエラー
- Phase 1 の変更とは無関係

### 4-3. `ingest` CLI の動作確認未完了

- `python -m src.pipeline.run_pipeline ingest --race-id 202401010101` がクラウド環境では OOM/タイムアウトで終了
- Playwright を使う scrape がメモリを大量消費する可能性
- ローカル環境（Playwright 導入済み）での確認が必要

---

## 5. Phase 3（将来）

Phase 2（featured_data の DB 保管 / `AbstractDataProcessor` の DB 直読み / ダッシュボード統計）は
**完了**（3-8 参照）。次の候補:

- 古い featured スナップショットの自動 retention（世代数上限・古い version の自動 delete）
- スナップショット間の特徴量列 diff 表示（列ドリフトの可視化）
- raw processor の DB 直読みを pipeline 構築サイト（`run_pipeline._FullPipelineBuilder` 等）に実配線し、
  pickle を介さない経路を既定化する

---

## 6. ダッシュボード（Streamlit）の注意点

- `特徴量データ ✗` → `data/raw/featured_data.pkl` が存在しない（`FeatureEngineering` を実行する必要あり）
- `オッズスナップ ✗` → `data/raw/odds_snapshots.pkl` が存在しない（段階オッズ収集 cron が未実行）
- ダッシュボードはデータ収集・学習のコントロール機能を持たない（CLI / ノートブック経由で実施）

---

## 7. テスト・CI

```bash
python -m pytest tests/ -q          # 590 件
python -m ruff check src/ app/
python -m mypy --config-file pyproject.toml
lint-imports
```

期待: pytest pass（590 件）、mypy clean（Phase 1 対象モジュール）、import-linter 4 contracts kept

---

## 8. よく使うコマンド

```bash
# ブランチ確認
git branch --show-current

# 日次取込
python -m src.pipeline.run_pipeline ingest --race-id 202401010101

# 強制上書き取込（誤情報修正時）
python -m src.pipeline.run_pipeline ingest --race-id 202401010101 --force

# 週次再学習
python -m src.pipeline.run_pipeline retrain

# ダッシュボード起動
streamlit run app/Home.py

# ノートブック
jupyter lab

# 初期 DB 移行（既存 pickle → DB 一括取込）
python -c "
from src.storage import RawDataRepo
from src.constants._local_paths import LocalPaths
repo = RawDataRepo()
for alias, path in [
    ('raw_results', LocalPaths.RAW_RESULTS_PATH),
    ('raw_race_info', LocalPaths.RAW_RACE_INFO_PATH),
    ('raw_return_tables', LocalPaths.RAW_RETURN_TABLES_PATH),
    ('raw_horse_results', LocalPaths.RAW_HORSE_RESULTS_PATH),
    ('raw_horse_info', LocalPaths.RAW_HORSE_INFO_PATH),
    ('raw_peds', LocalPaths.RAW_PEDS_PATH),
]:
    print(alias, repo.auto_migrate_if_empty(alias, path))
"

# DB から pickle 復元
python -c "
from src.storage import RawDataRepo
df = RawDataRepo().read('raw_results')
df.to_pickle('data/raw/results.pkl')
print(df.shape, df.index.name, 'race_id in cols:', 'race_id' in df.columns)
"
```

---

## 9. プロジェクト構成（レイヤ依存）

```
src/
├─ constants/       定数・列名・閾値・馬券種・ログ設定（最下層）
├─ storage/         raw データ SQLite 永続化（Phase 1）
├─ preprocessing/   前処理（結果/払戻/血統/特徴量エンジニアリング）
├─ preparing/       データ収集アダプタ（Playwright スクレイパ・オッズ取得）
├─ policies/        馬券/スコア戦略・Harville 確率・オッズ供給
├─ training/        学習器（KeibaAI / スタッキング / sample weights）
├─ portfolio/       資金配分（ケリー）
├─ simulation/      バックテスト・指標・可視化
├─ operation/       運用モード・発注アダプタ・設定
└─ pipeline/        継続学習 CLI（ingest / retrain）
```

レイヤ依存の方向:
```
constants → storage → preprocessing / preparing → policies → training → portfolio → simulation → operation → pipeline → ui
```
